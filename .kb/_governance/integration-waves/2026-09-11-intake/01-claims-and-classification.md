# Wave `2026-09-11-intake` — claims and classification

Every claim the five staged files carry, labelled against the **accepted decision corpus** — the
fifty-nine `kind: decision, status: accepted` atoms in `.kb/decisions/`, read rather than inferred
from titles.

The four labels:

- **aligns** — the claim is already true under an accepted decision, and adds no obligation.
- **extends** — the claim adds evidence, scope or consequence to something accepted, without
  contradicting it.
- **conflicts** — the claim contradicts an accepted decision or the tree.
- **requires-new-decision** — the claim is a commitment nothing accepted covers.

**The wave's shape in one line:** three `requires-new-decision`, **two `conflicts` — both with
the tree, neither with a decision** — and everything else `extends` or `aligns`. The two
conflicts are the same conflict twice: ADR-0062 and ADR-0063 describe a `ProjectionStore` whose
`begin` is `async` and whose gate is off, and this worktree's `HEAD` carries neither. It is not a
contradiction between two authorities; it is a decision recorded ahead of the tree it binds, and
`02`'s Adjudication 1 says how the wave carries that without pretending otherwise.

File abbreviations, used throughout:

`adr-0004` = `2026-09-08-adr-0004-msrv-becomes-a-promise-at-publication.md` ·
`citation-scan` = `2026-09-08-intake-is-outside-the-citation-scan.md` ·
`host` = `2026-09-09-the-measurement-host-and-its-clock.md` ·
`adr-0062` = `2026-09-10-adr-0062-the-probe-seam-moves.md` ·
`adr-0063` = `2026-09-11-adr-0063-the-projection-port-is-frozen.md`

---

## A. ADR-0004 loses `provisional` at publication (1 file — `adr-0004`)

The file's first line: *"HELD OUT OF THE INGEST WAVE — do not pass this file to
`/redkiln:kb-ingest`."* It was passed. The wave reads it, honours its header, and takes the one
sentence in it the corpus does not already hold.

| # | Claim | Label | Against |
| --- | --- | --- | --- |
| A1 | `RUNBOOK.md`'s phase-12 list carries *"ADR-0004 loses `provisional`; the MSRV becomes a promise"*; the marker was always conditional on publication | **aligns** | `kb-decision-0037` states this as its whole decision, with the tense marked: *"what `0.2.0` **will** bind when phase 12 lands, not a fact already true of the alpha"* |
| A2 | It is not true yet — nothing is on crates.io at `0.2.0`; lifting the marker now would put a promise in the record before the thing that makes it a promise exists | **aligns** | 0037's own body: *"`RUNBOOK.md`'s phase table has phase 12 … at `not started`"*. The general shape is the one Adjudication 1 applies to ADR-0062/0063 |
| A3 | An accepted atom is immutable; lifting a marker changes what the decision asserts and so needs a new atom, not an edit — *"unlike a drifted line number"* | **aligns** | `kb-governance-referent-not-reasoning-001`'s test, verbatim in shape. The *"drifted line number"* aside is what `citation-scan` (B6) then exercises |
| A4 | Step 3 — *"run `/redkiln:kb-ingest`, which authors the superseding atom"* — is wrong on its own terms: 0037 records this as an **amendment lineage**, ADR-0004 and ADR-0029 stay accepted and byte-identical, neither gets a `superseded_by` flip | **aligns** | 0037's summary and its *"What this does not decide"*; the 2026-09-04 wave's Adjudication 10. The header retracts step 3 itself |
| A5 | The floor moved at phase 2 because of a dependency's build script; five of five database crates declare no `rust-version`; `cargo hack --rust-version` and `resolver = "3"` cannot protect a floor against them; the `msrv` job is vacuous until pin and floor diverge | **aligns** | 0037 §"Which compiler, and why that one" and §"What a bump costs" carry every clause of this, and `kb-decision-0029` carries the original |
| A6 | *Measured 2026-09-08:* `max_stable_version` is `0.0.0` for all seven crates, three additionally carry `0.2.0-alpha.1`, nothing is yanked | extends | `kb-open-question-stale-0-0-0-name-reservations-001`'s *"What is true today"* was read on 2026-09-03/04 and has **five** crates at `0.0.0`; two more reservations have landed since. Not in any accepted decision, and not in 0037, which counts four publishable crates for a different reason |

## B. The staging directory is outside the citation scan (1 file — `citation-scan`)

No digest reached this phase for this file; claims are extracted here.

| # | Claim | Label | Against |
| --- | --- | --- | --- |
| B1 | `cargo xtask ci` was red on `main` from the 2026-09-07 wave's merge (`6acdf24`) until the `0.2.0` closeout session, on one line: `kb-decision-0058:33` cited `event_store.rs:1229-1296` and line 1229 was blank. Nobody ran the gate between the merge and that session | extends | Verified: all five commits resolve; 0058's ranges now read `:1396-1431` after `4e13ee2`. Evidence for the docs-citation OQ and the governance atom; no decision is touched |
| B2 | The citation was **stale on arrival**, inherited from a 2026-09-04 brief; `a4616ca` repointed fifteen citations the merge moved and correctly touched nothing under `.kb/` — nothing there was in scope | extends | The exact mechanism `kb-reference-intake-citation-drift-census-001` predicted and `kb-open-question-docs-citation-anchor-contradiction-001`'s postscript measured. This is the predicted event arriving, at the moment the postscript said was *not* the one that matters — it turns out both are |
| B3 | The hole, mechanically: `CITATION_SCAN_EXCLUDE` excludes `.kb/_intake`; the exclusion's reason is sound; what follows was not intended — the ingest promotes unchecked prose into a checked directory, at the one moment nobody is looking at line numbers | extends | `xtask/src/lints.rs:2488` (the file cites `:2003`; drifted). Owner: the docs-citation OQ's sub-question 2 |
| B4 | **Decided at review: re-anchor at promotion.** `/redkiln:kb-ingest` resolves every `path:line` against `HEAD` as it authors and refuses the *wave* when one does not resolve. Not a change to this repository — the workflow is redkiln's, which already carries `fix-kb-ingest-defects`. Until it lands: run `cargo xtask ci` immediately after an ingest wave merges | extends | A decision about a tool outside this tree, with one alternative rejected by name (scanning `_intake` as a warning *"puts the check on the wrong side of the boundary"*). It answers the OQ's sub-question 2 and does not close the OQ — sub-questions 1 and 3 stand, and the fix has not landed |
| B5 | The two repairs are not alternatives; re-anchoring is the stronger because it fails the wave rather than the next person to run the gate | extends | Body of B4 |
| B6 | Repairing 0058 meant editing an accepted body, taken under *rewrite the referent, never the reasoning* — not one word of reasoning moved | **aligns** | `kb-governance-referent-not-reasoning-001`, applied. A fifth worked instance, and the first on a `.kb` decision atom's *line ranges* rather than an ADR record's crate names |
| B7 | **`redkiln validate --kb`'s immutability check compares the working tree against `HEAD`.** It refuses an uncommitted edit and passes once committed: a dirty-tree guard that cannot tell a referent repair from a reversal, weaker than the surrounding prose implies, and wrong in both directions | extends | `CLAUDE.md:216` records the same finding with the transcript; no `.kb` atom does. The file says *"worth an atom of its own"* and it is — an open question |
| B8 | What is open: a stronger check (hash the accepted body into the atom's own frontmatter, so history is what is compared) or a documented carve-out for referent-only edits | extends | Nothing owns it. `kb-open-question-references-adr-correction-policy-001` is the nearest sibling and is about `references/adr/`, not the check |
| B9 | Superseding 0058 to fix the citation was considered and is worse: it marks a correct, unreversed decision `superseded` — a lie to every later reader — and **does not fix the gate**, because the citation lint reads every atom regardless of status | extends | A rejected alternative the governance atom does not yet name, and a mechanical fact about `citation_ranges_resolve` worth one sentence |

## C. The measurement host and its clock (1 file — `host`)

| # | Claim | Label | Against |
| --- | --- | --- | --- |
| C1 | Two atoms are probably owed — a decision and a reference — and the ingest should adjudicate | — | Procedural framing addressed to this phase. Honoured, and a third atom is found (C6) |
| C2 | Benchmark instability is written down three times independently: `benchmarks/README.md` §"What none of this shows" (40%), `RUNBOOK.md` (2.7×–3.0×), `references/adr/0022-…:120-144` (45%, 4×). Three instances is what makes this a decision; the existing answer (interleave, ratios, allocation counts) makes numbers comparable without making the machine quiet | extends | `kb-decision-0022` carries the third instance as evidence. Rationale for C3, in C3's atom |
| C3 | **The decision:** a dedicated Linux host is the reference machine for benchmarks and build/test; its conditions are **data** in `ops/host/host.env`, applied by a systemd unit, asserted by `preflight.sh` — not README prose and a shell ritual. Two load-bearing properties: **one script writes, another only reads** (a script that did both could always make its own check pass); **`--restore` restores captured values, never assumed defaults** | **requires-new-decision** | Nothing accepted covers the machine. `kb-decision-0022` and the two benchmark reference atoms *depend* on a host and name its conditions; none decides what a host must be |
| C4 | The CF-34 boundary: `preflight.sh` asserts on the **environment**, and every value it reads exists **before the first sample** — the property that separates it from a budget. Structurally unreachable from the gate: no step, no `verify:` command, no CI job invokes it, and `ops/` is on `is_inert`'s INERT list. **Residual risk named:** registering the host as a self-hosted runner would make the preflight reachable from a merge-blocking job — *"a decision, not a configuration change"* | extends | CF-34 is `[PROVISIONAL]` at `spec/SPECIFICATION.md:9021` (the file cites `:8747`; drifted). `kb-open-question-cf-33-cf-34-scope-001` owns *clock-adjacent assertions outside the testkit*; this is a third instance and the first built to be unreachable. Body of C3, and a merge |
| C5 | **The clocksource finding.** The kernel refuses this host's TSC at boot (11,225,813,293-cycle warp), falls back to `hpet`, and `clock_gettime(CLOCK_MONOTONIC)` costs 1,390 ns against 19 ns — 73×. `tsc=reliable` was tried and was wrong. Three instruments; the first two failed their own controls; the third (`tsc-migrate.c`) found **62,493 backwards reads per million migrations on `tsc`, 0 in 500,000 on `hpet`**. Proportions 1/16, 1/4, **1/2 on one physical core** (CPU0/CPU1 disagree by 6.46 ms) — a fixed per-CPU offset. `tsc_adjust` absent from `/proc/cpuinfo`: no kernel parameter can fix it, only firmware | extends | A dated, one-machine measurement — `reference/README.md`'s shape exactly. `ops/host/README.md:143-230` carries it in full, including what the brief omits: the paired runner's own control **fails** on `hpet`, and a pinned-CPU regime that would repair it is measured and not taken |
| C6 | **The transferable lesson is not about clocks:** a control that can only fire on a machine fault is worth less than one that can fire on the *instrument*. The SMT-sibling row exists because two threads of one core reading one counter cannot disagree — and it condemned both wrong instruments before either was believed. `controls_fire.rs` encodes the habit for the harness; the same habit was needed one layer down and was not there until it was needed twice | extends | Method + measurement + two rejected instruments, each with the control that killed it. `kb-decision-0010` states the principle for the conformance suite; no playbook carries the method |
| C7 | **Four explicit non-decisions:** frequency cap and SMT stay as found (capping before flakiness is measured makes the improvement unattributable); `benchmarks/results/` is **not** re-taken (four `[PROVISIONAL]` clauses and two doc comments reason from Windows-host figures); `RESOLVABLE_FLOOR` stays documented against Windows's ~15.6 ms tick (`cpu.rs:58`; lowering it wants its own measurement); a Windows absolute and a Linux absolute are never compared — only same-run ratios cross | extends | `kb-decision-0023`'s *"What this decision does not settle"* shape. Body of C3. The second bullet is what keeps `kb-reference-busy-timeout-margin-001` and `kb-reference-append-condition-experiment-001` true: their conditions are the Windows host's, and stay so |
| C8 | Note to the ingest: `.kb/_intake` is outside the citation scan; every `path:line` above is promoted into a scanned directory — re-resolve each against `HEAD` | **aligns** | `citation-scan` B4's remedy, invoked. Applied mechanically: two of this file's citations had drifted (`00` Provenance) and the atoms carry the re-anchored lines |

## D. ADR-0062 — the probe seam moves (1 file — `adr-0062`)

| # | Claim | Label | Against |
| --- | --- | --- | --- |
| D1 | **The decision:** the seam moves, all of it — `probe_write`, `probe_delete_all`, `probe_read_through` take `&mut Self::Batch` and return `impl Future<Output = Result<…, Self::Error>>`, no `Send` bound — **and `begin` moves with it**: `async fn begin(&self) -> Result<Self::Batch, Self::Error>`, because with the probe moved and `begin` unmoved a live store could report itself and still could not exist for `sqlx`. The phase-10b refutation was a property of `begin`'s signature, not of the axis | **requires-new-decision** | `kb-decision-0060` named this change and declined it: *"PS-2's owner's call rather than an adapter lane's"*, and *"not sufficient alone"*. ADR-0062 is that owner making the call and moving more than 0060's proposal (all three probe methods **and** `begin`). **Conflicts with the tree at `HEAD`:** `projection.rs:460` is `fn begin(&self) -> Self::Batch` — Adjudication 1 |
| D2 | **PS-6's falsifier had fired and nobody had said so.** *"An adapter that must reserve something from the server before the first write"* is `sqlx`'s `BEGIN`. PS-6's MUST is rewritten to the discipline the signature protected — a buffering adapter's `begin` resolves at its first poll and never fails — held by two tests: `begin_makes_no_round_trip` (Neon, over a transport that fails every request) and `begin_resolves_at_its_first_poll_without_a_runtime` (contract crate, polled once, no executor) | extends | PS-6 is `[PROVISIONAL]`; its MUST at `spec/SPECIFICATION.md:5183` is unrewritten at `HEAD`. A falsifier that fired and *was acted on* — the opposite arm of `kb-open-question-provisional-falsifiers-001`'s observation. And a second instance of `kb-playbook-require-property-not-mechanism-001`: the MUST named the mechanism, the tests now hold the property |
| D3 | Breaking to every implementer; free because `unstable-projection` carries the exemption, *"which is what the gate was kept for"*. Eight probe impls, one runner error arm (`ProjectionError::Begin`), 117 call sites, one constitution fence whose `compile_fail` would otherwise have started passing for the wrong reason | extends | `kb-decision-0036`'s exemption, spent on exactly the change `kb-decision-0060` kept it for. The counts are the brief's and are **unverifiable here** |
| D4 | **The far end, built.** `LivePostgresProjectionStore` in `happenstance-postgres`, *beside* the buffered store; `Batch` owns `sqlx::Transaction<'static, Postgres>`; `READS_THROUGH_BATCH = true` is a true statement about it; **20 of 20** against live PostgreSQL, with `batch_reads_reflect_pending_writes` and `rebuild_is_chunk_size_invariant` returning `Ran` for the first time against anything but an in-process map. The buffered store stayed green | extends | The falsifier `kb-decision-0060` named — *"a live-transaction adapter that passes **and** can truthfully declare `READS_THROUGH_BATCH = true`"* — fired, on purpose. **Absent at `HEAD`**: no such type in the crate. Brief's statement |
| D5 | What the two ends disagreed about: **nothing the port had to move for.** The live end needs an explicit `ROLLBACK` on the regression path; PS-7 is met by rollback-on-drop; a refused statement poisons the transaction and surfaces through the seam's new `Result`. Each is the far end being harder, not the port being wrong. **PS-2's MUST is met as written** | extends | PS-2 is `[FROZEN]`; its Rule is amended, its MUST untouched — the same repair-not-amendment discipline `kb-playbook-repair-frozen-clause-001` states. Body of D1 |
| D6 | **Deliberately not decided: lifting the gate.** A semver promise on a published crate is its own record. What that record must weigh: `Projection::apply` is synchronous, so an application can push into a buffered batch and cannot issue a statement into a live one; the live store is an instrument for the *port's* freeze; whether `apply` moves is the typed layer's axis | extends | Deferred by name, and deferred again in `adr-0063` E3 in the same words. No atom owns it — **the wave's cross-file cluster**, Adjudication 4 |
| D7 | Alternatives rejected: `rusqlite` at the far end with `begin` kept synchronous (an owned-connection batch forces `begin` to acquire on faith); `probe_read_through` alone (0060 said insufficient, confirmed); splitting `READS_THROUGH_BATCH` (removes the lie, leaves the hole); rewording PS-2 to drop the far end (the monoculture its `Rejects` forbids); replacing the buffered Postgres batch (the buffered one is the product) | extends | Body of D1. The first confirms `kb-decision-0060`'s `rusqlite` mechanism from the other side |
| D8 | Falsifier: reopened by an adapter at either end the moved seam still cannot describe, or by a buffering adapter the `async` `begin` genuinely cost a round trip. **Not** reopened by `apply` staying synchronous — *"the next record's subject"* | extends | Body of D1; the last clause is D6's pointer forward |
| D9 | *Resolves:* `kb-open-question-probe-read-through-signature-001` — the seam moved, at seam grain, and `begin` with it | extends | The OQ's question — *does the signature move, or does the adapter get built against today's shape* — is answered *moves*, wider than the OQ's own recommendation. Resolved in the corpus's convention: `status: superseded`, a dated resolution section, no `superseded_by` (a decision resolved it, not another question) |

## E. ADR-0063 — the projection port is frozen (1 file — `adr-0063`)

| # | Claim | Label | Against |
| --- | --- | --- | --- |
| E1 | **The decision: the port is frozen.** `happenstance-core`'s `projection` module and re-exports unconditional; `MemoryProjectionStore` behind `memory` alone; `conformance` implies nothing again. A signature change on `ProjectionStore`, its value types or `ProjectionProbe` is now a breaking change with a decision record behind it — what *frozen* means for `EventStore` | **requires-new-decision** | `kb-decision-0036` and `kb-decision-0060` both decided *gate the port*; both were conditional on PS-2's bar (PS-3's SHOULD: *"until PS-2's bar is met"*) and ADR-0062 met it. Discharged, not contradicted — Adjudication 5. **Conflicts with the tree at `HEAD`:** `Cargo.toml:110` still reads `conformance = ["unstable-projection"]` |
| E2 | **The feature name stays, empty.** `unstable-projection = []` remains on `happenstance-core`, off by default, gating nothing — removing a Cargo feature is breaking and a `0.2.0` manifest names it. Two `xtask` tests inverted rather than deleted; a third holds that no in-tree crate still forwards the retired feature, because a forward of an empty feature compiles | extends | Body of E1. Bears on `kb-open-question-projection-module-exemption-scope-001`, whose *"What is true today"* has the testkit forwarding the feature — true at `HEAD`, and the brief says the lane removes it |
| E3 | **The typed layer keeps a gate of the same name for a different reason.** `happenstance`'s `unstable-projection` still gates the runner and no longer forwards to the contract crate. `Projection::apply` is synchronous, so the far end the port was proved against is one the runner cannot drive for a projection that writes rows; freezing `apply` now would freeze a shape proved at one end of its axis — the mistake ADR-0060 refused for the port | extends | The second statement of D6, one record later. Same open question |
| E4 | **Clauses.** PS-3 and PS-34 retired (`[NON-NORMATIVE]`, IDs kept); PS-4, PS-5, PS-12 frozen on both ends of the axis standing on them; PS-6 stays provisional under ADR-0062; PS-9, PS-11, PS-15 and the rebuild cluster stay on their own falsifiers — *"the RUNBOOK ledger row that grouped them under PS-2 alone was a simplification the clauses never made, and is narrowed"*. Census 138/46/12/5 → 141/41/12/7 | extends | Maturity-marker moves are an ADR's act and this is the ADR. `kb-decision-0017`'s PS-9/PS-11 and PS-15 falsifiers are **not** claimed — consistent with 0017 and with last wave's ADR-0025 refusal. The *"thirteen on PS-2 alone"* sentence in `kb-open-question-provisional-falsifiers-001` is what the last clause narrows. Census unmoved at `HEAD` |
| E5 | Lineage: acts on ADR-0062; *"resolves the decision of ADR-0036 and ADR-0060 — both said gate the port; this record says the reason is discharged. Neither is superseded in its reasoning, which was right when written"* | **aligns** | The ADR-0037-over-ADR-0004 shape, which the corpus has run twice and `kb-governance-referent-not-reasoning-001` generalises. No governance merge: the atom already holds this shape and 0037 in `related`; a third restatement is the two-copies failure |
| E6 | Alternatives rejected: lifting both gates (freezes `apply` unproved); renaming the runner's feature (breaking for a distinction docs can carry); removing the empty feature (breaking for a free name); freezing PS-6 the day its MUST was rewritten; freezing PS-9/11/15 on the ledger's word against the clauses' | extends | Body of E1 |
| E7 | Falsifier: reopened by a breaking change an adapter the suite passes turns out to need, or by `apply` moving to a shape that requires the port to move with it. Not reopened by the runner staying gated another release | extends | Body of E1 |

## F. Claims that earn no operation

Recorded so the next wave does not re-derive them.

| Claim | Source | Why no operation |
| --- | --- | --- |
| A1–A5, the MSRV proposition and its wiring | `adr-0004` | Every sentence is in `kb-decision-0037`'s body, in tense-marked form, and 0037 is accepted and immutable. The file's own header says so and asks to be held out. The digests' `merge_existing` into 0037 is not an operation this corpus has |
| A3/A4, amendment-not-supersession | `adr-0004` | `kb-governance-referent-not-reasoning-001` already carries the ADR-0029/ADR-0004 lineage and lists 0037 in `related`. The 2026-09-04 wave's Adjudication 10 is the record |
| C1, "two atoms are probably owed" | `host` | Framing addressed to this phase; answered by the split in `02` |
| C8, "re-resolve each citation" | `host` | An instruction to this phase; executed (`00` Provenance), and its general form is B4's merge |
| D3's counts — eight impls, 117 sites, one arm, one fence | `adr-0062` | Unverifiable here; carried in the decision body as the brief's statement, never as a reference atom (a dated measurement with no raw output to cite is the false reference `reference/README.md` names) |
| D4's 20 of 20 | `adr-0062` | Same. The decision body records that the brief reports it and that the crate at `HEAD` has no `LivePostgresProjectionStore` |
| E4's census delta | `adr-0063` | A `spec/SPECIFICATION.md` artefact, not `.kb` content; recorded in the decision body as the brief's amendment, unmoved at `HEAD` |
| E5, discharged-not-superseded | `adr-0063` | Realised as **authoring constraints** on Op 5's frontmatter — `supersedes: null`, `depends_on` carrying 0036 and 0060 — and as an absence: two accepted bodies untouched, two `status` fields unflipped. The decision map's row carries *"discharged by ADR-0063"* against both |
| B1's red gate | `citation-scan` | Repaired at `4e13ee2` before this wave. History, in the OQ merge and the governance merge; nothing is owed |
| The `host` file's and `ops/host/README.md`'s `:8747` for CF-34 | `host` | Outside `.kb` (`ops/` is not in `CITATION_SCAN_DIRS`), and a repair to a file this wave does not own. The atoms cite `:9021`; the README's drift is noted in `unresolved[]` as a handoff, not fixed |
