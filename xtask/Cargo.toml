[package]
name = "xtask"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
anyhow.workspace = true

# Only so the repository README's examples can be compiled as doctests of this
# crate's lib target — see src/lib.rs for why they cannot live in a published
# crate. xtask itself uses neither.
[dev-dependencies]
happenstance = { workspace = true, features = ["std", "memory"] }

[lints]
workspace = true
