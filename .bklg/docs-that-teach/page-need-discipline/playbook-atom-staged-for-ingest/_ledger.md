---
item: HS-S0153
stage: implement
created: 2026-08-17T13:16:11.865Z
updated: 2026-08-17T13:16:11.865Z
---

# Acceptance ledger — The playbook atom staged under .kb/_intake/, never hand-authored into .kb/

The machine-checkable Definition-of-Done ledger for this story (RFC §6.5). One row per spec
`AC-###`. Planning authors every row with `satisfied: false`; the implementer may only flip a row to
`satisfied: true` and MUST cite real evidence (a `file:line` and/or the verifying test id) — never
edit, remove, or re-word a criterion, and never flip a satisfied row back. `redkiln verify --grain
story` reads the fenced block below and blocks `implement → report` unless every spec AC is present,
`satisfied: true`, and carries non-placeholder evidence (and no AC that was satisfied on the base
branch has regressed). Scope changes are a human decision recorded through `redkiln advance`, not a
quiet ledger edit.

**Two things about this ledger in particular**, both from the spec and both easy to violate by
accident:

1. **`redkiln validate --kb` green is AC-010's evidence and nothing else.** The validator skips every
   `_`-prefixed directory by design (`.kb/_intake/README.md:21-27`), so it is silent about the staged
   file's own frontmatter. Citing it under AC-002, AC-003 or AC-005 is EC-005 and is rejected at
   review.
2. **Six of the eleven rows are discharged by command transcripts, and two require a non-author**
   (AC-007's rejection cross-walk and AC-009's portability walk). Capture the transcripts as the
   commands are run, and schedule the two walks early — they are a scheduling constraint on the
   slice, not a formatting one.

The evidence sections referenced below (`§Mount`, `§Frontmatter walk`, `§Mechanical negatives`,
`§Summary adjudication`, `§Resolution transcript`, `§Non-occlusion`, `§Method`, `§Rejection
cross-walk`, `§Commitment audit`, `§Portability`, `§Divergence`, `§Composition`) are written into the
implementation report and cited from the `evidence` field by name plus `file:line`.

`<atom>` stands for `.kb/_intake/lesson-page-need-declaration-discipline.md` throughout.

```yaml
- id: AC-001
  criterion: "GIVEN the ingest operator runs /redkiln:kb-ingest with no argument at initiative closeout, WHEN the default glob .kb/_intake/*.md is expanded (.kb/_intake/README.md:3-5), THEN exactly one new payload file — .kb/_intake/lesson-page-need-declaration-discipline.md — is swept in beside the directory's README, under a `lesson-` name the operator reads as payload rather than scaffolding, and it is registered in no manifest, index or config anywhere else"
  satisfied: true
  evidence: "`implementation-report.md:89` §Mount, all transcripts verbatim. `git diff --name-status main -- .kb/_intake` -> exactly one line, `A\\t.kb/_intake/lesson-page-need-declaration-discipline.md`, and the commit-scoped `git diff --name-status --cached HEAD -- .kb/_intake` returns the same single line — both bases agree, because no earlier story in this project touched `.kb/`. `ls .kb/_intake` -> exactly two files, `README.md` and `lesson-page-need-declaration-discipline.md`. The staged file is named in full in §Mount so HS-P0025's operator drops `README.md` at the approval gate and nothing else (`durable-audience-closeout/project.md:198`). The `lesson-` prefix follows the two staged sources this repository has already ingested (`.kb/playbooks/anchoring-citations-in-a-long-lived-document.md` and `.kb/playbooks/landing-a-stricter-gate-without-a-red-baseline.md`, both citing a `.kb/_intake/lesson-...md` path in `source_paths`). Registered nowhere: `.kb/_intake/*.md` is the default glob and needs no registration (`.kb/_intake/README.md:3-5`), and the repository-wide sweep under AC-010 finds no reference to the path outside the file itself. Base note: `main` predates this project's merged dependency stories in general, but **not for `.kb/`** — no earlier story in this project touched that tree — so the spec's `main`-based command runs and passes here, and the commit-scoped diff is recorded beside it as the stronger base for a story-scoped promise (NF-004: the recorded command is the command run)."
  mount_point: ".kb/_intake/lesson-page-need-declaration-discipline.md"
  verifying_test: "git diff --name-status main -- .kb/_intake (exactly one A line, and it is <atom>) + git diff --name-status --cached HEAD -- .kb/_intake (the same single line) + ls .kb/_intake (exactly two files) — all three transcripts in the implementation report §Mount, which also names <atom> in full for HS-P0025's approval gate"

- id: AC-002
  criterion: "GIVEN the ingest operator adjudicating this file against the corpus, WHEN they read its first six frontmatter lines and nothing else, THEN they see kind: playbook, authority_tier: guideline and status: proposed — a finished proposal awaiting a decision that is theirs to take, neither claiming an acceptance no gate conferred nor inviting a full re-derivation from raw material"
  satisfied: true
  evidence: "Transcript verbatim, `rg -n '^(id|title|kind|status|authority_tier|summary|depends_on|related|source_paths|last_reviewed):' <atom>` -> `2:id: kb-playbook-declared-page-need-001`, `3:title: Making the need a page answers a declared, singular, checkable property`, `4:kind: playbook`, `5:status: proposed`, `6:authority_tier: guideline`, `7:summary: >-`. All three values are inside the first six lines, so the operator reads them before anything else. Rows 3-5 of the walk at `implementation-report.md:111` §Frontmatter walk each carry the one-sentence reason the rejected value lost: `accepted` lost because acceptance is conferred by the ingest wave that adjudicates the atom against the corpus, and asserting it in the one directory nothing validates would claim a state no gate granted; `draft` lost because it invites the wave to treat the file as raw material and re-derive its claims at full cost, which is the cost this atom is authored to avoid. `kind: playbook` is fixed by DR-11 and `.kb/playbooks/README.md:3-5`; `authority_tier: guideline` is the layer's tier and a convention the schema does not enforce (`.kb/README.md:31-35`), which is why it is walked by hand here."
  mount_point: ".kb/_intake/lesson-page-need-declaration-discipline.md"
  verifying_test: "rg -n '^(kind|status|authority_tier):' .kb/_intake/lesson-page-need-declaration-discipline.md — transcript verbatim in §Frontmatter walk rows 3-5, each carrying the one-sentence reason `accepted` and `draft` lost (spec Context pack 4)"

- id: AC-003
  criterion: "GIVEN that redkiln validate --kb skips every _-prefixed directory by design and is therefore silent about this file (.kb/_intake/README.md:21-27), WHEN the implementer closes the story, THEN _ledger.md carries a field-by-field walk of all ten required fields against .kb/README.md:16-26 — id, title, kind, status, authority_tier, summary, depends_on, related, source_paths, last_reviewed — each row citing the staged file's own line number, and no row of that walk cites the green validate --kb run as its evidence"
  satisfied: true
  evidence: "`implementation-report.md:111` §Frontmatter walk — **ten rows**, one per required field of `.kb/README.md:16-26`, each citing the staged file's own line: `id` `<atom>:2`; `title` `:3`; `kind` `:4`; `status` `:5`; `authority_tier` `:6`; `summary` `:7-17`; `depends_on` `:18-19`; `related` `:20-23`; `source_paths` `:24-32`; `last_reviewed` `:33`. Each row states why the value is well-formed rather than merely that it is present — the `id` is the corpus's `kb-playbook-<slug>-001` shape and is a proposal the wave may merge away under a different id; `last_reviewed: 2026-08-18` is the date the atom was written against the shipped tree, not the design's sign-off date. **No row of that walk, and no part of this evidence string, cites the green CLI run.** That run is recorded separately at `implementation-report.md:412` §Mechanical negatives and is labelled there as proving AC-010's negative only, because the validator is blind to `_`-prefixed directories by design (`.kb/_intake/README.md:21-27`; EC-005)."
  mount_point: ".kb/_intake/lesson-page-need-declaration-discipline.md"
  verifying_test: "§Frontmatter walk has ten rows, each with a <atom>:NN cite; the AC-003 evidence string contains no `validate --kb` (EC-005). The green CLI run is recorded separately in §Mechanical negatives, labelled as AC-010's evidence only"

- id: AC-004
  criterion: "GIVEN the ingest operator at the approval gate, who must decide merge-or-spawn before opening the body, WHEN they read summary alone, THEN it adjudicates: it states the problem, the properties that decide the design, the options ruled out and the discriminator, composed as the corpus's playbook summaries are — a paragraph in the folded-block form, not a one-line label and not a restatement of the title"
  satisfied: true
  evidence: "`implementation-report.md:131` §Summary adjudication quotes all four elements out of `<atom>:7-17` and nothing else. **Problem:** `A prose tree teaches unevenly and nothing sees it: the need each page answers is implicit and therefore arguable.` **Properties that decide the design:** `one need per page, declared in the page's own visible body text, spelled from a closed enumerated set, checked mechanically — then the check documents the judgement it cannot make, and a written non-author walk supplies it.` **Options ruled out:** `Front matter, an HTML comment, a filename convention, a sidecar manifest and a badge all lost as declaration forms; an open set lost because no check tests membership in one; a persona taxonomy lost because a persona is a property of the reader.` **Discriminator:** `a check sees a declaration and never an answer, so any form that hides it from the reader or makes it unenumerable hands the discipline back to judgement.` Form: a folded block, `summary: >-` at `<atom>:7`, running to `:17`. **Sentence count: 4**, against the two corpus exemplars measured side by side — `.kb/playbooks/landing-a-stricter-gate-without-a-red-baseline.md:7-15` at **5** sentences and `.kb/playbooks/anchoring-citations-in-a-long-lived-document.md:7-15` at **4**, both folded blocks stating problem, deciding properties, ruled-out options and discriminator in that order. It is neither a one-line label nor a restatement of the title at `<atom>:3`."
  mount_point: ".kb/_intake/lesson-page-need-declaration-discipline.md"
  verifying_test: "§Summary adjudication — side-by-side against .kb/playbooks/landing-a-stricter-gate-without-a-red-baseline.md:7-15 and .kb/playbooks/anchoring-citations-in-a-long-lived-document.md:7-15, with the four elements quoted out of the drafted summary and its sentence count recorded"

- id: AC-005
  criterion: "GIVEN a reader who does not yet trust the atom, WHEN they follow any entry in source_paths or any id in depends_on / related, THEN every path resolves in the tree at merge and every id resolves to an atom that exists — so the wave's merge bias has somewhere real to land rather than a near-duplicate to spawn"
  satisfied: true
  evidence: "`implementation-report.md:153` §Resolution transcript. **All eight `source_paths` entries resolve** — `standards/pages/README.md`, `standards/pages/00-one-need.md`, `standards/pages/10-the-need-set.md`, `standards/pages/20-the-fold-line.md`, `standards/pages/40-reviewing-a-page.md`, `xtask/src/lint_pages.rs`, `.bklg/docs-that-teach/page-need-discipline/_design.md`, `.bklg/docs-that-teach/_discovery/distillation/interaction-patterns.md` — listed together and returned together with no error, which is the same assertion as one `test -f` per entry and is the transcript pasted in §Resolution transcript. **All four ids resolve to an atom that exists**, `rg -n '^id: <id>' .kb` returning one canonical hit each: `.kb/playbooks/verify-the-referent-and-report-coverage.md:2:id: kb-playbook-verify-referent-report-coverage-001` (the single `depends_on`), `.kb/playbooks/landing-a-stricter-gate-without-a-red-baseline.md:2:id: kb-playbook-ratchet-gate-landing-001`, `.kb/playbooks/anchoring-citations-in-a-long-lived-document.md:2:id: kb-playbook-anchoring-citations-001`, `.kb/governance/rewrite-the-referent-never-the-reasoning.md:2:id: kb-governance-referent-not-reasoning-001`. The sweep also matched `.kb/_governance/integration-waves/**`, the wave records that created those atoms; §Resolution transcript says so rather than hiding the extra hits. Every id was read out of the tree; none is invented, so the wave's merge bias has four real places to land."
  mount_point: ".kb/_intake/lesson-page-need-declaration-discipline.md"
  verifying_test: "§Resolution transcript — one `test -f` line per source_paths entry (all present) and one `rg -n \"^id: <id>\" .kb` line per depends_on/related id (each a single hit), including the four pre-resolved ids at .kb/playbooks/verify-the-referent-and-report-coverage.md:2, .kb/playbooks/landing-a-stricter-gate-without-a-red-baseline.md:2, .kb/playbooks/anchoring-citations-in-a-long-lived-document.md:2, .kb/governance/rewrite-the-referent-never-the-reasoning.md:2"

- id: AC-006
  criterion: "GIVEN the practitioner on a different part of the system, who has this problem and twenty minutes, WHEN they open the atom in a GitHub blob view and read top to bottom clicking nothing, THEN the four-step method, the evidence that grounds each step, and a named section stating the conditions under which the discipline stops holding are all visible in document order — none of the three behind a <details>, a fold, a tab, or a link to another file"
  satisfied: true
  evidence: "`implementation-report.md:180` §Non-occlusion: `rg -n '<details>|<summary>|<!--' <atom>` -> **exit 1, no hit**. Nothing in the atom is folded, tabbed or commented out; every load-bearing region is persistent chrome. `implementation-report.md:191` §Method records the three obligations in document order. **The four steps**, `<atom>:49-78`: declare the need in the artefact's own visible body text (`:49`); spell the token from a closed, enumerated set held in one place (`:57`); check the declaration mechanically, and document what the check cannot see (`:65`); supply a written non-author procedure for the judgement the check cannot make (`:73`). **The evidence grounding each step** is inline in the same paragraph as an `**Evidence.**` run-in — a file, a cited `RP-NN-N` rule id, a constant, or a captured failing message (`docs/append-conditions.md:4 — declares 'explanation' and 'how-to'; a page answers one need`). **The stop conditions** are carried by `## Where this is the wrong instrument` at `<atom>:123`, the corpus's own heading for the obligation (`.kb/playbooks/landing-a-stricter-gate-without-a-red-baseline.md:98`), with two of its four quoted verbatim in §Method: `A medium that does not render the declaration in document order. A renderer that strips leading blockquotes, relocates them into a metadata layer, or demands front matter invalidates the form, and the decision re-opens rather than quietly contradicting itself.` and `A subject whose reference surface is not already owned elsewhere. The set here subtracts a 'reference' token because two reference surfaces already existed; where they do not, the subtraction is wrong and the set is a different set.` None of the three is behind a link to another file."
  mount_point: ".kb/_intake/lesson-page-need-declaration-discipline.md"
  verifying_test: "rg -n '<details>|<summary>|<!--' .kb/_intake/lesson-page-need-declaration-discipline.md returns no hit — transcript in §Non-occlusion; §Method names the heading carrying the stop conditions (corpus shape: `## Where this is the wrong instrument`, .kb/playbooks/landing-a-stricter-gate-without-a-red-baseline.md:98) and quotes at least two named conditions verbatim"

- id: AC-007
  criterion: "GIVEN the next page author asking why the discipline is shaped the way it is, WHEN they reach the discarded-alternatives section, THEN each of the five rejection families is present with the reason it lost — the four rejected homes for the tree, the five rejected declaration forms, DT-2's three rejected need taxonomies, DT-3's two rejected findability options, DT-8's two rejected fold policies — each compressed and cited to _design.md rather than re-litigated, and no rejection appears that the design did not take"
  satisfied: true
  evidence: "`implementation-report.md:216` §Rejection cross-walk — **five rows**, each naming the family, the atom's line and a `_design.md` line cite. (1) the declaration's form, `<atom>:82`, `_design.md:128-147`, five rejected: front matter, an HTML comment, a filename convention, a sidecar manifest, a badge or coloured admonition. (2) the need set, `<atom>:89`, `_design.md:188-202`, three rejected: a four-box taxonomy adopted literally, dropping the enumeration, a persona-keyed taxonomy. (3) findability, `<atom>:96`, `_design.md:231-237`, two rejected: routing as an implicit byproduct, a bespoke navigation widget. (4) the fold line, `<atom>:100`, `_design.md:275-280`, two rejected: reviewer judgement per page, a permanent ban on collapsible content. (5) the tree's home, `<atom>:107`, `_design.md:88-96`, four rejected: atoms in the code-standards tree, a hand-authored knowledge-base corpus, a subtree of the user documentation, nothing at all. Each carries the reason it lost in one clause, compressed from the design rather than re-derived, and the section is `## The alternatives that lost` at `<atom>:80`. **Walked by a NON-AUTHOR, as the criterion requires.** Walker: the **adversarial slice reviewer for `binding-beyond-this-project`**, the review pass that produced this slice's findings, which drafted no part of the atom, of this ledger or of the implementation report. Date: **2026-08-18**. Verdict transcribed verbatim: *five families, each mapping to a real design rejection at the cited ranges (`_design.md:88-95`, `:128-150`, `:188-202`, `:231-237`, `:275-280`), no sixth family invented, none attributed a reason the design does not state* — **PASS**, so every rejection in the atom maps to a row, every row maps to a rejection the design actually took, and EC-008 does not fire. Two of the walker's ranges read one boundary wider than the atom's own (`:88-95` against the atom's `:88-96`; `:128-150` against `:128-147`); the atom's are the tighter, verified ends of the same two rejection blocks, and the five families are identical either way. **What this replaced:** the first-merge capture of this walk was performed by the implementer, who drafted the atom, so it is not evidence for a criterion that names a non-author; it has been removed from this row and kept unedited at `implementation-report.md:216` under **What this replaced, and why**, exactly as the precedent one slice back did it (`reviewer-and-citation-procedures/_ledger.md:84`)."
  mount_point: ".kb/_intake/lesson-page-need-declaration-discipline.md"
  verifying_test: "§Rejection cross-walk — five rows, each naming the family, the atom's line and a .bklg/docs-that-teach/page-need-discipline/_design.md line cite; walked by a NON-AUTHOR who confirms every rejection in the atom maps to a row and that no sixth family was invented (.kb/playbooks/README.md:15-20)"

- id: AC-008
  criterion: "GIVEN a reader looking for what actually binds them, WHEN they search the atom for a commitment, THEN they find none: no must / shall / must not whose reversal would take a decision, and every binding claim appears instead as a cited RP-NN-N rule id whose text is not restated — so the enforceable half stays where the gate reads it and a commitment is not stripped of the immutability that makes it enforceable"
  satisfied: true
  evidence: "`implementation-report.md:248` §Commitment audit. **Half one, no commitment:** `rg -n '\\b(must|shall|MUST)\\b' <atom>` -> **exit 1, zero hits**, so there is no surviving hit to adjudicate and the atom carries no commitment at all. The binding half of the discipline stays where the gate reads it — the `RP-NN-N` rules in `standards/pages/`, read on every `cargo xtask ci` by the `every page declares one need` step. **Half two, the restatement audit, now run for real over ten cited ids.** `rg -n 'RP-[0-9]{2}-[0-9]' <atom>` -> seven lines carrying **ten** ids: `RP-00-1`, `RP-00-2` (`<atom>:52`); `RP-10-1`, `RP-10-4` (`:62-63`); `RP-40-1`, `RP-40-2` (`:76-77`); `RP-20-1`, `RP-20-2`, `RP-20-3`, `RP-20-4` (`:103-104`). Each is diffed against the shipped rule text in the report's ten-row table, and each is a **cite**: the atom names what the rule governs (`RP-00-1` \"the count\", `RP-20-2` \"closed class list\", `RP-40-2` \"the paraphrase spot check\") and reproduces none of its `Why.`, `Do`, `Not` or `Rejects.` prose. **The restatement this audit was written to catch:** at first merge the atom reproduced RP-00-2's own italicised reader's test verbatim from `standards/pages/00-one-need.md:52-53` and paraphrased its imperative beside it, while citing only a file path — so this clause of AC-008 had no id to run over and the breach was invisible. Both are gone; `rg -n 'read nothing but the region' <atom>` -> **exit 1**, and the paragraph now says only that the count and the position are cited rather than repeated (`<atom>:51-52`). **Anti-pattern 15 sweep**, the same one the rules tree is held to: `rg -in 'consider|use judgement|use judgment|as appropriate|if it seems' <atom>` -> **exit 1, no hit**, so no step of the described procedure hands back the judgement it exists to replace. Filing a commitment here would have stripped it of the immutability that makes it enforceable (`.kb/playbooks/README.md:29-31`; EC-007)."
  mount_point: ".kb/_intake/lesson-page-need-declaration-discipline.md"
  verifying_test: "rg -n '\\b(must|shall|MUST)\\b' .kb/_intake/lesson-page-need-declaration-discipline.md — transcript in §Commitment audit, adjudicated line by line (each surviving hit annotated as method, not commitment); every RP- id in the atom checked against the shipped standards/pages/ rule text for restatement (.kb/playbooks/README.md:29-31,37-38; project.md DR-11)"

- id: AC-009
  criterion: "GIVEN the practitioner outside this initiative applying the atom to their own corpus, WHEN they read it, THEN the subject is the shape — declare the answered need as a singular enumerated property; check the declaration mechanically; name the check's blind spot in its own documentation; supply a written non-author procedure for the judgement it cannot make — with happenstance as the cited instance and at least one recurrence outside this initiative named; and the atom describes standards/pages/ and the checker module as they stand at this story's HEAD, any divergence from _design.md being recorded rather than smoothed over"
  satisfied: true
  evidence: "`implementation-report.md:313` §Portability. **The subject is the shape**: `## What makes it portable` at `<atom>:113` states the transferable half as `a mechanical check that verifies a declaration, a written non-author procedure for the part it cannot verify, and the check's own documentation naming the seam between them`, and the four method steps at `<atom>:49-78` are the same four the criterion enumerates, written for any corpus of prose artefacts rather than for this one. **A recurrence outside this initiative is named**: RS-81-1 (`standards/rust/81-checks-that-cannot-be-types.md:11`), which states the pairing as a rule for **any** check, together with `kb-playbook-verify-referent-report-coverage-001`, where a check verified an address and reported its coverage instead of what it could not see — neither is about documentation. **Walked by a NON-AUTHOR, with the walker's identity recorded as the criterion requires.** Walker: the **adversarial slice reviewer for `binding-beyond-this-project`**, the review pass that produced this slice's findings, which drafted no part of the atom, of this ledger or of the implementation report. Date: **2026-08-18**. Verdict on `.kb/playbooks/README.md:33-35`, transcribed verbatim: *portability verdict PASSES — the subject is the shape, happenstance is the cited instance, and RS-81-1 (`standards/rust/81-checks-that-cannot-be-types.md:11`) is a genuine recurrence on a subject that is not documentation.* **What this replaced:** the first-merge verdict was reached by the implementer, who drafted the atom, so it is not evidence for a criterion that names a non-author and asks for the walker's identity; it has been removed from this row and kept unedited at `implementation-report.md:313` under **What this replaced, and why** (precedent: `reviewer-and-citation-procedures/_ledger.md:84`). **The atom describes the tree that shipped, and §Divergence (`implementation-report.md:342`) lists three divergences rather than stating none:** (1) the permitted-fold-mechanism list is a `| Mechanism |` table in `standards/pages/20-the-fold-line.md` with zero data rows and deliberately **not** a `const` — `xtask/src/lint_pages.rs` asserts its absence — where `_design.md` discusses it as a named list; (2) the router's link check is `xtask/src/lint_pages.rs`'s own deliberate copy sharing no code with `lint_constitution.rs`'s `check_router`, which is what `_design.md` cites as the pattern (RS-81-3); (3) the governed set excludes `README.md` (`xtask/src/lint_pages.rs:496`), which `_design.md` does not discuss and which the slice-mate observed. The atom describes the shipped form in each case. Anti-pattern 16: `rg -n 'NEEDS|orientation|tutorial|how-to|explanation|reference' <atom>` shows the atom names no token the shipped `NEEDS` does not carry, and mentions `reference` only as the token that was subtracted — the same thing band 10 does."
  mount_point: ".kb/_intake/lesson-page-need-declaration-discipline.md"
  verifying_test: "§Portability — a NON-AUTHOR performs the portability test of .kb/playbooks/README.md:33-35 and records the verdict, the named recurrence and their own identity; §Divergence lists every place the atom's description differs from _design.md, or states 'none observed' explicitly and never blank (.kb/governance/rewrite-the-referent-never-the-reasoning.md)"

- id: AC-010
  criterion: "GIVEN an initiative whose whole thesis is links that resolve, and an ingest that deletes this file on success (.kb/_intake/README.md:13-19), WHEN closeout clears _intake/, THEN nothing that outlives the file points at it and nothing was written into the checked knowledge base ahead of the wave: git diff main -- .kb ':!.kb/_intake' is empty, .kb/maps/domain-map.md is untouched, and the handoff travels as provenance in source_paths and _ledger.md rather than as a cross-tree hyperlink"
  satisfied: true
  evidence: "`implementation-report.md:412` §Mechanical negatives. `git diff --stat main -- .kb ':!.kb/_intake'` -> **exit 0, no output**, and the commit-scoped `git diff HEAD --stat -- .kb ':!.kb/_intake'` -> **exit 0, no output**: nothing landed in the checked knowledge base ahead of the wave, so EC-001's failure — the one this repository already made and reverted at `0269720` — did not recur. `git diff --stat main -- .kb/maps` and `git diff HEAD --stat -- .kb/maps` -> **exit 0, no output** on both bases: `.kb/maps/domain-map.md` is untouched, and the new domain area is appended during ingest's own Maps phase (`.kb/maps/README.md:34-36`). `rg -n '_intake/lesson-page-need' -g '!.bklg/**'` -> **exit 1, no hit**: no durable tree — not `standards/pages/README.md`, not `docs/README.md`, not a rule atom's Evidence list — links a path that a successful ingest deletes (EC-006). The handoff therefore travels as provenance: the atom's `source_paths` at `<atom>:24-32`, and this ledger plus `implementation-report.md:89` §Mount naming the staged file in full. `redkiln validate --kb` -> `redkiln: validate passed.` and `redkiln doctor 2>&1 | rg -c 'template-drift|differs SUBSTANTIVELY'` -> **6**, with `doctor found no problems` — **exactly six** template-drift advisories, so EC-009 does not fire and no template moved in this diff. **Those two green CLI runs prove this AC and not AC-003**: the validator skips `_`-prefixed directories by design (`.kb/_intake/README.md:21-27`). Base note: `main` predates this project's merged dependency stories in general, but **both bases were run here and both are empty**, because no earlier story in this project touched `.kb/` — so the spec's `main`-based commands are the ones recorded in `verifying_test`, with the commit-scoped pair captured beside them as the stronger base for a story-scoped promise (NF-004)."
  mount_point: ".kb/_intake/lesson-page-need-declaration-discipline.md"
  verifying_test: "§Mechanical negatives — git diff --stat main -- .kb ':!.kb/_intake' empty (and git diff HEAD --stat -- .kb ':!.kb/_intake' empty); git diff --stat main -- .kb/maps empty (and the HEAD-scoped equivalent empty); rg -n \"_intake/lesson-page-need\" -g '!.bklg/**' returns only <atom>; redkiln validate --kb && redkiln doctor green with exactly six template-drift advisories, labelled as proving THIS AC and not AC-003"

- id: AC-011
  criterion: "GIVEN the same atom opened in three places the corpus is actually read — a GitHub blob view, a plain-text pager, and an editor at 100 columns — WHEN it renders in each, THEN it is composed, not merely marked up: the corpus's playbook shape (an H1, ## section headings, run-in bold markers, monospace path:line citations), meaning that survives greyscale (no colour, icon, emoji or badge carries any distinction), no `> **Answers:**` declaration line — this is a .kb atom, not a governed page, and a declaration here would claim a governance the checker's pinned trees do not extend to it — and a body inside the measured corpus envelope: <= 8,192 bytes and <= 100 source columns outside tables"
  satisfied: true
  evidence: "`implementation-report.md:365` §Composition. **Heading inventory**, `rg -n '^#{1,2} ' <atom>`: `36:# Making the need a page answers a declared, singular, checkable property`, `38:## The situation`, `47:## The method, in four steps`, `80:## The alternatives that lost`, `113:## What makes it portable`, `123:## Where this is the wrong instrument` — one H1 and five `##` sections, in the corpus's own order (`summary` before the body, method before its rejected alternatives, stop conditions last), compared against `.kb/playbooks/landing-a-stricter-gate-without-a-red-baseline.md:32-98`'s section run. Hierarchy is carried by position, heading level, the `**Bold.**` run-in marker and monospace `path:line` citations — never by colour, icon or size. **Budgets, re-measured from scratch after the review pass, not carried forward:** `wc -c <atom>` -> **8161**, inside the 8,192 ceiling, against a corpus measured at 5,161-7,137 bytes (largest `.kb/playbooks/repairing-a-frozen-clause-without-amending-it.md` at 7,137); `awk '{if(length($0)>m)m=length($0)}END{print m}' <atom>` -> **91**, inside the 100 ceiling, against the three exemplar atoms' measured maximum of exactly 100. Both were **red first, and red twice**: the first draft measured 10,324 bytes and 101 columns; three compression passes brought it to 8,184 bytes / 92 columns, leaving 8 bytes of headroom; adding AC-008's ten `RP-NN-N` citations and removing RP-00-2's restated sentence then forced a fourth pass, which removed prose and no obligation and landed at 8,161 / 91 (`implementation-report.md:12` §TDD Evidence carries the four-column measurement table). **`rg -n '^> \\*\\*Answers:\\*\\*' <atom>` -> exit 1, no hit**: the deliberate absence of a governed-page declaration, because this is a `.kb` atom outside both pinned trees and a declaration here would claim a governance the checker does not extend to it. Greyscale: the atom carries no colour, icon, emoji or badge anywhere, so nothing is lost in a plain-text pager, in greyscale, or in print."
  mount_point: ".kb/_intake/lesson-page-need-declaration-discipline.md"
  verifying_test: "§Composition — the atom's heading inventory; wc -c <atom> <= 8192 against the measured corpus (six playbook atoms run 5,161-7,137 bytes, largest .kb/playbooks/repairing-a-frozen-clause-without-amending-it.md); awk '{if(length($0)>m)m=length($0)}END{print m}' <atom> <= 100 against the corpus maximum of 100; rg -n '^> \\*\\*Answers:\\*\\*' <atom> no hit; greyscale legibility confirmed by inspection (_design.md 'Density budget', 'Hierarchy', anti-patterns 2 and 3)"
```

## Where the two non-author walks stand

**Both have now been run by a non-author, and the author-run captures have been removed from the
rows.** AC-007's rejection cross-walk and AC-009's portability walk were both performed at first
merge by the implementer, who drafted the atom. AC-007 requires a walker who did not draft the atom
and AC-009 requires the walker's identity recorded here, so an author-run walk is not evidence for
either — the precedent one slice back is explicit about that
(`reviewer-and-citation-procedures/_ledger.md:84`, where an author-run calibration was struck from
the row and the report's residual paragraph kept as the audit trail).

**Walker for both: the adversarial slice reviewer for `binding-beyond-this-project`** — the review
pass that produced this slice's findings, which drafted no part of the atom, of `_ledger.md` or of
`implementation-report.md`. **Date: 2026-08-18.**

| walk | AC | verdict | recorded at |
| --- | --- | --- | --- |
| rejection cross-walk | AC-007 | **PASS** — five families, each mapping to a real design rejection at the cited ranges; no sixth family invented; none attributed a reason the design does not state | `implementation-report.md:216` §Rejection cross-walk |
| portability walk (`.kb/playbooks/README.md:33-35`) | AC-009 | **PASS** — the subject is the shape, happenstance is the cited instance, and RS-81-1 is a genuine recurrence on a subject that is not documentation | `implementation-report.md:313` §Portability |

The superseded author-run captures are kept unedited in those two report sections, under
**What this replaced, and why**, so the state each row was in stays auditable.

## What the review pass changed in the atom itself

The same pass found AC-008 unmet in a way no transcript in this ledger could have caught. The atom
cited `standards/pages/*.md` **paths** but not one `RP-NN-N` **id**, so AC-008's second clause —
*every `RP-` id in the atom checked against the shipped rule text for restatement* — had nothing to
run over, and it was hiding a genuine restatement: RP-00-2's own italicised reader's test
(`standards/pages/00-one-need.md:52-53`) reproduced verbatim in the atom's step 1, with RP-00-2's
imperative paraphrased beside it. Ten ids are now cited and none is restated; the audit is at
`implementation-report.md:248` §Commitment audit, and it cost a fourth compression pass, so AC-011's
two budgets were re-measured from scratch rather than carried forward.

> The machine-readable acceptance block that used to sit here has been **moved up**,
> to immediately below this file's opening prose. Nothing in it changed — not a row,
> not a criterion, not a byte of evidence. `redkiln verify --grain story` locates the
> block by scanning from the `# … ledger` heading for the first fence and **abandons
> the search the moment it meets another heading** (`ledgerBlock`, redkiln 0.19.0
> `dist/index.js:14761-14779`), so the two sections standing between the heading and
> the fence made this ledger read as *having no acceptance block at all* rather than
> as having a malformed one. It read that way from `dc58e80`, this story's own
> checkpoint, and nothing before the story gate looked. The narrative sections below
> are the story's evidence and keep their order; only the fenced block moved.
