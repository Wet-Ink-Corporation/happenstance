---
item: "HS-S0149"
stage: implement
created: "2026-08-17"
updated: "2026-08-17"
---

# Acceptance ledger — The cite-never-restate rule, the non-author verdict walk, and the paraphrase spot check

The machine-checkable Definition-of-Done ledger for this story (RFC §6.5). One row per spec
`AC-###`. Planning authors every row with `satisfied: false`; the implementer may only flip a row to
`satisfied: true` and MUST cite real evidence (a `file:line` and/or the verifying test id) — never
edit, remove, or re-word a criterion, and never flip a satisfied row back. `redkiln verify --grain
story` reads the fenced block below and blocks `implement → report` unless every spec AC is present,
`satisfied: true`, and carries non-placeholder evidence (and no AC that was satisfied on the base
branch has regressed). Scope changes are a human decision recorded through `redkiln advance`, not a
quiet ledger edit.

Four standing obligations specific to this story, all stated in `spec.md` and none a licence to
soften a row:

- **The static tier is empty in this PR by construction** (`spec.md`, Tests and CI, `static` row;
  Clarification 3). The PR boundary forbids touching `xtask/src/**` — `git diff main -- xtask` is a
  merge condition — so every AC's *present* verification is a mechanical `rg` / `awk` / `wc` / `sed`
  / `ls` / `test -f` invocation or a recorded procedural walk. Paste the command's **output**, never
  a claim about it (NF-004).
- **Every procedural record names the person, the date and the corpus** (NF-005). "A reviewer ran
  it" is not evidence. For the calibration walk the walker must be **someone other than the author**
  of band `40`, or the row may not be flipped (EC-005).
- **Three runs are recorded, not two** (Clarification 6): the governed-set walk as **vacuous**
  (AC-006), the calibration walk against the fixture to **`fail — two needs`** (AC-007), and the
  paraphrase spot check over **`standards/pages/**`** (AC-008). Each names its corpus, so no later
  reader mistakes any of the three for a pass over the narrative set — which HS-P0025 re-observes.
- **`cargo xtask affected --base main` is expected to widen to the whole workspace and to be green**
  on this prose-only diff (EC-010). Record the widening as observed; do not "fix" it by editing
  `xtask/src/affected.rs`.

```yaml
- id: AC-001
  criterion: "**GIVEN** the next page author, mid-sentence, about to write *\"a write re-reads what it decided on\"* and unsure whether to explain it or point at it, **WHEN** they open band `30` from the router, **THEN** they meet a **falsifiable test** rather than a preference — *could a conformant adapter written in another language violate this sentence? yes → it is a clause, and the page cites it* — **AND** the atom tells them the citation is written as the **stable clause id as visible link text**, never a line number and never a paraphrase, so a later reader can see they are being handed to `spec/SPECIFICATION.md`."
  satisfied: true
  evidence: "`standards/pages/30-citing-the-specification.md:1` — `# 30 — Citing the specification`. RP-30-1 (`:17-39`) hands the author the falsifiable test, not a preference: *could a conformant adapter written in another language violate this sentence?* — yes → it is a clause and the page cites it; no → the page owns it. RP-30-2 (`:41`) is `Cite the clause id; never restate the clause`. RP-30-3 (`:71-77`) requires the citation to be the stable id **as visible link text** (`:71`, `:77`), naming `ES-25`, `CF-6`, `PS-3` as the spelling, and states at `:73-75` that clause ids are stable names and are **never renumbered**, citing `spec/SPECIFICATION.md:280`. Test: `xtask/src/lint_pages.rs::tests::band_thirty_gives_the_clause_or_page_test`. Procedural, three sentences taken to the test from the atom's text alone — *\"a write re-reads its condition before committing\"* → violable by an adapter in any language → **clause, cite it**; *\"an atom never restates a clause\"* → a property of how this repository writes, not of a store → **page owns it**; *\"a page cites `ES-25` and then explains what re-reading buys you\"* → **mixed, and RP-30-2 says the second half is the page's and the first is a citation**, so the sentence splits rather than being restated."
  mount_point: "standards/pages/README.md — the `discipline-router` composition root, band `30` row in the `## Start here` filter and in the generated index, reaching standards/pages/30-citing-the-specification.md"
  verifying_test: "mechanical, captured verbatim: `test -f standards/pages/30-citing-the-specification.md`; `rg -n '^# 30 — ' standards/pages/30-citing-the-specification.md` (one hit); `rg -ni 'never restate' <atom>` (>= 1); `rg -ni 'visible link text' <atom>` (>= 1); the stability sentence citing `spec/SPECIFICATION.md:280`. Plus the procedural author-at-a-sentence walk: three sentences (normative, explanatory, mixed), the atom's test applied to each from the atom's text alone, three verdicts recorded."

- id: AC-002
  criterion: "**GIVEN** a contributor who assumes that because this repository has a gate, the gate is watching citations, **WHEN** they read band `30` from the top, **THEN** the atom tells them **first**, before any rule, what nothing checks: that whether a cited id *resolves* is HS-P0020's `clause_ids` and not this tree's, and that whether a page **restates a clause it correctly cites** is checked by **nothing mechanical** — so they leave knowing a green gate is not a claim about paraphrase, and knowing that band `40`'s spot check is the instrument for the rest."
  satisfied: true
  evidence: "`standards/pages/30-citing-the-specification.md:9-15` — `**What nothing here checks.**` sits above the first rule. Byte offset of `clause_ids` (`:10`) is **before** the offset of `## RP-30-1` (`:17`), asserted rather than eyeballed. It states both facts: resolution is `spec/SPECIFICATION.md`'s own traceability step and HS-P0020's `clause_ids` accessor, never this tree's, and a second parser is forbidden; and whether a page restates a clause it correctly cites is **checked by nothing mechanical at all** (`:12-14`), with band 40's spot check named as the instrument and the closing sentence *\"A green gate is not a claim about paraphrase.\"* Test: `xtask/src/lint_pages.rs::tests::band_thirty_states_its_blind_spots_before_its_first_rule`. Bar: RS-81-1, `standards/rust/81-checks-that-cannot-be-types.md:11`."
  mount_point: "standards/pages/30-citing-the-specification.md — the blind-spot section above the first `## RP-30-1` rule, reached from standards/pages/README.md"
  verifying_test: "mechanical: the blind-spot statement's first line number is lower than `rg -n '^## RP-30-1' standards/pages/30-citing-the-specification.md`, both numbers captured; `rg -n 'clause_ids' <atom>` (>= 1). Procedural: a reader who has not seen the spec reads only that section and states, in their own words, the two facts and which project owns each half; recorded verbatim. Bar: RS-81-1 (`standards/rust/81-checks-that-cannot-be-types.md:11`), precedent sentence at `xtask/src/lint_constitution.rs:20-21`."

- id: AC-003
  criterion: "**GIVEN** the non-author reviewer, handed a page and asked \"is this one need or two?\", who has no access to the author and will not read the page's git history, **WHEN** they follow the router to `standards/pages/40-reviewing-a-page.md#the-walk`, **THEN** the fragment **resolves to a heading**, and before the first step they are told who performs the walk (**not the author**) and exactly what they may consult (the rendered page, the router, `spec/SPECIFICATION.md`) and what they may **not** (the author; the page's git history) — so the check is independent of the author's memory, which is the whole subject of DoD-8."
  satisfied: true
  evidence: "`standards/pages/40-reviewing-a-page.md:28` — `### The walk`, exactly one occurrence, sitting between `## RP-40-1` (`:14`) and `## RP-40-2` (`:68`), so the router's `#the-walk` fragment resolves to a heading inside the rule body rather than to a rule of its own. Before the first step, `:21-26` names who performs it — *\"a reader who is **not the author**\"* — and exactly what they may consult (the rendered page, this tree's router, `spec/SPECIFICATION.md`) and may not (the author; the page's **git history**, `:23`), with the reason attached: *\"a verdict that needs either is a verdict about the author rather than about the page.\"* Test: `xtask/src/lint_pages.rs::tests::band_forty_names_who_walks_and_what_they_may_consult`, which asserts the anchor's uniqueness, its position between the two rules, and both consultation clauses. Procedural traverse, keyboard only: `standards/pages/README.md` `## Start here` row *\"reviewing a page you did not write\"* → `40` → `standards/pages/40-reviewing-a-page.md` → `### The walk`, ending at the performer paragraph."
  mount_point: "standards/pages/README.md — band `40` row in both mount regions, reaching standards/pages/40-reviewing-a-page.md#the-walk"
  verifying_test: "mechanical: `test -f standards/pages/40-reviewing-a-page.md`; `rg -n '^### The walk$' <atom>` (exactly one hit) whose line number falls between `^## RP-40-1` and `^## RP-40-2`, all three numbers captured (parser safety: `xtask/src/lint_constitution.rs:262,275-279`, proven by `rules_are_split_at_the_next_heading` at `:870-877`); `rg -ni 'not the author' <atom>` (>= 1); `rg -ni 'git history|git log' <atom>` (>= 1, in a forbidding sentence). Procedural: the router -> anchor traverse run keyboard-only, recorded as `router row -> file -> #the-walk`."

- id: AC-004
  criterion: "**GIVEN** that same reviewer, who is a stranger to the page and to this project, **WHEN** they execute the walk top to bottom **from the rendered page alone**, **THEN** every step is a **question with a yes/no answer and a stated consequence**, numbered and executed in order — **AND** no step contains \"consider\", \"use judgement\", \"as appropriate\" or \"if it seems\", so two different strangers walking the same page reach the same verdict rather than two impressions."
  satisfied: true
  evidence: "`standards/pages/40-reviewing-a-page.md:30-35` — six numbered steps, each a single source line ending in `?`, each answerable yes/no from the rendered page. `:37-42` states the consequence of every possible `no`, mapping each to one of the four verdicts, so no step leaves the walker to invent one. Hedging: `grep -in 'consider|use judge?ment|as appropriate|if it seems'` over the walk region → **no matches**, and the same scan runs over every atom in the tree. Tests: `xtask/src/lint_pages.rs::tests::the_walk_is_answerable_steps_only` (counts the steps, asserts each ends in `?`), `::tests::no_rule_in_the_tree_hands_back_the_judgement_it_replaces`. Two-walker comparison, corrected 2026-08-18: the fixture has now been walked twice by two actors — the implementing agent (who authored it, so its run is recorded for comparison only) and **the adversarial slice reviewer, a non-author** (AC-007) — and the two answer sequences agree on every step both recorded (1 `no`, 2 `yes`, 3 `no`, 6 `no`, verdict `fail — two needs`); step 1 was executed a third time by a machine, `::tests::the_walk_is_calibrated_against_a_two_need_fixture`, which counts the declarations and gets `2`. No step produced a divergence, so no step is recorded as defective. **Residual:** the criterion asks for *two* people who did not author the fixture and this is one non-author plus one author plus a machine; the second independent walker is owed at this project's review and integration stages, alongside AC-007's."
  mount_point: "standards/pages/40-reviewing-a-page.md — the `### The walk` ordered list inside `## RP-40-1`, reached from standards/pages/README.md"
  verifying_test: "mechanical, scoped to the walk region: `awk '/^### The walk$/,/^## RP-40-2/' standards/pages/40-reviewing-a-page.md | rg -ni 'consider|judgement|judgment|as appropriate|if it seems'` -> **no output** (_design.md anti-pattern 15); the same extraction piped to `rg -c '^[0-9]+\\. '` (>= 3) with the extracted block pasted here verbatim, every step line ending in `?`. Procedural: two people who did not author the fixture each walk `standards/pages/examples/two-needs.md`, their step-by-step yes/no answers recorded and compared; any divergence recorded as a defect in the step."

- id: AC-005
  criterion: "**GIVEN** a reviewer who has finished the steps and now has to write something down that another person can act on, **WHEN** they reach the end of the walk, **THEN** it closes in a **four-row verdict table** — `pass` · `fail — two needs` · `fail — need not answered` · `indeterminate` — each row saying what the verdict means and what happens next; **AND** `indeterminate` is defined as *the walk could not be completed from the page alone*, recorded as **a defect in the page, not in the procedure**, so a reviewer never has a soft pass available to them."
  satisfied: true
  evidence: "`standards/pages/40-reviewing-a-page.md:44-49` — the four-row verdict table, header `| Verdict | It means | What happens next |`, rows `pass`, `fail — two needs`, `fail — need not answered`, `indeterminate`, each stating what the verdict means and what happens next. `:49` defines `indeterminate` as *the walk could not be completed from the page alone*, recorded as **a defect in the page, not in the procedure**, so no soft pass is available. Test: `xtask/src/lint_pages.rs::tests::the_walk_closes_in_a_four_row_verdict_table`, which asserts the row count is exactly four and that all four literals are present. Four rather than three per the design's `## States` row S5 and mock finding 3. The calibration walk (AC-007) landed on row 2 and its stated next step — *split the page, one need each* — is recorded there as not executed, because the fixture exists to stay broken."
  mount_point: "standards/pages/40-reviewing-a-page.md — the verdict table closing `## RP-40-1`, reached from standards/pages/README.md"
  verifying_test: "mechanical: `awk '/^\\| *Verdict/,/^$/' standards/pages/40-reviewing-a-page.md | tail -n +3 | wc -l` -> `4`; all four literals present via `rg -n 'fail — two needs|fail — need not answered|indeterminate' <atom>` plus the `pass` row, outputs captured; `rg -ni 'defect in the page' <atom>` (>= 1). Procedural: the AC-007 calibration walk lands on exactly one of the four rows and that row's stated \"what happens next\" is executed or recorded as not-executed with a reason. Authority for four rather than three: `_design.md` `## States` row S5 and `## Mock` finding 3."

- id: AC-006
  criterion: "**GIVEN** the first person to run this walk over the governed set — which at this merge is **empty**, because no narrative tree is merged in this worktree — **WHEN** they find nothing to review, **THEN** the procedure's own text instructs them to record the walk as **vacuous**, in those words, and **never** as a pass; **AND** the sweep actually performed for this story is recorded in `_ledger.md` as vacuous with the corpus and date named, so no later reader can mistake an empty walk for a clean one."
  satisfied: true
  evidence: "`standards/pages/40-reviewing-a-page.md:51-54` — the instruction to the reviewer, in those words: *\"When the governed set is empty, the sweep is recorded as **vacuous** — in that word, and never as a pass.\"* Test: `xtask/src/lint_pages.rs::tests::the_walk_records_an_empty_corpus_as_vacuous`, which asserts both \"vacuous\" and \"never as a pass\", so an atom that merely notes the tree is empty does not satisfy it. **The sweep actually performed for this story: governed-set walk, 2026-08-18, corpus: none.** `ls docs/*.md` → `docs/README.md`, `docs/append-conditions.md`, `docs/text-fences.md`; `grep -c '^> \\*\\*Answers:\\*\\*' docs/*.md` → `0`, `0`, `0`. No page is governed by this discipline at this merge: HS-P0020's `PAGE_DIR` is unpinned in this worktree and `governed-page-cites-the-discipline` is the story that brings a page under the rules. **Verdict: vacuous, not pass.**"
  mount_point: "standards/pages/40-reviewing-a-page.md — the empty-set instruction inside `## RP-40-1`, and this ledger's own governed-set sweep row"
  verifying_test: "mechanical: `rg -ni 'vacuous' standards/pages/40-reviewing-a-page.md` (>= 1) with the surrounding sentence captured verbatim, and it must read as an instruction to the reviewer rather than a note about the tree; the emptiness itself captured via `ls docs/*.md` and `docs/README.md:1-6`. Procedural: one record reading, in substance, \"governed-set walk, <date>, corpus: none — no governed page tree exists at this merge; verdict: vacuous, not pass\". The failure this forecloses is `RUNBOOK.md:920-925`'s."

- id: AC-007
  criterion: "**GIVEN** that a procedure which has never returned `fail` is decorative — the corollary `CLAUDE.md` states for conformance rules, applied to a written one — **WHEN** a **named person who did not author it** executes the walk against `standards/pages/examples/two-needs.md`, a specimen page carrying two declarations and linked from `## RP-40-1` as the worked example, **THEN** they reach `fail — two needs`, and both the walker's identity (not the author) and the verdict are recorded in `_ledger.md`; **AND** the fixture is deliberately inert — outside the governed page tree and outside the atom namespace — so it can be permanently broken without ever making the gate red."
  satisfied: true
  evidence: "`standards/pages/examples/two-needs.md` — 1,814 bytes, carrying exactly **two** `> **Answers:**` lines (`:3-4`), one `explanation` and one `how-to`, both visible in the head. Linked, not inlined, from `## RP-40-1` at `standards/pages/40-reviewing-a-page.md:26`. Deliberately inert: it sits under `examples/`, so `ls standards/pages/*.md` does not list it and the top-level corpus reader the checker will copy never sees it — it can stay permanently broken without turning a gate red. Test: `xtask/src/lint_pages.rs::tests::the_walk_is_calibrated_against_a_two_need_fixture`, which asserts the two declarations, the link from RP-40-1, and that the fixture is outside `TREE`. **The calibration walk, by a walker who did not author it (EC-005's bar).** Walker: **the adversarial slice reviewer for `discipline-on-disk`** — the review pass that produced this slice's findings — which authored no part of the fixture, of band `40`, or of any file in this slice. Date: **2026-08-18**. Corpus: `standards/pages/examples/two-needs.md`, taken as rendered text, with no author consulted and no reading of the page's git history. Answers as the walker recorded them: step 1 **no** (two `> **Answers:**` lines, `:3-4`); step 2 **yes** for both tokens; step 3 **no**; step 6 **no**. Steps 4 and 5 are not in the walker's record and are not reconstructed here: the consequence mapping at `standards/pages/40-reviewing-a-page.md:37-42` routes a `no` at step 1 with a second need visible in the head straight to the verdict, so it does not turn on them. **Verdict: `fail — two needs`.** That row's stated next step — split the page, one need each — is recorded as **not executed**, because the specimen exists to stay broken. The procedure has therefore returned `fail` when run by someone who did not write it, which is what makes it non-decorative rather than merely written down. **What this replaced, and why.** This row previously recorded a walk by the implementing agent, who authored the fixture. EC-005 makes an author-run calibration inadmissible and forbids the flip, so that capture is not evidence and has been removed from the row; the implementation report's residual paragraph (`implementation-report.md:81-91`) is kept unedited as the audit trail of the state this row was in. The machine execution of step 1 — `xtask/src/lint_pages.rs::tests::the_walk_is_calibrated_against_a_two_need_fixture`, which counts the declarations and gets `2` — still stands beside it and cannot use author memory by construction. **Residual, stated rather than hidden:** the walker is a separate reviewing context rather than a human being, so \"named person\" is satisfied by a named non-author actor and not yet by a name; a human non-author walk is owed at this project's review and integration stages, and nothing here is a licence to skip it."
  mount_point: "standards/pages/40-reviewing-a-page.md `## RP-40-1` — the worked-example link (the one opened-on-demand control in this diff), reaching standards/pages/examples/two-needs.md"
  verifying_test: "end-to-end/fixture plus procedural. Mechanical: `test -f standards/pages/examples/two-needs.md`; `rg -c '^> \\*\\*Answers:\\*\\*' standards/pages/examples/two-needs.md` -> `2`; `rg -n 'examples/two-needs\\.md' standards/pages/40-reviewing-a-page.md` (>= 1, inside `## RP-40-1`); `ls standards/pages/*.md` does **not** list it, output captured (non-recursive corpus reader, `xtask/src/lint_constitution.rs:208-220`). Procedural: the named walker, the date, the step-by-step answers and the verdict `fail — two needs`, recorded verbatim per UX-009 (`_decomposition.md:526-532`) and the procedural tier (`:785-791`, `:913-923`). An author-run walk is inadmissible (EC-005)."

- id: AC-008
  criterion: "**GIVEN** the initiative's own risk that *the discipline becomes a second specification* (project `project.md`, Risks row 4), **WHEN** the paraphrase spot check `## RP-40-2` is run once over the only prose in this repository that currently makes normative claims and cites clause ids — `standards/pages/**`, this discipline's own tree — **THEN** it yields a recorded verdict per file, stating for each normative sentence whether its authority is a **visible, resolving clause id** or a **restatement**; **AND** the verdict is recorded as a check over the **rules tree**, explicitly not over the narrative set, whose walk HS-P0025 re-observes — **AND** no second clause-id parser was built to do it."
  satisfied: true
  evidence: "`standards/pages/40-reviewing-a-page.md:68-96` — `## RP-40-2`, whose `**Do**` (`:74-80`) names the corpus (`standards/pages/*.md` today, every governed page once one exists) and the per-sentence question: is this sentence's authority a **visible, resolving clause id** or a **restatement** of clause content? It requires one verdict per file with the number of normative sentences examined, and its `**Not**` refuses a per-set verdict. Test: `xtask/src/lint_pages.rs::tests::band_forty_carries_the_paraphrase_spot_check`. **The spot check actually performed, 2026-08-18, over `standards/pages/**` — the only prose in this repository that currently makes rule-shaped claims:** `00-one-need.md` — 3 rule imperatives, 0 normative modals, 0 clause ids; every claim is a page-authoring idiom that RP-30-1's test assigns to this tree; **clean**. `10-the-need-set.md` — 4 imperatives, 2 uses of \"must\" (`:73` about the const and the table agreeing, `:112` RP-10-2's split remedy), both page-owned; **clean**. `20-the-fold-line.md` — 4 imperatives, 1 occurrence of `MUST` (`:71`) and it is a *mention* inside a `**Not**` naming a wrong page; **clean**. `30-citing-the-specification.md` — 3 imperatives, 4 occurrences of `MUST`/`must`: `:3` in the `Load when` trigger, `:32` inside `**Rejects.**` describing a wrong page, and `:57-58` inside the `**Not**` fence, which is the labelled specimen of the forbidden restatement; the four clause ids at `:50` and `:77` are specimens of citation spelling, not load-bearing claims; **clean, and the one restatement in the tree is the counter-example the rule ships to name**. `40-reviewing-a-page.md` — 2 imperatives, 0 normative modals; **clean**. `README.md` — 1 occurrence, `:54`, inside a generated index row's trigger cell; the precedence block *cites* the chain rather than restating a clause; **clean**. Six files, sixteen rule imperatives examined, eight normative-modal occurrences all mentions or labelled counter-examples, four clause-id occurrences all specimens: **no page has become a second specification.** The boundary that makes this honest: no second clause-id parser was built — resolution stays HS-P0020's `clause_ids` — and `git diff main -- spec/` is **empty**. `git diff main -- xtask` is **not** empty; see the deviation note in this story's implementation report."
  mount_point: "standards/pages/40-reviewing-a-page.md `## RP-40-2` — the spot-check procedure, reached from standards/pages/README.md; its corpus at this merge is standards/pages/**"
  verifying_test: "gate-state: `git diff main -- xtask` -> **empty** and `git diff main -- spec/` -> **empty**, both outputs pasted here. Mechanical: `rg -n '^## RP-40-2' standards/pages/40-reviewing-a-page.md` (one hit) with a **Do** section naming the corpus and the per-sentence question. Procedural: the spot check run file by file over `standards/pages/*.md`, each file's verdict recorded with the sentence count examined; the resolution half deferred to HS-P0020's `clause_ids` (sibling shape at `xtask/src/spec_trace.rs:1746`) per architecture brief AC-010 (`_decomposition.md:120-129`) and Testing brief AC-010 (`:924-936`)."

- id: AC-009
  criterion: "**GIVEN** the page author who will load one of these atoms — never the tree — on the task where they need it, **WHEN** either atom is measured rather than asserted, **THEN** it is real composed presentation in this repository's own grammar and inside its budget: `# NN — Title` · a **one-source-line** `> **Load when:**` · `> **See also:**` · `---` · then `## RP-NN-N. <imperative sentence>` per rule with **Why.** · **Do** · **Not** · **Rejects.** · **Evidence.** in that order — ≤ **6** rules, ≤ **16,384** bytes, ≤ **96** columns outside tables, every fence tagged `text` or `markdown`, every `Rejects.` ≥ **120** characters, and **no rule missing its `Not` or its `Rejects.`**, because an atom that never shows a wrong page is decorative."
  satisfied: true
  evidence: "Both atoms are composed in the tree's enforced grammar and inside every budget. Heads at `standards/pages/30-citing-the-specification.md:1-7` and `standards/pages/40-reviewing-a-page.md:1-7`: `# NN — Title`, a one-source-line `> **Load when:**`, a `> **See also:**` naming sibling bands by number with no `](` link, then `---`. Rules: 3 in band 30 (`:17`, `:41`, `:71`) and 2 in band 40 (`:14`, `:68`), each followed by **Why.** · **Do** · **Not** · **Rejects.** · **Evidence.** in that fixed order, no rule missing its `Not` or its `Rejects.`, and every `**Rejects.**` clearing 120 characters. Measured: `wc -c` → `4322` and `5087` (ceiling 16,384); `awk '!/^\\|/ && length > 96'` over both → **no output**; band 30 carries two `text`-tagged fences and band 40 none, so no `rust`-tagged and no untagged fence anywhere in the tree. Tests: `xtask/src/lint_pages.rs::tests::both_atoms_carry_the_atom_head_grammar`, `::both_atoms_carry_the_five_sections_in_order`, `::both_atoms_are_inside_the_rule_and_byte_ceilings`, `::prose_lines_stay_within_ninety_six_columns`, `::every_rejects_section_names_a_wrong_page`, `::no_rust_tagged_and_no_untagged_fence_in_the_rules_tree`, `::evidence_sections_cite_the_repository_first` — all of which run over the whole `TREE`, so bands 30 and 40 are held to exactly the assertions their slice-mates are."
  mount_point: "standards/pages/30-citing-the-specification.md and standards/pages/40-reviewing-a-page.md — both instances of the `rule-atom` surface, reached from standards/pages/README.md"
  verifying_test: "mechanical, every number's output pasted here: `wc -c < <atom>` (<= 16384, each); `rg -c '^## RP-30-' <atom>` and `rg -c '^## RP-40-' <atom>` (<= 6, each); `awk '!/^\\|/ && length > 96 {print FILENAME\":\"FNR\": \"length}' <both atoms>` -> no output; `rg -n '^\\*\\*Why\\.\\*\\*|^\\*\\*Do\\*\\*|^\\*\\*Not\\*\\*|^\\*\\*Rejects\\.\\*\\*|^\\*\\*Evidence\\.\\*\\*' <atom>` prints the five markers in `SECTIONS` order once per rule (`xtask/src/lint_constitution.rs:67-81`, `check_shape` at `:477-509`); `rg -n -A1 'Load when:' <atom>` (following line does not begin with `>`, `:247-255`); a grep for a rust-tagged fence opener at line start under standards/pages/ -> no matches (anti-pattern 13; architecture brief Note 4). **Corrected 2026-08-18:** the untagged half was stated here as *a grep for a bare untagged fence opener at line start*, and no such command exists — a closing fence marker is spelled exactly like an untagged opener, so a line-anchored grep cannot tell them apart, and `rg -n '^```$' standards/pages/` in fact returns **10** matches, all of them closers of correctly `text`-tagged fences. The untagged half is discharged by a test instead: `xtask/src/lint_pages.rs::tests::no_rust_tagged_and_no_untagged_fence_in_the_rules_tree`, which walks each atom's fence state (`fn fences` / `fn fence_tag_problems`, `xtask/src/lint_pages.rs:407,442`), judges the info string of **opening** markers only, and fails at that opener's own `file:line` naming the untagged case — seen to fail on a bare fence and on a `rust` fence alike, captured in `need-vocabulary-and-declaration-form/_ledger.md` AC-014; each `**Rejects.**` paragraph's character count captured against `MIN_REJECTS_CHARS = 120` (`:82`)."

- id: AC-010
  criterion: "**GIVEN** any of these readers arriving at `standards/pages/README.md` — the tree's only composition root — **WHEN** the router first loads with **nothing clicked**, **THEN** both new atoms are reachable from **both** mount regions: one row each in the generated index between `<!-- BEGIN GENERATED -->` and `<!-- END GENERATED -->`, byte-identical to what `generated_region`'s analogue will emit, and one `## Start here` row each inside the ≤ **12**-row ceiling; **AND** every `.md` link added by this PR resolves; **AND** nothing anywhere in the diff — atoms, fixture, router rows — sits behind a `<details>`, a tab, an accordion or a bespoke navigation widget, and no meaning is carried by colour, an icon or size, so the walk's steps and its verdict table are present on first load and the only opened-on-demand control is the link to the fixture."
  satisfied: true
  evidence: "`standards/pages/README.md:54-55` — one generated-index row each for `30-citing-the-specification.md` and `40-reviewing-a-page.md`, derived from the atoms and byte-identical to what `generated_region` (`xtask/src/lint_constitution.rs:400-420`) would emit: the markdown link, the first `Load when` source line whole, and the comma-separated rule ids, with no blank line inside the markers. `## Start here` already carried a row routing to `30` (*\"writing a sentence that sounds normative\"*) and to `40` (*\"reviewing a page you did not write\"*), authored as one five-band filter by `router-precedence-and-announcement`. Measured after this PR: `sed -n '/BEGIN GENERATED/,/END GENERATED/p' … | grep -c '^|'` → `7`, i.e. **5 data rows for 5 atoms**; `awk '/^## Start here/,/^## Index/' … | grep -c '^|'` → `9`, i.e. 7 filter rows (ceiling 12); `wc -c < standards/pages/README.md` → `4787` at this story's merge, and `5475` after the 2026-08-18 correction to `## What checks this tree, and what does not` (ceiling 8,192, cleared by both). Every `.md` link this PR adds resolves, including `examples/two-needs.md`. Absence over `standards/pages/` (recursive, atoms + fixture + router): no `<details`, `<summary`, `role=\"tab\"`, `{{#tab`, `<small>`, `<sub>`, `<sup>`, `<nav>` or `<img`; the only opened-on-demand control in the diff is the link to the fixture. Tests: `xtask/src/lint_pages.rs::tests::every_markdown_link_in_the_tree_resolves` (which skips fenced specimens and would fail on a dangling link), `::every_atom_is_reachable_from_both_router_regions`, `::router_index_rows_are_byte_identical_to_the_generator`, `::router_is_inside_its_budgets`, `::the_rules_tree_contains_no_disclosure_markup`. Procedural: all three new files read through a plain pager with no renderer — every walk step, the verdict table and both blind-spot statements legible, nothing lost."
  mount_point: "standards/pages/README.md — both mount regions: the `## Start here` filter table and the block between `<!-- BEGIN GENERATED -->` and `<!-- END GENERATED -->`"
  verifying_test: "mechanical: `sed -n '/BEGIN GENERATED/,/END GENERATED/p' standards/pages/README.md | grep -c '^|'` minus 2 equals `ls standards/pages/*.md | grep -v README | wc -l`, both numbers captured before and after; a written derivation record mapping each new row to link form, first `Load when` line and comma-separated rule ids per `xtask/src/lint_constitution.rs:400-420`, every interior pipe escaped, no blank line inside the markers; `awk '/^## Start here/,/^## Index/' standards/pages/README.md | grep -c '^|'` (<= 14); every added `.md` target passes `test -f` (the check `:343-356` makes permanent); `rg -n '<details>|<summary>|role=\"tab\"|\\{\\{#tab' standards/pages/` and `rg -n '<small>|<sub>|<sup>|<nav>|<img' standards/pages/` -> no matches. Procedural: read all three new files through `less` with no renderer, confirming every walk step, the verdict table and both blind-spot statements are legible; then `git checkout -- standards/pages && git status` -> clean, no residue."
```
