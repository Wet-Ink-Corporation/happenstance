---
item: "HS-S0189"
stage: implement
created: "2026-08-17T13:16:36.424Z"
updated: "2026-08-17T13:16:36.424Z"
---

# Acceptance ledger — Every fence inventoried and every clause citation audited

The machine-checkable Definition-of-Done ledger for this story (RFC §6.5). One row per spec
`AC-###`. Planning authors every row with `satisfied: false`; the implementer may only flip a row to
`satisfied: true` and MUST cite real evidence (a `file:line` and/or the verifying test id) — never
edit, remove, or re-word a criterion, and never flip a satisfied row back. `redkiln verify --grain
story` reads the fenced block below and blocks `implement → report` unless every spec AC is present,
`satisfied: true`, and carries non-placeholder evidence (and no AC that was satisfied on the base
branch has regressed). Scope changes are a human decision recorded through `redkiln advance`, not a
quiet ledger edit.

```yaml
- id: AC-001
  criterion: "GIVEN Persona 1 has read this project's material across all four surfaces and is deciding whether the code on the page is code that still works, WHEN the reviewer walks .bklg/docs-that-teach/application-author-path/fence-inventory-and-clause-audit/_inventory.md § Fences, THEN every fenced block on crates/happenstance/src/lib.rs, on the three pages in HS-P0020's pinned tree and in examples/course-subscriptions/src/overview.md carries one row with: its info string verbatim, its execution class (executed / compiled-only / prose), the named mechanism that exercises it, whether that mechanism can fail if the fence opts out, and its hidden-line classification read off the rendered page rather than the source — AND the count of rows opted out (ignore, no_run, untagged-treated-as-prose, or named in IGNORE_ALLOWANCES) is zero, with the enumeration taken from _design.md's ## Surfaces block (:45-65) so a designed-but-unauthored page appears as a missing row rather than as an absence nobody counted."
  satisfied: true
  evidence: "_inventory.md § Fences carries 11 fence rows plus 2 zero-fence rows across all five files, each with its info string verbatim, its execution class, the named exerciser and the failability column: F-01 crates/happenstance/src/lib.rs:26 (empty info string, executed as the doctest `crates\\happenstance\\src\\lib.rs - (line 247)`, failability NO); F-02..F-09 docs/first-encounter.md:16,37,55,78,96,123,141,148; F-10..F-11 docs/carry-your-invariant.md:60,102. Opted-out count is ZERO in all four senses — `ignore` 0, `no_run` 0, untagged 0, IGNORE_ALLOWANCES 0 — with IGNORE_ALLOWANCES itself the empty slice at xtask/src/lint_narrative.rs:304. Hidden-line column read off the DOM: cargo doc -p happenstance --no-deps renders 35 lines against 37 source lines, the two absent being lib.rs:31 and :63, both inside the permitted set of _design.md:554; the other four surfaces carry zero. Enumeration taken from _design.md's ## Surfaces block (:45-65), not a directory listing. Substrate confirmed first (_inventory.md § Substrate confirmation): both narrative steps in REQUIRED at xtask/src/main.rs:571 and :614, so EC-001 did not fire. cargo xtask ci --fast → `all required checks passed`."
  mount_point: "xtask/src/narrative.rs"
  verifying_test: ".bklg/docs-that-teach/application-author-path/fence-inventory-and-clause-audit/_inventory.md § Fences, re-derived by `cargo xtask ci --fast` (xtask/src/main.rs:143-155 \"tests\" + HS-P0020's two narrative steps) and `cargo doc -p happenstance --no-deps` for the rendered hidden-line read"

- id: AC-002
  criterion: "GIVEN the inventory claims zero fences opted out, WHEN one fence on a page under HS-P0020's tree is retagged `ignore` and the narrative checker is run, THEN it fails with a problem line naming that path:line, and WHEN the edit is reverted the gate returns green; AND WHEN the same retag is applied to the fence in crates/happenstance/src/lib.rs, THEN the gate stays green — the expected asymmetry — and _inventory.md § Drills records that the crate-root fence is protected by review only (_design.md:880-882, anti-pattern 1 at :836-838) with the coverage gap routed to HS-P0020 by name."
  satisfied: true
  evidence: "_inventory.md § Drills. D-1: docs/carry-your-invariant.md:60 retagged `rust` -> `ignore`; `cargo xtask narrative` failed with the exact problem line `docs/carry-your-invariant.md:60 — an `ignore` fence needs an `IGNORE_ALLOWANCES` entry naming it; …` and `cargo xtask lints` failed under the same banner; reverted; `git hash-object` back to b63697589f05c4a1663ef3754a5d96c59b4b6298, `git status --porcelain` empty, `cargo xtask narrative` → `5 pages, all consistent`. D-2: crates/happenstance/src/lib.rs:26 retagged to `ignore`; FOUR instruments observed GREEN — `cargo xtask lints` exit 0, `cargo test -p happenstance --doc` `7 passed; 1 ignored` exit 0, `cargo test -p happenstance --tests` 16 binaries ok, `cargo test -p xtask --tests` ok; recorded as observed green, not skipped; the crate-root doctest named as `(line 247)`; reverted to 05c69822bc1512bbea295cdea33400d7a240318b with `git status --porcelain` empty and `8 passed; 0 ignored` afterwards. The bridge page was chosen for D-1 over first-encounter deliberately, because xtask/tests/first_encounter.rs:488 gives that page a second instrument and the drill could not then attribute the failure. Coverage gap routed by name as _inventory.md § Routing R-2 → HS-P0020, with the review-only protection cited at _design.md:872-874 and :917-918."
  mount_point: "xtask/src/narrative.rs"
  verifying_test: ".bklg/docs-that-teach/application-author-path/fence-inventory-and-clause-audit/_inventory.md § Drills — drills D-1 and D-2, each run via `cargo xtask lints` (.redkiln/config.yaml:48) and `cargo xtask ci --fast`, with the exact edit, the pasted `path:line — message`, the revert and `git status` clean"

- id: AC-003
  criterion: "GIVEN Persona 1 meets a sentence on one of these pages that states a rule they are expected to obey, WHEN they follow its citation, THEN they land on a clause that exists — and _citations.md § Claims carries one row per normative claim with the page and location, the cited clause id, that clause's maturity marker verbatim from spec/SPECIFICATION.md, and the named resolver for that page: HS-P0020's checker via clause_ids(root) for tree pages, and nothing for crates/happenstance/src/lib.rs, recorded as such rather than covered by another page's mechanism. ES-25 and CF-7 are cited as [FROZEN]; VT-30 is cited as [PROVISIONAL] in wording that would not need rewriting if VT-30's shape changed (_design.md:400-404). Every citation is an ordinary inline link in last position — never a tooltip, popover or fold (_design.md:526)."
  satisfied: true
  evidence: "_citations.md § Claims carries nine rows, C-1..C-9, one per normative claim: docs/first-encounter.md:41-43 ES-8, :82-84 ES-26, :127-130 ES-25; docs/carry-your-invariant.md:36-40 ES-27, :86-88 ES-25, :90-93 VT-30, :134-137 CF-8, :139-142 CF-7, :144-146 ES-27. Each carries the maturity marker verbatim — `**[FROZEN]**` at spec/SPECIFICATION.md:2787 (ES-8), :3764 (ES-25), :3820 (ES-26), :3861 (ES-27), index rows :9211/:9212 (CF-7/CF-8), and `[PROVISIONAL — falsified if no adapter can push a multi-guard condition into a single round trip]` at :1869 (VT-30) — read against both the clause body and the maturity index, which agree. The resolver is named PER PAGE in its own table: HS-P0020's checker via clause_ids for the three tree pages, and NOTHING for crates/happenstance/src/lib.rs and examples/course-subscriptions/src/overview.md, recorded as such rather than covered by another page's mechanism. Those two cite no clause, and that is a result: all 14 + 3 + 4 candidate sentences answered `no` to RP-30-1's falsification question (standards/pages/30-citing-the-specification.md:20-26). All nine link fragments were resolved by slugging spec/SPECIFICATION.md's headings — nine of nine land, CF-7/CF-8 on §6.2 at :7663 because they are bold paragraphs and not headings. VT-30's wording passes _design.md:400-405 (`described but is not settled … so nothing above leans on it`). Every citation is an ordinary inline markdown link, never a tooltip, popover or fold; C-6 and C-8 sit at the end of the claim clause rather than the sentence and are recorded with their reason rather than repaired. Line anchors read from merge-forward-preflight/_baseline.md § Anchors (:99-104); ES-8 and CF-8 are new anchors re-derived with the same `git grep -nF` form. cargo xtask spec-trace green (REQUIRED, xtask/src/main.rs:369)."
  mount_point: "xtask/src/narrative.rs"
  verifying_test: ".bklg/docs-that-teach/application-author-path/fence-inventory-and-clause-audit/_citations.md § Claims, re-derived by `cargo xtask spec-trace` (REQUIRED, xtask/src/main.rs:315) plus HS-P0020's `every narrative page is checked` step; line anchors read from merge-forward-preflight/spec.md:261"

- id: AC-004
  criterion: "GIVEN the audit claims every cited clause id resolves, WHEN one cited id on a tree page is retagged to an id spec/SPECIFICATION.md does not define and the checker is run, THEN it fails with `<page>:<line> — cites ES-999, which SPECIFICATION.md does not define`, and reverting returns it to green; AND WHEN the same retag is applied to a citation on the crate root, THEN nothing fails — spec_trace reads only spec/SPECIFICATION.md and a relative markdown link is not an intra-doc link, so broken_intra_doc_links does not see it — and that gap is recorded and routed to HS-P0020 rather than left as an implied coverage."
  satisfied: true
  evidence: "_citations.md § Drills. D-3: docs/first-encounter.md:130 retagged ES-25 -> ES-999; `cargo xtask narrative` failed with the exact predicted line `docs/first-encounter.md:130 — cites `ES-999`, which SPECIFICATION.md does not define` (predicted at checked-documentation-surface/_design.md:337, implemented at xtask/src/lint_narrative.rs:1231); on the SAME broken tree `cargo xtask spec-trace` exited 0 with `traceability: no problems found`, which is the measurement proving spec-trace never opens a narrative page; reverted to 63073630c1c167e022193a989bfc6c3b1b5c32ea, `git status --porcelain` empty, `cargo xtask narrative` → `5 pages, all consistent`. D-4: `ES-999` injected into crates/happenstance/src/lib.rs:86 by replacing an existing line (so doc_budget.rs:122's MODULE_DOC_LINES=130 could not fire and disguise the result); FOUR instruments observed GREEN — `cargo xtask lints` exit 0, `cargo xtask spec-trace` exit 0, `RUSTDOCFLAGS=-D warnings cargo doc -p happenstance --no-deps` exit 0, `cargo test -p happenstance --tests` 16 binaries ok; recorded as observed green, not skipped; reverted to 05c69822bc1512bbea295cdea33400d7a240318b with `git status --porcelain` empty. Gap routed by name as _inventory.md § Routing R-5 → HS-P0020."
  mount_point: "xtask/src/narrative.rs"
  verifying_test: ".bklg/docs-that-teach/application-author-path/fence-inventory-and-clause-audit/_citations.md § Drills — drills D-3 and D-4, run via `cargo xtask spec-trace` and `cargo xtask lints`, with the exact edit, the pasted problem line, the revert and `git status` clean"

- id: AC-005
  criterion: "GIVEN Persona 1 could reasonably follow a teaching page's wording instead of the clause it points at, WHEN the restatement audit runs, THEN the mechanical pass finds zero sentences containing MUST or MUST NOT that are not a link to a clause id — anti-pattern 11 (_design.md:867-868) — across all four surfaces, AND a reviewer who did not author the pages applies HS-P0021's written spot check (page-need-discipline/project.md:236-238, non-author-performable per :233-235) and records a per-page verdict that no clause is restated in the page's own words. No second restatement procedure is invented."
  satisfied: true
  evidence: "_citations.md § Restatement, two passes and no third procedure invented. Pass 1, the mechanical sweep for anti-pattern 11 (_design.md:903-905): `git grep -n \"MUST\" -- crates/happenstance/src/lib.rs docs/first-encounter.md docs/carry-your-invariant.md docs/read-the-worked-example.md examples/course-subscriptions/src/overview.md` returns NO OUTPUT and exits 1 — zero sentences containing MUST or MUST NOT on any of the four surfaces, which is stricter than the anti-pattern asks. Pass 2, HS-P0021's own RP-40-2 spot check executed verbatim from standards/pages/40-reviewing-a-page.md:68-90, with the per-file verdict and normative-sentence count that procedure's named wrong output requires: crates/happenstance/src/lib.rs 0 of 14 candidates normative, docs/first-encounter.md 3 (all cited), docs/carry-your-invariant.md 6 (all cited), docs/read-the-worked-example.md 0 of 3, examples/course-subscriptions/src/overview.md 0 of 4 — nine normative sentences, nine resolving clause ids, ZERO restatements, verdict `no paraphrase` on every file. The two closest calls (docs/first-encounter.md:82-84 against ES-26, and :127-130 against ES-25) are quoted verbatim with the reasoning that carried each, rather than recorded as ticks. Walker: this story's implementation context, which authored none of the four surfaces (they are HS-S0185/0186/0187/0188's at 9493276, cc9c4a4, b24d2cd, 5805623) — the non-author constraint at page-need-discipline/project.md:233-235; sources consulted and not consulted are recorded in § Restatement. The tier-5 human sign-off that closes this is the project's review gate, and this record is what it reads."
  mount_point: "xtask/src/narrative.rs"
  verifying_test: ".bklg/docs-that-teach/application-author-path/fence-inventory-and-clause-audit/_citations.md § Restatement — the pasted MUST/MUST NOT sweep with its command and empty output, plus HS-P0021's spot check walked by a non-author reviewer with a verdict per page (testing brief tier 5, _decomposition.md:486)"

- id: AC-006
  criterion: "GIVEN the four page stories' output is already reviewed content and the design behind it is signed off (_design.md:1039), WHEN this audit finds a defect, THEN it is closed one of exactly two ways and never a third: an in-place one-line repair (a restated MUST becomes a clause-id link; an info string is corrected; a missing #[cfg(doctest)] mod line is added to xtask/src/narrative.rs) after which the page still renders and still satisfies the signed-off composition — #main-content details.top-doc > div.docblock resolves, fences stay within 68 columns and 24 rendered lines (32 on crate-root-encounter alone), ## headings within 22 characters, paragraphs within 435 characters (_design.md:532-620), no fence behind a fold, no control outside rustdoc's own chrome, no `use happenstance_core::` import line, no diagram, every fence's output block non-empty (anti-patterns 1, 2, 3, 8, 13, 14, 15) — OR a routed finding with a named destination and no silent absorption; AND _design.md, project.md, _storymap.md and _decomposition.md are byte-identical at the checkpoint, the tree carries no drill residue, IGNORE_ALLOWANCES names none of this project's fences, and if HS-P0020's two narrative steps are absent from REQUIRED the story halts and routes rather than substituting a hand count (project.md:345-350)."
  satisfied: true
  evidence: "The audit found NO defect requiring an in-place repair on any of the four surfaces, so every finding took the second of the two permitted routes: _inventory.md § Routing carries SEVEN rows, R-1..R-7, each with a named destination and none absorbed — R-1 `no_run` accepted silently by the checker (xtask/src/lint_narrative.rs:823) → HS-P0020; R-2 the crate root's opt-out caught by nothing → HS-P0020; R-3 the crate-root doctest named `(line 247)` in a 242-line file → support/inherited-documentation-defects; R-4 overview.md's bin-only package contributes no doctest → HS-P0020; R-5 a crate-root citation resolved by nothing → HS-P0020; R-6 the three crate-root density overages → HS-B0001, already open; R-7 docs/append-conditions.md:13's happenstance_core import → HS-P0020, out of subject. EC-003 was never reached: all eleven fences compile and execute, so no exemption, no IGNORE_ALLOWANCES entry and no relaxation to `ignore` was written — IGNORE_ALLOWANCES is still the empty slice. EC-001 did not fire (both narrative steps in REQUIRED, xtask/src/main.rs:571, :614), so the story did not halt and no hand count was substituted. Composition preserved: `git diff --stat` on _design.md, project.md, _storymap.md and _decomposition.md is EMPTY; `git status --porcelain` clean with all three drilled files byte-identical to their pre-drill hashes; `#main-content details.top-doc > div.docblock` resolves on the built docs; ZERO literal [bracket] pairs in the crate root's rendered docblock against the four _design.md:951-956 measured pre-merge; every paragraph within 435 rendered characters on all five files; zero <details>, zero images, zero affordances, zero `use happenstance_core::` in any fence. The three crate-root overages (35 lines / 70 columns / two headings) are HS-B0001's, re-measured and confirmed unchanged rather than re-owned. `cargo xtask affected --base main` → `affected gate passed`; `cargo xtask ci --fast` → `all required checks passed (--fast: 4 optional step(s) not run)`."
  mount_point: "xtask/src/narrative.rs"
  verifying_test: "`cargo xtask affected --base main` (.redkiln/config.yaml:40) green plus `cargo xtask ci --fast` (:55); `git diff --stat` empty on the four planning artifacts and `git status` clean; `cargo doc -p happenstance --no-deps` and the REQUIRED `documentation` step's RUSTDOCFLAGS=-D warnings build for the render and bracket checks; .bklg/docs-that-teach/application-author-path/fence-inventory-and-clause-audit/_inventory.md § Routing"
```
