---
item: "HS-S0143"
stage: report
created: "2026-08-17T13:16:05.099Z"
updated: "2026-08-17T13:16:05.099Z"
---

# Report — The frozen documentation MUSTs are enumerated by clause id in one place

## Findings Ledger

Eight ACs, all satisfied by reachable behaviour with a `file:line` citation and a test or a
recorded gate run behind each. Nothing stubbed, nothing skipped, nothing deferred out of this
story's own set. Four findings below are not AC rows — they are what the re-derivation turned
up, and they are the most important thing in this report.

| Finding | Evidence | Follow-up |
| ------- | -------- | --------- |
| **AC-001 — one enumeration, every candidate disposed of, the rule stated where the reader meets it, and no written count.** Twenty-one entries: eight pinned with a clause id, a discharge-site path and a verbatim anchor; thirteen excluded with a one-line reason on the entry. | `xtask/src/lint_narrative.rs:1321-1483`, rule at `:1296-1320`; `_rederivation.md`; `::tests::every_pin_entry_is_disposed_with_a_site_and_an_anchor`, `::tests::the_pin_holds_no_written_count` | None. The written count test went red twice on this story's own prose — once on a line number's digits, once on the comment quoting the very defect it warns against — and was strengthened rather than relaxed. |
| **AC-002 — every pinned id resolves through the shared set, once per run, and a failure names the pin rather than a page.** | `:1638-1647`, `:1795-1805`; `::tests::a_pinned_id_absent_from_the_specification_is_a_problem` (red: `got: []`), `::tests::the_specification_is_resolved_once_per_run` | None. `check_pin` takes `&Clauses`, not a `&Path`, which is what makes the one-call-per-run contract structural rather than a review note. |
| **AC-003 — the path moved and the discharging text moved are two problems with two sentences.** | `:1655-1662`, `:1708-1735`; `::tests::a_missing_anchor_and_a_missing_site_are_two_different_problems` (red: left 0 right 2), `::tests::the_whole_pin_holds_against_the_real_tree` | None. The real-tree test is the one that fails on the day a `happenstance-core` doc comment un-discharges a `[FROZEN]` clause, which is the whole point of the story. |
| **AC-004 — reflow-insensitive, and whole-identifier everywhere a clause id is matched inside text.** | `:1486-1511` (`normalised`, applied to both sides), `:1514-1526` (`names_clause`, copied from `lints.rs:492-500`); `::tests::the_anchor_match_survives_a_reflowed_doc_comment`, `::tests::a_short_clause_id_does_not_match_a_longer_one` | None. Resolution and the census are set membership, which is exact by construction; the one in-text match — `guard_pin`'s anchor-equals-its-id refusal — goes through `names_clause`. No bare `contains` on a clause id survives the diff. |
| **AC-005 — the failure names which side moved, in two sentences that share no phrase.** The derived side scans clause bodies with whitespace collapsed first, which is what finds `PS-34`'s obligation, wrapped across `spec/SPECIFICATION.md:5546-5547` and invisible to a line scan. | `:1670-1700`, `:1552-1580`, `:1258-1266`; `::tests::an_unclassified_candidate_says_the_document_grew`, `::tests::an_entry_that_is_no_longer_a_candidate_says_the_enumeration_is_stale` (asserts the *absence* of the other's wording, RS-81-5) | This is what turns the unmerged sibling branch's residual risk into a gate failure on the merge-forward commit rather than an HS-P0025 closeout re-check. |
| **AC-006 — every problem in one run, in place, never truncated, and the green surface unchanged.** | `:1805`, printed at `:1814-1817`; `::tests::every_pin_problem_is_reported_not_just_the_first`, `::tests::the_pin_never_truncates_its_problem_list`; observed failing and passing runs of `cargo xtask narrative` | None. `xtask/src/main.rs` is unchanged in the diff: no new step, banner, subcommand, `print_help` line, `REQUIRED` entry or `lint_steps` member. |
| **AC-007 — location first, inside the 48-column prefix, no ANSI and no box-drawing.** | `::tests::every_pin_problem_line_begins_with_its_artifact`, which drives **all five** conditions from the pin array and the candidate scan rather than from five literals | None. The test went red at `left: 4 right: 5` because one condition was unreachable from the pin it was given; the fix was to drive the fifth properly, not to lower the number. |
| **AC-008 — three limits, before the pin, and nothing claiming a discharge is correct.** | `:81-99` inside the module's existing `# What this does not verify` (`:10`, still first); `::tests::the_module_states_the_pins_limits_before_the_pin`, `::tests::no_pin_message_claims_a_discharge_is_correct` | The six-item *contents* of the limits section remain `documented-blind-spots-and-their-proofs`'. Milestone 2's `nothing_in_the_module_claims_a_page_teaches` caught two drafting slips in this story's prose, which is why they are not in the diff. |
| **Finding — the re-derivation disagrees with the expected set, and the re-derivation wins.** Kept: ES-19, ES-23, ES-24, VT-15, VT-17. Dropped: **VT-3** (its correction is stated under a heading reading "Prose, not a clause" — it words no documentation MUST and is not a candidate), **ES-17** (`[PROVISIONAL]`, likewise not a candidate), **ES-40** (`[PROVISIONAL]`). Added: **VT-13**, **VT-32**, **VT-33**, each `[FROZEN]`, on the contract's own documentation, discharged today, and none of them written down anywhere before. | `_rederivation.md`; `spec/SPECIFICATION.md:681-688` (VT-3), `:3290-3295` (ES-17), `:4358` (ES-40), `:1075-1082`, `:1191-1200`, `:1242-1250` | The spec anticipated this in its own clarification 4 — *"if the re-derivation disagrees, the re-derivation wins and the record says so"*. ES-40 and ES-17 become pinned entries the day their clauses freeze; both anchors were located and are recorded in `_rederivation.md` so that is a one-line change. |
| **Finding — three `[FROZEN]` documentation MUSTs on the contract's own documentation are not discharged at all: ES-26, PS-31, PS-36.** ES-26 requires the inclusive/exclusive asymmetry to be documented *as deliberate*; both halves are documented separately and nothing says it is deliberate. PS-31 and PS-36 require `projection.rs` to say two things it does not say. | `spec/SPECIFICATION.md:3753-3759`, `:5480-5482`, `:5601-5603`; `crates/happenstance-core/src/store.rs:118`, `append.rs:135`, `projection.rs` (whole file) | **Handed to HS-P0023.** They are excluded with that reason rather than pinned, because pinning would land the gate red and the only repair is a `happenstance-core` doc-comment edit — that story's boundary, under `.kb/governance/rewrite-the-referent-never-the-reasoning.md`, and the spec's own implementation note says exactly this. |
| **Finding — the "nine" was never a count of clauses.** Nine = eight evidence-table rows + ES-19, but two of those rows are comment repairs rather than clause discharges and one row carries two clause ids. Neither document says which number it is counting. | `RUNBOOK.md:3830-3845`; `references/evaluation/phase-4-5-reconciliation.md:119-142`; `_rederivation.md` | None. That ambiguity is the defect BR-10 exists to prevent one level up, and it is now impossible: the count is the array's length and nothing restates it. |
| **Deviation — the specification's bytes are read twice per gate run.** The **parse** is one: `clause_ids` is called once and the set is shared. The bytes are read twice — once inside `clause_ids`, once by `check` for the candidate scan, which needs the text and not the ids. | `xtask/src/lint_narrative.rs:1795-1797`; NF-001 | Narrowing it needs a second narrow accessor in `xtask/src/spec_trace.rs`, which is outside this story's one permitted edit to that file (EC-009). Cost is one 567 KB read inside the noise floor of the step. |
| **Deviation — the derived scan needed a declaration-position decision, and `parse_clauses` is private.** `declared_clause` decides position only; **resolution stays `clause_ids`' set membership**, so no fourth family list exists. The shape is `spec_trace::clause_id`'s, copied rather than shared (Note 8). | `:1535-1550`; measured over the real document: `declared=200 ids=200 missing=[] extra=[]` | None. The equality with the parser is a measurement rather than an argument, so a divergence would surface rather than drift. |
| **Behaviour decided at implementation.** An entry whose clause id the document no longer declares is reported **once**, by assertion 1, and skipped by the census. Two messages for one defect, the second blaming the enumeration for a clause renumbered underneath it, is the opposite of what RS-81-5 asks the two census sentences to distinguish. | `:1685-1692` | Reviewer decision, if any. Recorded so the choice is visible rather than discovered. |
| **Nothing outside the boundary moved.** No `spec/SPECIFICATION.md` edit, no `happenstance-core` doc comment, no page, no `xtask/src/main.rs`, no manifest entry, no shared helper. `xtask/src/spec_trace.rs` needed no edit at all — the slice-mate had already deleted the foundation's marker. | `cargo xtask spec-trace` unchanged; `cargo xtask lint-constitution` → `27 atoms, all consistent`; `git status` clean after a failing run | None. No conformance rule, no port change, no changelog obligation, no ADR owed. |

**What is advanced.** Initiative DoD scenario **11** — *"The frozen documentation MUSTs are
still discharged. The pinned set of clause ids is enumerated in one place, and the
specification cross-reference step passes over the tree as it stands"* — turns from an
assertion into a check at this commit, and the closing criterion at `initiative.md:662-663`
with it. What the pin proves is that each discharge is **present**; that it is **adequate** is a
review question this step does not ask, and the module says so first.
