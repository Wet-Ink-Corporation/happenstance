---
item: "HS-S0142"
stage: report
created: "2026-08-17T13:16:04.504Z"
updated: "2026-08-17T13:16:04.504Z"
---

# Report — A clause id a page cites either resolves or fails the gate

## Findings Ledger

Eight ACs, all satisfied by reachable behaviour with a `file:line` citation and a test or a
recorded gate run behind each. Nothing stubbed, nothing skipped, nothing deferred out of this
story's own set. Two deviations are recorded below as findings rather than buried in a note.

| Finding | Evidence | Follow-up |
| ------- | -------- | --------- |
| **AC-001 — resolution goes through the parser, once per run, and the foundation's marker is deleted here.** One `Clauses::new(clause_ids(root)?)` in `check`, before the page loop; the families are derived from that set's own ids by `split_once('-')`, never listed. | `xtask/src/lint_narrative.rs:1271`, `:988-1001`; `::tests::the_recognised_families_are_derived_from_the_resolved_set` (a fictional `ZZ-1` makes `ZZ-9` a *dangling id*, which a literal six-prefix list cannot produce; red: `got: []`), `::tests::the_specification_is_resolved_once_per_run`, `::tests::the_resolvers_dead_code_marker_is_gone` | None. The one-call-per-run contract is now a text assertion rather than a review note, so a second call site fails a test rather than a reading. |
| **AC-002 — a resolving citation passes, silently, and the green surface is still one line.** | `:1177-1181`; `::tests::a_citation_naming_a_declared_clause_is_not_a_problem`; observed `  1 pages, all consistent` from `cargo xtask narrative`, with the fixture page's `(ES-40)` in the tree | None. Provenance is checked, not narrated. |
| **AC-003 — a dangling id names the page, the line and the id, in the design's own drawn form.** | `:1182-1185`, composed by `check_page` at `:1211-1214`; `::tests::a_dangling_id_names_the_page_the_line_and_the_id` asserts the whole line character for character and the ≤ 48-character location prefix; observed in the recorded failing run | None. This is the falsifier the story exists to close: *a page cites a clause id that does not exist, and the gate passes*. |
| **AC-004 — never a silent skip, and the three wordings stay apart.** An undeclared family is reported with the families the specification does declare; a near-miss inside a declared family is reported as one the checker cannot read. | `:1152-1170`, `:1186-1193`; `::tests::a_citation_shaped_token_in_an_undeclared_family_is_a_problem` (asserts all six families are listed), `::tests::a_near_miss_inside_a_declared_family_is_a_problem` (asserts the cannot-read wording *and* the absence of the does-not-define wording) | None. Both forms were observed side by side in one recorded run, which is where their distinctness actually matters. |
| **AC-005 — both boundaries, and correct prose stays green.** `RS-81-1`, `RS-00-1`, `ADR-0001`, `ADR-0029`, `HS-S0142` and `HS-P0020` all report nothing; the token each predicate protects is named in a comment beside it. | `:1049-1058` (leading), `:1112-1116` (trailing), `:1094-1105` (the shape `HS-S0142` fails); four tests, of which `::tests::the_boundaries_are_both_load_bearing` is the one that stops the other three being satisfied by an always-empty check | None. The real fixture page carries `HS-P0021` on line 3 and passes, so the carve-out is exercised by the tree as well as by the tests. |
| **AC-006 — every problem, source order, count last, fences included, one page read.** | `:1146-1195`, accumulating onto the module's existing `Vec<String>` ahead of its `bail!` at `:1300`; `::tests::five_dangling_ids_on_one_page_report_five_problems_in_source_order`, `::tests::a_clause_id_inside_a_fence_is_still_checked`, `::tests::the_citation_check_consumes_the_page_text_already_read` | None. Excluding fenced regions would have put a hiding place inside the one region that *is* the checked artifact; the cost — a clause-shaped token that is genuinely data — is stated as a limit and has no allowance list. |
| **AC-007 — the artifact that broke is the one named.** The resolver runs first, so its error can never arrive with a page's name attached; the page read is `?`-propagated and names the page. | `:1266-1272`, `:418-420`, `:396`; `::tests::an_unreadable_specification_is_propagated_not_swallowed` asserts the chain contains `reading spec/SPECIFICATION.md` **and no `docs` at all** | See the deviation two rows below for how the page half is asserted. |
| **AC-008 — the limits land in the existing section, before the guarantee, and nothing claims teaching.** Four bullets: a resolving citation says nothing about the sentence above it nor about deferral-versus-restatement; a fenced token is not told apart from prose; an undeclared-family near-miss is silent; a doubly-declared id collapses upstream. | `:58-80` inside the module's existing `# What this does not verify` (`:10`, still the first heading); `::tests::the_citation_check_documents_its_limits_before_its_guarantee`, plus the pre-existing `::tests::the_module_states_its_limits_first` and `::tests::nothing_in_the_module_claims_a_page_teaches`, both still green | The six-item *contents* of the limits section remain `documented-blind-spots-and-their-proofs`'. This story wrote only the limits its own check creates. |
| **Deviation — the unreadable-*page* assertion is made at the read seam plus a source match, not by making a present page unreadable.** `xtask` carries no `tempfile` dev-dependency and DR-12's standing trade says why, so the test drives `read_page` against a page that is not there (asserting `reading docs/no-such-page.md`) and asserts the production source calls `read_page(root, &rel)?` — the `let Ok(text) = … else { continue }` being the rejected shape. | `::tests::an_unreadable_page_is_a_hard_error_naming_the_page`; the text-matching half is `lint_constitution.rs:424-425`'s in-house pattern | Reviewer decision, if any: the alternative was adding a temp-directory dependency to the gate's own tool crate, which every `cargo xtask ci` would then build. Recorded so the trade is visible rather than discovered. |
| **Deviation — the existing `walk` test helper now resolves a hand-built six-family set instead of nothing.** An empty resolution is not neutral: with no declared families every citation classifies as an undeclared family, which turned the two fixture-page tests red. `ES-40` is in the set, which is what keeps those tests meaning what they meant. | `xtask/src/lint_narrative.rs`, `mod tests`, `DECLARED` and `walk`; the two tests are `the_wrapped_fixture_page_fails_by_file_and_line` and `the_same_fixture_page_without_its_wrapper_is_clean` | None. No assertion was weakened; two call sites gained an argument. The set is hand-built rather than read from `spec/SPECIFICATION.md`, per the testing brief's instruction not to build a parallel fixture corpus. |
| **Nothing outside the boundary moved.** No page content, no `xtask/src/main.rs`, no new step or banner, no `spec/SPECIFICATION.md` edit, no manifest entry, no refactor of the neighbouring checkers. The only edit to `spec_trace.rs` is a deletion. | `cargo xtask spec-trace` byte-identical to the pre-commit run; `cargo xtask lint-constitution` → `27 atoms, all consistent`; `cargo xtask ci --fast` → `all required checks passed` | None. No conformance rule, no port change, so CF-29's changelog obligation is not triggered; no `[FROZEN]` clause is edited, so no ADR is owed. |

**What is advanced, and what is not.** Initiative DoD scenario 12's *mechanical* half — each
normative claim a teaching page makes is a citation that resolves — is now checked rather than
asserted. The spot check that a page *defers* to a clause rather than restating it stays
HS-P0021's, is written into this module's limits as its first entry, and this story must not be
read as evidence of it.
