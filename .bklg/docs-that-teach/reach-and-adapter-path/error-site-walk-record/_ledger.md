---
item: HS-S0157
stage: implement
created: "2026-08-17"
updated: "2026-08-17"
---

# Acceptance ledger — The observed error-site walk

The machine-checkable Definition-of-Done ledger for this story (RFC §6.5). One row per spec
`AC-###`. Planning authors every row with `satisfied: false`; the implementer may only flip a row to
`satisfied: true` and MUST cite real evidence (a `file:line` and/or the verifying test id) — never
edit, remove, or re-word a criterion, and never flip a satisfied row back. `redkiln verify --grain
story` reads the fenced block below and blocks `implement → report` unless every spec AC is present,
`satisfied: true`, and carries non-placeholder evidence (and no AC that was satisfied on the base
branch has regressed). Scope changes are a human decision recorded through `redkiln advance`, not a
quiet ledger edit.

**Note on evidence in this story.** Nothing in `cargo xtask ci` can observe a walk
(`_design.md`, `## Density budget`, gap 2). Evidence here is therefore a `file:line` into
`walk-record.md` plus, where one exists, the re-runnable command named in `verifying_test` —
never a green gate, which proves only that this story disturbed nothing.

```yaml
- id: AC-001
  criterion: "GIVEN Persona 2 has just hit the two-flavour collision and searches the string rustc printed, WHEN the walker reproduces it themselves at the pinned toolchain rather than quoting the rewrite back at itself, THEN walk-record.md carries a reproduction stanza with the verbatim scratch source, the exact cargo invocation, `rustc -V` as printed, and the whole `error[E0034]` stderr including both `= note:` candidate lines and `TraitVariantBlanketType` — and no file under `crates/` is added by this PR."
  satisfied: true
  evidence: "Reproduction stanza at walk-record.md:94-176 — verbatim scratch source (`Cargo.toml` :105-115, `rust-toolchain.toml` :119-122, `src/main.rs` :126-134), the exact invocation `cargo build` (:138-140), `rustc -V` as printed, `rustc 1.97.1 (8bab26f4f 2026-07-14)` (:144-146), and the whole stderr pasted un-reflowed (:152-173) carrying both `= note:` candidate lines (:159-160) and `TraitVariantBlanketType` (:160). Produced by a real `cargo build` of a scratch crate outside the repository, not quoted from store.rs. Mechanical: `rg -cF 'TraitVariantBlanketType' walk-record.md` = 5; `rg -cF '= note:' walk-record.md` = 6 (>= 2); `git status --porcelain` shows the story touches only .bklg/, no file under crates/."
  mount_point: ".bklg/docs-that-teach/reach-and-adapter-path/project.md"
  verifying_test: "Re-run of the reproduction stanza in .bklg/docs-that-teach/reach-and-adapter-path/error-site-walk-record/walk-record.md at rust-toolchain.toml's channel 1.97.1, plus `rg -F 'TraitVariantBlanketType' .bklg/docs-that-teach/reach-and-adapter-path/error-site-walk-record/walk-record.md`, `rg -cF '= note:'` on the same file, and `git diff --name-only main...HEAD -- crates/` returning empty."

- id: AC-002
  criterion: "GIVEN the project may not stand in for HS-P0024's non-insider instrument, WHEN the record makes its claim, THEN it names the walker, states in one sentence that they authored neither `store-error-site-rewrite` nor `adapter-reasoning-account` and what they knew of the surface beforehand, and carries an explicit bounded-claim line saying this is evidence that the path exists and not that a stranger finds it — with no sentence anywhere in the record implying otherwise."
  satisfied: true
  evidence: "walk-record.md:36-60 names the walker (Claude Opus 5 running as HS-S0157's implementation context) and states in one sentence that it authored neither `store-error-site-rewrite` (f2c7dbe, 76e9424) nor `adapter-reasoning-account` (af9a241) and what it knew beforehand. The bounded-claim line is walk-record.md:309-315, in the entry body and not a footnote: 'It does not claim that a stranger finds it… That claim needs a different instrument and belongs to comprehension-evidence (HS-P0024).' Authorship cross-check run: `git log --format='%an %ae' -- crates/happenstance-core/src/store.rs` lists only Ryan Britton. The cross-check's weakness — both dependency commits carry a `Co-Authored-By: Claude Opus 5` trailer, so the walker's model name appears on the work it witnessed — is disclosed at walk-record.md:47-60 rather than left to be found, per the spec's own risk-table note that the trailer is a cross-check and the stated relationship is the criterion."
  mount_point: ".bklg/docs-that-teach/reach-and-adapter-path/project.md"
  verifying_test: "Authorship cross-check `git log --format='%an %ae' -- crates/happenstance-core/src/store.rs` (and the reasoning account's page path) must not list the named walker, plus a human read of .bklg/docs-that-teach/reach-and-adapter-path/error-site-walk-record/walk-record.md against project.md's risk-table row on non-authors versus non-insiders."

- id: AC-003
  criterion: "GIVEN initiative DoD scenario 10 fixes the starting conditions at the file and the message and nothing else, WHEN the walk begins, THEN the record states the cold-start protocol before its first hop: what the walker had open at t=0 (their own failing build output and `crates/happenstance-core/src/store.rs`) and what was forbidden (the backlog, this spec, the sibling stories, the author, and prior knowledge of where the account was filed), with any contamination disclosed rather than omitted."
  satisfied: true
  evidence: "Cold-start protocol at walk-record.md:62-93: permitted at t=0 (the walker's own failing build output and crates/happenstance-core/src/store.rs) at :64-66, forbidden at :68-71 (the backlog, sibling specs and reports, the dependency stories' artefacts, _design.md's composition tables, the authors, and prior knowledge of where the account was filed). Two contaminations are disclosed rather than omitted at :73-88 — the walker read this story's spec.md, and `git show --stat af9a241` printed the destination's path before t=0 — each with what it bought and what it cannot have touched, and :90-93 records the honest consequence that this entry is a weaker record than an uncontaminated walk. Document order checkable from the heading outline alone: protocol :62 precedes reproduction :94 precedes Observation A :179 precedes the hop table :213."
  mount_point: ".bklg/docs-that-teach/reach-and-adapter-path/project.md"
  verifying_test: "Heading-order check on .bklg/docs-that-teach/reach-and-adapter-path/error-site-walk-record/walk-record.md — protocol before reproduction before Observation A before the hop table — read against .bklg/docs-that-teach/initiative.md DoD scenario 10 and this story's discover.md wrong-implementation line."

- id: AC-004
  criterion: "GIVEN Persona 2 wants the reasoning behind the two-flavour split and not only the fix, WHEN they take the offer at the end of `## Import one flavour, not both`, THEN the record's hop table reaches the adapter reasoning account in exactly one hop and proves arrival at the passage: it names the account's stated answered-need, the position marker of the section landed on (`n of 6`), and whether the caveat that `MemoryEventStore` is the conformance oracle and reference implementation and not an adapter was present where `memory.rs` is first sequenced — two hops, or an arrival that cannot be located in the sequence, is recorded as a finding."
  satisfied: true
  evidence: "Hop table at walk-record.md:219-222 carries exactly one row sourced at store.rs — row 2 (:222), store.rs's `## Import one flavour, not both` -> docs/adapter-reading-order.md — asserted at :224-226. Arrival check at walk-record.md:237-268 proves arrival at the passage: the stated answered-need quoted verbatim (:243, matching docs/adapter-reading-order.md:3), the position markers `## 1 of 6` through `## 6 of 6` with the arrival passage named as `## 4 of 6 — why there are two flavours` (:245-250), and walk-backability from entry 4's own 'Fourth, not first' sentence (:252-254). The MemoryEventStore caveat is confirmed present and in position at entry 1 where memory.rs is first sequenced (:256-268), naming it the conformance oracle and reference implementation and stating it is not an adapter, paired with the PgStore at the other end of the axis — matching _design.md's `## Composition` regions 1, 3 and 4 for adapter-reasoning-account. No second lookup, no intermediate page."
  mount_point: ".bklg/docs-that-teach/reach-and-adapter-path/project.md"
  verifying_test: "Hop-row count in .bklg/docs-that-teach/reach-and-adapter-path/error-site-walk-record/walk-record.md (exactly one row sourced at store.rs), plus `rg -F` of the quoted answered-need and position marker against the reasoning account's page in HS-P0020's pinned tree, compared to _design.md's `## Composition` regions 1, 3 and 4 for adapter-reasoning-account."

- id: AC-005
  criterion: "GIVEN a keyboard-only reader who searches before they read, WHEN each hop is taken, THEN the record states that no pointing device was used and names the affordance and the keys per hop (plain link, rustdoc search `S` or `/`, a `#[doc(alias)]` key, or a fragment), and at least one hop or recorded attempt uses a string the reader would really type — `E0034` or `TraitVariantBlanketType` — with what it returned written down."
  satisfied: true
  evidence: "walk-record.md:215-217 states that no pointing device was used at any point — no mouse, no trackpad, no touch — and that every affordance was reached by typing and pressing `Enter`. Neither hop row has an empty affordance cell: row 1 (:221) names the typed workspace search `rg -F 'TraitVariantBlanketType'` at the shell prompt with `Enter`; row 2 (:222) names `/` to search within the open file for the destination path, then the path typed into the editor's open-file prompt with `Enter`. Real search strings and their results are written down: row 1 returned 20 matches in 8 files of which 9 are in store.rs, against a measured baseline of six workspace hits and none in store.rs. A third recorded attempt at :228-235 uses rustdoc search (`S` or `/`) against the `#[doc(alias = \"E0034\")]` and `#[doc(alias = \"TraitVariantBlanketType\")]` keys at store.rs:139-140, verified by building docs with `cargo doc -p happenstance-core --no-deps` and confirming both strings are present in target/doc/search.index/ — with the limitation (index read, browser not driven) stated rather than glossed."
  mount_point: ".bklg/docs-that-teach/reach-and-adapter-path/project.md"
  verifying_test: "Column completeness check on the hop table in .bklg/docs-that-teach/reach-and-adapter-path/error-site-walk-record/walk-record.md (no empty affordance cell; at least one recorded search string and its result), read against the accessibility floor in .bklg/docs-that-teach/reach-and-adapter-path/_decomposition.md and its UX-AC-09."

- id: AC-006
  criterion: "GIVEN invariant 1, that getting unstuck must never require leaving the surface the reader is on, WHEN the walker reaches `store.rs` and before any hop is taken, THEN the record answers first whether `store.rs` alone named which call was ambiguous and how to resolve it, quoting the sentence that did it verbatim from the file (or recording that none did) — and that observation appears in document order ahead of the hop table."
  satisfied: true
  evidence: "Observation A at walk-record.md:179-211 answers yes to both halves before any hop is taken, and states at :181-183 that nothing was followed and the destination was not opened until after the section was written. Which call is ambiguous, quoted verbatim from crates/happenstance-core/src/store.rs:52-54 at walk-record.md:187-191; how to resolve it, quoted verbatim from store.rs:56-58 at walk-record.md:195-198. Both quotes are the file's own, not paraphrases: `rg -n -F 'is ambiguous because both' crates/happenstance-core/src/store.rs` hits :53 and `rg -n -F 'Import only the one you are binding on' crates/happenstance-core/src/store.rs` hits :56. Deliberate falsification of invariant 1 recorded at walk-record.md:203-208 — with the account hypothetically deleted the quoted sentence still unblocks the reader, because the fix names the method, both traits, which to import and the fully-qualified escape hatch, and depends on nothing downstream. Heading order: Observation A at :179 precedes the hop table at :213."
  mount_point: ".bklg/docs-that-teach/reach-and-adapter-path/project.md"
  verifying_test: "`rg -F` of the record's quoted sentence against crates/happenstance-core/src/store.rs returning a hit on the merged tree, plus the heading-order check that Observation A precedes the hop table in .bklg/docs-that-teach/reach-and-adapter-path/error-site-walk-record/walk-record.md."

- id: AC-007
  criterion: "GIVEN that a walk which was not written down is indistinguishable from one that did not happen, WHEN the PR merges, THEN walk-record.md exists in this story's folder carrying the ISO date of the walk itself, and project.md's `## Companions` links it with self-describing link text naming the record and the walk it carries, so project Definition-of-done item 4 resolves against a file — with nothing else in project.md changed."
  satisfied: true
  evidence: "`test -f .bklg/docs-that-teach/reach-and-adapter-path/error-site-walk-record/walk-record.md` passes. The entry heading is `## Walk of 2026-08-20` (walk-record.md:28) and the date is stated to be the walk's, not the commit's — walk-record.md:302 reads 'that on 2026-08-20, at tree d14affd', and the tree walked is pinned at :34. Mount: `rg -n 'error-site-walk-record/walk-record.md' .bklg/docs-that-teach/reach-and-adapter-path/project.md` hits project.md:359, inside `## Companions` (which opens at project.md:349). `git diff -- .bklg/docs-that-teach/reach-and-adapter-path/project.md` is a five-line pure addition at the end of `## Companions` with no frontmatter line changed and no pre-existing line moved, satisfying the non-occlusion invariant. Link text is 'the observed error-site walk record' — five words, a noun phrase naming the destination, no 'here'/'this'/'docs', no bare URL."
  mount_point: ".bklg/docs-that-teach/reach-and-adapter-path/project.md"
  verifying_test: "`test -f .bklg/docs-that-teach/reach-and-adapter-path/error-site-walk-record/walk-record.md`; `rg -n 'error-site-walk-record/walk-record.md' .bklg/docs-that-teach/reach-and-adapter-path/project.md` hitting inside `## Companions`; and `git diff main...HEAD -- .bklg/docs-that-teach/reach-and-adapter-path/project.md` showing only the added bullet and no frontmatter line."

- id: AC-008
  criterion: "GIVEN invariant 5 and _design.md's Refused state, WHEN a hop fails, resolves nowhere, or takes two, THEN that entry is closed in the record as a failed walk with the finding routed by owning story slug (`store-error-site-rewrite` for the pointer, `adapter-reasoning-account` for the destination) and nothing repaired in this PR, and any later attempt is a new dated entry citing the failed one — the file's history shows entries appended, never a closed entry edited."
  satisfied: true
  evidence: "The failure protocol is declared before the first entry, at walk-record.md:7-27 under `## How this file is kept`, so it binds whether or not a failure occurred — which is what makes this record capable of failing even in the entry where it did not. It states append-only ('Entries are appended, never edited… A later walk that succeeds where an earlier one failed is a new dated entry citing the earlier one', :12-14), the failed-walk rule with routing by owning story slug (`store-error-site-rewrite` for the pointer, `adapter-reasoning-account` for the destination, :15-19), and the blocked-versus-failed distinction (:20-22). No hop failed on this walk: outcome recorded at walk-record.md:30-32. Three observations were noticed, checked against the signed-off design, and recorded as not routed with the reason each was already a decision design had taken (walk-record.md:270-298) — so no repair was made and no dependency's file was touched: `git status --porcelain` shows this story changed only project.md and walk-record.md, nothing under crates/ or docs/. This is the first entry, so there is no closed entry to edit."
  mount_point: ".bklg/docs-that-teach/reach-and-adapter-path/project.md"
  verifying_test: "The declared failure protocol in the record's protocol stanza, plus `git log -p -- .bklg/docs-that-teach/reach-and-adapter-path/error-site-walk-record/walk-record.md` showing additions only below the last closed entry, and `git diff --name-only main...HEAD` showing no file of a routed-to story touched in this PR."

- id: AC-009
  criterion: "GIVEN the standing composition constraints every story in this project inherits, WHEN the record and its mount are rendered by GitHub's Markdown renderer, THEN the record is composed prose plus one hop table with a header row and its named columns, and neither it nor the project.md link introduces raw HTML, an inline `style=` attribute, a folded or tabbed element, a See also / Next steps / Further reading block, a fewer-than-three-row table used as a navigation device, link text reading here / this / docs, or a bare URL into this repository's own tree."
  satisfied: true
  evidence: "The record is composed prose plus exactly one hop table (walk-record.md:219-222) with a header row and its four named columns — ordinal, surface on screen, affordance used and the keys, destination passage reached — and every cell on both rows is filled. Mechanical, over walk-record.md and project.md both: `rg -n '<details|<table|<div|style='` returns no matches in either file, and `rg -in '^#+ *(see also|next steps|further reading)'` returns no matches in either. Nothing is folded or tabbed: the reproduction stanza is an open fence (walk-record.md:152-173), not a `<details>`. Heading ladder is unskipped — `#` at :1, `##` at :7 and :28, `###` at :36/62/94/179/213/237/270/300 — and document order inside the entry is the binding one: protocol, reproduction, Observation A, hop table, arrival check, findings, claims. The bounded-claim line sits in the entry body at :309-315, not as a footnote. Copy fidelity is preserved over the density budget per _design.md's F3/F4: the 109-character `= note:` candidate-#2 line at :160 is left un-reflowed and stated as taking a horizontal scrollbar (:148-151). The mount's visible link text is 'the observed error-site walk record' (project.md:359) — five words, a noun phrase naming the destination, and no bare URL into this repository's tree appears in either file."
  mount_point: ".bklg/docs-that-teach/reach-and-adapter-path/project.md"
  verifying_test: "`rg -n '<details|<table|<div|style='` and `rg -in '^#+ *(see also|next steps|further reading)'` over walk-record.md and project.md returning nothing, plus a rendered read of both against _design.md's `## Anti-patterns` items 3, 5, 6, 9, 10 and its `## Density budget` link-text minimum."
```
