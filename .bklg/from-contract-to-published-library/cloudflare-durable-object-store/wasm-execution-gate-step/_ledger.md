---
item: "HS-S0048"
stage: implement
created: "2026-08-12"
updated: "2026-08-12"
---

# Acceptance ledger — A wasm32 conformance run inside cargo xtask ci, not beside it

The machine-checkable Definition-of-Done ledger for this story (RFC §6.5). One row per spec
`AC-###`. Planning authors every row with `satisfied: false`; the implementer may only flip a row to
`satisfied: true` and MUST cite real evidence (a `file:line` and/or the verifying test id) — never
edit, remove, or re-word a criterion, and never flip a satisfied row back. `redkiln verify --grain
story` reads the fenced block below and blocks `implement → report` unless every spec AC is present,
`satisfied: true`, and carries non-placeholder evidence (and no AC that was satisfied on the base
branch has regressed). Scope changes are a human decision recorded through `redkiln advance`, not a
quiet ledger edit.

```yaml
- id: AC-001
  criterion: "GIVEN a gate reader on a clean checkout who has been told this workspace's !Send port design is real, WHEN they run one `cargo xtask ci` (and one `cargo xtask ci --fast`) and read the output top to bottom, THEN a named wasm32 step reports conformance rules that actually executed on wasm32-unknown-unknown under wasm-bindgen-test-runner — a per-rule pass or a named failure, not a compile line — and the gate is green on every runner the gate job matrices over"
  satisfied: true
  evidence: "xtask/src/main.rs:412 (the `wasm32 run of the conformance rules` REQUIRED row) driving xtask/src/proof.rs `wasm_run`. `cargo xtask ci --fast` prints `=== wasm32 run of the conformance rules ===`, then one enumerate-and-execute block per registered target — `89 rules enumerated, 9 named` / `test result: ok. 89 passed` for memory_conformance_wasm, the same 89 for local_conformance against the !Send LocalMemoryEventStore, and `17 rules enumerated, 6 named` / `17 passed` for projection_conformance_wasm — 195 rule executions in one run, per-rule lines rather than compile lines, ending `all required checks passed`. Full scroll: implementation-report.md, ## Amendment. Test: xtask/src/main.rs:1538 tests::the_wasm32_conformance_run_executes_rather_than_checks, which rejects a sixth `cargo check`. Three-runner availability verdict: implementation-report.md, ## Notes."
  mount_point: "xtask/src/main.rs — `const REQUIRED: &[Step]` at :105, iterated by run_ci (:828-833) and run_fast (:853-860)"
  verifying_test: "`cargo xtask ci` and `cargo xtask ci --fast` end to end over the new REQUIRED row, executing crates/happenstance-testkit/tests/memory_conformance_wasm.rs; the CI `gate` job on ubuntu/windows/macos (.github/workflows/ci.yml:34-39)"
- id: AC-002
  criterion: "GIVEN an adapter author who will later insert a step above this one, WHEN they run `cargo xtask wasm` or read `cargo xtask --help`, THEN the execution step is selected by name and not by index — it is a REQUIRED row whose `name` string appears in wasm_steps(), an unresolvable name panics in steps_named, `cargo xtask wasm` runs it too, and print_help's wasm description no longer claims the wasm32 tasks merely check that things build"
  satisfied: true
  evidence: "xtask/src/main.rs:1009 wasm_steps() lists both new names, resolved through steps_named (:1045), which panics on an unresolvable name. Test: xtask/src/main.rs:1455 tests::wasm_steps_resolve_and_the_check_family_keeps_its_order — asserts the resolved names equal WASM_STEPS exactly (seven) with the five checks at their original indices; RED was five names against seven. The print_help wasm description is rewritten at xtask/src/main.rs:934-942 and held by xtask/src/main.rs:1633 tests::the_help_text_says_the_wasm32_tasks_execute_rules (RED: 'still describes the family as builds and checks alone'). `cargo xtask wasm` prints seven sections."
  mount_point: "xtask/src/main.rs — the new step's name added to wasm_steps() at :784-791, resolved through steps_named at :816-826; print_help at :735-737"
  verifying_test: "new `#[cfg(test)]` unit tests in xtask/src/main.rs asserting wasm_steps() resolves five steps and contains the execution step's exact name, run by `cargo test -p xtask`; plus `cargo xtask wasm`"
- id: AC-003
  criterion: "GIVEN a gate reader who has been burned by a step that a deletion fails and an emptying passes (xtask/src/proof.rs:9-23), WHEN the executed target is truncated to its #![cfg(…)] attribute, or a rule test is renamed or #[ignore]d, THEN the gate fails with a message naming the missing test rather than exiting 0 on `running 0 tests` — because the executed wasm32 target is registered in the xtask/src/proof.rs shape and its named rules are asserted out of --list before the run, or, where the runner cannot enumerate, an equivalent mandatory assertion is made over the run's own reported count against the rule enumeration"
  satisfied: true
  evidence: "Two mechanisms, both green. (1) xtask/src/proof.rs wasm_run asserts every rule the row's OWN enumeration declares out of `-- --list` BEFORE running — 89 of 89 for the two event-store rows, 17 of 17 for the projection row; the runner does honour --list, so EC-003's fallback was not needed. (2) xtask/src/proof.rs wasm_enumeration, a probe-free REQUIRED row (xtask/src/main.rs:354), reads each target's source with no runner at all, and also scans crates/happenstance-testkit/tests for a wasm32 harness carrying no row. Negative controls run by hand and pasted into implementation-report.md: the target truncated to its #![cfg] line fails both halves by name; a rule renamed in registry.rs fails with 'names acknowledged_writes_survive_a_reopen, which for_each_event_store_rule! does not declare'; a rule renamed to an unreadable spelling makes the enumeration parser refuse rather than silently shrink; and a deleted row fails with 'holds 1 wasm32 conformance harness(es) with no row in WASM_TARGETS'. Tests: every_named_wasm_rule_is_one_the_enumeration_declares, every_executed_wasm_target_carries_its_own_package_and_target, every_wasm32_capable_harness_has_a_row, prose_naming_a_rule_is_not_a_subset_list."
  mount_point: "xtask/src/proof.rs — a fourth registered entry in the ARTEFACTS shape (:133-149), reached from REQUIRED through the `each phase's proof artefacts` step (xtask/src/main.rs:176-191)"
  verifying_test: "`cargo xtask proof-artefact` (xtask/src/main.rs:681) covering the new entry; a `#[cfg(test)]` test in xtask/src/proof.rs asserting the wasm entry's expectation list is non-empty and each name appears in crates/happenstance-testkit/src/registry.rs:94-140; the emptied-target negative control recorded in the implementation report"
- id: AC-004
  criterion: "GIVEN an adapter author on a machine without wasm-bindgen-cli installed, WHEN they run `cargo xtask ci --fast`, THEN what happens is a stated decision they can read in the file, and in no configuration does the wasm32 execution silently not run while the gate still prints green: either the step is `probe: None` and the gate fails outright, or it is probe-gated AND a mandatory non-skippable assertion over the target and its rule enumeration runs regardless — with the choice, its cost and its rejected alternative written into the step's own comment as ADR-0023 material"
  satisfied: true
  evidence: "Shape (ii), chosen and argued in the step's own comment at xtask/src/main.rs:373-411 — why this tool is not in the class rust-toolchain.toml can supply, with shape (i) named as the rejected alternative and its cost priced. The probe is xtask/src/main.rs:414, Some(&[proof::WASM_RUNNER, \"--version\"]); the mandatory compensator is xtask/src/main.rs:354 with probe: None. Test: xtask/src/main.rs:1580 tests::the_wasm32_run_is_probed_and_its_compensator_never_is, which asserts the compensator's probe is None and that the run's comment names both 'mandatory' and 'rust-toolchain.toml'. Control with the runner moved off PATH, recorded in implementation-report.md: the compensator ran and reported '9 named rules, all declared by the one enumeration of 89' while the run printed 'skipped: wasm-bindgen-test-runner --version did not succeed'. NF-005's one-sentence local cost is in ## Notes."
  mount_point: "xtask/src/main.rs — the new step's `probe` field in REQUIRED (:105), against the skip path at :873-878 and the rule stated at :192-202"
  verifying_test: "a unit test in xtask/src/main.rs asserting the execution step's probe is None (or that a `probe: None` compensator row exists in REQUIRED), run by `cargo test -p xtask`; a manual `cargo xtask ci --fast` with the runner removed from PATH, output recorded in the implementation report"
- id: AC-005
  criterion: "GIVEN a gate reader asking what could this runtime not do, and why, WHEN they read the wasm32 step's output in the same terminal scroll as the rest of the gate, THEN every rule the run touched is attributable to the one for_each_event_store_rule! enumeration with no wasm-only subset list anywhere in the tree, and any declined capability's `SKIP <rule>: <reason>` line is visible rather than swallowed — the step passes the runner's --nocapture equivalent, because println! is a silent discard on this target"
  satisfied: true
  evidence: "--nocapture is passed in wasm_run with the reason on the line; --show-output was measured and is rejected outright by this runner. SKIP <rule>: <reason> lines appear in the gate's own scroll for all three registered targets — including LocalFixture's declined REOPEN and MemoryProjectionFixture's two — against zero in the same run without the flag, which is the measurement (implementation-report.md). Single-sourcing: no rule added, removed or #[cfg]-ed anywhere in the diff; registry::no_orphan_rules stays green under the gate's full test step; the derived check in wasm_run asserts every rule the row's own enumeration declares reaches its listing, and the_executed_wasm_targets_name_no_rule_of_their_own forbids a hand-written subset in the target's CODE — scanned through code_only, because three of these rule names appear in local_conformance.rs's prose and a whole-file scan refuses a correct file (prose_naming_a_rule_is_not_a_subset_list is the standing control on both directions)."
  mount_point: "xtask/src/main.rs — the new step's `args` (the --nocapture equivalent) and `env` in REQUIRED at :105; the observed enumeration is crates/happenstance-testkit/src/registry.rs:94-140 via crates/happenstance-testkit/tests/memory_conformance_wasm.rs:23-27"
  verifying_test: "the executed step's captured output in `cargo xtask ci`, pasted into the implementation report; `registry::no_orphan_rules` green under `cargo test --workspace --all-features` (crates/happenstance-testkit/src/registry.rs:413-424)"
- id: AC-006
  criterion: "GIVEN the adapter author of every-rule-under-workerd (HS-S0054), WHEN they come to run the Cloudflare conformance target under the same runner, THEN they add it by registering a row in a declared list of executed targets — the way ARTEFACTS takes a row — and write no second execution step, no second runner wiring and no second Step.env; an implementation that hard-codes `-p happenstance-testkit --test memory_conformance_wasm` into Step.args is the named wrong implementation this criterion rejects"
  satisfied: true
  evidence: "xtask/src/proof.rs WASM_TARGETS is a declared list of rows (package, target, module, source, rule family, rules); the runner wiring, the version check, the enumeration assertions and both gate steps are shared and target-independent. Demonstrated rather than asserted: two further rows — local_conformance and projection_conformance_wasm — were added by registration alone during the slice-review amendment, which is also what surfaced the one field the first cut lacked (`family`, without which a non-event-store row cannot be expressed). No Step.args names a package or a target — the two rows invoke wasm-conformance and wasm-conformance-enumeration (xtask/src/main.rs:412 and :354). Tests: xtask/src/main.rs:1664 tests::no_gate_step_hard_codes_an_executed_wasm_target, which walks REQUIRED and OPTIONAL against every registered target name and is the named wrong implementation's detector, and every_executed_wasm_target_carries_its_own_package_and_target. HS-S0054's entry point is stated in implementation-report.md, ## Notes."
  mount_point: "xtask/src/proof.rs — the declared executed-target list in the ARTEFACTS shape (:133-149) and its shared argument builder cargo_args (:164-174), reached from REQUIRED (xtask/src/main.rs:105)"
  verifying_test: "a unit test in xtask walking the declared executed-target list and asserting it is a list of rows each carrying its own package and target rather than fixed arguments, run by `cargo test -p xtask`; code review against the named wrong implementation"
- id: AC-007
  criterion: "GIVEN a gate reader looking at CI after this merges, WHEN they compare .github/workflows/ci.yml against the in-gate step, THEN there are not two unexplained wasm32 execution paths: the standalone wasm-conformance job is either retired in favour of the in-gate step — with the wasm-bindgen-cli-version-resolved-from-Cargo.lock behaviour preserved wherever the work now lives, never hard-coded — or kept with its continuing purpose stated in its own comment, its stale `Those arrive at phase 9` sentence corrected either way; and the recorded verdict says whether the runner is available and deterministic on all three matrix runners, any platform restriction carrying its reason, with an escalated blocking finding rather than a downgrade of AC-004 to a `cargo check` if it is not"
  satisfied: true
  evidence: "Retired, and the retirement is now a subsumption rather than a claim of one — satisfied on the second pass; the first registered one row while the retired job had run three harnesses, deleting 106 wasm32 executions (implementation-report.md, ## Amendment, records the defect and its correction). .github/workflows/ci.yml carries a retirement note in place of the job, naming the three targets that moved, where each now runs, and — separately — what the gate does NOT add (the job was mandatory on its runner; the in-gate step is probed); 'the same target' and 'strictly more' are gone. The list's completeness is checked rather than asserted: xtask/src/proof.rs unregistered_wasm_harnesses, run inside the mandatory probe-free row, fails a wasm32 harness in crates/happenstance-testkit/tests that has no row. The stale 'Those arrive at phase 9' sentence is corrected and points at HS-S0054 as a ROW in WASM_TARGETS. The Cargo.lock-resolved wasm-bindgen version and the wasm-bindgen-cli install moved into the three-runner gate job (with shell: bash for the Windows runner), never hard-coded — and the gate now makes the same comparison itself in check_wasm_runner_version. No platform restriction is imposed; the three-runner verdict, including what is verified and what is not, is recorded in implementation-report.md, ## Notes. cargo xtask spec-trace is green after the CF-23 Rule: line edit (spec/SPECIFICATION.md:8374-8377), with the FROZEN marker and every normative sentence untouched; spec/SPECIFICATION.md:8541's claim that LocalMemoryEventStore passes the suite on wasm32 is checked again by the local_conformance row."
  mount_point: ".github/workflows/ci.yml — the wasm-conformance job at :189-237 and the gate matrix at :34-39, against the new REQUIRED row in xtask/src/main.rs:105"
  verifying_test: "review of the .github/workflows/ci.yml diff against ../_decomposition.md DEPLOY-AC-05 and Deployment Notes §2; the three-runner `gate` job green (or the documented restriction with its reason in the file); `cargo xtask spec-trace` green; the verdict recorded in the implementation report"
```
