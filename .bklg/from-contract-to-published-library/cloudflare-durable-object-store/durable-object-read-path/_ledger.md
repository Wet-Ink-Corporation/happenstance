---
item: "HS-S0051"
stage: implement
created: "2026-08-12"
updated: "2026-08-12"
---

# Acceptance ledger — A lazy read that is still one sample: ADR-0011's ceiling-and-page

The machine-checkable Definition-of-Done ledger for this story (RFC §6.5). One row per spec
`AC-###`. Planning authors every row with `satisfied: false`; the implementer may only flip a row to
`satisfied: true` and MUST cite real evidence (a `file:line` and/or the verifying test id) — never
edit, remove, or re-word a criterion, and never flip a satisfied row back. `redkiln verify --grain
story` reads the fenced block below and blocks `implement → report` unless every spec AC is present,
`satisfied: true`, and carries non-placeholder evidence (and no AC that was satisfied on the base
branch has regressed). Scope changes are a human decision recorded through `redkiln advance`, not a
quiet ledger edit.

```yaml
- id: AC-001
  criterion: "A replay exists at all. GIVEN an adapter author with a Durable Object holding events written through this crate's one construction root, WHEN they call EventStore::read with Query::all() and default ReadOptions and drain the returned stream to exhaustion, THEN they receive every stored event as a SequencedEvent in ascending position order — no todo!() is reached, nothing panics, and the replay half of the backbone activity \"store and replay events inside a Durable Object\" is real rather than an instrument. Traces project AC-001."
  satisfied: true
  evidence: "read is real at crates/happenstance-cloudflare/src/event_store.rs:587 (the body), :902 (the hand-written poll_next), :1073 (render_read) and :1136 (decode_row); no todo!() survives anywhere in crates/happenstance-cloudflare/ and the scoped #![allow(clippy::todo)] left with the last of them (crates/happenstance-cloudflare/src/lib.rs:143). Passing on wasm32-unknown-unknown under wasm-bindgen-test-runner: event_store::read_path_tests::read_all_replays_every_appended_event (:2006), ::a_replayed_event_carries_everything_it_was_appended_with (:2024), ::read_reaches_the_store_only_through_new (:2050), ::read_pages_at_the_shipped_page_size (:2065). RED first: with render_read and decode_row at todo!(), 33 of these tests failed on that panic; GREEN, 69 passed / 0 failed. cargo clippy -p happenstance-cloudflare --all-targets --all-features -- -D warnings and the same for --target wasm32-unknown-unknown are green without the allow."
  mount_point: "crates/happenstance-cloudflare/src/event_store.rs — the `impl EventStore for CloudflareEventStore` block (:145-196), reached through the crate's single construction root `CloudflareEventStore::new(sql)` (:70-87)"
  verifying_test: "crates/happenstance-cloudflare/src/event_store.rs::read_path::read_all_replays_every_appended_event and ::read_reaches_the_store_only_through_new (cargo test -p happenstance-cloudflare); plus the clippy -D warnings step of `cargo xtask ci --fast` over the three read-path items"

- id: AC-002
  criterion: "The shape a caller depends on survives the diff. GIVEN a caller writing generic code that binds EventStore — the weaker bound that accepts both flavours — and spawns work over the stream it gets back, WHEN this story's read compiles, THEN read is still a non-async method returning impl Stream at the top level with no + Send and no #[async_trait] anywhere, so the derived Send flavour keeps its stream bound; the state machine stays hand-written and the stream stays !Send by its fields. Traces project AC-001; enforces CLAUDE.md constraints 1, 3 and 4 (ADR-0001, ADR-0008)."
  satisfied: true
  evidence: "read stayed a non-async method returning `impl Stream` at the top level with no `+ Send` (crates/happenstance-cloudflare/src/event_store.rs:587-591) and SqlRowStream's state machine stayed hand-written (:899-1009), so its Send-ness is still decided by its fields. Proved three ways. (1) event_store::read_path_tests::read_is_usable_from_generic_code_binding_the_bare_port (:2088) drives the replay through `replay_through_the_port<S: EventStore>` (:1935-1950) — generic code binding the *bare* flavour, calling `read` without `.await` and pinning the stream at the top level; an `async fn read` refactor or a `+ Send` on the stream stops it compiling. (2) The four !Send probes still pass on both targets: tests::the_probe_is_not_vacuous / ::the_js_boundary_types_are_not_send / ::the_error_type_is_not_send / ::the_send_flavour_does_not_imply_a_send_error on the host and wasm_tests::* on wasm32 (crates/happenstance-cloudflare/src/lib.rs:220-363), and the SqlRowStream assertion at :285-288 is the one that would fail had the machine become a coroutine. (3) crates/happenstance-core/src/memory.rs is untouched by this diff (git diff --stat) and `cargo test -p happenstance-core` is green, including send_flavour_stream_is_send_in_generic_code and spawns_from_generic. send_shape.rs's two doctests, one of them compile_fail,E0277, still pass under `cargo test -p happenstance-cloudflare --doc`."
  mount_point: "crates/happenstance-cloudflare/src/event_store.rs — the `impl EventStore for CloudflareEventStore` block (:145-196), reached through the crate's single construction root `CloudflareEventStore::new(sql)` (:70-87)"
  verifying_test: "crates/happenstance-cloudflare/src/lib.rs:180-259 (the_store_is_not_send, the_stream_is_not_send, the_error_type_is_not_send, the_probe_is_not_vacuous) and crates/happenstance-cloudflare/src/send_shape.rs; plus crates/happenstance-core/src/memory.rs::send_flavour_stream_is_send_in_generic_code and ::spawns_from_generic unedited and green (cargo test -p happenstance-cloudflare -p happenstance-core)"

- id: AC-003
  criterion: "One read is one sample, or the failure is escalated rather than absorbed. GIVEN a constrained-runtime developer replaying a stream inside a re-entrant Durable Object while another future appends to the same object, WHEN they poll that read to exhaustion across several pages, THEN the events they see are exactly the events that existed at one moment no later than the first poll — a ceiling H is captured then and every statement after the first is bounded by position <= H (>= H under backwards) — so nothing appended mid-drain appears part-way through; AND if that ceiling cannot be afforded against the real SqlStorageCursor, the compiled reason is recorded and escalated as ES-11/ES-12 falsifier evidence (ADR-0023 material), never absorbed as a declined capability and never softened into a weaker promise. Traces project AC-007(a); discharges ES-11."
  satisfied: true
  evidence: "The ceiling is afforded, so ES-11's named phase-9 falsifier does not bite and nothing is escalated. It is captured on the first poll (crates/happenstance-cloudflare/src/event_store.rs:919-939, via max_position at :1018 — the same statement `head` uses) and every later statement is bounded by it (render_read, :1086-1087). Passing on wasm32: event_store::read_path_tests::read_is_stable_under_an_interleaved_append (:2103) drains two of a four-event log at page size 2, appends, drains the rest and finds the late event absent; ::the_ceiling_is_captured_no_later_than_the_first_poll (:2142) fixes the sample point; ::every_page_statement_carries_the_ceiling_bound (:2182) is the committed detector — it reads the statements the object was actually issued and asserts the bound is on every page. Negative controls, run as a mutation sweep and each rejected: dropping `AND position <= ?` from render_read failed read_is_stable_under_an_interleaved_append, the_ceiling_is_captured_no_later_than_the_first_poll, every_page_statement_carries_the_ceiling_bound and all_items_of_one_query_share_one_ceiling (4 failed / 65 passed); re-capturing the ceiling per page failed the same three plus one_ceiling_is_captured_per_read_not_one_per_item (4 failed / 65 passed). Cost, recorded rather than discovered: one `max(position)` statement per read plus one statement per page of PAGE_SIZE = 128 rows (:799)."
  mount_point: "crates/happenstance-cloudflare/src/event_store.rs — the `impl EventStore for CloudflareEventStore` block (:145-196), reached through the crate's single construction root `CloudflareEventStore::new(sql)` (:70-87)"
  verifying_test: "crates/happenstance-cloudflare/src/event_store.rs::read_path::read_is_stable_under_an_interleaved_append and ::the_ceiling_is_captured_no_later_than_the_first_poll, with the ceiling-less paging negative control (cargo test -p happenstance-cloudflare); standing later as read_result_is_stable_under_concurrent_append (crates/happenstance-testkit/src/registry.rs:213, crates/happenstance-testkit/src/suite.rs:5592)"

- id: AC-004
  criterion: "A multi-item DCB query is one sample, not several. GIVEN an application author whose decision boundary is expressed as a Query with several QueryItems, WHEN the adapter issues more than one statement to serve it, THEN all items share the one ceiling — an event matching an earlier item that lands between two statements has a position above H and is excluded by the same predicate that bounds every other statement — so the caller never sees a boundary assembled from two different moments, and no second isolation mechanism is introduced to achieve it. Traces project AC-007(a); discharges ES-12 (which reduces to ES-10 plus the ceiling)."
  satisfied: true
  evidence: "ES-12 is discharged by the *same* ceiling rather than by a second mechanism: a query's items are UNION-ed into one `position IN (…)` membership test (crates/happenstance-cloudflare/src/query_sql.rs:53-71) and the single ceiling is AND-ed onto the page that serves all of them (crates/happenstance-cloudflare/src/event_store.rs:1084-1087). Passing on wasm32: event_store::read_path_tests::all_items_of_one_query_share_one_ceiling (:2208) interleaves an append matching each of two items between two statements and finds neither present; ::one_ceiling_is_captured_per_read_not_one_per_item (:2248) counts `max(position)` statements at the storage seam over a three-item query and asserts exactly one — a count the returned rows cannot reveal; ::query_union_is_item_concatenation (:2275) shows an event matching two items is yielded once. Negative control, rejected: re-capturing the ceiling per page turned the count into three and failed one_ceiling_is_captured_per_read_not_one_per_item and all_items_of_one_query_share_one_ceiling."
  mount_point: "crates/happenstance-cloudflare/src/event_store.rs — the `impl EventStore for CloudflareEventStore` block (:145-196), reached through the crate's single construction root `CloudflareEventStore::new(sql)` (:70-87)"
  verifying_test: "crates/happenstance-cloudflare/src/event_store.rs::read_path::all_items_of_one_query_share_one_ceiling, including the assertion that exactly one ceiling capture occurs per read (cargo test -p happenstance-cloudflare); standing later as query_items_share_one_snapshot (crates/happenstance-testkit/src/registry.rs:214, crates/happenstance-testkit/src/suite.rs:5696)"

- id: AC-005
  criterion: "A live replay never wedges the object. GIVEN a Durable Object that is single-threaded but re-entrant, WHEN a read stream is suspended mid-drain and an append is issued on the same handle — or a read is issued while an append on that handle is itself suspended — THEN both complete: the stream holds no RefCell borrow and no live cursor across a poll boundary, and a genuinely re-entered borrow is reported as SqlError::AlreadyBorrowed rather than panicking inside borrow_mut. Traces project AC-007(a); honours ES-36 and the read-during-suspended-append obligation no clause states (spec/SPECIFICATION.md:4178-4250)."
  satisfied: true
  evidence: "No cursor and no RefCell borrow crosses a poll boundary: drain_page (crates/happenstance-cloudflare/src/event_store.rs:1038-1049) executes a page and drains its cursor completely before returning, so the SqlCursor is dead before the caller can suspend, and the stream's Paging state holds only a VecDeque of already-decoded rows (:866-894). Passing on wasm32: event_store::read_path_tests::a_live_read_stream_does_not_block_an_append_on_one_handle (:2297) appends on the same handle mid-drain; ::a_read_issued_during_a_suspended_append_completes (:2321) creates an append future, leaves it unpolled, drains a whole read and only then completes the append; ::two_live_reads_interleave_without_borrowing_the_object (:2344) alternates two live reads at page size 1 and both complete in full — the assertion a held cursor fails with SqlError::CursorInvalidated and a held borrow fails with SqlError::AlreadyBorrowed. Re-entrancy stays *reported* rather than panicked, unchanged by this diff and still green: sql_storage::tests::reentrant_borrow_is_reported_not_panicked (crates/happenstance-cloudflare/src/sql_storage.rs:502) and ::a_cursor_polled_after_another_statement_is_invalidated (:562)."
  mount_point: "crates/happenstance-cloudflare/src/event_store.rs — the `impl EventStore for CloudflareEventStore` block (:145-196), reached through the crate's single construction root `CloudflareEventStore::new(sql)` (:70-87)"
  verifying_test: "crates/happenstance-cloudflare/src/event_store.rs::read_path::a_live_read_stream_does_not_block_an_append_on_one_handle, ::a_read_issued_during_a_suspended_append_completes and ::re_entrancy_is_reported_not_panicked, with the held-cursor negative control (cargo test -p happenstance-cloudflare); standing later as a_live_read_stream_does_not_block_an_append (crates/happenstance-testkit/src/registry.rs:210)"

- id: AC-006
  criterion: "The window the caller asked for is the window they get — even though paging exists underneath. GIVEN an application author who has expressed a decision boundary as a DCB query with ReadOptions, WHEN they read, THEN the algebra is rendered, not approximated — types OR within an item, tags AND within an item, items OR across the query, supersets match and partial overlaps do not, an item naming no tags still matches an untagged event, and no event is yielded twice across items; from is a threshold and not a seek (a position nothing occupies yields the next match above it, or below it under backwards, and never an error); to is inclusive and composes with the internal ceiling as the tighter of the two, with from the higher bound and to the lower under backwards; limit is spent on matched events after ordering — a page that scanned excluded rows must not consume it — and limit(0) yields nothing; and a store nobody has written to yields an empty stream and no error, with the options exercised against it rather than short-circuited. Traces project AC-001; satisfies ES-9, ES-13, ES-14, ES-16 on this runtime."
  satisfied: true
  evidence: "The algebra is rendered rather than approximated — types OR within an item, tags AND with superset matching, items OR across the query, and `Query::all` short-circuiting past the tag index so an untagged event still matches (crates/happenstance-cloudflare/src/query_sql.rs:42-117) — and the four bounds compose into one WHERE rather than being chosen between (crates/happenstance-cloudflare/src/event_store.rs:1084-1119). All passing on wasm32: read_from_is_inclusive (:2400), read_from_a_gap_position (:2414, the gap arranged store-side rather than asserted as a literal), read_to_is_inclusive (:2436), read_from_and_to_bound_a_closed_window (:2449), read_backwards_from_with_limit (:2466), read_to_under_backwards_bounds_the_older_end (:2484), read_limit_applies_after_filtering (:2503), read_backwards_limit_applies_after_filtering (:2526), read_limit_zero_yields_nothing (:2550, which also asserts the store was not asked at all), limit_applies_across_items_not_per_item (:2567), query_union_is_item_concatenation (:2275), tags_and_within_an_item_types_or_within_an_item (:2585), an_item_naming_no_tags_matches_an_untagged_event (:2618) and reading_an_empty_store_yields_nothing (:2380, which exercises five option sets against the empty store rather than short-circuiting). Every assertion compares against positions the store assigned; no literal position appears. Negative controls, rejected: the NullHeadPagingStore shape — ceiling arithmetic on `head() == None` — failed reading_an_empty_store_yields_nothing (1 failed / 68 passed); never spending the caller's budget failed read_limit_applies_after_filtering, read_backwards_limit_applies_after_filtering, read_backwards_from_with_limit and limit_applies_across_items_not_per_item (4 failed / 65 passed)."
  mount_point: "crates/happenstance-cloudflare/src/event_store.rs — the `impl EventStore for CloudflareEventStore` block (:145-196), reached through the crate's single construction root `CloudflareEventStore::new(sql)` (:70-87)"
  verifying_test: "crates/happenstance-cloudflare/src/event_store.rs::read_path — the table-driven tests mirroring crates/happenstance-testkit/src/registry.rs:112-138 (read_from_is_inclusive, read_from_a_gap_position, read_backwards_from_with_limit, read_to_is_inclusive, read_from_and_to_bound_a_closed_window, read_to_under_backwards_bounds_the_older_end, read_limit_applies_after_filtering, read_backwards_limit_applies_after_filtering, read_limit_zero_yields_nothing, limit_applies_across_items_not_per_item, query_union_is_item_concatenation, reading_an_empty_store_yields_nothing) plus the null-head ceiling negative control (cargo test -p happenstance-cloudflare)"

- id: AC-007
  criterion: "Nothing is lost or reinterpreted on the way out. GIVEN a stored row this runtime cannot faithfully hand back — a position outside 1..=2^53 because Workers SQL widens integers through a JS number, a column count that disagrees with the SELECT, an undecodable SqlValue, or a stored event type that fails validation — WHEN the caller polls the stream, THEN they receive the corresponding CloudflareEventStoreError (StoredPosition, RowShape, ColumnType, StoredEventType) as an error item on the stream, never a silently narrowed position and never a panic; AND for every row that is representable, data and metadata travel as opaque blobs that are never inspected, parsed or re-encoded, with metadata: None and Some(<empty>) still two distinguishable values and no serde entering this crate on the read path. Traces project AC-007(b) (read side only); honours ADR-0003."
  satisfied: true
  evidence: "decode_row is total and never narrowing (crates/happenstance-cloudflare/src/event_store.rs:1136-1194): a column-count disagreement is RowShape, an undecodable SqlValue is ColumnType naming the column, a stored type that fails validation is StoredEventType, and a position outside 1..=2^53 comes back through the shared decode_position (:745) as StoredPosition. Passing on wasm32: decode_row_reports_row_shape (:2639), ::decode_row_reports_column_type (:2654), ::decode_row_reports_stored_event_type (:2670), ::decode_row_reports_stored_position_at_the_boundary (:2687, which *constructs* the out-of-range condition as a SqlValue::Real rather than asserting a bare number) and ::the_well_formed_row_decodes (:2798), the control that stops the five above passing against a decoder that refuses everything. Reached the way a caller reaches it by ::an_unrepresentable_position_is_an_error_item_on_the_stream (:2705), which seeds 9007199254740993 as a SQL literal — a binding would have widened it on the way *in* — and finds the error as an item on the stream rather than a panic or a narrowed position. Payloads stay opaque: ::metadata_none_and_some_empty_stay_distinguishable (:2728) keeps NULL and an empty blob apart, ::a_replayed_event_carries_everything_it_was_appended_with (:2024) round-trips data and metadata untouched, ::decode_row_returns_recorded_at_as_stored (:2758) and ::decode_row_rebuilds_the_identity_from_the_stored_origin (:2773) hold the two facts a read must not invent. No dependency was added to crates/happenstance-cloudflare/Cargo.toml by this diff, so no serde entered the read path. Negative controls, rejected: stamping now() on read and rebuilding the EventId from the row's own position failed decode_row_returns_recorded_at_as_stored and decode_row_rebuilds_the_identity_from_the_stored_origin (2 failed / 67 passed)."
  mount_point: "crates/happenstance-cloudflare/src/event_store.rs — the `impl EventStore for CloudflareEventStore` block (:145-196), reached through the crate's single construction root `CloudflareEventStore::new(sql)` (:70-87)"
  verifying_test: "crates/happenstance-cloudflare/src/event_store.rs::read_path::decode_row_reports_row_shape, ::decode_row_reports_column_type, ::decode_row_reports_stored_event_type, ::decode_row_reports_stored_position_at_the_boundary (condition constructed, never a bare literal position) and ::metadata_none_and_some_empty_stay_distinguishable (cargo test -p happenstance-cloudflare); dependency surface observed by the --no-default-features and cargo hack steps of cargo xtask ci"
```
