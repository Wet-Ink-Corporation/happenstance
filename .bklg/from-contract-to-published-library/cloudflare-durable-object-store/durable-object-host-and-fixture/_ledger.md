---
item: "HS-S0053"
stage: implement
created: "2026-08-12"
updated: "2026-08-12"
---

# Acceptance ledger — A Durable Object host and the CloudflareFixture mounted on it

The machine-checkable Definition-of-Done ledger for this story (RFC §6.5). One row per spec
`AC-###`. Planning authors every row with `satisfied: false`; the implementer may only flip a row to
`satisfied: true` and MUST cite real evidence (a `file:line` and/or the verifying test id) — never
edit, remove, or re-word a criterion, and never flip a satisfied row back. `redkiln verify --grain
story` reads the fenced block below and blocks `implement → report` unless every spec AC is present,
`satisfied: true`, and carries non-placeholder evidence (and no AC that was satisfied on the base
branch has regressed). Scope changes are a human decision recorded through `redkiln advance`, not a
quiet ledger edit.

```yaml
- id: AC-001
  criterion: "**The adapter author has something to hang the store off, and it is reachable from where a suite lives.** GIVEN a `happenstance-cloudflare` whose read and write paths are complete but which no Durable Object hosts, WHEN the adapter author compiles the crate's test targets, THEN a Durable Object host class and the entrypoint the wasm32 runner addresses exist, are declared in a module list the real build compiles (`crates/happenstance-cloudflare/src/lib.rs`), and are **nameable from a second compilation unit** — not a bare `#[cfg(test)]` module in `src/lib.rs`, which an integration test cannot reach"
  satisfied: true
  evidence: "crates/happenstance-cloudflare/src/lib.rs:281 declares `pub mod host`, unconditional and public; the host itself is crates/happenstance-cloudflare/src/host.rs (promoted from the `#[cfg(all(test, target_arch = \"wasm32\"))] mod test_object` it used to be). Reachability from a SECOND compilation unit is proven by crates/happenstance-cloudflare/tests/fixture_contract.rs::on_the_object::the_host_is_reachable_from_an_integration_test, which drives one append through `CloudflareEventStore::new(host.storage())` and reads it back — green under `cargo run -p xtask -- wasm-conformance`. RED beat recorded: the identical test failed `E0432: could not find `host` in `happenstance_cloudflare`` while the module was still test-gated, which is EC-005's exact failure mode."
  mount_point: "crates/happenstance-cloudflare/src/lib.rs:281 (the module list); crates/happenstance-cloudflare/tests/fixture_contract.rs:26 and tests/durable_object_conformance.rs (`mod support;`); xtask/src/proof.rs WASM_UNIT_TARGETS row 2"
  verifying_test: "crates/happenstance-cloudflare/tests/fixture_contract.rs::the_host_is_reachable_from_an_integration_test"

- id: AC-002
  criterion: "**The fixture the author hands to the macro is bound on the weaker flavour, and stays `!Send`.** GIVEN the only `!Send` adapter in the workspace, WHEN `CloudflareFixture` implements `Fixture` with `type Store = CloudflareEventStore`, THEN the bound is the bare `EventStore` (never `SendEventStore`), only one of the two trait names is in scope per module, `Store` is an owned associated type rather than a GAT, every handle the fixture holds is `Rc`-shaped, and the four standing `!Send` probes including `the_probe_is_not_vacuous` still pass"
  satisfied: true
  evidence: "crates/happenstance-cloudflare/tests/support/mod.rs:148 — `type Store = CloudflareEventStore`, bound on the bare `EventStore` (only `happenstance_testkit::Fixture` is imported; `SendEventStore` is named nowhere in this crate's tests). It is an owned associated type, not a GAT. Every refcount on the path is an `Rc`: `DurableObjectHost` holds a `JsHandle` (crates/happenstance-cloudflare/src/host.rs:226), which holds `Rc<JsValue>`. Proven by crates/happenstance-cloudflare/tests/fixture_contract.rs::the_fixture_store_is_not_send — a host-native autoref-specialisation probe over `<CloudflareFixture as Fixture>::Store` carrying its own positive control — and by the four standing probes crates/happenstance-cloudflare/src/lib.rs::tests::{the_probe_is_not_vacuous, the_js_boundary_types_are_not_send, the_error_type_is_not_send, the_send_flavour_does_not_imply_a_send_error} plus their `wasm_tests::` twins, all green."
  mount_point: "crates/happenstance-cloudflare/tests/support/mod.rs:145 (`impl Fixture for CloudflareFixture`), declared by both integration targets"
  verifying_test: "crates/happenstance-cloudflare/src/lib.rs::not_send_probe (all four, :180-259) plus crates/happenstance-cloudflare/tests/fixture_contract.rs::the_fixture_store_is_not_send"

- id: AC-003
  criterion: "**Two fixture instances share nothing, so the suite's isolation rule can bite.** GIVEN the one adapter mistake no upstream test can observe — every fresh fixture instance quietly pointed at the same Durable Object storage (`_decomposition.md:668-681`) — WHEN two `CloudflareFixture` instances are alive at the same time and each appends, THEN neither instance's store reads any event the other appended, because a fresh instance is a fresh object id / namespace entry and **not** a shared object cleared in between"
  satisfied: true
  evidence: "One instance is one object: `CloudflareFixture::new` calls `DurableObjectHost::new()` (crates/happenstance-cloudflare/tests/support/mod.rs:109-117), and each `DurableObjectHost::new` evaluates the shim afresh, standing up a new `DatabaseSync(':memory:')` nothing else can see (crates/happenstance-cloudflare/src/host.rs:244-250). Proven by crates/happenstance-cloudflare/tests/fixture_contract.rs::on_the_object::two_instances_alive_at_once_observe_none_of_each_others_appends, which constructs BOTH instances before either appends — the arrangement that tells isolation apart from clearing. RED beat recorded: with the instances wired to one `thread_local` host the same test failed on the isolation assertion (`[\"OnlyInTheFirst\", \"OnlyInTheSecond\"]` where one was required)."
  mount_point: "crates/happenstance-cloudflare/tests/support/mod.rs:109 (`CloudflareFixture::new`); crates/happenstance-cloudflare/src/host.rs:236 (`DurableObjectHost::new`)"
  verifying_test: "crates/happenstance-cloudflare/tests/fixture_contract.rs::two_instances_alive_at_once_observe_none_of_each_others_appends"

- id: AC-004
  criterion: "**`connect()` gives a second handle onto one object, and the schema seam runs once per object.** GIVEN CF-16 `[FROZEN]` (`spec/SPECIFICATION.md:7548-7568`), whose rule uses `must!` and fails rather than skips on a declined value, WHEN the adapter author calls `connect()` twice on one fixture instance, THEN `SECOND_HANDLE` is `Capability::SUPPORTED`, each handle observes the other's appends, `migrate()` has run exactly once for that instance, and no second store-id incarnation was minted for the second handle (ADR-0014)"
  satisfied: true
  evidence: "`SECOND_HANDLE = Capability::SUPPORTED` at crates/happenstance-cloudflare/tests/support/mod.rs:157; `connect()` hands back a `CloudflareEventStore` over a CLONE of the one `SqlStorage` (:215-222), whose `Clone` aliases the object's storage. `migrate()` is called once, in `new()` (:111-114), and nowhere else. Proven by three cases in crates/happenstance-cloudflare/tests/fixture_contract.rs::on_the_object: `two_handles_from_one_instance_observe_each_others_appends`, `migrate_runs_once_per_instance_not_per_connect` (counts `CREATE TABLE IF NOT EXISTS event (` in the host's own issued-statement log and requires exactly 1 after two `connect()`s) and `a_second_handle_does_not_re_mint_the_store_id` (the second handle still answers `contains_event_id` true for an event the first appended). RED beat recorded: a `migrate()` inside `connect()` made the migration count 3, and `SECOND_HANDLE` declined failed `the_fixture_expression_satisfies_the_macro_arm`."
  mount_point: "crates/happenstance-cloudflare/tests/support/mod.rs:157/:215 (fixture); crates/happenstance-cloudflare/src/event_store.rs:227 `CloudflareEventStore::new` and :284 `migrate` (the one construction seam)"
  verifying_test: "crates/happenstance-cloudflare/tests/fixture_contract.rs::two_handles_from_one_instance_observe_each_others_appends, ::migrate_runs_once_per_instance_not_per_connect, ::a_second_handle_does_not_re_mint_the_store_id"

- id: AC-005
  criterion: "**The gate reader is told what this runtime cannot do, and why it cannot.** GIVEN that a `#[cfg]`-ed-out rule is indistinguishable from a passing one in the emitted binary (`crates/happenstance-testkit/src/contract.rs:26-42`, CF-18 `[FROZEN]` at `spec/SPECIFICATION.md:7597`), WHEN any `Capability` on this fixture is declined, THEN its reason is non-empty and names *why this runtime cannot* rather than *that it cannot*, no rule is `#[cfg]`-ed out of this crate, and the reason survives into a line a human reads — `RuleOutcome::skip_line` into `console_log!` under `__emit_wasm`, because `report`'s `println!` is a measured no-op on `wasm32-unknown-unknown`"
  satisfied: true
  evidence: "The one capability this fixture declines is `MID_BATCH_FAULT`, and its reason names this runtime rather than this fixture: crates/happenstance-cloudflare/tests/support/mod.rs:184-190 (\"this Durable Object host can throw on a chosen statement, so the mechanism exists; what has not been settled … is which statement of this adapter's write path is the k-th row's\"). Proven by crates/happenstance-cloudflare/tests/fixture_contract.rs::every_declined_capability_names_this_runtime, which walks all three `Capability` constants through `Capability::reason()`, rejects an empty reason and rejects a generic refusal by requiring this runtime's vocabulary. RED beat recorded: `declined(\"not decided yet\")` and `declined(\"not supported\")` both failed it by name. No rule is `#[cfg]`-ed out of this crate — the rule set comes only from `for_each_event_store_rule!` — and the emitting half of CF-18 (the `SKIP` line reaching a human through `console_log!`) is `every-rule-under-workerd`'s, discharged there."
  mount_point: "crates/happenstance-cloudflare/tests/support/mod.rs:184 (the declared reason)"
  verifying_test: "crates/happenstance-cloudflare/tests/fixture_contract.rs::every_declined_capability_names_this_runtime"

- id: AC-006
  criterion: "**Nothing this fixture claims is claimed without the method behind it.** GIVEN that the trait ties no `SUPPORTED` constant to its override and the provided bodies **panic** (`contract.rs:290-307`, `:330-352`), and that the reachable author path is *declare supported, forget the override, run the suite*, WHEN `REOPEN` or `MID_BATCH_FAULT` is declared `SUPPORTED`, THEN `reopen()` / `arm_mid_batch_fault()` is overridden with a real seam; and WHEN either is declined, THEN it is **restated explicitly in this impl** with this runtime's own words rather than inherited from the trait default, so no sentence written for a different store appears in this run's log"
  satisfied: true
  evidence: "`REOPEN = Capability::SUPPORTED` (crates/happenstance-cloudflare/tests/support/mod.rs:169) comes WITH its override: `reopen()` at :224-240 replaces the binding with a fresh one taken off the retained `state` (crates/happenstance-cloudflare/src/host.rs:265), which discards the cursor generation and the cached store-id incarnation and touches no row. `MID_BATCH_FAULT` is restated in the impl at :184 rather than inherited. Proven by crates/happenstance-cloudflare/tests/fixture_contract.rs::on_the_object::a_supported_capability_has_its_method_overridden — which calls the method for every capability declared supported, so it reaches the provided body's panic if the override is missing, and then asserts the reopened store still reads what was committed — and by ::mid_batch_fault_is_restated_not_inherited, which compares against a transcribed copy of the trait's default text. RED beat recorded: the inherited default failed the latter with both sides printed identical."
  mount_point: "crates/happenstance-cloudflare/tests/support/mod.rs:169 (`REOPEN`), :224 (`reopen` override), :184 (`MID_BATCH_FAULT`)"
  verifying_test: "crates/happenstance-cloudflare/tests/fixture_contract.rs::a_supported_capability_has_its_method_overridden, ::mid_batch_fault_is_restated_not_inherited"

- id: AC-007
  criterion: "**The shipped macro can consume this fixture unchanged, with its limits stated deliberately and its hand-offs named.** GIVEN that a limit is a fact rather than a trade and a guessed number fails `append_reports_exceeded_store_limits` in one direction or the other (`contract.rs:214-279`, CF-40 `[PROVISIONAL]` at `spec/SPECIFICATION.md:7661`), WHEN the adapter author writes the five-line `event_store_conformance!` invocation, THEN `fixture = CloudflareFixture::new()` type-checks against the general arm unchanged, `MAX_EVENT_DATA_LEN` / `MAX_TAGS_PER_EVENT` / `MAX_EVENTS_PER_BATCH` are each **written out** in this impl — never inherited by omission — with `measured-store-limits` named as the owner of the values, and the concurrency family's non-invocation is documented with its reason rather than left as an absence"
  satisfied: true
  evidence: "crates/happenstance-cloudflare/tests/fixture_contract.rs::the_fixture_expression_satisfies_the_macro_arm binds `CloudflareFixture::new()` behind the macro arm's own shape — an `async fn __conformance_fixture() -> impl Fixture` handed to an `impl AsyncFn() -> F` parameter — so the slice-mate's three-line target compiles by construction. The three ceilings are each WRITTEN OUT at crates/happenstance-cloudflare/tests/support/mod.rs:205, :209, :213 with `measured-store-limits` named on each as the owner of the value; ::the_three_store_limits_are_stated_here reads the impl's own source and rejects inheritance by omission (RED beat: with the constants absent it failed naming `MAX_EVENT_DATA_LEN`). The concurrency family's reasoned non-invocation — `event_store_concurrency_conformance!` binds `F::Store: EventStore + Send` and its module is `#[cfg(not(target_arch = \"wasm32\"))]` — and the model family's `proptest`/target-scope status are documented at crates/happenstance-cloudflare/tests/support/mod.rs:36-59."
  mount_point: "crates/happenstance-cloudflare/tests/support/mod.rs:145-214 (fixture + its docs); consumed at crates/happenstance-testkit/src/lib.rs:494-521"
  verifying_test: "crates/happenstance-cloudflare/tests/fixture_contract.rs::the_fixture_expression_satisfies_the_macro_arm, ::the_three_store_limits_are_stated_here"
```
