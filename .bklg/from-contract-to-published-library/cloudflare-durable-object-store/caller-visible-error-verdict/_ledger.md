---
item: HS-S0052
stage: implement
created: 2026-08-12T13:46:50.413Z
updated: 2026-08-12T13:46:50.413Z
---

# Acceptance ledger — The ES-6 artefact: constraint violation versus transport fault, recovered by a caller

The machine-checkable Definition-of-Done ledger for this story (RFC §6.5). One row per spec
`AC-###`. Planning authors every row with `satisfied: false`; the implementer may only flip a row to
`satisfied: true` and MUST cite real evidence (a `file:line` and/or the verifying test id) — never
edit, remove, or re-word a criterion, and never flip a satisfied row back. `redkiln verify --grain
story` reads the fenced block below and blocks `implement → report` unless every spec AC is present,
`satisfied: true`, and carries non-placeholder evidence (and no AC that was satisfied on the base
branch has regressed). Scope changes are a human decision recorded through `redkiln advance`, not a
quiet ledger edit.

Two rows carry a conditional shape settled at implementation, not here. AC-006's `verifying_test`
names the host-native tier (the testing brief's recommended default); if `cargo test -p
happenstance-cloudflare` stops linking once `worker` is real, the same tests move behind
`#[cfg(all(test, target_arch = "wasm32"))]` and re-emit through `wasm_bindgen_test` — the evidence
cites whichever ran, and the probes move with them rather than being deleted (spec EC-004). AC-003's
evidence must additionally cite the accessor added under EC-003, if one was needed.

```yaml
- id: AC-001
  criterion: "GIVEN a library consumer appending under an AppendCondition against a Durable Object whose SQLite refuses the write with the real text `UNIQUE constraint failed: event.position`, WHEN they call append on a store built the only way there is — CloudflareEventStore::new(sql) — THEN they receive Err(AppendError::ConditionViolated) and nothing else, so their next move is \"re-read and rebuild the decision model\" and never \"retry the transport\". The thrown value is constructed in-test with that exact text, not round-tripped through a live Durable Object, so the artefact stands whether or not the workerd runner exists yet"
  satisfied: true
  evidence: "es6_reconstruction::constraint_violation_reaches_the_caller_as_condition_violated at crates/happenstance-cloudflare/src/lib.rs:530, in the crate root beside the !Send probes. It builds the store the only way there is — CloudflareEventStore::new(sql) — arms the exact documented text `UNIQUE constraint failed: event.position` on the next INSERT, calls append under an AppendCondition, and asserts AppendError::is_condition_violated() plus the *absence* of a Store arm. The thrown value is constructed in-test (test_object::arm_throw, crates/happenstance-cloudflare/src/test_object.rs:191, which evaluates `throw new Error(message)` inside the shim) and then travels the production path: worker's real wasm-bindgen externs → SqlError::from_worker → JsThrow::is_constraint_violation → classify_write (crates/happenstance-cloudflare/src/event_store.rs:701). Nothing is mocked between the throw and the caller and no workerd runner is involved. No ConditionViolated variant was added to CloudflareEventStoreError; write_path_tests::the_error_type_has_no_condition_violated_variant still guards that at compile time. RED: with classify_write's constraint arm disabled, this test failed together with the_distinction_is_reachable_from_outside_the_crate and write_path_tests::a_unique_constraint_message_classifies_as_condition_violated (3 failed / 71 passed). GREEN: 74 passed / 0 failed."
  mount_point: "crates/happenstance-cloudflare/src/lib.rs"
  verifying_test: "crates/happenstance-cloudflare/src/lib.rs::tests::constraint_violation_reaches_the_caller_as_condition_violated (cargo test -p happenstance-cloudflare)"
- id: AC-002
  criterion: "GIVEN the same consumer, and a Durable Object that fails for a reason that is not a constraint violation, WHEN append returns, THEN they receive Err(AppendError::Store(CloudflareEventStoreError::Sql(SqlError::Thrown(..)))) and can recover, from the public surface alone, that this was a transport fault and not a conflict — enough to retry rather than rebuild. \"Enough\" is asserted on the variant plus the thrown text reachable through Display or core::error::Error::source, never on which SQL statement ran"
  satisfied: true
  evidence: "es6_reconstruction::transport_fault_reaches_the_caller_distinguishably at crates/happenstance-cloudflare/src/lib.rs:561. The same store, the same call, a non-constraint throw (`network connection lost`): the caller receives AppendError::Store(CloudflareEventStoreError::Sql(SqlError::Thrown(..))) — asserted as a shape, not as a statement — and can still recover *what the store said*. \"Enough\" is read by one named helper, what_the_store_said (:498), which walks Display plus the public core::error::Error::source chain and touches no private field, no pub(crate) helper and no #[cfg(test)] back door; names_the_underlying_failure (:514) is the predicate over it, and the test asserts it true for the transport text and false for `constraint failed`. Nothing asserts which SQL statement ran. RED: with classify_write discarding the evidence (the Store arm rebuilt as a contentless CloudflareEventStoreError), this test failed alongside an_evidence_discarding_classifier_is_rejected (2 failed / 72 passed). GREEN: 74 passed / 0 failed."
  mount_point: "crates/happenstance-cloudflare/src/lib.rs"
  verifying_test: "crates/happenstance-cloudflare/src/lib.rs::tests::transport_fault_reaches_the_caller_distinguishably (cargo test -p happenstance-cloudflare)"
- id: AC-003
  criterion: "GIVEN a downstream consumer — someone who has only happenstance-cloudflare as a dependency and cannot see its internals — WHEN they attempt the reconstruction AC-001 and AC-002 assert, THEN they can do it: the tests reach the store through CloudflareEventStore::new(sql), bind EventStore (not SendEventStore, CLAUDE.md constraint 4), and read only pub items — no private field, no pub(crate) helper, no #[cfg(test)] back door. If the real worker bindings leave the fact unreachable, the minimal accessor that restores it is added, documented per standards/rust/70-rustdoc-obligations.md, keeps CloudflareEventStoreError #[non_exhaustive], and is recorded in this spec's behavior table on merge"
  satisfied: true
  evidence: "**No accessor was needed, and that is the finding: the real `worker` bindings left the fact reachable, so EC-003 did not fire and this diff adds no public item at all.** Every path AC-001 and AC-002 take is already `pub`: CloudflareEventStore::new, EventStore::append, AppendError's arms and is_condition_violated, CloudflareEventStoreError's variants (still #[non_exhaustive]), SqlError::Thrown, and Display/source through core::error::Error. what_the_store_said (crates/happenstance-cloudflare/src/lib.rs:498) reads only those. Proved from a consumer's seat rather than asserted: es6_reconstruction::the_distinction_is_reachable_from_outside_the_crate (:648) contains `classify_like_a_consumer<S: EventStore>` — generic over the **bare** flavour, the weaker bound (CLAUDE.md constraint 4) — which branches to \"rebuild\" / \"retry\" / \"accepted\" using nothing but the port and Display, and returns all three against the same store. It is a compile-time proof as much as a runtime one: if the distinction stopped being reachable without reaching inside, the function would stop compiling rather than quietly keep passing. Surfaces confirmed by RUSTDOCFLAGS=\"-D warnings\" cargo doc -p happenstance-cloudflare --no-deps --document-private-items (clean) and cargo clippy -p happenstance-cloudflare --all-targets --all-features -- -D warnings (clean). The only new item anywhere is test_object::arm_throw, which is pub(crate) and #[cfg(all(test, target_arch = \"wasm32\"))] — not public surface and not on any path the assertions read."
  mount_point: "crates/happenstance-cloudflare/src/lib.rs"
  verifying_test: "crates/happenstance-cloudflare/src/lib.rs::tests (AC-001 + AC-002 bodies reaching only public paths) plus `cargo doc -p happenstance-cloudflare` and `cargo clippy --workspace --all-targets --all-features -- -D warnings`"
- id: AC-004
  criterion: "GIVEN the adapter author who wants to trust this artefact rather than admire it, WHEN they read the diff, THEN they find a named wrong error shape in this crate's own test tree — D3's classifier that picks the right AppendError arm and then discards the evidence — and the same assertion AC-002 uses rejects it. Modelled on the_probe_is_not_vacuous, which exists because the !Send module would otherwise pass with a broken probe; without it this is a rule no adapter can fail (CLAUDE.md, a rule that no adapter can fail is decorative)"
  satisfied: true
  evidence: "es6_reconstruction::an_evidence_discarding_classifier_is_rejected at crates/happenstance-cloudflare/src/lib.rs:608. D3's shape exactly — not the blunt mutant but the one a careful implementer reaches honestly: the right AppendError arm, the evidence thrown away. The test holds the real failure and the flattened one side by side, runs **the same predicate** names_the_underlying_failure (:514) over both, and asserts true for the real one and false for the wrong shape; it then asserts the two agree on is_condition_violated(), which is the point — the arm alone cannot tell them apart, so a check that read only the arm would be a rule no adapter can fail. Confirmed against the real classifier rather than only against a local double: mutating classify_write (crates/happenstance-cloudflare/src/event_store.rs:701) into that exact shape turned the whole store into the wrong implementation and this test failed (2 failed / 72 passed), together with transport_fault_reaches_the_caller_distinguishably. Modelled on the_probe_is_not_vacuous (:295), which exists for the same reason one module up."
  mount_point: "crates/happenstance-cloudflare/src/lib.rs"
  verifying_test: "crates/happenstance-cloudflare/src/lib.rs::tests::an_evidence_discarding_classifier_is_rejected (cargo test -p happenstance-cloudflare)"
- id: AC-005
  criterion: "GIVEN the gate reader who opens the crate documentation to find out what ES-6 resolved to, WHEN they read the Findings section, THEN it reads as observation against a real worker::Error rather than prediction against a stand-in — findings 2 and 3 in particular — and names adr-0023-and-atom-resolutions as where the atom lands. AND the diff writes no file under .kb/: the verdict is recorded here and minted there, through /redkiln:kb-ingest, never hand-written (project.md, DR-7)"
  satisfied: true
  evidence: "Finding 2 gains an observational paragraph at crates/happenstance-cloudflare/src/lib.rs:94-101 — 'Now observed rather than predicted, and from the caller's seat rather than this module's' — naming the tests that do the observing. Finding 3 gains :115-120, which states the half the absence of a ConditionViolated variant does *not* excuse: the error still owes a caller enough to tell a transport fault from a conflict, and names the evidence-discarding shape as what fails that. A new `# The ES-6 verdict` section at :131-144 records the outcome — ADR-0009's decision holds and this adapter is the evidence for it rather than the exception — and names `adr-0023-and-atom-resolutions` as where the atom is minted, through /redkiln:kb-ingest. The Findings section was not restructured: findings 1-4 keep their order, their headings and their text, and the verdict sits after finding 4 rather than inside the list. Mechanically: `git status --short -- .kb/` is empty and `git diff --name-only` for this story contains no .kb/ path — nothing under .kb/ is written and .kb/decisions/0009-error-send-sync.md is untouched; `redkiln validate --kb` reports `validate passed`, which is what checks accepted atoms against HEAD; RUSTDOCFLAGS=\"-D warnings\" cargo doc -p happenstance-cloudflare --no-deps --document-private-items builds the rewritten section clean."
  mount_point: "crates/happenstance-cloudflare/src/lib.rs"
  verifying_test: "crates/happenstance-cloudflare/src/lib.rs:12-21 and :63-95 reviewed in the diff; `git diff --name-only` against the merge base contains no .kb/ path; `redkiln validate --kb && redkiln doctor` clean; docs step of `cargo xtask ci`"
- id: AC-006
  criterion: "GIVEN a contributor with no workerd toolchain — an ordinary inner loop — WHEN they run one cargo test, THEN they reach AC-001 through AC-004 and the four !Send probes: ES-6's information half and its auto-trait half answer to the same command. Which target hosts them is settled at implementation, not before — host-native beside the probes if `cargo test -p happenstance-cloudflare` still links once worker is real (the testing brief's recommended default), otherwise behind #[cfg(all(test, target_arch = \"wasm32\"))] re-emitting through wasm_bindgen_test, with the probes moved too and never deleted"
  satisfied: true
  evidence: "**The tier question is settled by measurement, and the answer is the EC-004 branch — for the reconstruction only.** `worker`'s bindings resolve to panicking stubs off the target, so a store cannot be *driven* on the host at all: no `CloudflareEventStore` can be constructed there, whatever the tests want. The reconstruction therefore lives at crates/happenstance-cloudflare/src/lib.rs:452 under #[cfg(all(test, target_arch = \"wasm32\"))], re-emitting through #[wasm_bindgen_test] — the same tier `durable-object-write-path` and `durable-object-read-path` already put their targeted tests in, and the same Cargo.toml dev-dependency block `worker-binding-layer` already added. No probe was deleted or relocated to get there: the four !Send assertions are written once in `not_send_assertions` (:266) and wrapped twice, by `tests` on the host (:352) and by `wasm_tests` on wasm32 (:384), so `cargo test -p happenstance-cloudflare` with no wasm toolchain at all still runs the_probe_is_not_vacuous, the_js_boundary_types_are_not_send, the_error_type_is_not_send and the_send_flavour_does_not_imply_a_send_error (4 passed) plus send_shape's two doctests. One command reaches both halves: `cargo test -p happenstance-cloudflare --target wasm32-unknown-unknown --lib` under wasm-bindgen-test-runner lists the five es6_reconstruction tests *and* wasm_tests::the_probe_is_not_vacuous — 74 passed / 0 failed. `cargo xtask affected --base main`: `affected gate passed`. `cargo xtask ci --fast`: `all required checks passed` at slice close."
  mount_point: "crates/happenstance-cloudflare/src/lib.rs"
  verifying_test: "cargo test -p happenstance-cloudflare lists the four new tests and the_probe_is_not_vacuous (or the crate's wasm-bindgen-test invocation if EC-004 fires); `cargo xtask affected --base main` at story close and `cargo xtask ci --fast` at slice close"
- id: AC-007
  criterion: "GIVEN the gate reader again, WHEN the slice merges, THEN nothing this story did weakened a standing detector: the_error_type_is_not_send, the_js_boundary_types_are_not_send, the_send_flavour_does_not_imply_a_send_error and the_probe_is_not_vacuous all still pass; send_shape::send_flavour::SendStoreWithLocalError still compiles; Error's bound is untouched and .kb/decisions/0009-error-send-sync.md unedited; ES-6's [FROZEN] clause is discharged with evidence, not amended"
  satisfied: true
  evidence: "Every standing detector is still loud. The four probes pass on **both** targets — host `cargo test -p happenstance-cloudflare` (4 passed) and wasm32 `wasm_tests::*` inside the 74-test run — the_probe_is_not_vacuous included, which is the one that stops the other three passing for the wrong reason (crates/happenstance-cloudflare/src/lib.rs:266-334, :352, :384). send_shape::send_flavour::SendStoreWithLocalError still compiles, and its two doctests — one passing, one compile_fail,E0277 — still pass under `cargo test -p happenstance-cloudflare --doc` (1 + 1). `Error`'s bound is untouched: `git status` shows no change anywhere under crates/happenstance-core/, so neither port's definition moved. `.kb/decisions/0009-error-send-sync.md` is unedited and `redkiln validate --kb` reports `validate passed`, which is the check that compares accepted atoms against HEAD. `cargo xtask spec-trace` reports `traceability: no problems found` over 201 clauses and 401 citations with ES-6's markers, its `Rule:` line and its citations into src/js.rs unchanged — the clause is discharged with evidence, not amended, and spec/SPECIFICATION.md is not in this diff at all. `cargo xtask lint-constitution`: `27 atoms, all consistent` after seven file:line citations into src/lib.rs were re-pointed to follow the documentation edits."
  mount_point: "crates/happenstance-cloudflare/src/lib.rs"
  verifying_test: "crates/happenstance-cloudflare/src/lib.rs:180-259 green under cargo test -p happenstance-cloudflare; `cargo xtask spec-trace` green with spec/SPECIFICATION.md:2629 unchanged; `redkiln validate --kb` clean"
```
