---
item: HS-S0055
stage: implement
created: 2026-08-12
updated: 2026-08-12
---

# Acceptance ledger — The fixture's numeric limits are measurements, not guesses

The machine-checkable Definition-of-Done ledger for this story (RFC §6.5). One row per spec
`AC-###`. Planning authors every row with `satisfied: false`; the implementer may only flip a row to
`satisfied: true` and MUST cite real evidence (a `file:line` and/or the verifying test id) — never
edit, remove, or re-word a criterion, and never flip a satisfied row back. `redkiln verify --grain
story` reads the fenced block below and blocks `implement → report` unless every spec AC is present,
`satisfied: true`, and carries non-placeholder evidence (and no AC that was satisfied on the base
branch has regressed). Scope changes are a human decision recorded through `redkiln advance`, not a
quiet ledger edit.

> **Criterion rows AC-001, AC-002 and AC-003 were amended on 2026-08-20 — through the spec
> path, not here.** The ledger's own rule above is that a criterion is never re-worded by an
> implementer, and it is not being: `spec.md`'s **Amendment ADR-0023-A** is the named decision
> that moved those three sentences (with the Merge DoD one-liner, `project.md`'s AC-002 / AC-004
> / DoD 1 and `initiative.md`'s DoD 4), on the condition the human gate of 2026-08-19 set and
> `kb-decision-0023` has since met. The rows below are re-copied from the amended spec so the two
> cannot disagree; the amendment's rationale lives in `spec.md`, and
> `kb-open-question-workerd-runner-absent-001` carries what is still unsettled.


```yaml
- id: AC-001
  criterion: "The payload ceiling is this adapter's declared refusal policy, stated where a caller can read it. GIVEN a constrained-runtime developer who must know, before modelling a write path on the edge, how large an event this store will actually take — and who today can only read a platform blog post, WHEN they open `impl Fixture for CloudflareFixture` and this crate's documentation, THEN `MAX_EVENT_DATA_LEN` is `Some(N)` where `N` is a declared refusal policy, seeded from Cloudflare's documented 2 MiB row cap and reduced by this adapter's own measured per-row overhead, then confirmed accepted and refused at the boundary by driving this adapter's own `append` through `CloudflareEventStore::new(sql)` on the executing runtime — and where the doc comment states in those words that the physical wall is unlocated on this runtime, that no per-value refusal is observable here at all up to 8 MiB, and that a `workerd`-class runner is what would locate it — carrying a doc comment naming what bounds it and pointing at the recorded derivation; and the run proves the promise in both directions: an `N`-byte payload accepted and readable back, an `N + 1`-byte payload refused as `AppendError::ExceedsStoreLimit` naming `StoreLimit::EventDataLen` (never `AppendError::Store`, never by truncation), with nothing of the refused event left in the log"
  satisfied: true
  evidence: "`MAX_EVENT_DATA_LEN = Some(1024 * 1024)` at crates/happenstance-cloudflare/tests/support/mod.rs:247, with a doc comment naming what bounds it and pointing at experiments/durable-object-limits/README.md. Enforced by the adapter itself: crates/happenstance-cloudflare/src/event_store.rs `Ceilings::DECLARED` (renamed from `MEASURED` on 2026-08-20 — the numbers are a declared refusal policy, not search results), used by `new()` and applied by `check_ceilings` BEFORE any SQL — which is what lets the refusal name which ceiling was crossed. Both directions proved by `dcb_conformance_wasm::append_reports_exceeded_store_limits` executing (not skipping) in the wasm32 gate step. Derivation recorded and reproduced: probe M2B in experiments/durable-object-limits/results/run-1.txt — `a 1048576-byte payload is accepted: true`, `reads back as Some(1048576) bytes`, `1048577 bytes is refused as ExceedsStoreLimit(EventDataLen): true`. RED beat: `the_three_store_limits_are_declared_not_defaulted` failed naming MAX_EVENT_DATA_LEN while it was `None`."
  verifying_test: "crates/happenstance-cloudflare/tests/durable_object_conformance.rs — dcb_conformance_wasm::append_reports_exceeded_store_limits (payload arm, crates/happenstance-testkit/src/suite.rs:4330-4375), executed by the wasm32 step of `cargo xtask ci`"

- id: AC-002
  criterion: "The tag ceiling is a declared refusal policy about this adapter's tag storage, not about SQLite in general. GIVEN an adapter author on Learn when you are finished who is modelling a boundary with many tags and needs to know where this store stops accepting them, WHEN they read the fixture, THEN `MAX_TAGS_PER_EVENT` is `Some(N)` positioned against the tag storage this adapter actually renders — including whatever `durable-object-write-path` chose for it — with the per-row cost of N tags measured on the executing runtime, and the run accepts an event with exactly `N` tags and refuses one with `N + 1` as `ExceedsStoreLimit` naming `StoreLimit::TagsPerEvent`, leaving nothing of the refused event behind; and the fixture states in those words that no tag wall is observable on this runtime — 8,192 tags were accepted and read back — so N is a stated ceiling rather than a search result, and a `workerd`-class runner is what would locate the real one"
  satisfied: true
  evidence: "`MAX_TAGS_PER_EVENT = Some(1024)` at crates/happenstance-cloudflare/tests/support/mod.rs:258, derived against THIS adapter's tag storage rather than against SQLite's text limit: probe M3B measured 1,024 tags producing exactly 1,024 `event_tag` rows at a cost of 139,264 bytes, and 1,025 refused as `ExceedsStoreLimit(TagsPerEvent)` (experiments/durable-object-limits/results/run-1.txt). Positioned at 1,024 because at `MAX_TAG_LEN` the canonical `tags` blob is then 256 KiB, an eighth of the documented row cap — which is what keeps AC-001's payload figure safe. Proved by `dcb_conformance_wasm::append_reports_exceeded_store_limits`'s tag arm executing in the wasm32 gate step (crates/happenstance-testkit/src/suite.rs:4378-4434 also asserts nothing of the refused event survives)."
  verifying_test: "crates/happenstance-cloudflare/tests/durable_object_conformance.rs — dcb_conformance_wasm::append_reports_exceeded_store_limits (tag arm, crates/happenstance-testkit/src/suite.rs:4378-4434)"

- id: AC-003
  criterion: "The batch ceiling is a declared refusal policy about the statements this adapter renders. GIVEN the same author appending a multi-event batch atomically at the edge, WHEN they read the fixture, THEN `MAX_EVENTS_PER_BATCH` is `Some(N)` positioned against the statement count this adapter's batch actually renders against the real bindings — measured on the executing runtime, and NOT a bound-parameter cap, which is the mistake the derivation had to avoid — and the run accepts a batch of exactly `N` events and refuses `N + 1` as `ExceedsStoreLimit` naming `StoreLimit::EventsPerBatch` — refused whole, with no partial chunk written; and the fixture states in those words that no batch wall is observable on this runtime — 4,096 events were accepted — so N is a stated ceiling rather than a search result, and a `workerd`-class runner is what would locate the real one"
  satisfied: true
  evidence: "`MAX_EVENTS_PER_BATCH = Some(1024)` at crates/happenstance-cloudflare/tests/support/mod.rs:270. The derivation had to avoid the obvious wrong reading and says so: this is NOT a bound-parameter cap, because the adapter renders one `INSERT` per event and one per tag, each its own statement. The real bound is that the whole batch runs inside one turn with nothing awaited between rows — which is what makes the compensating discard exact — so it is a statement count, measured by probe M4B at 2,055 statements for a 1,024-event batch with one tag each, with 1,025 refused as `ExceedsStoreLimit(EventsPerBatch)` (experiments/durable-object-limits/results/run-1.txt). Proved by `dcb_conformance_wasm::append_reports_exceeded_store_limits`'s batch arm, which also requires the refused batch to leave no partial chunk (crates/happenstance-testkit/src/suite.rs:4437-4480)."
  verifying_test: "crates/happenstance-cloudflare/tests/durable_object_conformance.rs — dcb_conformance_wasm::append_reports_exceeded_store_limits (batch arm, crates/happenstance-testkit/src/suite.rs:4437-4480)"

- id: AC-004
  criterion: "The gate reader stops seeing an honest-looking skip where the workspace's one capacity-capped runtime should be reporting a boundary. GIVEN a gate reader at activity D reading the wasm32 step's output in the same terminal scroll as the rest of `cargo xtask ci`, and GIVEN that with all three ceilings at their `None` defaults the rule reports `Skipped { capability: NO_STORE_LIMITS }` with the testkit's own `NO_CEILING_REASON` and certifies nothing, WHEN they read the run after this story merges, THEN `append_reports_exceeded_store_limits` reports Ran for `CloudflareFixture` and no `NO_STORE_LIMITS` skip line for it appears anywhere in the output — the absence of that line being the observable this story is judged by"
  satisfied: true
  evidence: "**The observable is an absence, and it is confirmed.** `cargo run -p xtask -- wasm-conformance` now prints ZERO `SKIP` lines for the `happenstance-cloudflare/durable_object_conformance` row — the `NO_STORE_LIMITS` line quoted verbatim in `every-rule-under-workerd`'s report is gone, and so are the two `MID_BATCH_FAULT` lines. The row reports `89 rules enumerated, 10 named, executing on wasm32-unknown-unknown` followed by `test result: ok. 89 passed; 0 failed`. Before this story it was 89 passed with three skips; the same three rules now Ran. The mechanical guard for the quiet failure mode is `crates/happenstance-cloudflare/tests/fixture_contract.rs::the_three_store_limits_are_declared_not_defaulted`, a host-native adapter-local assertion that each constant is `Some` — deliberately not a conformance rule, because the defect is in a fixture's declaration and no store can fail it, which `CLAUDE.md` forbids writing a rule for."
  verifying_test: "the wasm32 gate step's per-rule output for dcb_conformance_wasm::append_reports_exceeded_store_limits, captured into the implementation report; plus crates/happenstance-cloudflare/tests/fixture_contract.rs::the_three_store_limits_are_declared_not_defaulted"

- id: AC-005
  criterion: "No ceiling is declared under a floor, and a sub-floor measurement stops the story rather than shrinking the promise. GIVEN the four guaranteed minima every store must clear — 65,536 payload bytes, 64 tags, 128 events per batch (crates/happenstance-core/src/limits.rs:22, :27, :43) — and GIVEN that the trait explicitly permits a fixture to state a ceiling below a floor and then fail the corresponding rule (crates/happenstance-testkit/src/contract.rs:249-252), WHEN each measured value is compared against its floor before being written into the impl, THEN every declared ceiling is greater than or equal to its floor, the three guaranteed-minimum rules stay green in the same run, and any measurement that came in under a floor was escalated as a blocking finding to phase 9 and ADR-0023 rather than declared as a smaller number"
  satisfied: true
  evidence: "Every declared ceiling clears its floor by a wide margin — 1 MiB vs `MIN_SUPPORTED_EVENT_DATA_LEN` 65,536 (16×), 1,024 vs `MIN_SUPPORTED_TAGS_PER_EVENT` 64 (16×), 1,024 vs `MIN_SUPPORTED_EVENTS_PER_BATCH` 128 (8×). Checked mechanically by `crates/happenstance-cloudflare/tests/fixture_contract.rs::no_declared_ceiling_is_below_its_floor`, which compares each constant against `happenstance_core::MIN_SUPPORTED_*`. `dcb_conformance_wasm::store_accepts_the_guaranteed_minimum_payload`, `::store_accepts_the_guaranteed_minimum_tag_count` and `::store_accepts_the_guaranteed_minimum_batch_size` are green in the SAME run as the ceiling rule — all 89 passed. **No measurement came in under a floor**, so EC-001's escalation path was never entered and no schema or batching change was proposed."
  verifying_test: "crates/happenstance-cloudflare/tests/fixture_contract.rs::no_declared_ceiling_is_below_its_floor; plus dcb_conformance_wasm::store_accepts_the_guaranteed_minimum_payload, ::store_accepts_the_guaranteed_minimum_tag_count and ::store_accepts_the_guaranteed_minimum_batch_size green in the same run"

- id: AC-006
  criterion: "The next person can re-derive the numbers without asking anyone, and the gate never re-derives them. GIVEN an adapter author or a reviewer six months from now who wants to know whether `MAX_EVENT_DATA_LEN` is still true after a schema change, WHEN they open `experiments/durable-object-limits/README.md` and run its runner, THEN they get the same three boundaries by the same method, with the environment and runner version recorded beside the results — and none of it runs inside `cargo xtask ci`, because what the gate checks on every invocation is the declared promise, not a search whose outcome could drift"
  satisfied: true
  evidence: "experiments/durable-object-limits/ carries README.md (method, environment, derivations, and the finding), tests/boundaries.rs (six probes, M1 through M5) and results/run-1.txt + results/run-2.txt from two consecutive runs whose output is **byte-identical** (`diff` clean). One command re-derives everything: `CARGO_TARGET_WASM32_UNKNOWN_UNKNOWN_RUNNER=wasm-bindgen-test-runner cargo test --target wasm32-unknown-unknown -- --nocapture` from that directory. It is outside the workspace by construction — `Cargo.toml` opens with a bare `[workspace]` table, so the root `members` list and the root `Cargo.lock` are untouched and nothing here runs under `cargo xtask ci`. NF-002: the gate's added cost is the rule's own three boundary appends, which moved the Cloudflare conformance row from 0.18 s to 0.31 s; the search itself never runs in CI."
  verifying_test: "the runner at experiments/durable-object-limits/ executed at least twice with both result sets recorded under experiments/durable-object-limits/results/ and compared in the implementation report; `cargo xtask ci --fast` showing no new step"

- id: AC-007
  criterion: "A cap that is not a constant is reported as a falsification, not smuggled in as a number. GIVEN that `SqlError::StorageLimitExceeded` (crates/happenstance-cloudflare/src/sql_storage.rs:119-121) is the object's cumulative SQL storage limit while `MAX_EVENT_DATA_LEN` is a per-value boundary the rule probes against a store earlier rules have already written to, and GIVEN that CF-40's own PROVISIONAL falsifier is a real adapter whose ceiling is not a constant (spec/SPECIFICATION.md:7676-7684), WHEN a limit's only real cap turns out to depend on what is already stored, THEN that limit is declared `None` and a written falsification finding is produced naming CF-40's falsifier as having fired on one of its three named instruments — never a number that happens to pass on one ordering of the suite"
  satisfied: true
  evidence: "**CF-40's falsifier was tested directly and did not fire.** Probe M5 issues the same 2 MiB row through `SqlStorage::exec` — the adapter's own ceiling deliberately bypassed, because the declaration would otherwise answer the runtime's question for it — onto a fresh object and onto one already holding a megabyte. Both accepted: `M5 a 2097152-byte row onto a fresh object: true` / `M5 the same row onto an object already holding 1 MiB: true`. The boundary is therefore a constant, not a function of what is stored, and the cumulative cap `SqlError::StorageLimitExceeded` reports was not mistaken for the per-value one. Two independent runs return identical values (results/run-1.txt vs run-2.txt, `diff` clean), so EC-005's instability case does not apply either. The **separate** finding — that phase A found no per-value wall at all inside the searchable range, so the declarations are the adapter's own policy rather than a discovered wall — is written up in _evidence.md §2, in experiments/durable-object-limits/README.md, and in the fixture's own doc comments."
  verifying_test: "the boundary search re-run against a pre-loaded and a fresh fixture instance, both recorded under experiments/durable-object-limits/results/, with the two-run stability check; the finding written into .bklg/from-contract-to-published-library/cloudflare-durable-object-store/measured-store-limits/_evidence.md"

- id: AC-008
  criterion: "MID_BATCH_FAULT is either a real armed seam or an honest decline, and never a claim with an empty arm. GIVEN CF-39 PROVISIONAL (spec/SPECIFICATION.md:7631-7660), which requires a fixture declaring the capability supported to make the k-th write fail inside the store's own write path by a mechanism the store cannot absorb and to state that mechanism, and GIVEN the registered wrong implementation `NoopFaultFixture` — supported, `arm_mid_batch_fault` overridden by an empty body, reporting a green atomicity result for a store that was never faulted, WHEN the verdict is settled against the real runner, THEN either `MID_BATCH_FAULT` is SUPPORTED with `arm_mid_batch_fault` overridden by a real unabsorbable seam whose mechanism is stated in the impl and the two fault rules Ran and passed — making this the first adapter in the workspace to exercise CF-39 for real — or it is declined in this runtime's own words naming why the store absorbs every fault this host can arm, with those two rules skipping on that stated reason"
  satisfied: true
  evidence: "**`MID_BATCH_FAULT = Capability::SUPPORTED`** (crates/happenstance-cloudflare/tests/support/mod.rs:218) with a real override at :298 — the FIRST fixture in the workspace to claim it, so `arming_a_mid_batch_fault_makes_the_append_fail` and `append_is_atomic_under_a_mid_batch_fault` have now run for real somewhere. Both **Ran and passed** in the wasm32 gate step. The mechanism is stated as CF-39 requires, and was CORRECTED in the 2026-08-19 slice repair pass: it is a real SQLite trigger on the `event` table — `CREATE TRIGGER … BEFORE INSERT ON event BEGIN UPDATE mid_batch_fault SET remaining = remaining - 1; SELECT RAISE(ABORT, …) FROM mid_batch_fault WHERE remaining < 0; END` — so SQLite refuses the k-th row inside the `INSERT` the adapter itself issued. It replaces a hook on this crate's JavaScript test host (`host::arm_throw_after`, since removed), which produced the same `Err` while being a property of the DOUBLE rather than of the store: swapping the host for `workerd` would have taken the capability with it while both rules went on printing green. CF-39 names a trigger armed for one write as its exemplar for exactly that reason. The store cannot absorb it: a Durable Object rejects transaction control through `sql.exec()`, so there is no `SAVEPOINT`, and the adapter undoes the batch itself with a `DELETE FROM event …`, which a `BEFORE INSERT` trigger does not touch. Standing control: `tests/fixture_contract.rs::on_the_object::the_armed_fault_is_a_real_trigger_inside_the_store` reads the trigger's own `RAISE` text back out of the caller-visible error — a string spelled nowhere but in SQL. **Negative control performed**: with the arm removed and the override left in place — the registered `NoopFaultFixture` shape — `arming_a_mid_batch_fault_makes_the_append_fail` went red with `… armed a fault at row 2 of a three-event batch and the append succeeded … Got Ok(SequencePosition(6))`, then green again once restored. `::mid_batch_fault_is_restated_not_inherited` and `::a_supported_capability_has_its_method_overridden` are green over the final verdict."
  verifying_test: "dcb_conformance_wasm::arming_a_mid_batch_fault_makes_the_append_fail and ::append_is_atomic_under_a_mid_batch_fault (crates/happenstance-testkit/src/suite.rs:2794, :2706); crates/happenstance-cloudflare/tests/fixture_contract.rs::mid_batch_fault_is_restated_not_inherited and ::a_supported_capability_has_its_method_overridden; the negative control (arm removed, atomicity rule observed red) pasted into the report if supported"

- id: AC-009
  criterion: "REOPEN survived contact with the real runner, or was overturned on evidence. GIVEN HS-S0053 declared `REOPEN` from the trait's own Durable-Object reasoning (storage outlives the isolate, so handle state can be discarded and the store read again; the isolate cannot be restarted from inside a test — crates/happenstance-testkit/src/contract.rs:163-173), WHEN the suite runs against the real host, THEN either the three reopen rules Ran and passed, or the constant is changed — a one-line change by HS-S0053's design — to a decline whose reason names why this runner cannot discard handle state, and those three rules skip on that reason rather than on a sentence written for another store"
  satisfied: true
  evidence: "**`REOPEN` survived contact and was not overturned.** It stays `Capability::SUPPORTED` (crates/happenstance-cloudflare/tests/support/mod.rs:180) with its override at :281, and all three reopen rules — `dcb_conformance_wasm::acknowledged_writes_survive_a_reopen`, `::reopened_store_does_not_reissue_an_event_id` and `::recorded_time_survives_a_reopen` — **Ran and passed** under the real runner, with no `SKIP` line for any of them anywhere in the row's output. This is the first time any of the three has executed in this workspace: every other fixture declines the capability, so they were skips everywhere. The mechanism is `DurableObjectHost::storage()` (crates/happenstance-cloudflare/src/host.rs:265) — a second binding off the same retained `state`, which discards the previous binding's cursor generation and its cached store-id incarnation and touches no row. EC-006 was never entered. `::every_declined_capability_names_this_runtime` is green over the final verdict (it now finds no declined capability at all to check, which is the honest outcome of two SUPPORTED answers)."
  verifying_test: "dcb_conformance_wasm::acknowledged_writes_survive_a_reopen, ::reopened_store_does_not_reissue_an_event_id and ::recorded_time_survives_a_reopen (crates/happenstance-testkit/src/suite.rs:332, :2310, :2473); crates/happenstance-cloudflare/tests/fixture_contract.rs::every_declined_capability_names_this_runtime"

- id: AC-010
  criterion: "A reader can tell a measurement from an omission without running anything. GIVEN that no store can fail a rule whose defect is in the fixture's declaration and that CLAUDE.md forbids writing a conformance rule nothing can fail, so the `None` defect gets a review obligation instead, WHEN a reviewer reads the diff and a consumer reads the crate's docs, THEN CHANGELOG.md carries a CF-29-shaped entry naming the defect each newly-reachable rule now detects — a sentence, not a listing — and the crate documentation states the three declared limits and what bounds each, discharging VT-21's obligation that a store documents its actual limit"
  satisfied: true
  evidence: "CHANGELOG.md carries a CF-29-shaped `[Unreleased] / Added` entry naming, in sentences rather than a listing, the defect each newly-reachable rule detects: a store that accepts a payload one byte over its stated ceiling, that refuses through `AppendError::Store` where a caller cannot tell capacity from transience, that truncates instead of refusing, or that clamps a batch and answers `Ok` for the part it wrote; and for CF-39, a fixture that claims the capability with an empty arm, and a store that leaves the rows it managed to write behind. `cargo run -p xtask -- lint-changelog` is green (via `cargo xtask lints`: `CF-29: all 112 rules in 4 file(s) have a changelog entry`). VT-21's obligation is discharged in the crate documentation: crates/happenstance-cloudflare/src/lib.rs carries a table of the three declared limits, what each is refused as, and — the part that makes a measurement distinguishable from an omission — that they are STATED ceilings rather than physical maxima, with the finding and the derivation cited. `cargo doc -p happenstance-cloudflare --no-deps` is clean; `cargo run -p xtask -- spec-trace` reports no problems."
  verifying_test: "`cargo xtask lint-changelog` (xtask/src/lints.rs:504-593) green with the new entry read at review for the sentence-not-listing bar; `cargo doc -p happenstance-cloudflare` output read at review; `cargo xtask spec-trace` green"

- id: AC-011
  criterion: "The evidence leaves this story complete, and no .kb/ file is written here. GIVEN that accepted decision atoms are immutable and `redkiln validate --kb` checks them against HEAD, and that every KB write in this project belongs to `adr-0023-and-atom-resolutions` (HS-S0058), WHEN this story closes, THEN its folder carries an evidence package containing the three numbers with the derivation of each, the CF-39 verdict and its mechanism or decline reason, the REOPEN verdict, the CF-40 ownership finding (that declaring numbers was not blocked by the ownership contradiction, because the clause text is identical under either reading — narrowing the atom to its own sub-question 2), the coordination check against `sqlite-durable-store` so CF-40 is minted once, any falsification finding from AC-007, and the stale PROVISIONAL marker sentences quoted verbatim as a residual — and `git status` shows no file under .kb/ touched by this PR"
  satisfied: true
  evidence: ".bklg/from-contract-to-published-library/cloudflare-durable-object-store/measured-store-limits/_evidence.md carries all eight sections HS-S0058 needs: the three numbers with each derivation (§1); the finding that the physical wall was never located on this host, so the declarations are policy rather than discovery (§2); CF-40's falsifier tested and NOT fired (§3); CF-39's verdict, mechanism and negative control (§4); CF-17's confirmed `REOPEN` (§5); the CF-40 ownership finding — that declaring numbers was **not** blocked by the ownership contradiction, because the clause text is identical under either reading, which narrows the atom to its own sub-question 2 — together with the coordination check showing `sqlite-durable-store` (HS-P0012) closed at `df90dcc` without minting an answer and `.kb/open-questions/cf-40-fixture-limits-ownership.md` unchanged (§6); the two stale `[PROVISIONAL]` falsifier sentences quoted as residuals rather than edited (§7); and what was deliberately not done (§8). `git status` over this slice shows **no** `.kb/**` path and no `spec/SPECIFICATION.md` path."
  verifying_test: "`git diff --name-only` over the PR showing no .kb/** path; `redkiln validate --kb && redkiln doctor` green; the evidence package reviewed against the six items AC-011 enumerates"
```
