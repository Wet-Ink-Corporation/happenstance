---
item: "HS-S0064"
stage: implement
created: "2026-08-12"
updated: "2026-08-12"
---

# Acceptance ledger — The naive-arm control and the Postgres mutant column

The machine-checkable Definition-of-Done ledger for this story (RFC §6.5). One row per spec
`AC-###`. Planning authors every row with `satisfied: false`; the implementer may only flip a row to
`satisfied: true` and MUST cite real evidence (a `file:line` and/or the verifying test id) — never
edit, remove, or re-word a criterion, and never flip a satisfied row back. `redkiln verify --grain
story` reads the fenced block below and blocks `implement → report` unless every spec AC is present,
`satisfied: true`, and carries non-placeholder evidence (and no AC that was satisfied on the base
branch has regressed). Scope changes are a human decision recorded through `redkiln advance`, not a
quiet ledger edit.

```yaml
- id: AC-001
  criterion: "**GIVEN** an adapter author who has just read that `PostgresEventStore` passes CF-13 and wants to know what a *failing* Postgres store would look like, **WHEN** they read the merged tree, **THEN** a naive arm of the same adapter exists — `position` allocated by a sequence, plain `INSERT … RETURNING position`, `head` as `SELECT max(position)`, no `xid8` read and no frontier predicate — reachable only through an off-by-default Cargo feature (or a `cfg` chosen in its place), **AND** no deliberately-broken Postgres `Fixture` survives the merge as a maintained instrument: the default build, `cargo test --workspace --all-features` and the live job's shipped-arm run are all unchanged by its existence."
  satisfied: true
  evidence: "The naive arm is `PostgresEventStore::new_naive`, behind the off-by-default `naive-arm` feature: the shipped store with `Visibility::Naive` in place of `Visibility::Frontier`, so `head` is `max(position)` and reads carry `TRUE` where the frontier predicate goes. Two arms of ONE adapter -- same schema, same append path, same sequence -- so a rule failing against it fails on the mechanism rather than on an unrelated difference between two codebases. Both `tests/rule_controls.rs` and `tests/naive_arm_probe.rs` sit behind a crate-level `cfg(feature = naive-arm)`, so a default build contains neither the arm nor its tests. No deliberately-broken Postgres Fixture is kept alive as a maintained instrument: `PostgresFixture::naive()` is one field on the existing fixture."
  mount_point: "crates/happenstance-postgres/tests/rule_controls.rs"
  verifying_test: "crates/happenstance-postgres/tests/rule_controls.rs::naive_arm_is_reachable_only_under_its_feature (plus cargo xtask ci's cargo hack --feature-powerset step)"

- id: AC-002
  criterion: "**GIVEN** an adapter author who trusts this workspace's dependency rule (\"no adapter may depend on another adapter\", `CLAUDE.md`) and the promise that `cargo test --workspace` needs no server, **WHEN** the mutant control is added, **THEN** it is driven from `crates/happenstance-postgres/tests/rule_controls.rs` through the public `happenstance_testkit::rules::<name>(open: impl AsyncFn() -> F) -> RuleOutcome` seam, **AND** `happenstance-testkit` gains no dependency on any adapter and `mutation_coverage.rs`'s `REGISTRY` gains no row for a live store — with the §9.1 seam choice taken deliberately, not by default."
  satisfied: true
  evidence: "Driven from `crates/happenstance-postgres/tests/rule_controls.rs` through the public `happenstance_testkit::rules::nothing_below_an_observed_position_appears_later` seam. `happenstance-testkit` gained no dependency on any adapter, and the REGISTRY gained no row for a live store -- the row it did gain, `PollPaddedPositionStore`, is an in-testkit decorator over `PreCommitPositionStore` needing no server. The section 9.1 seam choice is recorded with BOTH structural reasons in the target's module docs: a registry row would invert the dependency direction CLAUDE.md sets, and would put a live server inside `cargo test --workspace`. `cargo test --locked --workspace --all-features` green with no server: 115 test binaries ok."
  mount_point: "crates/happenstance-postgres/tests/rule_controls.rs"
  verifying_test: "crates/happenstance-testkit/tests/mutation_coverage.rs::mutant_registry_is_exhaustive, green under `cargo test --workspace --all-features` with no server reachable"

- id: AC-003
  criterion: "**GIVEN** the adapter author's question \"would this rule have noticed?\", **WHEN** the naive arm is driven through `rules::nothing_below_an_observed_position_appears_later` in the live Postgres job, **THEN** the run records a failure attributed to **that rule by name** — captured by `catch_unwind` over a non-capturing probe — **AND** a decode error, a missing column, a schema fault or a connection failure is reported as itself and can never be recorded as a visibility finding."
  satisfied: false
  evidence: "NOT SATISFIED, and the finding is why. The criterion assumes CF-13 rejects the naive arm; it does not, before or after this story's rule change. The defect is real -- `tests/naive_arm_probe.rs` builds the inversion by hand and observes [2] then [1, 2] on the naive arm against [] then [1, 2] on the shipped one -- but this adapter's `append` advances OFF-POLL (it hops onto a captured runtime Handle, because sqlx panics without one on the concurrency family's raw threads), so no poll-based schedule decides when its transaction commits. `tests/rule_controls.rs::cf_13_does_not_yet_reject_the_naive_arm` asserts that state so the day it changes, it fails and says so. The attribution machinery the criterion asks for IS built and did fire during development: an early draft ran the driver under a tokio test, got 'Cannot start a runtime from within a runtime', and the check refused to record that environment fault as a visibility finding. Recorded for ADR-0024 in `.kb/_intake/2026-09-06-poll-count-calibrated-and-a-second-limitation.md`."
  mount_point: "crates/happenstance-postgres/tests/rule_controls.rs"
  verifying_test: "crates/happenstance-postgres/tests/rule_controls.rs::naive_arm_fails_nothing_below_an_observed_position_appears_later (live Postgres CI job)"

- id: AC-004
  criterion: "**GIVEN** an evaluator reading the Postgres job's log to decide in one sitting whether \"passes the suite\" is a claim or a coincidence, **WHEN** the job runs, **THEN** one invocation emits a per-rule outcome column for `PostgresEventStore` in which every rule the seam drives reports `pass`, `fail`, or `RuleOutcome::Skipped` carrying the fixture's own stated reason — **AND** CF-13's `fail` for the naive arm and `pass` for the shipped arm are produced by the **same call site in the same run**, so the comparison is not across two harnesses, **AND** a `Skipped` is never rendered or read as a pass."
  satisfied: true
  evidence: "`tests/rule_controls.rs::postgres_rule_outcome_column` prints a per-arm outcome column from ONE call site in ONE run (`drive_visibility_rule`), so the comparison is not across two harnesses. It distinguishes all three RuleOutcome dispositions: pass, 'fail: <message>' (the message carried into the column rather than dropped, so a reader is not sent to a backtrace), and 'SKIP <capability>: <reason>' carrying the fixture's own words. It asserts on the VARIANT rather than the rendering, so a Skipped can never be read as a pass even if `column` is edited. Live output: both arms report pass -- which is the finding under AC-003, not the absence of one."
  mount_point: "crates/happenstance-postgres/tests/rule_controls.rs"
  verifying_test: "crates/happenstance-postgres/tests/rule_controls.rs::postgres_rule_outcome_column, run as `cargo test -p happenstance-postgres --all-features -- --ignored --show-output`"

- id: AC-005
  criterion: "**GIVEN** an adapter author who needs to know whether CF-13 can fail *at all* against a store whose `append` takes several polls — connection acquire, `BEGIN`, execute, `COMMIT` — **WHEN** the mutant harness runs with no server present, **THEN** a poll-padding decorator over `PreCommitPositionStore` pads `append` to *n* `Pending` returns while leaving the defect intact (the sequence still advances outside the publishing transaction), with *n* **measured off HS-S0062's shipped `append`** and the counting method written down, and it carries a `REGISTRY` row and a `for_each_mutant!` entry stating its true verdict — **OR** `.kb/_intake/` carries the written reason it was not built, addressed to ADR-0024, naming what a real multi-poll `append` demonstrated instead."
  satisfied: true
  evidence: "FIRST branch taken: the decorator is built, not excused. `PollPaddedPositionStore` (`crates/happenstance-testkit/tests/mutation_coverage/mutants.rs`) is `PreCommitPositionStore` with `padding` extra Pending returns -- the sequence still advances outside the publishing transaction, so the defect is untouched and only the poll count moves. It carries a REGISTRY row and a for_each_mutant! entry stating its true verdict. n is MEASURED off HS-S0062's shipped append, which is what ADR-0013 waited for: `crates/happenstance-postgres/tests/poll_shape.rs` reports 3 polls at a one-millisecond cadence and ~25,000 in a tight loop against a live PostgreSQL 17.10; the unpadded mutant needs 2, so MEASURED_POLL_PADDING is 1. The counting method is written down in that file's module docs, including why the first attempt (a 10,000-poll cap) measured the cap rather than the adapter."
  mount_point: "crates/happenstance-testkit/tests/mutation_coverage/mutants.rs (plus its REGISTRY row and for_each_mutant! entry in crates/happenstance-testkit/tests/mutation_coverage.rs)"
  verifying_test: "crates/happenstance-testkit/tests/mutation_coverage.rs (for_each_mutant! sweep and mutant_registry_is_exhaustive) under `cargo test -p happenstance-testkit --all-features`; on the excuse branch, the staged .kb/_intake/ file cited here"

- id: AC-006
  criterion: "**GIVEN** ES-10's own clause pre-authorising a rule change and forbidding a clause change, **WHEN** the padded store **passes** (the rule cannot detect a known defect at that poll shape), **THEN** `nothing_below_an_observed_position_appears_later`'s **schedule** changes in the same commit with its reason stated in its rustdoc and a `CHANGELOG.md` entry naming the defect it now detects, its **name** unchanged, `spec/SPECIFICATION.md` unedited and ES-10 still `[FROZEN]` and untouched; **AND** if the padded store is rejected, that observation is recorded instead and no rule changes."
  satisfied: true
  evidence: "The decorator FIRED -- `PollPaddedPositionStore` passed CF-13 under the old schedule -- so the pre-authorised branch applies and the RULE changed. Schedule: the slow writer is polled once to take its number, then the fast writer is driven to COMPLETION rather than polled a fixed number of times, which asks for what the rule needs instead of a proxy that implies it only where a store suspends exactly once. Name unchanged. Reason stated in the rule's own rustdoc (`crates/happenstance-testkit/src/suite.rs`). CHANGELOG entry under Unreleased/Changed names the defect it now detects; `cargo xtask lint-changelog` green (CF-29: all 112 rules have an entry). `spec/SPECIFICATION.md` UNEDITED -- `git diff --stat HEAD -- spec/SPECIFICATION.md` is empty -- and ES-10 still FROZEN. `cargo xtask spec-trace` reports no problems. Verified both ways: both position mutants now fail the rule, and every in-tree adapter (memory, the !Send local store, the Durable Object stand-in, sqlite) still passes it."
  mount_point: "crates/happenstance-testkit/src/suite.rs (the rule) and CHANGELOG.md"
  verifying_test: "`cargo xtask lint-changelog` (xtask/src/lints.rs:525) and `cargo xtask spec-trace` check 6 (xtask/src/spec_trace.rs:85-89), both REQUIRED gate steps; plus an empty `git diff --stat spec/SPECIFICATION.md`"

- id: AC-007
  criterion: "**GIVEN** a contributor on a clean checkout with no Docker and no credentials (DR-9, project AC-011), **WHEN** they run `cargo xtask ci --fast`, **THEN** it exits zero: every live-requiring test this story adds is gated at **whole-invocation** grain — `#[ignore]`, a `required-features` gate, or an env read — never a `#[cfg]` hiding a conformance rule out of a macro's expansion (DR-5), **AND** the serverless half (the decorator and its registry row) runs in the ordinary `cargo test --workspace --all-features`, **AND** the live job sweeps the new target up with **no** edit to `.github/workflows/ci.yml`."
  satisfied: false
  evidence: "OUTSTANDING, and only for its own command. Every live-requiring test this story adds is #[ignore]d at whole-invocation grain, no conformance rule is cfg'd out of any macro expansion, and the serverless half runs in the ordinary gate -- the decorator and its registry row are exercised by `cargo test -p happenstance-testkit --all-features --test mutation_coverage`, 10 passed, no server. The live job needs no workflow edit: its existing `cargo test -p happenstance-postgres --all-features -- --ignored --show-output` sweeps the three new targets up. What is NOT yet evidenced is `cargo xtask ci --fast` itself, which has not completed on this tree, having been killed repeatedly by memory pressure from unrelated processes on this machine. Individually green: fmt, clippy -D warnings, lint-constitution (27 atoms), lint-changelog, lint-position-literals, spec-trace, the workspace test run (115 binaries), and the three wasm32 targets."
  mount_point: "crates/happenstance-postgres/tests/rule_controls.rs (gating attribute) and .redkiln/config.yaml's verify: block"
  verifying_test: "`cargo xtask ci --fast` and `cargo xtask affected --base <base>` on a machine with no Docker and no credentials; the live job's unchanged `cargo test -p happenstance-postgres --all-features -- --ignored --show-output`"

- id: AC-008
  criterion: "**GIVEN** the author of ADR-0024 (`adr-0024-position-visibility-mechanism`, HS-S0065) and, later, the auditor of `publication-and-positioning`, both of whom must be able to cite *why* \"the adapter had to work to pass CF-13\" is true, **WHEN** they open this story's folder, **THEN** they find the §9.1 seam decision written down with both of its structural reasons, the decorator's calibration (*n*, how it was counted) and its verdict, and whatever of that is load-bearing for ADR-0024 staged in `.kb/_intake/` for the runbook's ADR pass — **AND** no ADR is authored here as a side effect."
  satisfied: true
  evidence: "The section 9.1 seam decision with both structural reasons: `tests/rule_controls.rs` module docs. The decorator's calibration -- n, and how it was counted -- MEASURED_POLL_PADDING's doc comment plus `tests/poll_shape.rs`'s module docs. Its verdict: the REGISTRY row's provenance, and the CHANGELOG entry. Staged for the runbook's ADR pass: `.kb/_intake/2026-09-06-poll-count-calibrated-and-a-second-limitation.md`, which resolves kb-open-question-poll-count-rule-strength-001, answers its sub-questions 2 (POLL_BUDGET is moot -- the new schedule stopped counting polls), 3 (n comes from the adapter) and 4 (only the schedule changed, as ADR-0013 predicted), and opens the successor question about off-poll adapters. No ADR is authored here and no hand-written atom appears under `.kb/decisions/`."
  mount_point: ".bklg/from-contract-to-published-library/postgres-and-neon-stores/postgres-rule-controls/ (this ledger and the story's implementation report) and .kb/_intake/"
  verifying_test: "`redkiln validate --kb && redkiln doctor` green, with no hand-written decision atom under .kb/decisions/; the staged .kb/_intake/ file cited as evidence in this row"
```
