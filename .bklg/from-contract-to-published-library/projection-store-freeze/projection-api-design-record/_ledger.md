---
item: HS-S0003
stage: implement
created: 2026-08-12T13:45:57.874Z
updated: 2026-08-12T13:45:57.874Z
---

# Acceptance ledger — DT-3 and DT-8 resolved in the public-API design record

The machine-checkable Definition-of-Done ledger for this story (RFC §6.5). One row per spec
`AC-###`. Planning authors every row with `satisfied: false`; the implementer may only flip a row to
`satisfied: true` and MUST cite real evidence (a `file:line` and/or the verifying test id) — never
edit, remove, or re-word a criterion, and never flip a satisfied row back. `redkiln verify --grain
story` reads the fenced block below and blocks `implement → report` unless every spec AC is present,
`satisfied: true`, and carries non-placeholder evidence (and no AC that was satisfied on the base
branch has regressed). Scope changes are a human decision recorded through `redkiln advance`, not a
quiet ledger edit.

**A note on evidence for this story specifically.** This is a prose deliverable: the Testing brief
assigns project AC-006 the tier *None (design-stage artefact)* and project AC-007 a tier conditional on
the DT-8 arm taken (`.bklg/from-contract-to-published-library/projection-store-freeze/_decomposition.md:776-777`).
Evidence is therefore a `file:line` range in the amended `_design.md` plus the named deterministic
check's output — never a test id invented to fill the field. Where the machine half of a criterion runs
in a later story, the `verifying_test` column names that check and the story that runs it; the evidence
cited here is the record's own half.

```yaml
- id: AC-001
  criterion: "**GIVEN** P2 has built a projection fixture whose storage genuinely cannot provide one of the port's guarantees, **WHEN** they open `_design.md` to learn who is entitled to write the sentence a consumer reads, **THEN** the DT-3 resolution names **exactly one** authoritative source and, for each of the three reason-writers that already exist in this tree — the fixture-written `Capability::declined` (`crates/happenstance-testkit/src/contract.rs:355-419`), the testkit-written `NO_STORE_LIMITS` / `NO_CEILING_REASON` (`:435-456`), and per-adapter prose — states which defers to which and why, so P2 is never left choosing between two policies; and it **cites** `.kb/open-questions/cf-40-fixture-limits-ownership.md` as authoritative rather than preferring a reading of it."
  satisfied: true
  evidence: ".bklg/from-contract-to-published-library/projection-store-freeze/_design.md, '## Shape decision' > 'DT-3 - one authoritative source for \"this guarantee does not apply\"'. Names exactly one authoritative source (the suite's own output; per-adapter documentation may repeat it and may not contradict it) and disposes of all three reason-writers in one table: the fixture-written `Capability::declined` (`crates/happenstance-testkit/src/contract.rs:355-419`) is authoritative and the default; the testkit-written `NO_STORE_LIMITS` / `NO_CEILING_REASON` (`contract.rs:435-456`) is authoritative for its one case and is the stated exception, quoting its own doc's reason; per-adapter prose is subordinate and non-normative, and where it disagrees with the run the run wins. `.kb/open-questions/cf-40-fixture-limits-ownership.md` is cited as authoritative and NOT re-litigated - the record states only what the projection fixture does given the unresolved ownership (it declares no numeric-limit constants at all, so it never reaches CF-40's surface, and the projection port has no ExceedsStoreLimit analogue). Static check red then green: `rg 'Capability::declined|NO_CEILING_REASON|per-adapter'` and `rg 'cf-40-fixture-limits-ownership'` over _design.md returned nothing before this story and return hits inside DT-3's resolution now."
  mount_point: ".bklg/from-contract-to-published-library/projection-store-freeze/_design.md"
  verifying_test: "Design-stage review of `_design.md` § DT-3 (project.md DoD 7), against UX brief AC-U01 (.bklg/from-contract-to-published-library/projection-store-freeze/_decomposition.md:84-96); deterministic half: rg -n \"Capability::declined|NO_CEILING_REASON|per-adapter\" .bklg/from-contract-to-published-library/projection-store-freeze/_design.md and rg -n \"cf-40-fixture-limits-ownership\" over the same file"

- id: AC-002
  criterion: "**GIVEN** P3 runs the suite on `wasm32-unknown-unknown`, the target BR-12 exists to protect, **WHEN** a projection capability is declined there, **THEN** DT-3's resolution has already accounted for it: the record states that `RuleOutcome::report` is a **measured** no-op on that target (`crates/happenstance-testkit/src/contract.rs:511-531`), that the projection wasm harness routes `skip_line` through `console_log!` exactly as `__emit_wasm` already does (`crates/happenstance-testkit/tests/memory_conformance_wasm.rs:25`), and that the machine-checked half of \"told, with a reason\" is a `RuleOutcome`-**value** assertion — a projection sibling of `capability_skips_are_reported` (`crates/happenstance-testkit/tests/mutation_coverage.rs:3184`) — never stdout."
  satisfied: true
  evidence: ".bklg/from-contract-to-published-library/projection-store-freeze/_design.md, DT-3 > 'DT-3's answer survives the constrained target'. States all three claims: `RuleOutcome::report` is a MEASURED no-op on `wasm32-unknown-unknown` (`crates/happenstance-testkit/src/contract.rs:511-531`, measured under wasm-bindgen-test-runner with --nocapture rather than assumed); the projection wasm harness routes `skip_line` through `console_log!` exactly as `__emit_wasm` already does (`crates/happenstance-testkit/tests/memory_conformance_wasm.rs:25`); and the machine-checked half of 'told, with a reason' is a `RuleOutcome`-VALUE assertion, a projection sibling of `capability_skips_are_reported` (`crates/happenstance-testkit/tests/mutation_coverage.rs:3184`), never stdout - with the native half's honest limitation quoted too (libtest suppresses a passing test's stdout unless --show-output, which makes the line reachable but does not make anyone read it). Static check red then green: `rg 'wasm32|__emit_wasm|RuleOutcome'` over _design.md returned nothing, now returns all three inside DT-3."
  mount_point: ".bklg/from-contract-to-published-library/projection-store-freeze/_design.md"
  verifying_test: "Design-stage review against UX brief AC-U09/AC-U11 and project.md AC-005; deterministic half: rg -n \"wasm32|__emit_wasm|RuleOutcome\" .bklg/from-contract-to-published-library/projection-store-freeze/_design.md. Machine half deferred to the projection sibling of crates/happenstance-testkit/tests/mutation_coverage.rs:3184 (`capability_skips_are_reported`) at story `projection-capability-skips`"

- id: AC-003
  criterion: "**GIVEN** the implementer of `projection-capability-skips` (HS-S0008), whose story explicitly takes *\"the projection capability set fixed by DT-3's recorded resolution rather than invented here\"* (`_storymap.md:60`), **WHEN** they open this record, **THEN** the projection fixture's capability constants are **enumerated**, each carrying whether it is a MUST in `SECOND_HANDLE`'s sense and **who writes its reason** under AC-001's policy — at minimum the reset-refusal capability `refused_reset_changes_nothing` needs (`spec/SPECIFICATION.md:5200-5217`) — with PS-12's gate recorded as the `ProjectionProbe` const `READS_THROUGH_BATCH` rather than a fixture const; and if the set ends with **nothing declinable at all**, that outcome is recorded inside DT-3's resolution as a finding, not treated as licence to drop the reporting discipline."
  satisfied: true
  evidence: ".bklg/from-contract-to-published-library/projection-store-freeze/_design.md, DT-3 > 'The projection fixture's capability set'. Three constants enumerated in one table, each with where it is declared, whether it is a MUST in SECOND_HANDLE's sense, who writes its reason under AC-001's policy, and which rule it gates: `RESET_REFUSAL` (fixture trait, declinable, fixture-written reason, gates `refused_reset_changes_nothing`, `spec/SPECIFICATION.md:5200-5217`); `SECOND_HANDLE` (fixture trait, a MUST, because `commit_is_atomic_with_the_read_model` reads through fresh handles and a fixture without a second handle cannot observe PS-1 at all); `READS_THROUGH_BATCH` (a `ProjectionProbe` const, NOT a fixture const, declinable, testkit-written reason on the `NO_CEILING_REASON` precedent, gates `batch_reads_reflect_pending_writes`). Three notes follow: why the probe const is not a fixture const, that CF-18's reported-skip obligation survives the move, and - the 'nothing declinable' finding - that two of the three are declinable so the set is NOT empty, recorded explicitly as the finding it would have been rather than left to be noticed later. HS-S0008 can name every constant it implements by quoting this table."
  mount_point: ".bklg/from-contract-to-published-library/projection-store-freeze/_design.md"
  verifying_test: "Design-stage review against Architecture brief Note 5 (.bklg/from-contract-to-published-library/projection-store-freeze/_decomposition.md:517-544), the UX brief's `What would make this brief wrong` (:252-259) and .kb/decisions/0010-the-suite-must-prove-itself.md; deterministic half: every constant `projection-capability-skips` implements is quotable from `_design.md`"

- id: AC-004
  criterion: "**GIVEN** an adapter author this repository did not write and does not supervise — the seventh adapter P2's persona generalises to (`…/personas-and-journeys.md:115-121`) — **WHEN** they ask whether the suite's bar is held for them, **THEN** `_design.md` names DT-8's arm **and its cost to them**: on the outside-author arm the extension surface is the documented pair `projection_store_conformance!` + `ProjectionProbe`, bounded at *one feature flag on a dependency the adapter already has and no new edge in the dependency graph* (`spec/SPECIFICATION.md:5015-5031`); on the narrow arm the recorded scope is placed **where an outsider meets it** — the testkit's own crate doc — and that file is named as `documented-extension-surface`'s (HS-S0015) obligation. Either way the arm is legible enough that HS-S0015 is scopable from this file alone."
  satisfied: true
  evidence: ".bklg/from-contract-to-published-library/projection-store-freeze/_design.md, '## Shape decision' > 'DT-8 - whose adapter-author bar the suite holds'. Names the arm (the bar is held for an author this repository did not write) and its cost to that author: the extension surface is the documented pair `projection_store_conformance!` + `ProjectionProbe`, bounded at one feature flag on a dependency the adapter already has and no new edge in the dependency graph, with the orphan-rule argument quoted from `spec/SPECIFICATION.md:5015-5031` and a concrete `[dev-dependencies]` block showing what the author actually pays. The obligation the arm creates is named so it cannot be dropped: `documented-extension-surface` (HS-S0015) must build a fixture from the documentation alone, not copied from `crates/happenstance-testkit/src/fixtures.rs`, and clear the mutant-registry exactness check and the capability-skip rule. What is not claimed is stated too - the port ships behind `unstable-projection` with a documented semver exemption (PS-3), and whether the feature is exposed at publish is HS-P0016's. HS-S0015 is scopable from this section without opening initiative.md."
  mount_point: ".bklg/from-contract-to-published-library/projection-store-freeze/_design.md"
  verifying_test: "Design-stage review against UX brief AC-U02 (.bklg/from-contract-to-published-library/projection-store-freeze/_decomposition.md:97-108) and project.md AC-007; conditional machine half deferred to story `documented-extension-surface` (Testing brief row AC-007, _decomposition.md:777) — a fixture built from the documentation alone run against the mutant-registry exactness check and the capability-skip rule"

- id: AC-005
  criterion: "**GIVEN** the rule author inside the testkit and the implementer of `owned-batch-port-shape` (HS-S0004), **WHEN** they open `## Items` and `## Signatures` to build against a shape someone agreed to, **THEN** the target trait is transcribed item for item — `type Batch;` with no lifetime, `fn begin(&self) -> Self::Batch` neither `async` nor fallible, `checkpoint -> Checkpoint`, `commit(batch, id, position, Authority) -> Result<(), CommitError<E>>`, `reset(batch, id) -> Result<(), ResetError<E>>`, `rollback`, plus `ProjectionProbe`'s five items — each signature carrying its `PS-*` clause id, and where any sentence disagrees with accepted ADR-0017 / ADR-0018 / ADR-0019, the **atom wins** and the record says which sentence yielded rather than editing the atom."
  satisfied: true
  evidence: ".bklg/from-contract-to-published-library/projection-store-freeze/_design.md, '## Signatures'. The target trait is transcribed item for item from `spec/SPECIFICATION.md:4632-4731` and `:4998-5013`: `type Batch;` with no lifetime, `fn begin(&self) -> Self::Batch` neither async nor fallible, `checkpoint -> Checkpoint`, `commit(batch, id, position, Authority) -> Result<(), CommitError<Self::Error>>`, `reset(batch, id) -> Result<(), ResetError<Self::Error>>`, `rollback`, plus `ProjectionProbe`'s five items. Every signature carries its `PS-` clause id; static check counts 56 `PS-` occurrences and zero unattributed signatures (red: 0). The block closes by stating the ADR relationship explicitly, and against the ACCEPTED atoms rather than the long-form records alone: ADR-0017, ADR-0018 and ADR-0019 are `status: accepted` at `.kb/decisions/0017-what-a-projection-batch-owns.md`, `0018-returning-a-projection-to-never-run.md` and `0019-what-happens-when-apply-fails.md`, ingested by the `2026-08-13-projection-adrs` wave at `493a194` and merged at `d05d2b3`. The block was re-read against them clause by clause and the reconciliation is written into `## Signatures`: `type Batch;` owned with no lifetime and no universal write vocabulary plus `ProjectionProbe` behind `feature = \"conformance\"` (`0017:65-88`); `reset(batch, id)` as one unit with the caller's deletes, scope cited to ADR-0007, refusal as a port mechanism, and the three-variant `Checkpoint` over `(Option<SequencePosition>, bool)` (`0018:60-86`); the port growing nothing for apply failure and `rollback` surviving on PS-30's `AssertUnwindSafe` (`0019:59-77`). **No sentence has had to yield**, and that is now a checked claim rather than a forward-looking one. Two points where an atom hands a question to this record are answered here and nowhere else: `ResetError::Refused` stays bare (`0018:108-110`), and DT-8 states what the probe's `conformance` gate costs an outside author. If a later SUPERSEDING atom differs, this block yields and the sentence that yielded is named here rather than the atom being edited - an accepted atom is immutable. The derives the signatures need are also stated now: `Debug, Clone, Copy, PartialEq, Eq` on `Checkpoint` and `Authority` because every rule and the doctest spell `assert_eq!`, and `AppendError`'s set (`crates/happenstance-core/src/error.rs:212`) including `thiserror::Error` on `CommitError`/`ResetError` because `commit(...).await?` must rise through a `Box<dyn core::error::Error>`."
  mount_point: ".bklg/from-contract-to-published-library/projection-store-freeze/_design.md"
  verifying_test: "Design-stage review against spec/SPECIFICATION.md:4632-4731 and :4998-5013 and crates/happenstance-core/src/projection.rs:87-139; deterministic half: rg -n \"PS-\" .bklg/from-contract-to-published-library/projection-store-freeze/_design.md shows no unattributed signature. Compile-time proof deferred to story `owned-batch-port-shape`"

- id: AC-006
  criterion: "**GIVEN** `CLAUDE.md`'s reader — fluent in the domain, new to idiomatic Rust — **WHEN** they meet a shape that looks gratuitously unusual, **THEN** `## What it costs a caller` has already named the alternative that lost, at the call site, for each of the four load-bearing choices: `Checkpoint` as a three-variant enum rather than `(Option<SequencePosition>, bool)` because the tuple can spell *\"authoritative, never run — which means nothing\"* (`spec/SPECIFICATION.md:4643-4658`); `CommitError` and `ResetError` staying two enums so `commit`'s caller never matches `Refused` (`:4722-4727`); `begin` infallible because `async` + fallible implies a round trip Neon's one-shot transport cannot afford (`:4884-4897`); `reset` taking the caller's own deletes because *\"the port has no idea what the read model is\"* (`:5165-5170`) — each stated **once**, sited in the item's own rustdoc rather than only in an ADR (RS-70-5, `standards/rust/70-rustdoc-obligations.md:243`), and each error carrying the adapter error as a type parameter, never a `String` (`standards/rust/30-error-taxonomy.md:15,71`)."
  satisfied: true
  evidence: ".bklg/from-contract-to-published-library/projection-store-freeze/_design.md, '## What it costs a caller'. Four rationale sentences, four items, each stated ONCE and each assigned the item's own rustdoc as its home rather than only an ADR (RS-70-5, `standards/rust/70-rustdoc-obligations.md:243`): `Checkpoint` as a three-variant enum because the tuple can spell `(None, true)` - 'authoritative, never run - which means nothing' (`spec/SPECIFICATION.md:4643-4658`); `CommitError` and `ResetError` staying two enums so `commit`'s caller need never match `Refused` (`:4722-4727`); `begin` infallible because async + fallible implies a round trip Neon's one-shot transport cannot afford (`:4692-4694`); `reset` taking the caller's own deletes because the port has no idea what the read model is (`:5165-5170`). Each error carries the adapter error as a type parameter, never a `String` (`standards/rust/30-error-taxonomy.md:15,71`), stated in the same table. The section closes by naming the density rule that keeps each sentence to one home."
  mount_point: ".bklg/from-contract-to-published-library/projection-store-freeze/_design.md"
  verifying_test: "Design-stage review against UX brief AC-U03 – AC-U05 (.bklg/from-contract-to-published-library/projection-store-freeze/_decomposition.md:112-138); deterministic half: four rationale sentences over four items, none repeated across sections (the RS-70-5 density budget, standards/rust/70-rustdoc-obligations.md:243)"

- id: AC-007
  criterion: "**GIVEN** an operator whose `reset` was declined by a store's own policy, **WHEN** they ask *which* policy declined it, **THEN** `_design.md` has decided out loud: either `ResetError::Refused` carries the store's stated reason, or it stays bare (`spec/SPECIFICATION.md:4676-4682`) and the record says where a caller is expected to find the reason instead, given the clause's own reasoning that *\"the port supplies the mechanism; the domain decides what to protect\"* (`:5210-5216`). Silence is the failure, not a passing default."
  satisfied: true
  evidence: ".bklg/from-contract-to-published-library/projection-store-freeze/_design.md, '## The states the API must express', final paragraph. Decided out loud: `ResetError::Refused` stays BARE, and the record says where a caller is expected to find the reason instead - the store's own documentation and logs - with the clause's own reasoning quoted ('the port supplies the mechanism; the domain decides what to protect', `spec/SPECIFICATION.md:5210-5216`) and the argument stated: a `&'static str` on the variant would push a domain sentence through a port type that cannot validate it, localise it, or keep it in step with the policy that produced it. The cost is named rather than hidden (an operator holding only the error value must go one place further), the behavioural half is routed to `reset-rules` (HS-S0012)'s `refused_reset_changes_nothing`, and the reversal path is stated (`#[non_exhaustive]` means a payload can be added later without breaking anyone). Static check: `rg 'ResetError::Refused'` returns a decision sentence, not only a signature."
  mount_point: ".bklg/from-contract-to-published-library/projection-store-freeze/_design.md"
  verifying_test: "Design-stage review against UX brief AC-U07 (.bklg/from-contract-to-published-library/projection-store-freeze/_decomposition.md:148-157); deterministic half: rg -n \"ResetError::Refused\" .bklg/from-contract-to-published-library/projection-store-freeze/_design.md returns a decision sentence, not only a signature. Behavioural half deferred to `refused_reset_changes_nothing` at story `reset-rules`"

- id: AC-008
  criterion: "**GIVEN** a downstream story about to land one of the new items, **WHEN** it reads `## Visibility and stability`, **THEN** the record is specific enough to be **violated**: `#[non_exhaustive]` on all four new types; re-export placement at `crates/happenstance-core/src/lib.rs:98-124`; `ProjectionProbe` behind `conformance` with `#[cfg_attr(docsrs, doc(cfg(…)))]` copying the `memory` pattern; `MemoryProjectionStore` behind `memory`; `unstable-projection` recorded as the arm AC-A04 takes; **both** halves of the mount named — the export block *and* the feature table, because an item mounted at one and not the other is an item no adapter can name (Architecture brief Note 1, `_decomposition.md:339-349`); and the rustdoc hazard stated, an intra-doc link into a feature-gated module being a hard error this workspace has already paid for once (`crates/happenstance-testkit/src/lib.rs:112-132`)."
  satisfied: true
  evidence: ".bklg/from-contract-to-published-library/projection-store-freeze/_design.md, '## Visibility and stability'. Eight items, each with exactly three lines - visibility, feature, placement: `#[non_exhaustive]` on all four new types (RS-40-5, `standards/rust/40-public-surface-and-evolution.md:212`); re-export placement at `crates/happenstance-core/src/lib.rs:98-124` with the specific line for each; `ProjectionProbe` behind `conformance` with `#[cfg_attr(docsrs, doc(cfg(...)))]` copying the `memory` pattern at `lib.rs:101-103` / `:120-122`, and bare flavour only; `MemoryProjectionStore` behind `memory`; `unstable-projection` recorded as the arm Architecture brief AC-A04 takes, with PS-3 cited. BOTH halves of the mount are named - the export block AND the feature table - in '## Placement and re-export', because an item mounted at one and not the other is an item no adapter can name (Architecture brief Note 1), and the testkit fixture trait's `__private` re-export is called out for the same reason. The rustdoc hazard is stated: an intra-doc link into a feature-gated module is a hard error this workspace has already paid for once (`crates/happenstance-testkit/src/lib.rs:112-132`), with the surviving shape and the `--no-default-features` doc build that catches it."
  mount_point: ".bklg/from-contract-to-published-library/projection-store-freeze/_design.md"
  verifying_test: "Design-stage review against RS-40-5 (standards/rust/40-public-surface-and-evolution.md:212) and Architecture brief Notes 1 and 9; deterministic half: three lines per public item (visibility, feature, placement) for every id in the spec's Integration contract `Public items` list. Compile-time proof deferred to stories `owned-batch-port-shape` and `projection-probe-conformance-feature`"

- id: AC-009
  criterion: "**GIVEN** P2 ten minutes into writing an impl, meeting `error[E0195] lifetime parameters or bounds on method 'commit' do not match the trait declaration` with, today, *nothing to copy from* (`references/adapter-shapes.md:186-194`), **WHEN** they open the port's page, **THEN** `## The doctest` carries the example text **and names the source item it will live on and the gate step that compiles it** (`cargo test --doc`, inside `cargo xtask ci`), because this record cannot compile it and a described example is not a checked one; and any `compile_fail` doctest demonstrating a reasonless declension is spelled **bare**, never `compile_fail,E0080` (`crates/happenstance-testkit/src/contract.rs:400-403`)."
  satisfied: true
  evidence: ".bklg/from-contract-to-published-library/projection-store-freeze/_design.md, '## The doctest'. Carries the example text in full - a `MemoryProjectionStore` round trip asserting `NeverRun`, an owned-batch commit at `SequencePosition::FIRST` with `Authority::Live`, a `probe_read` read-back, and a `reset` through `probe_delete_all` back to `NeverRun` - and names its home and the gate step that compiles it (`cargo test --doc`, inside `cargo xtask ci`'s `tests` step, `cargo test --locked --workspace --all-features`, `xtask/src/main.rs:143-152`), stating plainly that this record cannot compile it and that compilation is `memory-projection-store`'s under Testing brief row AC-012. The home is the `MemoryProjectionStore` module page - CORRECTED at slice review from the ungated `ProjectionStore` trait doc, which cannot host it: `probe_write`/`probe_delete_all` are `ProjectionProbe` methods (now in the `use` list, absent before, `error[E0599]`) and that trait is behind `feature = \"conformance\"`, off by default, while the trait doc renders ungated. `memory-projection-store`'s own AC-010 divides the two pages the same way - a toy-store impl doctest on the port with no `memory` dependency, the runnable walkthrough on the store. The example is additionally wrapped in a hidden `#[cfg(feature = \"conformance\")]` with a `#[cfg(not(...))] fn main() {}` arm so a bare `cargo test -p happenstance-core --doc` stays green, and its harness is `#[tokio::main(flavor = \"current_thread\")]` (the crate's idiom at `crates/happenstance-core/src/memory.rs:45-46`) rather than an `async fn` nobody polls - which would have made every `assert_eq!` in it dead. The example is shaped to answer the `E0195` trap by omission - there is no `Self::Batch<'_>` to spell - which is what an implementer copies. The owed `compile_fail` doctest for a reasonless declension is specified as spelled BARE, never `compile_fail,E0080`, with the reason (rustdoc on 1.97.1 silently ignores an error code it cannot match) and with where the check actually fires for an ASSOCIATED const: codegen, so `cargo build` and `cargo test` catch it and `cargo check` and `cargo clippy` do not (`crates/happenstance-testkit/src/contract.rs:400-419`)."
  mount_point: ".bklg/from-contract-to-published-library/projection-store-freeze/_design.md"
  verifying_test: "Design-stage review against UX brief AC-U06/AC-U12 and CLAUDE.md's `a doctest in place of a mock`; deterministic half: `## The doctest` names a real target path (crates/happenstance-core/src/projection.rs and/or the MemoryProjectionStore module) and a real gate step. Compilation deferred to `cargo test --doc -p happenstance-core` at story `memory-projection-store` (Testing brief row AC-012, _decomposition.md:782)"

- id: AC-010
  criterion: "**GIVEN** an implementer in a later slice reaching for a shape that would compile, **WHEN** they check `## Anti-patterns` first, **THEN** each forbidden move is listed **with its evidence**: no `#[async_trait]` (`.kb/decisions/0001-async-port-flavours.md`); no `type Batch: Send;` (PS-36 — `trait_variant` copies the bound into the `!Send` flavour and breaks wasm32, `spec/SPECIFICATION.md:5601-5627`); no borrowing GAT anywhere on the fixture (five-ingredient rustc ICE, still reproducing on 1.97.1, `crates/happenstance-testkit/src/contract.rs:97-111`); no merging the two error enums; no `async fn begin`; no second skip vocabulary and no projection-local skip type (Architecture brief `AC-A06`, UX brief `AC-U08`); no literal position assertions; no hardening of `ProjectionId::new` as a side effect (`.kb/open-questions/projection-id-is-unvalidated.md`)."
  satisfied: true
  evidence: ".bklg/from-contract-to-published-library/projection-store-freeze/_design.md, '## Anti-patterns'. Ten forbidden moves, every one carrying a path or an atom id: `#[async_trait]` (`.kb/decisions/0001-async-port-flavours.md`); `type Batch: Send;` (PS-36, `spec/SPECIFICATION.md:5601-5627` - trait_variant copies the bound into the !Send flavour and breaks wasm32); any borrowing GAT on the fixture (the five-ingredient rustc ICE, `crates/happenstance-testkit/src/contract.rs:97-111`, `experiments/rustc-ice-gat-foreign-trait/`); merging the two error enums (`:4722-4727`); `async fn begin` (`:4692-4694`); a second skip vocabulary or projection-local skip type (Architecture brief AC-A06, UX brief AC-U08); literal position assertions; `compile_fail,E0080` (`contract.rs:400-403`); hardening `ProjectionId::new` (`.kb/open-questions/projection-id-is-unvalidated.md`); and binding a concrete batch type while the port still declares `Self::Batch<'_>` (`references/adapter-shapes.md:186-194`). The section additionally flags to the reviewer that the prior sign-off covered 'the anti-patterns recorded above' when that section read N/A, so this table is new material inside a referenced section rather than a re-reading of what was approved."
  mount_point: ".bklg/from-contract-to-published-library/projection-store-freeze/_design.md"
  verifying_test: "Design-stage review against CLAUDE.md `Binding constraints` and `The rule that matters`, plus RS-91-3 (standards/rust/91-adapter-authoring-recipe.md:156-158); deterministic half: rg -n \"Anti-patterns\" -A 60 .bklg/from-contract-to-published-library/projection-store-freeze/_design.md shows a path or atom id on every entry"

- id: AC-011
  criterion: "**GIVEN** the repository owner, who signed the no-screen determination at the `/redkiln:plan` design gate on 2026-08-12 (`_design.md:96-101`), **WHEN** this record returns to that gate, **THEN** they can approve or decline the **amendment** without unpicking what they already approved: the no-screen determination, the `surfaces: []` manifest and the prior sign-off block survive **verbatim**; the added content occupies the bundled template's own section slots in the template's own order (`.redkiln/templates/_design.md`), replacing only the lines that read `N/A — no user-facing surface`, and only for the two non-visual surfaces the same file already enumerates at `_design.md:24-38`; and a **new sign-off block is appended unsigned**, naming what is added and what is untouched."
  satisfied: true
  evidence: "`git diff` over .bklg/from-contract-to-published-library/projection-store-freeze/_design.md: exactly ten lines removed anywhere in the file, all of them the literal placeholder `N/A - no user-facing surface.`, and lines 1-50 are byte-identical to HEAD (checked with `cmp`), so the no-screen determination, the `surfaces: []` fence and the prior `Approved.` sign-off block of 2026-08-12 all survive verbatim. The added content occupies the bundled template's own section slots in the template's own order (`.redkiln/templates/_design.md`): Items, Signatures, Shape decision, Placement and re-export, Visibility and stability, What it costs a caller, What a user meets first, The states the API must express, Anti-patterns, The doctest - none renamed, none reordered, none dropped. A new `## Amendment sign-off` block is appended and is explicitly `**Unsigned.**`, naming what is added, what is untouched, the one thing to look at first (the Anti-patterns section the prior approval referenced while it read N/A), and the escalation path if the reviewer holds the N/A determination was meant to cover the type surface too - a re-plan reported at this story's boundary, not a section widened quietly. Re-checked after the slice review's corrections, and the property is now provable in a stronger form: `git diff main --numstat` over this file is **759 insertions, 0 deletions** - against `main` the amendment is purely additive, because every line the corrections rewrote was a line this story itself added. Lines 1-85 are untouched (the earliest hunk starts at `:86`), so the `surfaces: []` fence, the no-screen determination and the 2026-08-12 `Approved.` block are byte-identical, and the corrections are recorded inside the still-unsigned `## Amendment sign-off` block under 'What changed after the slice review, before this block was ever signed' rather than left for the reviewer to diff for themselves."
  mount_point: ".bklg/from-contract-to-published-library/projection-store-freeze/_design.md"
  verifying_test: "git diff -- .bklg/from-contract-to-published-library/projection-store-freeze/_design.md shows an added-lines-only diff over :10-50 and :92-101 with the `surfaces: []` fence and the prior sign-off block byte-identical, and the appended block carrying no signature; confirmed at the design-stage review"

- id: AC-012
  criterion: "**GIVEN** this repository's rule that `.kb/` atoms are authored by `/redkiln:kb-ingest` from `.kb/_intake/` and that hand-writing them *\"produces the directory layout of the process without the process\"* (`CLAUDE.md`, reverted at `0269720`), **WHEN** this story merges, **THEN** the scope discipline is observable in the diff rather than asserted: no file under `crates/**`, `.kb/**` or `spec/**` changes, every changed path falls inside the PR-boundary block above, and any answer the record turned up that deserves an ADR appears **as a named gap for the runbook's ADR pass**, never as a minted decision."
  satisfied: true
  evidence: "Scope discipline observable in the diff rather than asserted: `git diff --stat main...HEAD -- crates/` is empty, `-- spec/` is empty, and `-- .kb/ ':!.kb/_intake/'` is empty, so no atom was hand-written, no open question's status changed, no ADR minted and no clause or maturity marker edited. Every path this story changed falls inside its PR boundary - `_design.md` and `.bklg/.../projection-api-design-record/**`. `redkiln validate` passes on this story's item, `redkiln validate --kb` passes, `cargo fmt --all --check` exits 0 and `cargo xtask affected --base main` reports no package affected. The one answer this record turned up that deserved a decision - at the checkpoint the ADR-0017/0018/0019 atoms were staged and not yet accepted, so the Signatures block records that it yields to them rather than the reverse - was written as a named dependency in the record and not minted here. Scope note for a later reader recomputing these commands: the `-- .kb/ ':!.kb/_intake/'` assertion is over THIS STORY'S commit. `.kb/` has changed on the branch since, at `493a194`, which is the `2026-08-13-projection-adrs` ingest wave and `projection-decision-atoms`'s AC-007 - a different story's deliverable, reached through the only authoring path the repository permits. This story still hand-writes no atom: `git diff main...HEAD -- crates/ spec/` remains empty, and every path THIS story changed is `_design.md` plus its own backlog folder."
  mount_point: ".bklg/from-contract-to-published-library/projection-store-freeze/projection-api-design-record/spec.md"
  verifying_test: "redkiln verify --grain story (reads the first fenced block under `PR boundary` in .bklg/from-contract-to-published-library/projection-store-freeze/projection-api-design-record/spec.md and this ledger) plus git diff --name-only main...HEAD; redkiln validate green on this story's item"
```
