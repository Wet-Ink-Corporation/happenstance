---
item: HS-S0011
stage: implement
created: 2026-08-12T13:46:05.705Z
updated: 2026-08-12T13:46:05.705Z
---

# Acceptance ledger — reset is one unit of work, scoped, refusable, and not commit-at-FIRST

The machine-checkable Definition-of-Done ledger for this story (RFC §6.5). One row per spec
`AC-###`. Planning authors every row with `satisfied: false`; the implementer may only flip a row to
`satisfied: true` and MUST cite real evidence (a `file:line` and/or the verifying test id) — never
edit, remove, or re-word a criterion, and never flip a satisfied row back. `redkiln verify --grain
story` reads the fenced block below and blocks `implement → report` unless every spec AC is present,
`satisfied: true`, and carries non-placeholder evidence (and no AC that was satisfied on the base
branch has regressed). Scope changes are a human decision recorded through `redkiln advance`, not a
quiet ledger edit.

```yaml
- id: AC-001
  criterion: "GIVEN an adapter author whose reset is the runbook procedure — two statements on two connections, the shape Norvant ran at 02:46:31 before the pod died at 02:46:33, leaving sixty-one events applied into an empty table and the runner reporting healthy (spec/E2E-CASES.md:458-481) — WHEN they run projection_store_conformance! against their own fixture, THEN reset_clears_rows_and_checkpoint_together fails by name, and a store that applies the caller's deletes and returns the checkpoint to NeverRun as one unit passes it: observed through a fresh handle, every key the rule wrote reads None and checkpoint(id) is Checkpoint::NeverRun — both, or neither, because the pairing is the claim and not either half. The interrupted half is asserted where the fixture can express a failing reset and is a reported skip where it cannot; it is never a silently absent assertion."
  satisfied: true
  evidence: "crates/happenstance-testkit/src/projection.rs:1112 (reset_clears_rows_and_checkpoint_together, mounted in the single enumeration at :1858); red-then-green against TwoStatementResetStore (tests/projection_mutation_coverage/mutants.rs:301, declared at tests/projection_mutation_coverage.rs:554, pinned at 'must return this projection's checkpoint to'). Passing tests: projection_conformance::reset_clears_rows_and_checkpoint_together (tokio and blocking) and projection_mutation_coverage::projection_mutants_fail_exactly_their_declared_rules. PS-16's failure-injecting half is asserted rather than absent: the rule offers reset a batch begun on a second store instance (projection.rs:1138-1180) and requires both halves unchanged, which TypeStampedBatchStore fails (projection_mutation_coverage.rs:440)."
  mount_point: "crates/happenstance-testkit/src/registry.rs — the for_each_projection_store_rule! enumeration"
  verifying_test: "reset_clears_rows_and_checkpoint_together, emitted by the projection harnesses (crates/happenstance-testkit/tests/projection_conformance*.rs, wasm sibling projection_conformance_wasm.rs); red against its two-statement mutant in crates/happenstance-testkit/tests/projection_mutation_coverage.rs"
- id: AC-002
  criterion: "GIVEN Kestrel Cold Chain, whose one projection store holds van_stock (rebuilt several times a day across 138 devices) and fgas_ledger (a hash chain a regulator already holds), WHEN the author's reset is a SqliteProjectionStore::reset() that truncates the checkpoint table, THEN reset_is_scoped_to_one_projection fails by name — because the rule commits probe rows and a checkpoint under two ProjectionIds in one store, resets one, and asserts the sibling's rows and checkpoint are untouched as well as the target's being gone. The sibling assertion is the rule; without it the same code passes and the regulatory ledger is destroyed in the field."
  satisfied: true
  evidence: "crates/happenstance-testkit/src/projection.rs:1237 (reset_is_scoped_to_one_projection, mounted at :1859): two ids in one store, the sibling's rows AND checkpoint compared against a fresh-handle before-state, plus the reset id's own checkpoint back to NeverRun as the non-vacuity anchor. Red against TruncatingResetStore (mutants.rs:330, declared projection_mutation_coverage.rs:591) and SingleRowCheckpointStore (projection_mutation_coverage.rs:522), both pinned at 'resetting one projection moved another one's checkpoint'."
  mount_point: "crates/happenstance-testkit/src/registry.rs — the for_each_projection_store_rule! enumeration"
  verifying_test: "reset_is_scoped_to_one_projection, emitted by the projection harnesses; red against TruncatingResetStore in crates/happenstance-testkit/tests/projection_mutation_coverage.rs"
- id: AC-003
  criterion: "GIVEN an adapter author whose store must protect one projection from reset (the fgas_ledger shape), WHEN they declare that capability and run the suite, THEN refused_reset_changes_nothing proves the refusal is real on both halves: the call returns Err(ResetError::Refused) and is never reported as success, and through a fresh handle the rows and the checkpoint are exactly as they were before the attempt. A store that reports the refusal correctly after deleting the rows fails, and so does one that reports success."
  satisfied: true
  evidence: "crates/happenstance-testkit/src/projection.rs:1350 (refused_reset_changes_nothing, mounted at :1860): asserts Err(ResetError::Refused) AND, through a fresh handle, that the row and the checkpoint are exactly the before-state. Red against both halves of the pair - RefusalAsSuccessStore (mutants.rs:386, declared projection_mutation_coverage.rs:649, pinned at 'must answer `Err(ResetError::Refused)`') and RefusalAfterTheFactStore (mutants.rs:415, declared projection_mutation_coverage.rs:669, pinned at 'must leave the read model exactly as it was')."
  mount_point: "crates/happenstance-testkit/src/registry.rs — the for_each_projection_store_rule! enumeration"
  verifying_test: "refused_reset_changes_nothing, emitted by the projection harnesses; red against both the refusal-as-success and refusal-after-the-fact mutants in crates/happenstance-testkit/tests/projection_mutation_coverage.rs"
- id: AC-004
  criterion: "GIVEN an adapter author whose store cannot refuse a reset at all, WHEN they run the suite, THEN refused_reset_changes_nothing still appears in their output as a test and reports RuleOutcome::Skipped carrying their fixture's own stated reason — never #[cfg]-ed out, never a pass, never a second skip vocabulary invented for the projection family. This is initiative AC-05 in full for this story: a guarantee that does not apply says so, with a reason, in the same line shape every other declined capability uses."
  satisfied: true
  evidence: "crates/happenstance-testkit/src/projection.rs:2042 - a_declined_reset_refusal_is_reported_with_the_fixtures_reason asserts the RuleOutcome VALUE Skipped { capability: \"RESET_REFUSAL\", reason } against MemoryProjectionFixture, comparing the reason against the fixture's own const rather than a repeated literal. Integration half: mutation_coverage::projection_capability_skips_are_reported (tests/mutation_coverage.rs:3481, whose assert_reference_projection_declensions pins the set) pins the reference fixture's skip set to exactly [failed_commit_leaves_both_unchanged, refused_reset_changes_nothing] and checks each skip's capability and reason. No projection-local skip type: Capability and RuleOutcome are reused unchanged."
  mount_point: "crates/happenstance-testkit/src/contract.rs:473,500-507 — RuleOutcome and skip_line, reused unchanged"
  verifying_test: "the projection sibling of capability_skips_are_reported in crates/happenstance-testkit/tests/projection_mutation_coverage.rs, asserting on RuleOutcome values rather than stdout"
- id: AC-005
  criterion: "GIVEN an operator asking the store about a projection it has never seen, WHEN the store answers, THEN fresh_projection_has_no_checkpoint requires the Checkpoint::NeverRun variant — and a store answering Live { through: FIRST } for that id fails by name. The assertion is a variant match; comparing against any SequencePosition is simultaneously a CF-6 violation and the exact value the defective store writes, so a rule written that way cannot tell the two mutants apart and both walk free."
  satisfied: true
  evidence: "SATISFIED, and the sequence matters more than the row: THE PRECONDITION WAS UNMET AT THE FIRST ATTEMPT AND IS MET NOW. First attempt (10ace94): the rule shipped cited to PS-19, and the slice review found EC-001 had fired unrecorded - AC-005's verification column requires an accepted atom widening PS-19, ADR-0018 is a different atom that named this defect as a gap and repaired nothing (.kb/decisions/0018-returning-a-projection-to-never-run.md:113-116), and the consequence was substantive: PresumedLiveCheckpointStore, registered as a mutant, was CONFORMANT with PS-19 as written. The rule and its store were withdrawn at 064687a rather than argued around, with the reason stated at each site and the `†` restored on §7.2. Second attempt, this run: the precondition is MET. .kb/decisions/0030-the-checkpoint-reports-the-commits-that-happened.md is `status: accepted` on this branch, merged at 9efda45 through the human-invoked 2026-08-15-adr-0030-checkpoint-progress wave. On the provenance the verification column names ('the PS-19 repair atom from projection-decision-atoms'): that column names where the repair was ROUTED, and an atom under .kb/decisions/ can only be produced by a human-invoked /redkiln:kb-ingest wave - hand-writing one is what 0269720 was reverted for. _slices.md:436-448 records this as path (a) completed and supersedes the standing 'do not restore the held rule' instruction by name. The atom did not widen [FROZEN] PS-19 (byte-identical across the decision, :74) but MINTED PS-38, whose second sentence is the obligation verbatim: 'a ProjectionId no successful commit has named MUST read as Checkpoint::NeverRun' (spec/SPECIFICATION.md:5447-5449). THE RULE, cited to PS-38 and not to PS-19: crates/happenstance-testkit/src/projection.rs:1467 - one id no commit has named, asserted against the Checkpoint::NeverRun VARIANT with no position compared anywhere in the body (CF-6 lint green). Mounted in the single enumeration at :1910, between refused_reset_changes_nothing and reset_is_not_commit_at_first, which is exactly where the placeholder marked. RED-THEN-GREEN: PresumedLiveCheckpointStore is restored (mutants.rs:465, REGISTRY row at projection_mutation_coverage.rs:717, for_each_projection_mutant! entry) and projection_mutants_fail_exactly_their_declared_rules asserts it FAILS this rule at the pinned assertion 'a projection this store has never seen must read back as'. Its non-conformance was re-checked against ADR-0030 before restoring, per the review's instruction: PS-38 rejects it, PS-19 alone did not. Passing tests: projection_conformance::fresh_projection_has_no_checkpoint (tokio, blocking and buffering harnesses; 17 rules each), projection_mutation_coverage's four exactness meta-tests, and cargo xtask spec-trace, whose --write pass CLEARED the `†` from both the PS-19 and PS-38 rows (the only two lines of the spec diff, both inside the BEGIN/END GENERATED region)."
  mount_point: "crates/happenstance-testkit/src/registry.rs — the for_each_projection_store_rule! enumeration (crates/happenstance-testkit/src/projection.rs:1910)"
  verifying_test: "fresh_projection_has_no_checkpoint, emitted by all three projection harnesses; red against PresumedLiveCheckpointStore in crates/happenstance-testkit/tests/projection_mutation_coverage.rs, and held green against the new AbsentAfterResetStore conformant variant (variants.rs:127) by projection_conformant_variants_pass_everything"
- id: AC-006
  criterion: "GIVEN the operator at 03:18 who \"resets properly\" by writing commit(empty_batch, id, SequencePosition::FIRST) — the substitute all six deployment scenarios reached for and all six got wrong (RUNBOOK.md:3904-3907) — WHEN the runner resumes, THEN reset_is_not_commit_at_first rejects it: the rule resets one id and commits-at-FIRST on a sibling id in the same store, asserts the two checkpoints differ by variant (NeverRun vs Live { .. }), and then derives a resume point from each under the port's own rule — strictly after a Live position, inclusive from the store's first position when NeverRun — asserting the event at the first position is included in the reset case and excluded in the commit-at-FIRST case. Event 1 skipped permanently and silently is the defect; the variant check alone would not see the resume consequence, and the resume check alone would not see the state."
  satisfied: true
  evidence: "crates/happenstance-testkit/src/projection.rs:1485 (reset_is_not_commit_at_first, mounted at :1865): resets one id, commits commit(empty_batch, id, FIRST, Live) on a sibling id in the same store, asserts the two checkpoints differ by core::mem::discriminant, then derives a resume point from each through resumes_over (projection.rs:362 - strictly after a recorded position, inclusive from the first position when NeverRun) and asserts the event at the first position is applied in the reset case and excluded in the substitute's. Red against CommitAtFirstResetStore (mutants.rs:356, declared projection_mutation_coverage.rs:611). No runner is built."
  mount_point: "crates/happenstance-testkit/src/registry.rs — the for_each_projection_store_rule! enumeration"
  verifying_test: "reset_is_not_commit_at_first, emitted by the projection harnesses; red against the commit-at-first substitute store registered in crates/happenstance-testkit/tests/projection_mutation_coverage.rs"
- id: AC-007
  criterion: "GIVEN an adapter author who wants to know whether reset_is_scoped_to_one_projection can actually fail, WHEN they open the projection mutant binary, THEN TruncatingResetStore is registered there with a fails list naming exactly that rule and a non-empty provenance describing the real mistake it models — a SqliteProjectionStore::reset() truncating the checkpoint table, cheap and obvious, which destroys the append-only regulatory ledger sharing the file. It is correct in every other step, so the rule that goes red goes red for the defect and not for the store."
  satisfied: true
  evidence: "crates/happenstance-testkit/tests/projection_mutation_coverage.rs:591 - TruncatingResetStore registered with fails: [reset_is_scoped_to_one_projection] and non-empty provenance naming the SqliteProjectionStore::reset() truncate and the van_stock / fgas_ledger consequence. Held by the four projection exactness meta-tests, all green: every_projection_rule_has_a_mutant, projection_mutant_registry_is_exhaustive, projection_mutants_fail_exactly_their_declared_rules and every_projection_mutant_states_its_provenance."
  mount_point: "crates/happenstance-testkit/tests/projection_mutation_coverage.rs — the projection REGISTRY: &[Declared]"
  verifying_test: "the three projection exactness meta-tests (CF-1/CF-2/CF-3 siblings) plus the CF-4 provenance meta-test in crates/happenstance-testkit/tests/projection_mutation_coverage.rs"
- id: AC-008
  criterion: "GIVEN that a rule nobody has checked is a rule that proves nothing, WHEN this story merges, THEN every one of its five rules has at least one registered wrong store that fails it — the commit-at-first substitute (declaring reset_is_not_commit_at_first, provenance: six scenarios out of six), TruncatingResetStore, and one each for the remaining three in the shapes §4.11 pre-specifies (the two-statement runbook procedure; a refusal reported as success; a store answering Live { through: FIRST } for an id it has never seen) — and each wrong store is a single overridden step over the shared correct-steps module, never a hand-written store. A store that differs in two ways makes a red rule red for the wrong reason, and the exactness meta-test would then be catching a defect in the instrument."
  satisfied: true
  evidence: "All FIVE rules, as written. Six new wrong stores, each ONE overridden Defect step over the shared correct core: TwoStatementResetStore (mutants.rs:301), TruncatingResetStore (:330) and CommitAtFirstResetStore (:356) override reset_writes; RefusalAsSuccessStore (:386) and RefusalAfterTheFactStore (:415) override refusal; PresumedLiveCheckpointStore (:465) overrides missing_checkpoint - rows at projection_mutation_coverage.rs:554,591,611,649,669,717. Every one of the five rules has at least one registered store that fails it, and the shapes are the ones §4.11 pre-specifies: the two-statement runbook procedure, the truncating reset, the commit-at-first substitute, a refusal reported as success, and a store answering Live { through: FIRST } for an id it has never seen. Three existing declarations grew rather than the new rules being weakened (UncommittedTransactionStore, TypeStampedBatchStore, ValidatingCommitStore). Held by the four projection exactness meta-tests, all green. No pass rate is computed or quoted anywhere. PresumedLiveCheckpointStore's row is the fifth store's, restored this run after ADR-0030 made it non-conformant against a clause (PS-38) rather than against a rule reaching past one - the row's own provenance records the withdrawal and the reason, so the registry states the sequence rather than a claim that was always true. A conformant variant on the same seam landed with it (AbsentAfterResetStore, variants.rs:127), which is what CF-5's positive control lacked when the over-convicting rule went out."
  mount_point: "crates/happenstance-testkit/tests/projection_mutation_coverage.rs — the projection REGISTRY: &[Declared] and its shared correct-steps module"
  verifying_test: "every_projection_rule_has_a_mutant, projection_mutant_registry_is_exhaustive and projection_mutants_fail_exactly_their_declared_rules in crates/happenstance-testkit/tests/projection_mutation_coverage.rs"
- id: AC-009
  criterion: "GIVEN initiative AC-04 — the adapter author is told when they are finished, \"with no rule silently absent from the run\" — WHEN they invoke the one entry point, THEN all five rule names are in the single for_each_projection_store_rule! enumeration, so the tokio, blocking and wasm32 harnesses each emit five more tests with no per-harness list to maintain, and a rule present in the module but absent from the enumeration is a build failure rather than an omission. The edge developer's runtime is not a separately maintained subset: projection_conformance_wasm.rs type-checks the same five in the same cargo xtask ci run."
  satisfied: true
  evidence: "All five rule names are in the single for_each_projection_store_rule! enumeration, crates/happenstance-testkit/src/projection.rs:1907-1911, so the tokio, blocking and wasm32 harnesses each emit five more tests with no per-harness list: projection_conformance and projection_conformance_blocking each report 17 passed (projection_conformance_buffering 18, its extra being the second batch shape's own test), and cargo check -p happenstance-testkit --tests --target wasm32-unknown-unknown type-checks projection_conformance_wasm.rs in the same cargo xtask ci run. The orphan direction is no_orphan_projection_rules (projection.rs, the include_str! scan of this file's own source), which is what would have caught a rule left in the module unenumerated - and is what kept the withdrawn rule's absence honest for the day it was out."
  mount_point: "crates/happenstance-testkit/src/registry.rs — the for_each_projection_store_rule! enumeration; crates/happenstance-testkit/tests/projection_conformance_wasm.rs"
  verifying_test: "the projection sibling of no_orphan_rules (crates/happenstance-testkit/src/registry.rs:412-424), plus cargo xtask ci's mandatory wasm32 step: cargo check -p happenstance-testkit --tests --target wasm32-unknown-unknown"
- id: AC-010
  criterion: "GIVEN an evaluator (initiative AC-08) reading the release notes to decide whether the suite is worth trusting, WHEN they look up any of these five rules, THEN CHANGELOG.md names the defect it detects rather than listing the rule, the gate's own rule lints see all five (they read RULE_FILES, so a projection rules module absent from that list makes them pass vacuously — reported, never patched here), and git diff over spec/SPECIFICATION.md touches nothing outside the BEGIN/END GENERATED region of §7.1–§7.2: no [FROZEN] clause text changed, no maturity marker moved and no rule citation hand-edited to make anything pass. (CRITERION TEXT RE-SYNCED, not re-worded by the implementer: spec.md:382 was amended on 2026-08-14 at 5f2cf02 and this row was left carrying the pre-amendment sentence — 'git diff over spec/SPECIFICATION.md is empty' — so the row the gate reads and the criterion the spec states were two different sentences. The words above are spec.md:382's, verbatim in the ledger's markdown-stripped style. The amendment's own argument is at spec.md:397-415.)"
  satisfied: true
  evidence: "cargo xtask spec-trace green: check 6 resolves all five rule names out of RULE_FILES, which already named crates/happenstance-testkit/src/projection.rs (EC-002 checked and clear, xtask/src/spec_trace.rs:88-93). CF-29's changelog lint green - 'all 112 rules in 4 file(s) have a changelog entry' - over five CHANGELOG.md entries each naming the defect its rule detects; the fifth is fresh_projection_has_no_checkpoint's own entry, which replaced the non-delivery notice the withdrawal had left in the release notes and which names both the defect (a runner told a read model nobody built is authoritative through FIRST) and the clause history, so an evaluator reading the release notes is not told the contract declines to oblige something it now obliges. CF-6's position lint green over 4 rule files: the new rule asserts on the Checkpoint VARIANT and names no position at all; reset_is_not_commit_at_first compares only positions it supplied itself. git diff over spec/ is TWO LINES, both inside the BEGIN/END GENERATED region and both a `†` removed by cargo xtask spec-trace --write as fresh_projection_has_no_checkpoint started existing (PS-19's row and PS-38's). The region is stale-checked by a gate step (xtask/src/spec_trace.rs), so leaving it unregenerated is itself a failure. NO clause text, NO maturity marker and NO rule citation changed — verified against PS-16 – PS-20 and PS-38 specifically, which are the clauses this story could have been tempted to widen; PS-19 is byte-identical across the whole episode, which was the entire point of routing the repair to ADR-0030."
  mount_point: "CHANGELOG.md; xtask/src/spec_trace.rs:85-89 (RULE_FILES) — the file list the gate's rule checks read"
  verifying_test: "cargo xtask spec-trace (check 6), the CF-29 changelog lint at xtask/src/lints.rs:511-531, the CF-6 position lint over the same file set, and git diff --stat main...HEAD -- spec/ crates/happenstance-core/ (empty)"
```
