---
item: HS-S0014
stage: implement
created: 2026-08-12T13:46:08.816Z
updated: 2026-08-12T13:46:08.816Z
---

# Acceptance ledger — The PS-3 evidence written as a finding, not a verdict

The machine-checkable Definition-of-Done ledger for this story (RFC §6.5). One row per spec
`AC-###`. Planning authors every row with `satisfied: false`; the implementer may only flip a row to
`satisfied: true` and MUST cite real evidence (a `file:line` and/or the verifying test id) — never
edit, remove, or re-word a criterion, and never flip a satisfied row back. `redkiln verify --grain
story` reads the fenced block below and blocks `implement → report` unless every spec AC is present,
`satisfied: true`, and carries non-placeholder evidence (and no AC that was satisfied on the base
branch has regressed). Scope changes are a human decision recorded through `redkiln advance`, not a
quiet ledger edit.

Two notes specific to this story, both from `spec.md`. First, AC-015 is typed **E2E (process,
derived from AC-004)** (`../_decomposition.md:785`), so several `verifying_test` values name a gate
step or a recorded mechanical comparison rather than a `#[test]` — that is the tier, not a gap.
Second, three rows (AC-003, AC-006, AC-007) are discharged by the story review, because no tool can
check whether a document reads as a verdict; their evidence must be the review's own `file:line`
findings against `references/evaluation/projection-batch-shape-evidence.md`, not a self-assessment.

```yaml
- id: AC-001
  criterion: "GIVEN a planner who has never read this project's implementation history, WHEN they follow a single path from the specification or the evidence index, THEN they land on `references/evaluation/projection-batch-shape-evidence.md`, whose first block states the date, the commit sha of the run it reports, and that it is superseded rather than edited — AND no `.kb/decisions/` atom, `.kb/reference/` atom or `.kb/_intake/` file was created by this story, so the planner is reading evidence they may still decide against, not a decision they would have to supersede."
  satisfied: true
  evidence: "references/evaluation/projection-batch-shape-evidence.md:1-11 — the file exists at exactly that path and its first block states all three lifecycle facts: Date 2026-08-14 (:3), Pinned to cfd9231 / cfd92313cc59e059655fe130f3c8c31b19dfcf08, named as the commit whose cargo xtask ci run it reports (:4-6), and 'immutable evidence. Superseded rather than edited', cited to references/evaluation/README.md:9-13 and :140-142 (:7-11). Both arrival paths land on it: the README index entry (references/evaluation/README.md:91-112) and PS-3's clause body (spec/SPECIFICATION.md:4788-4790 (the added paragraph)). KB emptiness is the check and it is clean: `git status --porcelain .kb/` returns nothing for this PR — no decision atom, no reference atom, no _intake file — and `redkiln validate --kb` reports 'validate passed.'"
  mount_point: "references/evaluation/README.md"
  verifying_test: "Static: the file exists at references/evaluation/projection-batch-shape-evidence.md with its lifecycle header (date, pinned sha, supersede-not-edit) per references/evaluation/README.md:9-13; `git status --porcelain .kb/` empty for this PR; `redkiln validate --kb` green"
- id: AC-002
  criterion: "GIVEN a planner who cannot re-run the suite themselves, WHEN they ask \"what was this actually observed on?\", THEN the finding names both fixtures by their real identifiers (the CF-5 buffering replay-at-commit variant in `crates/happenstance-testkit/tests/`, and apply-on-write `MemoryProjectionStore`), the harness invocation that drove them, and the single `cargo xtask ci` invocation with its sha — one run, not two `cargo test` runs reconciled by hand."
  satisfied: true
  evidence: "references/evaluation/projection-batch-shape-evidence.md:20-51 (§1 'What this was observed on'), which names the evidence base BEFORE it reasons from it. Both fixtures by their real identifiers, in a table: MemoryProjectionFixture over MemoryProjectionStore (crates/happenstance-core/src/projection_memory.rs:101) driven by crates/happenstance-testkit/tests/projection_conformance.rs, and BufferingProjectionFixture over BufferingProjectionStore (crates/happenstance-testkit/tests/projection_mutation_coverage/buffering.rs:252) driven by crates/happenstance-testkit/tests/projection_conformance_buffering.rs — both harness invocations named, both being ordinary tests/ targets of happenstance-testkit. ONE `cargo xtask ci` invocation, clean tree, 'all checks passed', pinned to cfd92313cc59e059655fe130f3c8c31b19dfcf08; `git cat-file -e cfd92313cc59e059655fe130f3c8c31b19dfcf08` resolves on initiative/from-contract-to-published-library. Not two cargo test runs reconciled by hand, and §1 says so. One deliberate divergence from this criterion's wording, made because a finding must not restate a falsehood: the document does NOT describe MemoryProjectionStore as apply-on-write, because it is not — its begin returns an owned batch holding a delta and its commit applies it (projection_memory.rs:280-330). §4 records that correction with its citation, and it is the finding's sharpest result."
  mount_point: "references/evaluation/README.md"
  verifying_test: "E2E (process, derived from AC-004): the slice-mate's single `cargo xtask ci` run (buffering-conformant-variant, HS-S0013) per ../_decomposition.md:774; `git cat-file -e <sha>` resolves the pinned sha on this branch; review of the fixture identifiers against crates/happenstance-testkit/tests/"
- id: AC-003
  criterion: "GIVEN two readers of the same evidence — the HS-P0016 planner and the HS-P0015 verdict author — WHEN each classifies the same observation, THEN they reach the same label, because the finding states the vocabulary operationally before the ledger: D1 per-shape handling · D2 asymmetric declension · D3 assertion loosened · D4 divergent observable behaviour, plus agreed and not comparable as distinct verdicts — and no fifth category is minted inside the ledger to accommodate a row."
  satisfied: true
  evidence: "references/evaluation/projection-batch-shape-evidence.md:53-71 (§2 'The vocabulary, fixed before the ledger'), which sits BEFORE §3's ledger in the reading order and defines all six labels operationally: D1 per-shape handling, D2 asymmetric declension (naming the capability constants — fixture-declared, or ProjectionProbe::READS_THROUGH_BATCH), D3 assertion loosened (marked retrospective, read off the diff rather than the run), D4 divergent observable behaviour, plus agreed and not comparable as distinct verdicts with the explicit instruction that the second is never folded into the first. Mechanical check, run over the built document: the set of verdict labels appearing in the ledger's Verdict column is {agreed, D2} — a subset of the six, no seventh minted. Extraction script and result recorded in this story's implementation report under ## Gates."
  mount_point: "references/evaluation/README.md"
  verifying_test: "Story review against spec.md Context pack §4 and discover.md:40-45, plus the mechanical check that the set of verdict labels used in the ledger column is a subset of the six defined labels"
- id: AC-004
  criterion: "GIVEN a planner who must know the evidence is complete and not curated, WHEN they compare the finding's ledger against the suite's own rule enumeration, THEN every rule named in `for_each_projection_store_rule!` appears exactly once, and no row names a rule that is not in it — so a rule cannot be silently absent from the evidence the way a rule must not be silently absent from a run."
  satisfied: true
  evidence: "references/evaluation/projection-batch-shape-evidence.md:79-96 — sixteen numbered ledger rows, one per rule, in enumeration order. The comparison was run mechanically in BOTH directions against the macro rather than eyeballed: arms of `for_each_projection_store_rule!` extracted from crates/happenstance-testkit/src/projection.rs:1843-1881 (the enumeration lives beside the projection rules, not in registry.rs where the event-store family's sits — the finding records that at :29-31) and compared with the ledger's rule-name set. Result: enumeration arms 16, ledger rows 16, order identical True, in-enum-not-in-ledger [], in-ledger-not-in-enum [], duplicate rows []. Script and output recorded in this story's implementation report under ## Gates. Run at the pinned sha, whose tree is the one this document reports (EC-006: the ledger does not silently acquire rows later)."
  mount_point: "references/evaluation/README.md"
  verifying_test: "Static (mechanical, both directions): ledger rule-name set == arm set of `for_each_projection_store_rule!` in crates/happenstance-testkit/src/registry.rs at the pinned sha, mirroring `no_orphan_rules` (crates/happenstance-testkit/src/registry.rs:412); the comparison is recorded in the implementation report"
- id: AC-005
  criterion: "GIVEN a planner deciding how much a \"skip\" weakens the evidence, WHEN they read any D2 row or any not-comparable row, THEN it cites the `RuleOutcome::Skipped` value and the capability constant that produced it — a fixture-declared `Capability`, or `ProjectionProbe::READS_THROUGH_BATCH` — never a remembered or quoted stdout line, because libtest suppresses that line for a passing test unless `--show-output` is passed."
  satisfied: true
  evidence: "Two D2 rows and no not-comparable rows, and both D2 rows are RuleOutcome-grounded rather than stdout-grounded. Row 3, `failed_commit_leaves_both_unchanged` (references/evaluation/projection-batch-shape-evidence.md:83): cites the capability constant ProjectionFixture::COMMIT_FAULT at crates/happenstance-testkit/src/contract.rs:640 and states in terms that 'the skip is a RuleOutcome::Skipped value collected by the harness (contract.rs:473-537), not a remembered line'. Row 12, `refused_reset_changes_nothing` (:92): cites ProjectionFixture::RESET_REFUSAL at contract.rs:604 and states 'the claim is the RuleOutcome::Skipped value, not the printed line'. The declension text quoted in each row is the fixture's own stated reason carried on the constant, not a transcription of terminal output — and the harness collects those pairs as data (SubjectReport::declines, crates/happenstance-testkit/tests/projection_mutation_coverage/harness.rs:394), which is what `the_second_batch_shape_answers_every_rule_with_a_pass` asserts over."
  mount_point: "references/evaluation/README.md"
  verifying_test: "Review: every D2 / not-comparable row carries a citation resolving into crates/happenstance-testkit/src/contract.rs:473-537 (`RuleOutcome`, `skip_line`, `report`), per ../project.md:194-197"
- id: AC-006
  criterion: "GIVEN a planner reading a ledger with no disagreements in it, WHEN they look for what that means, THEN the finding says so explicitly as a result and weighs Architecture brief Note 10 item 2's two candidate readings — the rules are shape-blind in a way that hides the axis, or the axis is not where §4.2 says it is — choosing one with its reasoning, or recording that this evidence cannot distinguish them. \"No disagreements observed\" as the whole of it does not discharge this, and neither does omitting the section because there was nothing to report."
  satisfied: true
  evidence: "references/evaluation/projection-batch-shape-evidence.md:121-172 — §4 'The null result, weighed', a named section that exists and is substantive. It states the result as a result first ('on the batch-representation axis the answer is: the two shapes did not disagree anywhere' — and it is the longer section, not the shorter one, exactly as AC-006 anticipates), then addresses BOTH of Note 10 item 2's readings by name. Reading A (rules shape-blind in a way that hides the axis): supported in part, with three concrete rows cited — 4, 5 and 13 — where a rule accepted two different mechanisms without noticing. Reading B (the axis is not where §4.2 places it): supported, and established as a fact rather than an interpretation, with the citation crates/happenstance-core/src/projection_memory.rs:280-330 showing MemoryProjectionStore is already a deferred write set. The conclusion is stated explicitly and is the admissible third answer, sharpened: reading B is established, and it makes reading A untestable on this evidence, 'because a rule cannot be shown blind to an axis that neither of the two subjects sits on either side of'. What the run DOES establish is then given in one usable sentence. Not 'no disagreements observed', and not omitted."
  mount_point: "references/evaluation/README.md"
  verifying_test: "Story review against ../_decomposition.md:716-719 and discover.md:89-98: a named section addressing both readings exists whenever the ledger is all-agreed, and its conclusion is one of {reading A, reading B, this evidence cannot distinguish them}"
- id: AC-007
  criterion: "GIVEN a planner who could otherwise inherit a conclusion as though it were evidence, WHEN they reach the end of the finding, THEN they meet two mandatory statements and no third: that PS-2's bar — two adapters at opposite ends of the batch-shape axis — is not met by anything this project built alone, citing PS-2's Rejects clause naming the two-instrument monoculture verbatim; and that no exposure verdict is made here, naming HS-P0016 as owner of the `unstable-projection` call and HS-P0015 as owner of the freeze verdict. The document contains no recommendation, preference, leaning or prediction about when PS-2's bar will be met."
  satisfied: true
  evidence: "Two mandatory statements, both terminal, and no third. PS-2: references/evaluation/projection-batch-shape-evidence.md:174-187 (§5) opens 'PS-2's bar — the suite green against two *adapters* at opposite ends of the batch-shape axis — is not met by anything this project built alone', cites spec/SPECIFICATION.md:4760-4775 and quotes the Rejects clause VERBATIM including 'both are the same storage shape wearing two hats — the exact monoculture CLAUDE.md's spread rule exists to catch', then states 'This document makes no claim about when PS-2's bar will be met, or by what.' The non-verdict: :207-222 (§7 'No verdict is made here'), the LAST section, naming publication-and-positioning (HS-P0016) via projection-port-ship-shape as owner of the unstable-projection call and ladybug-projection-store (HS-P0015) via freeze-verdict-document as owner of the freeze verdict, and closing 'Neither call is made here, and neither is recommended.' NF-004 lexical sweep run over the whole document: the only matches for should/recommend/suggests/leans/is ready/is proven are the two negations inside §7 that constitute the guard itself. The named wrong implementation at discover.md:75-87 — 'both batch shapes passed the whole suite; the port is proven against the batch-shape axis' — is refused twice over: §4 establishes that the pair does not span the axis, and §5 refuses the PS-2 reading."
  mount_point: "references/evaluation/README.md"
  verifying_test: "Story review reading for discover.md:75-87's named wrong implementation, with both statements checked against spec/SPECIFICATION.md:4760-4775, ../_decomposition.md:307-311,436-456 and ../project.md:129-131,301-307"
- id: AC-008
  criterion: "GIVEN a planner who does not already know this file's name, WHEN they arrive from either direction a reader actually travels — browsing `references/evaluation/README.md`, or following PS-3's clause body — THEN they find it: one index entry in the README's \"Later additions, which are neither\" section, and one additive sentence in PS-3 citing it by `file:line`. AND `cargo xtask ci` is green including `spec-trace`; PS-2's body is byte-identical; PS-3's `[PROVISIONAL]` marker, `Rule:`, `Cases:` and `Rejects:` fields are unchanged; no other clause is touched."
  satisfied: true
  evidence: "Both mounts, and one is machine-checked. Human path: one index entry in the README's 'Later additions, which are neither' section at references/evaluation/README.md:91-112, following that section's existing entry shape (name, date, pinned sha, the same 'immutable, superseded rather than edited' lifecycle sentence, that it is a byproduct rather than one of the fourteen, and that the runbook was not derived from it) — the shape review-citation-drift.md:43-48 and ps-clause-pairing-sweep.md's entry already use, so no third lifecycle is proposed. Machine path: one additive paragraph in PS-3's body at spec/SPECIFICATION.md:4788-4790 citing `references/evaluation/projection-batch-shape-evidence.md:1`. RED FIRST and for the right reason: the citation was added before the file existed and `cargo xtask spec-trace` failed with 'spec/SPECIFICATION.md:4790 — citation `references/evaluation/projection-batch-shape-evidence.md:1` names a file that does not exist' (1 traceability problem, exit 1). GREEN once the finding landed: 'traceability: no problems found; §7.1–§7.2 matches the checker', 359 citations checked (358 before). `cargo xtask ci` whole: 'all checks passed'. Diff discipline: `git diff spec/SPECIFICATION.md` is exactly +4 lines (one blank + a three-line paragraph) inside PS-3 and nothing else in the file — PS-2's body byte-identical, PS-3's [PROVISIONAL] marker and its Rule:/Cases:/Rejects: fields untouched, no other clause touched, no maturity marker changed, and the generated §7.1–§7.2 region unchanged."
  mount_point: "references/evaluation/README.md"
  verifying_test: "Static: `cargo xtask spec-trace` (mandatory step of `cargo xtask ci`) resolves the PS-3 citation via `check_citations` (xtask/src/spec_trace.rs:297-370, ANCHOR_SLACK=12 at :391); `git diff spec/SPECIFICATION.md` shows exactly one added sentence inside PS-3 and nothing inside PS-2; review of the README entry against references/evaluation/README.md:40-48"
```
