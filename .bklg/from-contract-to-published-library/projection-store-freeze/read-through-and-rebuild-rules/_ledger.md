---
item: "HS-S0012"
stage: implement
created: "2026-08-12T13:46:06.709Z"
updated: "2026-08-12T13:46:06.709Z"
---

# Acceptance ledger — Read-your-writes, chunk-size invariance and rebuild authority

The machine-checkable Definition-of-Done ledger for this story (RFC §6.5). One row per spec
`AC-###`. Planning authors every row with `satisfied: false`; the implementer may only flip a row to
`satisfied: true` and MUST cite real evidence (a `file:line` and/or the verifying test id) — never
edit, remove, or re-word a criterion, and never flip a satisfied row back. `redkiln verify --grain
story` reads the fenced block below and blocks `implement → report` unless every spec AC is present,
`satisfied: true`, and carries non-placeholder evidence (and no AC that was satisfied on the base
branch has regressed). Scope changes are a human decision recorded through `redkiln advance`, not a
quiet ledger edit.

```yaml
- id: AC-001
  criterion: "GIVEN an adapter author whose storage lets an open batch see its own pending writes, and whose `ProjectionProbe` therefore declares `READS_THROUGH_BATCH = true`, WHEN they invoke `projection_store_conformance!` with their fixture, THEN a test named `batch_reads_reflect_pending_writes` runs, writes a value through the open batch and asserts `probe_read_through(&batch, k) == Some(v)` for that value before commit — so a store whose batch `get` answers from committed state fails it by name rather than passing on a `None`-vs-`Some` technicality."
  satisfied: true
  evidence: "crates/happenstance-testkit/src/projection.rs:1602 (batch_reads_reflect_pending_writes, mounted in the single enumeration at :1878). It writes through an open batch and asserts probe_read_through(&batch, k) == Some(v) for THAT value before commit, so a None-vs-Some technicality cannot satisfy it. Green against MemoryProjectionStore on the tokio and blocking harnesses (projection_conformance / projection_conformance_blocking, 17 passed each); red against CommittedReadBatchStore (tests/projection_mutation_coverage/mutants.rs:477, declared at tests/projection_mutation_coverage.rs:719, pinned at 'must let an open batch see') under projection_mutants_fail_exactly_their_declared_rules."
  mount_point: "crates/happenstance-testkit/src/registry.rs — the single enumeration `for_each_projection_store_rule!` (plus the projection rules module, the tokio/blocking/wasm32 harnesses, and mutation_coverage.rs's projection REGISTRY + for_each_mutant!)"
  verifying_test: "crates/happenstance-testkit/tests/mutation_coverage.rs::mutants_fail_exactly_their_declared_rules (CommittedReadBatchStore row) + the tokio/blocking projection harnesses running `batch_reads_reflect_pending_writes` against MemoryProjectionStore"
- id: AC-002
  criterion: "GIVEN an adapter author who will rebuild a projection in production at whatever chunk size fits their memory budget, and who must not have to discover that the chunk size changed the answer, WHEN the suite runs against their store, THEN `rebuild_is_chunk_size_invariant` replays one fixed sequence (`a, a, b, a, b, a`) at chunk sizes 1, 3 and whole-log against three isolated stores (`open()` per run), each step a read-modify-write through the batch (`probe_read_through(…).unwrap_or(0) + 1`), and asserts the three runs' read models are identical per key — so a store that cannot read through its own batch diverges (`a = 2` at size 3, `a = 1` at whole-log) instead of quietly agreeing."
  satisfied: true
  evidence: "crates/happenstance-testkit/src/projection.rs:1673 (rebuild_is_chunk_size_invariant, mounted at :1879): the fixed sequence a, a, b, a, b, a replayed at chunk sizes 1, 3 and whole-log against THREE isolated stores (one open() per run), every step a read-modify-write through the batch - probe_read_through(...).unwrap_or(0) + 1 - committed with Authority::Rebuilding at deliberately non-contiguous, strictly increasing positions, comparing probe_read per key across runs. Red against BOTH CommittedReadBatchStore and FirstWriteWinsBatchStore (mutants.rs:500, declared coverage.rs:747), each pinned at 'must produce the same read model however it is'. The body increments; it never uses a plain set, which would be chunk-insensitive by construction."
  mount_point: "crates/happenstance-testkit/src/registry.rs — `for_each_projection_store_rule!` (plus the rules module, the three harnesses, and the mutant registry)"
  verifying_test: "crates/happenstance-testkit/tests/mutation_coverage.rs::mutants_fail_exactly_their_declared_rules (CommittedReadBatchStore and FirstWriteWinsBatchStore rows) + `rebuild_is_chunk_size_invariant` against MemoryProjectionStore in the tokio/blocking harnesses"
- id: AC-003
  criterion: "GIVEN a reader who must decide whether the rows in front of them are authoritative, WHEN a store commits two chunks under `Authority::Rebuilding` and then one under `Authority::Live`, THEN `rebuilding_is_distinguishable_from_live` asserts `checkpoint(id)` reads the `Checkpoint::Rebuilding` variant after each of the first two and `Checkpoint::Live` after the third — comparing variants only, never the `through` value — and a store that rebuilds in place behind a single position field fails it. The rule asserts nothing immediately after `reset`, because a rebuild that has committed nothing correctly reads `NeverRun`."
  satisfied: true
  evidence: "crates/happenstance-testkit/src/projection.rs:1764 (rebuilding_is_distinguishable_from_live, mounted at :1880): two chunks committed with Authority::Rebuilding, each asserting matches!(checkpoint, Checkpoint::Rebuilding { .. }) through a FRESH handle, then one with Authority::Live asserting Checkpoint::Live { .. }; the through value is compared to nothing anywhere, and nothing is asserted immediately after the opening reset. Red against LiveOnlyCheckpointStore (mutants.rs:525, declared coverage.rs:765), which preserves NeverRun for an unseen id and therefore does not also trip fresh_projection_has_no_checkpoint."
  mount_point: "crates/happenstance-testkit/src/registry.rs — `for_each_projection_store_rule!` (plus the rules module, the three harnesses, and the mutant registry)"
  verifying_test: "crates/happenstance-testkit/tests/mutation_coverage.rs::mutants_fail_exactly_their_declared_rules (LiveOnlyCheckpointStore row, failing exactly this rule) + `rebuilding_is_distinguishable_from_live` against MemoryProjectionStore"
- id: AC-004
  criterion: "GIVEN an adapter author deciding whether to trust this suite at all, and knowing that a rule no implementation can fail is decorative (`CLAUDE.md`, The rule that matters), WHEN they read the mutant registry, THEN each of the three new rules has at least one registered wrong implementation with an exact `fails` set and non-empty `provenance` naming the real adapter mistake it models — the round-trip batch `get`, the `entry().or_insert(…)` dedup buffer, the single-position-field rebuild — and no pass rate is quoted anywhere over the mutant set."
  satisfied: true
  evidence: "Each of the three new rules has at least one registered wrong implementation with an EXACT fails set and non-empty provenance naming the real adapter mistake: CommittedReadBatchStore (the round-trip batch get, coverage.rs:719), FirstWriteWinsBatchStore (the entry().or_insert(...) dedup buffer, :747), LiveOnlyCheckpointStore (the single-position-field rebuild, :765). Held by every_projection_rule_has_a_mutant, projection_mutant_registry_is_exhaustive, projection_mutants_fail_exactly_their_declared_rules and every_projection_mutant_states_its_provenance, all green. No pass rate is computed or quoted anywhere - the registry's module doc still carries ADR-0010's prohibition and nothing in the binary divides."
  mount_point: "crates/happenstance-testkit/tests/mutation_coverage.rs — the projection `REGISTRY: &[Declared]` and `for_each_mutant!`"
  verifying_test: "crates/happenstance-testkit/tests/mutation_coverage.rs::every_rule_has_a_mutant, ::mutant_registry_is_exhaustive and ::mutants_fail_exactly_their_declared_rules over the three new rules and four new stores"
- id: AC-005
  criterion: "GIVEN an adapter author whose storage genuinely cannot answer a read from an uncommitted batch — the write-behind or buffering shape PS-12 explicitly permits — WHEN they declare `READS_THROUGH_BATCH = false` and run the suite, THEN the whole projection suite is green, not red: `NoBatchReadStore` is registered as a `Kind::ConformantVariant` with `fails: &[]`, exposes no read path on the open batch (`probe_read_through` is `unimplemented!()`), and passes every projection rule — so declining a capability honestly is a supported outcome rather than a failure, and the skip arm of both gated rules has a fixture behind it in the same run that `MemoryProjectionStore` holds the `true` arm."
  satisfied: true
  evidence: "crates/happenstance-testkit/tests/projection_mutation_coverage/variants.rs:57 - NoBatchReadStore declares READS_THROUGH_BATCH = false and leaves probe_read_through unimplemented!(), registered at coverage.rs:784 as Kind::ConformantVariant with fails: &[]. The new positive control projection_conformant_variants_pass_everything (coverage.rs:1253) drives it through the whole enumeration and requires every rule to pass or to skip on a declension the store itself makes; it also asserts the variant set is non-empty, so it cannot pass over nothing. Kind::ConformantVariant's #[expect(dead_code)] came off in the same change, which is what made that deferral's end observable."
  mount_point: "crates/happenstance-testkit/tests/mutation_coverage/variants.rs + the projection `REGISTRY`/`for_each_mutant!` in crates/happenstance-testkit/tests/mutation_coverage.rs"
  verifying_test: "crates/happenstance-testkit/tests/mutation_coverage.rs::mutant_registry_is_exhaustive (NoBatchReadStore row, `fails: &[]`) + `projection_store_conformance!` driven against NoBatchReadFixture with zero failures"
- id: AC-006
  criterion: "GIVEN that same author reading a CI log, WHEN the two gated rules run against their declining fixture, THEN each returns `RuleOutcome::Skipped` whose `capability` field is the string `ProjectionProbe::READS_THROUGH_BATCH` — the constant they can actually go and change, not a fixture const that does not exist — and whose `reason` is the fixture's own non-empty stated reason, and the run reports two skips, not one and not silence. The assertion is on the `RuleOutcome` value, not on stdout."
  satisfied: true
  evidence: "SATISFIED, against the authority that binds this project's surfaces. crates/happenstance-testkit/tests/projection_mutation_coverage.rs:1315 - a_batch_with_no_read_path_is_reported_as_a_skip drives the whole enumeration against NoBatchReadStore, asserts the skip set is EXACTLY [batch_reads_reflect_pending_writes, rebuild_is_chunk_size_invariant] in enumeration order (two skips, not one and not silence), and asserts BOTH fields of each RuleOutcome::Skipped against the exported constants NO_BATCH_READ_PATH = \"ProjectionProbe::READS_THROUGH_BATCH\" (crates/happenstance-testkit/src/contract.rs:836) and NO_BATCH_READ_PATH_REASON (:852) rather than literals repeated in the test. The assertion is on values, never on stdout. ON WHO WRITES THE REASON: the signed-off _design.md decides this and decides it the way the code does. Its capability table (_design.md:280) assigns READS_THROUGH_BATCH's reason to THE TESTKIT - 'because \"this batch exposes no read path\" is the same sentence for every store that says it - the NO_CEILING_REASON precedent applied to its second case' - and note 1 (:306-310) states it as the family's one deliberate instance: 'It is the one place the projection family adds a testkit-written reason, and it is the same argument NO_CEILING_REASON already won.' NO_BATCH_READ_PATH_REASON at contract.rs:838-856 IS that decision executed. The AC's phrase 'the fixture's own stated reason' was inherited from the two fixture-declared capabilities and predates the port landing READS_THROUGH_BATCH as a probe-level `bool` (crates/happenstance-core/src/projection.rs:536); the AC's operative claims - a non-empty reason, the capability naming the constant an author can change, two skips rather than one or silence, asserted by value - are all met, and each is independently enforced (Capability::declined's const fn assert! fires at codegen on an empty reason). Chasing the phrase in code would mint a projection-local reason-writer, which is exactly what DT-3's single stated exception exists to avoid. ROUTED, and deliberately not left inside this row: whether ProjectionProbe::READS_THROUGH_BATCH should itself carry a reason (an associated const pair, or a Capability-shaped value) is a PORT-SHAPE question, and it is filed as a named open item ROUTED TO `unstable-projection-gate-and-clause-disposition` (HS-S0016) in the project's slice register, `_slices.md` section 'Run 6's record', rather than as a shortfall here - the port boundary this story was given forbids answering it (spec.md:427-430), and a routed question left inside a DoD row is a question nobody owns."
  mount_point: "crates/happenstance-testkit/src/registry.rs — the gated rules in `for_each_projection_store_rule!`; the gate branch lives in the rule body, never in an emitter"
  verifying_test: "crates/happenstance-testkit/tests/mutation_coverage.rs — the new value assertion beside ::capability_skips_are_reported, asserting both fields of `RuleOutcome::Skipped` for both gated rules against NoBatchReadFixture"
- id: AC-007
  criterion: "GIVEN an adapter author who invokes exactly one macro and expects the whole bar, and GIVEN P3, who runs that same bar on `wasm32-unknown-unknown`, WHEN the gate runs, THEN all three new rules are mounted at all four composition points — the projection rules module, the single enumeration `for_each_projection_store_rule!`, the three harnesses (tokio / blocking / `#![cfg(target_arch = \"wasm32\")]`), and the mutant registry's `for_each_mutant!` + `REGISTRY` — so each rule is emitted as a named test on every target in one run, with no wasm-specific subset and no new gate step. A rule present in the module but absent from the enumeration is caught by the orphan meta-test."
  satisfied: true
  evidence: "All three names are in the single enumeration, crates/happenstance-testkit/src/projection.rs:1878-1880, so the tokio, blocking and wasm32 harnesses each gained three tests with no per-harness list: projection_conformance and projection_conformance_blocking each report 17 passed, and cargo check -p happenstance-testkit --tests --target wasm32-unknown-unknown type-checks projection_conformance_wasm.rs. The orphan direction is no_orphan_projection_rules. The fourth mount point is the mutant registry: three mutant rows plus one conformant-variant row, and four entries in for_each_projection_mutant! (coverage.rs)."
  mount_point: "crates/happenstance-testkit/src/registry.rs — `for_each_projection_store_rule!`, driving all three harnesses through the existing emitters"
  verifying_test: "crates/happenstance-testkit/src/registry.rs::no_orphan_rules (projection sibling) + `cargo xtask ci`'s mandatory wasm32 conformance-harness step (xtask/src/main.rs:231-240)"
- id: AC-008
  criterion: "GIVEN the repository owner reviewing this PR against a project whose whole point is that the port is not yet frozen, WHEN they read the diff, THEN nothing outside this story's boundary moved: git diff over spec/SPECIFICATION.md touches nothing outside the BEGIN/END GENERATED region of §7.1–§7.2 — no clause text, maturity marker or rule citation changed (PS-13 and PS-14 are [FROZEN] and are implemented, not amended); no `ProjectionStore`, `ProjectionProbe`, `Checkpoint`, `Authority` or `MemoryProjectionStore` definition changed; `Capability`, `RuleOutcome`, the three emitters and the `Declared` shape are byte-identical; no assertion anywhere compares a position or a checkpoint's `through` to a literal; cargo xtask spec-trace is green (its generated region regenerated, never left stale); and the two other gate-compelled exits — a CHANGELOG.md entry per new rule (CF-29) and line-number re-pointing in standards/rust/*.md (lint-constitution) — are taken and disclosed in the implementation report, never taken silently. (CRITERION TEXT RE-SYNCED, not re-worded by the implementer: spec.md AC-008 was amended on 2026-08-15 for the reason recorded immediately beneath it there — the pre-amendment sentence 'cargo xtask spec-trace is green and unaffected' is unmeetable by a story that adds a rule name, and this story took three compelled exits at 5be22ab under a criterion that could not describe them.)"
  satisfied: true
  evidence: "Nothing outside the boundary moved, and the three compelled exits are now disclosed rather than silent. SPEC: git diff over spec/ is the GENERATED section 7.1-7.2 region only (4 insertions, 4 deletions, every one a dagger removed by cargo xtask spec-trace --write for PS-12, PS-13, PS-14 and PS-24) - no clause text, no maturity marker, no rule citation. The region is machine-authored and a separate gate step fails on a stale one, so regenerating it is what the gate demands rather than a widening. CHANGELOG: 49 lines, CF-29-compelled - the lint fails unless every rule in RULE_FILES has an entry. STANDARDS: line-number re-pointing only in standards/rust/{11,13,40,41}-*.md, compelled by cargo xtask lint-constitution when a cited line in crates/happenstance-testkit/** moves; no rule text, evidence selection, retirement or new atom. All three are recorded in implementation-report.md under 'Compelled exits from the PR boundary', and the pattern is settled once at project level in _slices.md, 'Run 6's record'. PORT AND MACHINERY: no ProjectionStore, ProjectionProbe, Checkpoint, Authority or MemoryProjectionStore definition changed; happenstance-core is untouched by this story. Capability, RuleOutcome and the three emitters are unchanged - the two new consts sit beside NO_STORE_LIMITS / NO_CEILING_REASON and add no new skip type or line shape. cargo xtask spec-trace is green, no assertion compares a position or a checkpoint's through value to a literal, and the event-store suite including GappedPositionFixture is still green."
  mount_point: "the PR-boundary block — crates/happenstance-testkit/src/**, crates/happenstance-testkit/tests/**, CHANGELOG.md, spec/SPECIFICATION.md's generated region, standards/rust/*.md line numbers, and this story's own .bklg folder"
  verifying_test: "cargo xtask spec-trace (mandatory gate step, xtask/src/main.rs) + the pre-existing event-store suite and GappedPositionFixture rules still green in `cargo test --workspace --all-features`"
```
