---
item: "HS-S0047"
stage: implement
created: "2026-08-18"
updated: "2026-08-18"
---

# Reconciliation — phase 8, `RUNBOOK.md:3810-3820` discharged once, by hand

The standing criterion has three bullets and they are not interchangeable. This
document carries all three: the clause-by-clause verdict table (bullet 1), the two
enumerated clause-range sets and their symmetric difference (bullet 2), and the
citation figures before and after (bullet 3).

**Baseline sha: `53a4764`** — the checkpoint that sealed the previous slice and
the merge-base this slice was implemented from. Every "before" figure below is
read from that tree.

## Citation figures

`cargo xtask spec-trace` emits `N citations checked (M anchored to their subject,
K external)` at `xtask/src/spec_trace.rs:995-1007`. Both numbers are compared,
because they catch different remedies: deleting a citation to silence a failure
lowers `checked`, and re-pointing one at a line whose subject no longer matches
lowers `anchored`.

| Figure | Before (`53a4764`) | After | Verdict |
| --- | --- | --- | --- |
| clauses | 201 (137 FROZEN, 47 PROVISIONAL, 12 DEFERRED, 5 NON-NORMATIVE) | 201 (137 / 47 / 12 / 5) | **unchanged** — no maturity marker moved, so §1.3's hand census needed no edit |
| conformance rules | 112 | 112 | unchanged |
| e2e cases | 58 | 58 | unchanged |
| citations `checked` | **389** | **401** | **+12**, above baseline |
| citations `anchored` | **76** | **80** | **+4**, above baseline |
| external | 12 | 12 | unchanged |
| §7.1–§7.2 | matches the checker | matches the checker | unchanged; regenerated by nothing, hand-edited by nothing |

**`anchored` fell to 75 mid-pass, and the drop was diagnosed rather than
absorbed.** Re-pointing §1.6's `SqliteProjectionStore` citation from
`projection_store.rs:235-260` to `:529` broke its derived subject: the sentence
had read "in `begin` and `rollback` (`…:235-260`)" and `subject_before`
(`xtask/src/spec_trace.rs:2158`) takes the code span immediately before the
citation, so replacing it with "in **all four** (`…:529`)" left no derivable
subject. That is EC-004 exactly — `checked` held and `anchored` fell — and the
remedy was to re-anchor properly rather than to widen the window: the sentence
now reads "in **all four** since phase 8, `begin` through `rollback`
(`…:529-679`)", which restores `rollback` as the subject and puts it inside the
twelve-line window of the cited range. The diagnosis used a temporary `eprintln!`
in `spec_trace.rs` and a script that swapped the base blob in and out; **both were
reverted, and `git diff` on `xtask/src/spec_trace.rs` is empty.**

## Clause ranges (bullet 2 of the criterion)

The phase's stated range is **three disagreeing statements** in the tree, and
ADR-0022's own queue row carries no parenthesised range at all — alone among the
queue's rows. Both sets are enumerated by ID rather than described.

**Set A — the phase's stated clause range**, the union of its three statements:

| Source | States |
| --- | --- |
| `RUNBOOK.md:4229-4230` (the phase body) | CF-14, CF-17 |
| `RUNBOOK.md:605-606` (the clause-ownership table, two rows) | CF-17, CF-34; and ES-35 in the portfolio row |
| `.bklg/from-contract-to-published-library/sqlite-durable-store/_intake-brief.md:76-92` | ES-35, CF-14, CF-17, CF-34, VT-21, VT-22, VT-23, VT-24 |

**A = { CF-14, CF-17, CF-34, ES-35, VT-21, VT-22, VT-23, VT-24 }** — eight
clauses. Reconciled into one set rather than averaged: the intake brief is a
superset of both runbook statements, and no statement names a clause the others
contradict.

**Set B — the union of the phase's ADRs' ranges.** Phase 8 has exactly one ADR,
ADR-0022, landed as `.kb/decisions/0022-append-condition-strategy.md`.

**B = { }** — and *that is the finding*, not an omission in this table. The atom
states no clause range in any form: its frontmatter has no clause field, its
`## Decision` names no clause ID, and `RUNBOOK.md:301`'s queue row for 0022 is the
only row in the queue with no parenthesised range. Two clause IDs appear in the
atom, and both as **non-verdicts with named owners** rather than as discharges —
ES-17 (`&[Event]` not lifted, `kb-open-question-es-17-two-adapter-measurement-001`)
and CF-40 (clause home still open, `kb-open-question-cf-40-ownership-001`).

**B′ = { ES-17, CF-40 }** — the clauses ADR-0022 *touches* without discharging,
recorded separately so the symmetric difference is honest in both directions.

**Symmetric difference A △ B′ = { CF-14, CF-17, CF-34, ES-35, VT-21, VT-22,
VT-23, VT-24, ES-17, CF-40 }** — every element, with a disposition:

| Clause | Side | Disposition |
| --- | --- | --- |
| CF-14 | A only | **already discharged elsewhere in phase 8** — by `reopen-negative-control-and-durability-verdicts`, which confirmed the deferral and narrowed it to the two implementations that have not answered. Read back here; unchanged |
| CF-17 | A only | **already discharged elsewhere in phase 8** — same story, rule shape confirmed against the first real file-backed adapter. Read back here; unchanged |
| CF-34 | A only | **discharged here, as prose** — the harness now exists and is separately gated; see the verdict table |
| ES-35 | A only | **already discharged elsewhere in phase 8** — falsifier restated to the *fault* far end. Read back here; unchanged |
| VT-21 | A only | **discharged here, as prose** — the documentation half; marker unmoved |
| VT-22 | A only | **discharged here, as prose** — as VT-21 |
| VT-23 | A only | **discharged here, as prose** — evaluated rather than refused, through chunking; marker unmoved |
| VT-24 | A only | **discharged here, as prose** — as VT-21 |
| ES-17 | B′ only | **owed by nobody, and escalated** — ADR-0012's falsifier wants two builds of the *same* adapter differing only in `append`'s ownership. `RUNBOOK.md:302` already records this as escalated by ADR-0022 §13 and *"owned by nobody yet"*. This pass adds nothing to it and does not settle it |
| CF-40 | B′ only | **ownership open by decision; falsifier repaired here.** Which document owns a fixture-constant clause is `kb-open-question-cf-40-ownership-001`'s and is not this pass's to close. What *is* this pass's is the clause's own `[PROVISIONAL]` text, which named the rusqlite adapter as an instrument that had not stated a ceiling — false on this tree. Repaired; see the verdict table. Dispositioning a clause on ownership without reading its marker is how the first pass missed it |

**The finding, stated plainly.** The phase-4 precedent is that queue rows named
35 clauses and the body discharged 64, and the 29 missing were invisible in
exactly the way a finished clause is invisible (`RUNBOOK.md:315-322`). Phase 8's
exposure is the same shape from the other side: **the ADR that settles the phase
states no clause range at all**, so the arithmetic has nothing to close against
and set B had to be derived from the phase's statements rather than from the
decision record. No range was synthesised to make it close. This is escalated as
an ADR-queue item rather than fixed here, because giving ADR-0022 a clause range
after the fact is writing into an accepted, immutable decision atom.

## Clause-by-clause verdicts (bullet 1 of the criterion)

Every clause in set A, plus every clause that **names this crate anywhere in its
own text — its prose, its `Rejects:`, or its maturity marker — by path or by
name.** Verdict is one of **unchanged**, **repaired** (prose or citation
corrected, the set of implementations the clause admits identical) or **gap**
(escalated, nothing normative changed). No row reads "looks fine".

**The inclusion rule was widened after it let one through.** Its first spelling
was "every clause whose *prose* cites `crates/happenstance-sqlite`" — a
path-shaped test, run with a path-shaped search. CF-40 names the adapter as *"the
rusqlite adapter at phase 8"* inside its `[PROVISIONAL]` marker and cites no path
at all, so it matched nothing, and a clause already sitting in the
symmetric-difference table above was dispositioned on **clause ownership** without
its marker text ever being read. The rule now reads on the identity rather than on
the spelling, and the marker is named explicitly because that is the field the
narrow rule could not see: `happenstance-sqlite`, `crates/happenstance-sqlite/…`,
"the rusqlite adapter" and "phase 8" are all in scope.

The discriminator is `.kb/playbooks/repairing-a-frozen-clause-without-amending-it.md`'s,
applied rather than improvised: *a correction to a FROZEN clause is a repair if
the set of implementations the clause admits is unchanged, and otherwise it is a
gap, which is an ADR's.*

### Set A

| Clause | Marker | Spec line read | Read against | Verdict |
| --- | --- | --- | --- | --- |
| CF-14 | DEFERRED | `spec/SPECIFICATION.md:7874-7922` | `crates/happenstance-sqlite/tests/support/mod.rs:220-260` (the reopen), `tests/conformance.rs` (3 reopen rules green) | **unchanged** — the verdict was written by the predecessor story; the deferral is confirmed and narrowed to `happenstance-cloudflare` and `happenstance-neon`, and its falsifier ("the first of those two that cannot express it with this shape") is live and 55 characters |
| CF-17 | PROVISIONAL | `spec/SPECIFICATION.md:7990-8012` | same | **unchanged** — rule shape confirmed against a real file-backed adapter; `SqliteFixture` named as the second fixture supplying `REOPEN` and the first over a medium outside the process |
| CF-34 | PROVISIONAL | `spec/SPECIFICATION.md:8743-8767` (`:8707-8745` before CF-40's repair inserted eleven lines above it) | `crates/happenstance-testkit/src/bench.rs`, `Cargo.toml:92`, `src/lib.rs:146` | **repaired** — the clause's separation was a plan and is now a fact. A paragraph records that `event_store_benchmarks!` exists behind an off-by-default `bench` feature, is not in `for_each_event_store_rule!`, and was ADR-0022's measurement instrument. MUST verbatim, marker unmoved, falsifier untouched |
| ES-35 | PROVISIONAL | `spec/SPECIFICATION.md:4213`, `:4231-4245` | `crates/happenstance-sqlite/tests/support/mod.rs` | **unchanged** — falsifier restated by the predecessor to the *fault* far end; `SqliteFixture` named as the reopen far end and as declining `MID_BATCH_FAULT` by scope |
| VT-21 | PROVISIONAL | `spec/SPECIFICATION.md:1512-1540` | `crates/happenstance-sqlite/src/event_store.rs:245`, `:482`; `tests/append.rs:238-251` | **repaired** — the documentation MUST is discharged and said so as a discharge, in a shared paragraph after VT-24. MUST verbatim, marker unmoved |
| VT-22 | PROVISIONAL | `spec/SPECIFICATION.md:1542-1562` | `event_store.rs:252`, `:488`; `tests/append.rs:258-267` | **repaired** — as VT-21 |
| VT-23 | PROVISIONAL | `spec/SPECIFICATION.md:1564-1583` | `crates/happenstance-sqlite/src/query_sql.rs` (chunking), `tests/wide_query.rs` (18 tests green) | **repaired** — as VT-21. The 128-item floor is *evaluated*, through chunked statements, rather than refused |
| VT-24 | PROVISIONAL | `spec/SPECIFICATION.md:1585-1608` | `event_store.rs:261`, `:475`; `tests/append.rs:273-286` | **repaired** — as VT-21 |

### Clauses naming this crate — by path, or by name

| Spec line (after) | Clause / section | Verdict | What was done |
| --- | --- | --- | --- |
| `:232-249` | §1.3 portfolio qualification | **repaired, twice** | First: "exactly one of them — durability" → "exactly two of them — durability and handle multiplicity", each tick's partiality stated. Then, in the review fix: → **three**, because batch shape's far end is one too, and the sentence that read "the remaining three — transport, batch shape and completeness — have nothing at either end" was **false when this pass wrote it**. It now names the two axes that are empty at both ends and states batch shape's asymmetry and PS-2's non-clearance |
| `:390` | §1.6 `EventStore` row | **repaired, twice** | First: "phase 8 put one there, at durability's, and nowhere else" → two, both through `SqliteFixture`. Then: the same "three … empty at both ends" sentence corrected to two, with batch shape recorded as `ProjectionStore`'s axis and one-sided |
| `:391` | §1.6 `ProjectionStore` row | **repaired** | "five skeleton impls" → four still skeletons; `SqliteProjectionStore` real in all four methods and running the suite; citation re-anchored `:235-260` → `:529-679`. This row is the one the §6.5 batch-shape row below **contradicted for a whole review cycle** |
| `:472` | §1.7 honest caveat | **repaired, twice** | First: "one adapter instrument at one far end" → two, with each one's residual named. Then: → three, and "the other three — transport, batch shape and completeness — are empty at both ends" → the other two, with batch shape's one-sidedness stated |
| `:2599` | ES-4's refuted second leg | **repaired, citation only** | `event_store.rs:202` → `:959`. The sentence is a quotation of a claim it calls false and is left word-for-word; only the pointer moved, to `read`'s `+ Send` return in the real impl |
| `:2690` | ES-6 / error `Send + Sync` | **repaired** | `:151-193` → `:857-950` (the line range now holds the `SqliteEventStore` struct, not the error enum), and "seven real variants" → **twelve**, counted from the enum |
| `:2997` | ES-11 / D7's read path | **repaired** | The parenthesis explained conformance by the behaviour of a `todo!()` body. Now: `read` is not `async` and may be called with no runtime, so the work and the ceiling move into `poll_next` (`event_store.rs:973`), and ADR-0022 §9's captured `Handle` is named as what settled the seam. The obligation (ceiling captured no later than the first poll) is ADR-0011's and is untouched |
| `:3382` | ES-17 `Rejects:` | **repaired** | `:47-53` pointed at `CREATE TABLE event`; the sentence is about the tag side table, which is `:62-67`. Re-anchored, and the shape recorded as landed rather than hypothetical |
| `:3901` | the append-condition clause's `Rejects:` | **repaired** | Same re-anchor; "the **planned** SQLite schema" → "the **landed** SQLite schema", with ADR-0022's 200x two-tag measurement as why `tag_cardinality` is a requirement |
| `:4022` | ES-30 (`head` required) | **repaired** | `event_store.rs:195` → `:952` (the `impl SendEventStore` head; `:195` is now `SCHEMA_VERSION`), and "seven impls today, **five** of them skeletons" → four, naming `happenstance-sqlite` as the one that stopped being one |
| `:4466` | ES-41 `[PROVISIONAL]` marker | **repaired** | The falsifier named `happenstance-sqlite` at phase 8 as its instrument, and the instrument has landed — a falsifier naming an event that has already happened is CF-38's failure mode in slow motion. Restated **inside the same level**: the in-process, connection-holding half is answered (the `UNIQUE (origin_store, origin_position)` pair, `event_store.rs:57`), the **transport** half is still owed by `happenstance-cloudflare` and `happenstance-neon`, and completeness is untouched by phase 8 |
| `:4665` | §7's "five implementers and not one adapter" | **repaired** | "and not one adapter" → "and, since phase 8, one adapter"; "All five are phase-2 skeletons, and none has run a suite because none exists" → four still are, and the fifth passes `projection_store_conformance!` against a real file. PS-2 restated as what a green run there does *not* buy |
| `:4690` | PS-5 / the `E0195` transcript | **repaired, citation only** | `projection_store.rs:215-224` → `:534-542` |
| `:7621` | CF-4 `Rejects:` | **repaired, citation only** | `:47-53` → `:62-67` |
| `:7688` | the tag-join gap paragraph | **repaired** | `:47-53` → `:62-67`; "the **planned** SQLite adapter" → "the **landed**" one |
| `:8067-8072` | CF-39's `[PROVISIONAL]` marker | **unchanged, and that is the verdict** | Caught by the inclusion rule this pass widened to *"the crate's identity — path or name, prose, `Rejects:` or maturity marker"*, and initially missed because the widened rule was not re-run to exhaustion. It is the **only** sentence left in the specification naming phase 8 in the future tense: *"The instruments are the rusqlite adapter at phase 8 and the Postgres adapter at phase 10, and **no adapter has armed a fault yet**."* Unlike ES-41 at `:4457` and CF-40 at `:8093`, that sentence is **still true on this tree**, so repairing it would make the specification say something false. `SqliteFixture` declines `MID_BATCH_FAULT` by **scope, not incapacity** (`crates/happenstance-sqlite/tests/support/mod.rs:173-183`): the injection CF-39 names is available here — an `AFTER INSERT` trigger through a second connection, exercised unabsorbed by `crates/happenstance-sqlite/tests/append.rs:856` `a_failure_mid_batch_leaves_nothing` — and is deliberately **not armed from the fixture**, because ES-35's marker belongs to `reopen-negative-control-and-durability-verdicts` and arming it here would retire that clause's falsifier from a story with no say in it. So the falsifier is live, its instrument is built and unused, and the row exists because a table claiming exhaustive coverage must show the clause it did *not* change as well as the ones it did |
| `:8108-8125` | CF-40's `[PROVISIONAL]` marker | **repaired** | The falsifier ended *"The instruments are the rusqlite adapter at phase 8, the Cloudflare adapter at phase 9 and the Postgres adapter at phase 10; **no adapter has stated a ceiling yet**"* — false on this tree, and the identical defect this table repaired for ES-41 at `:4457`: a marker waiting for an event that has already happened. Restated **inside the same level**, ES-41's way: the **constant-ceiling** half is answered by `happenstance-sqlite`, which states all three ceilings (`crates/happenstance-sqlite/tests/support/mod.rs:189-195`) mirrored from the adapter's own constants (`crates/happenstance-sqlite/src/event_store.rs:245`, `:252`, `:261`) — the same three this pass discharged VT-21, VT-22 and VT-24 against — with `append_reports_exceeded_store_limits` running rather than skipping. What stays live is the half that was always the point: a store whose ceiling is **not** a constant, the Postgres TOAST-threshold and KV per-value-cap cases, instruments at phases 9 and 10. MUST verbatim, marker unmoved, and clause ownership still `kb-open-question-cf-40-ownership-001`'s |
| `:5255-5266` | §4.5, the foreign-batch hole | **repaired** | Three drifts in six lines, all found by the mechanical sweep below rather than by eye. The citation `projection.rs:126-131` pointed into a module doc-comment and named a spelling — `batch: Self::Batch<'_>` — that ADR-0017 deleted; re-anchored to `crates/happenstance-core/src/projection.rs:474-480`, which is `commit`'s real signature, and the explanation re-stated without the lifetime (the hole is unchanged by its removal, which is the point). The sample `a.begin().await?` predates PS-6 making `begin` neither `async` nor fallible. And the sentence said a runner "can write one depot's inventory under another depot's checkpoint" — since phase 8 an adapter outside the testkit refuses exactly that at run time, so it now says the *program* type-checks and names PS-15 and the stamp (`crates/happenstance-sqlite/src/projection_store.rs:552`) as what stops it running. PS-15's own MUST is untouched, and its `Rejects:` was read and deliberately left: "every adapter that can be written today, all of which corrupt silently" was already false before phase 8, because `MemoryProjectionStore` stamps too, and it does not name this crate |
| `:8432` | §6.5's opening claim | **repaired** | *"It has been executed on four axes with a `fixture` and **on none with an adapter**"* — false since phase 8 put two adapter instruments in the table below it, and false in the same section that had just been repaired to say so. Now: four axes with a fixture and three far ends with an adapter, with the fact that all three arrived in one crate promoted from footnote to qualification. Not a grep hit — no crate name, no "phase 8" — and caught only by reading §6.5 whole, which is what the three consecutive misses in this section argued for |
| `:8537` | §6.5 **Transport** row | **unchanged** | Names rusqlite as a *near*-end implementation ("in-process, a handle held across awaits"), which is what `happenstance-sqlite` is. The far end is still `happenstance-neon`, still a phase-2 skeleton. Nothing phase 8 built touches this row |
| `:8539` | §6.5 **Batch shape** row | **repaired** | **The row this review found unrepaired, and the reason it matters is that it was false in three separate statements while the rest of the same pass said the opposite.** It read `SqliteProjectionStore` "in `begin` and `rollback`", "still there is no suite to run any of them against", and needed "the projection conformance suite, which does not exist" — each contradicted by this pass's own repairs at `:391` and `:4665`, by `crates/happenstance-sqlite/src/projection_store.rs:552`, `:566`, `:592`, `:673`, and by `crates/happenstance-sqlite/tests/projection.rs`. Now: real in **all four**, `begin` through `rollback`; `projection_store_conformance!` mounted against a real temporary file and passing; and what is empty named precisely — the **live-transaction near end**, which no rusqlite adapter can take on the `Send` flavour because `rusqlite::Transaction<'_>` is itself `!Send` (`crates/happenstance-sqlite/src/projection_store.rs:19-43`). The instrument column now asks for the near-end adapter instead of the suite that landed |
| `:8541` | §6.5 handle-multiplicity row | **repaired** | "Fixture yes, adapter no … no *connection* has ever been opened twice" → "Fixture yes, adapter yes — pooling still empty", citing `SqliteFixture::connect` and `connect_many`. See the verdict below |
| `:8542` | §6.5 **Durability** row | **unchanged** | Written by `reopen-negative-control-and-durability-verdicts` inside this phase and true on this tree: `SqliteFixture` supplies `REOPEN` over a real file, `RestampingFixture` is the second failing control, and nothing yet loses a write to a *fault*. Read back, not re-decided |
| `:8545-8565`, `:8586-8601` | §6.5 summary and the CF-16/CF-17/CF-13 paragraph | **repaired, twice** | First: one tick → two; "the three rows … moved are weaker still" → two of the three have gained a second tick, each a *different* claim rather than a stronger one. Then, in the review fix: two ticks → **three**, "three axes are empty at both ends" → two, and batch shape's tick described as the *one-sided* one — a pass at the end that was never the doubtful end, with the near end empty and PS-2 wanting both |

### The widened rule, run mechanically rather than by eye

**Three consecutive review cycles found a hit this table had missed — CF-40,
then CF-39, then §6.5's batch-shape row — and all three were found by a reader.**
That is a defect in the *method* and not in any one row: a rule stated as *"the
crate's identity — path or name, prose, `Rejects:` or maturity marker"* was still
being executed by eye, and widening the rule's wording did nothing about how it
was run. It is run here as one literal search, and **every hit is dispositioned,
including the ones nothing was done to** — CF-39's row already argued that a
table claiming exhaustive coverage must show the clauses it did *not* change, and
this is that principle applied to the whole sweep instead of to one row.

```console
$ grep -nE "happenstance-sqlite|rusqlite|Sqlite[A-Za-z]*|phase 8" spec/SPECIFICATION.md | wc -l
89
```

One alternation covering every spelling the rule names: the crate name,
`crates/happenstance-sqlite/…` paths, "the rusqlite adapter", every `Sqlite*`
type name, and "phase 8". **89 hit lines. 56 sit inside a passage this pass
changed** and carry a row above. **33 do not**, and all 33 are accounted for:

- **14** are further lines of a passage already rowed — `:2686` (ES-6), `:2993`
  (ES-11), `:4664` (§7), `:8071` (CF-39, whose verdict is *unchanged and that is
  the verdict*), `:8537` and `:8542` (§6.5's Transport and Durability rows), and
  the ES-35 group `:4213`, `:4228`, `:4240`, `:4241` plus the CF-17 group
  `:7998`, `:7999`, `:8009`, `:8010`.
- **7** are the CF-14 clause — `:7878`, `:7880`, `:7891`, `:7900`, `:7905`,
  `:7911`, `:7913` — whose verdict is in set A above: **unchanged**, the
  deferral confirmed and narrowed by the predecessor story.
- **12** were in no table at all before this fix, and are rowed here.

| Spec line | What names the crate | Verdict | Why it needed no change |
| --- | --- | --- | --- |
| `:281` | §1.3's marker census — "for ES-35 it is narrower since phase 8 built the adapter" | **unchanged** | Written by `reopen-negative-control-and-durability-verdicts` and true: ES-35's falsifier *is* now a fixture that arms a real fault against a real medium, which is what the repaired marker at `:4213` says. Read back, not re-decided |
| `:482` | §1.7's population sentence — "`MemoryEventStore`, a rusqlite adapter and a planned Durable Object all serialise their writers" | **unchanged** | Phase 8 made it *more* true, not less. `happenstance-sqlite` serialises every writer behind one connection mutex and takes the position inside the transaction, which is the shape the sentence warns the ports are overfitted to. Repairing it would blunt the warning the whole paragraph exists to carry |
| `:1031` | §2's coherence argument — "`impl IngestStore for SqliteEventStore` has a local type in the trait reference" | **unchanged** | A statement about the orphan rule, not about this adapter. It is true, no such impl exists, and phase 8 did not add one — `happenstance-sync` is untouched |
| `:3659` | ES-23's `Rejects:` — "a pooled rusqlite adapter that does its work in `spawn_blocking` and presents itself as cancellation-safe" | **unchanged**, and checked rather than assumed | The landed adapter *does* hop through `spawn_blocking`, so this is the `Rejects:` most likely to have been falsified by phase 8. It is not: the adapter is not pooled, and it does not present itself as cancellation-safe — it documents a cancelled commit as **genuinely ambiguous** (`crates/happenstance-sqlite/src/projection_store.rs:458-460`, `crates/happenstance-sqlite/src/event_store.rs:872`). The shape rejected stays hypothetical |
| `:4289` | ES-11/ES-12's rule prose — "a rusqlite adapter yielding rows from a live statement … holding the handle for as long as the caller holds the stream" | **unchanged**, and this is the closest call in the sweep | A real rusqlite adapter now exists and could have made this sentence describe it. It does not: all three rusqlite handles are `!Send`, so none can cross the `poll_next` boundary at all (`crates/happenstance-sqlite/src/event_store.rs:1126`), and the cursor makes a round trip per hop instead of holding a statement open. `BorrowHoldingStore` remains the only thing in the tree with that shape |
| `:4869`, `:4873` | PS-2's **Rule** — "one adapter holding a live transaction (rusqlite or `sqlx`)" — and its **Rejects:** | **unchanged, with a finding recorded** | Phase 8 established that a rusqlite adapter cannot hold a live transaction on the **`Send`** flavour at all (`crates/happenstance-sqlite/src/projection_store.rs:19-43`), so of PS-2's two named candidates only `sqlx` can supply that end for a `Send` store. The parenthetical is still not *false* — a bare-flavour rusqlite adapter could — but whether PS-2's bar may be met that way decides what the clause admits, and PS-2 is `[FROZEN]`. Escalated below rather than edited here; the finding itself is already in the specification at `:4682` and in `projection-store-passes-the-borrowed-suite/_ledger.md:98` |
| `:4961` | PS-5's prose — "`type Batch<'a> = rusqlite::Transaction<'a>` compiles on the bare flavour and fails on `SendProjectionStore` twice over" | **unchanged** | Nothing in it drifted; phase 8 *confirmed* it from the adapter's side, and both reasons are recorded against this crate at `crates/happenstance-sqlite/src/projection_store.rs:19-43`. Adding that citation would be new evidence rather than repaired drift, and this pass repairs drift |
| `:4986` | PS-4's `Rejects:` — a suite rule that "would certify rusqlite and fail a Durable Object for being correct" | **unchanged** | It rejects a rule nobody wrote, and phase 8 did not write it: none of `projection_store_conformance!`'s 17 rules asserts a transaction is open between `begin` and `commit`, and none could — `SqliteBatch` is a buffer |
| `:5357` | the `reset` clause's `Rejects:` — "`SqliteProjectionStore::reset()` that truncates the checkpoint table" | **unchanged**, and now checked against a real body | It names this type, and the body it names now exists: `reset` is scoped to one projection id (`crates/happenstance-sqlite/src/projection_store.rs:645`, `reset_locked` at `:859`) and `reset_is_scoped_to_one_projection` is green against `SqliteProjectionFixture`. The rejected implementation is one this adapter is not |
| `:5990` | ADR-0008's two-ports-one-scheme paragraph — "Norvant's rusqlite and Ladybug batches sit beside Postgres ones" | **unchanged** | Scenario prose about why both ports derive their flavours together. Phase 8 touched neither derivation |
| `:8458` | CF-25's `Rejects:` — "freezing `EventStore` against `MemoryEventStore`, a `RefCell` store, rusqlite and a Durable Object" | **unchanged**, and sharper than when it was written | The rusqlite adapter it names hypothetically is now real, and it serialises its writers under one connection mutex exactly as the clause predicted. That is why all three adapter ticks in §6.5 are in one crate, and saying so is the clause's own point |

### The routed item, settled

`reopen-negative-control-and-durability-verdicts` moved four §6.5 statements into
agreement and **routed one to this story rather than moving it in passing**: the
handle-multiplicity row still read *"no connection has ever been opened twice"*
while `SqliteFixture::connect` had already falsified it.

**Verdict: the axis has an adapter instrument at its far end, and the row is
repaired to say so.** The far end this table names is *"two or more handles onto
one backing store, concurrent"*. `SqliteFixture::connect` opens another
`rusqlite::Connection` through `SqliteEventStore::open`, configures it and runs
migration 1 (`crates/happenstance-sqlite/tests/support/mod.rs:208`) — not a
refcount bump — and `connect_many` opens up to 64 of them for the concurrency
family (`crates/happenstance-testkit/src/concurrency.rs:1015`), which is green.

**The narrower reading was considered and rejected with its reason**: that only a
*pool-backed* adapter should count. It renames the far end after the fact.
Pooling is a property of how handles are acquired, not of how many exist onto one
store, so it is recorded as **what is still missing** — alongside cross-*process*
handles — rather than as what the row always meant. CF-16 and CF-19 are `[FROZEN]`
and are **unchanged**: this is an edit to §6.5's evidence, not to either clause.

## One file outside the PR boundary, and the reason

`standards/rust/01-standard-of-evidence.md:128` cites
`spec/SPECIFICATION.md:5926 (silently ignores)`, and this pass's insertions moved
that sentence to `:5984`. `cargo xtask lints` — a `REQUIRED` gate step and half of
the `reachability_static` command — went red on it:

```
standards/rust/01-standard-of-evidence.md:128 — `spec/SPECIFICATION.md:5926` no
longer has `silently ignores` within 10 lines; the citation points at the wrong
place
```

**One digit changed: `5926` → `5984`, and the review fix moved it once more to
`:5993`.** The second move is recorded here rather than folded into the first,
because the two are different facts: the first was forced (the lint was red), the
second was **pre-emptive** — the §4.5 and §1.3 repairs put the anchor 9 lines from
its stated line, and `ANCHOR_SLACK` is 10 (`xtask/src/lint_constitution.rs:111`),
so the gate was green with one line of margin. Leaving it green-by-a-line is how
the next pass inherits a red `REQUIRED` step it did not cause. No rule text, no evidence claim, no atom
body. It is the same class of repair the slice's first story's AC-004 names —
repairing what this PR's own edits moved is part of "the gate is green", not scope
drift — and the alternative was to leave a `REQUIRED` step red through an edit
this pass caused. The constitution's own citation lint uses a ten-line window
(`xtask/src/lint_constitution.rs:111`), narrower than `spec-trace`'s twelve, which
is why it caught a shift `spec-trace` tolerated.

## Nothing `[FROZEN]` was amended

`git diff 53a4764 -- spec/SPECIFICATION.md` carries **202 insertions and 85
deletions**, and the two counts are kept apart because they are two different
questions: **zero** deleted lines contain the normative token `MUST`, and **two**
contain a lower-case "must". Both of the two were read. The first is the §1.6
`ProjectionStore` table row, whose PS-2 column ("a hostile store that commits the
checkpoint and drops the read-model write must *fail* the suite, and two adapters
at opposite ends of the batch-shape axis must pass it") is reproduced word-for-word
in the replacement; the second is a prose sentence reading "its work **must** move
into `poll_next`" — a description of an adapter's constraint, not a normative
clause. **No clause's MUST text changed, no maturity marker moved, no clause ID
was added or retired, and no §7.1/§7.2 generated row was hand-edited.**

**These figures were re-derived from the merged tree on 2026-08-18 and the old
ones are quoted rather than overwritten.** They read *"166 insertions and 75
deletions"* and *"exactly two deleted lines contain the word 'MUST'"* before the
§6.5 batch-shape repair below landed, and *"152 insertions / 72 deletions"* in
the AC-003 ledger row before that. Every one of those was a true reading of a
tree that had since moved; the arithmetic never changed sign, and what was wrong
each time was the quotation. That is the same hand-maintained-count-drifting
failure this story exists to close, and it has now occurred three times inside
the story's own artifacts — which is the strongest argument in
`.kb/_intake/0034-what-the-phase-8-reconciliation-cost.md` for mechanising the
figure rather than transcribing it.

Every edit is a repair under the playbook's discriminator: in each case the set of
implementations the clause admits is identical before and after, because what
changed is supporting prose, a `Rejects:` example's provenance, or a `file:line`.

## Gaps escalated, not applied

Three, all recorded here and none acted on:

1. **ADR-0022 states no clause range** (AC-004's finding). Escalate to the
   runbook's ADR queue as an amendment to `RUNBOOK.md:301`'s row, or as an
   explicit decision that phase-8 clause ownership lives in the intake brief. Not
   fixed here: an accepted decision atom is immutable, and hand-writing a range
   into it would be exactly the edit `redkiln validate --kb` exists to refuse.
2. **ES-17's two-build measurement is owed by nobody** (`RUNBOOK.md:302`).
   Already escalated by ADR-0022 §13 and already recorded as unowned; this pass
   confirms it is still unowned after phase 8's implementation stories and adds
   nothing.
3. **PS-2 names rusqlite as a live-transaction candidate, and phase 8 narrowed
   which flavour that could ever mean.** `spec/SPECIFICATION.md:4869`'s Rule
   wants the suite green against "one adapter holding a live transaction
   (rusqlite or `sqlx`)", and a rusqlite store cannot be that adapter on the
   **`Send`** flavour at all — `rusqlite::Transaction<'_>` is `!Send`, so
   `commit` is rejected on the batch parameter
   (`crates/happenstance-sqlite/src/projection_store.rs:19-43`). Whether a
   bare-flavour adapter may satisfy PS-2's near end decides what a `[FROZEN]`
   clause admits, which is an ADR's and `projection-store-freeze`'s, not this
   pass's. Recorded, unedited; the underlying finding is already in the
   specification at `:4682` and in
   `projection-store-passes-the-borrowed-suite/_ledger.md:98`.

## The open question stays open

`.kb/open-questions/nothing-owns-the-post-phase-reconciliation.md` is **unedited**
in this diff. Running the criterion once by hand answers none of its five ordered
sub-questions, and this pass deliberately did not:

- **no committed citation baseline** — the figures above live in this story's
  folder, not beside the list they count, per
  `.kb/playbooks/landing-a-stricter-gate-without-a-red-baseline.md`;
- **no new gate step and no new check** in `xtask`;
- **no machine-readable clause-range format**.

What the pass cost, and the two arguments it produced for mechanising some of it,
are staged at `.kb/_intake/0034-what-the-phase-8-reconciliation-cost.md` for
`/redkiln:kb-ingest` to adjudicate on its own schedule. No atom under
`.kb/decisions/`, `.kb/concepts/` or `.kb/playbooks/` was written or edited.
