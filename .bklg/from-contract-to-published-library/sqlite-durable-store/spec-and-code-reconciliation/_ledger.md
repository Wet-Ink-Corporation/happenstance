---
item: HS-S0047
stage: implement
created: 2026-08-12
updated: 2026-08-12
---

# Acceptance ledger — The standing reconciliation criterion, discharged

The machine-checkable Definition-of-Done ledger for this story (RFC §6.5). One row per spec
`AC-###`. Planning authors every row with `satisfied: false`; the implementer may only flip a row to
`satisfied: true` and MUST cite real evidence (a `file:line` and/or the verifying test id) — never
edit, remove, or re-word a criterion, and never flip a satisfied row back. `redkiln verify --grain
story` reads the fenced block below and blocks `implement → report` unless every spec AC is present,
`satisfied: true`, and carries non-placeholder evidence (and no AC that was satisfied on the base
branch has regressed). Scope changes are a human decision recorded through `redkiln advance`, not a
quiet ledger edit.

```yaml
- id: AC-001
  criterion: "GIVEN the maintainer of phase 9 opens any clause in phase 8's computed range, WHEN they read its MUST and its supporting prose against the merged tree, THEN every one of those clauses carries an explicit verdict — unchanged, repaired, or gap — in _reconciliation.md, and a clause whose MUST is untouched but whose prose describes a superseded implementation is recorded as a defect and not as a pass. No clause in the range is absent from the table, and none carries a verdict of \"looks fine\"."
  satisfied: true
  evidence: "EVERY CLAUSE IN THE COMPUTED RANGE CARRIES A VERDICT, AND NO ROW READS 'looks fine'. The table is at .bklg/from-contract-to-published-library/sqlite-durable-store/spec-and-code-reconciliation/_reconciliation.md, section 'Clause-by-clause verdicts'. Set A is eight clauses - CF-14, CF-17, CF-34, ES-35, VT-21, VT-22, VT-23, VT-24 - and each row cites the spec/SPECIFICATION.md lines it was read at AND the crates/happenstance-sqlite line it was read against. VERDICTS: three UNCHANGED (CF-14 at :7859-7907, CF-17 at :7975-7997, ES-35 at :4204 and :4222-4236 - all three already written by reopen-negative-control-and-durability-verdicts inside this same phase, read back here rather than re-decided), five REPAIRED (CF-34 at :8707-8745; VT-21 :1503-1531, VT-22 :1533-1553, VT-23 :1555-1574, VT-24 :1576-1599 through one shared discharge paragraph after VT-24). A SECOND TABLE covers the TWENTY-FOUR further passages whose text names this crate - by path, by name, inside a Rejects: or inside a maturity marker - and which sit outside the phase's stated range: :232-249, :390, :391, :472, :2599, :2690, :2997, :3382, :3901, :4022, :4466, :4665, :4690, :5255-5266, :7621, :7688, :8067-8072, :8108-8125, :8432, :8537, :8539, :8541, :8542, and :8545-8565 with :8586-8601 - each with its own verdict, because the criterion is about the clause AND its supporting prose. A THIRD TABLE, added by the review fix, carries the eleven passages that the MECHANICAL re-run of the widened rule turned up and the pass did not change - :281, :482, :1031, :3659, :4289, :4869 with :4873, :4961, :4986, :5357, :5990, :8458 - each an explicit unchanged verdict with the reason it needed none, so the sweep shows what it did NOT change as well as what it did. THE DEFECT CLASS THE CRITERION NAMES WAS FOUND EIGHT TIMES, and each is recorded as a defect rather than a pass: 'seven real variants' where the enum now has twelve; 'seven impls today, FIVE of them skeletons' where four are; 'Two of the five are todo!() throughout ... Nothing runs against any of the five', where SqliteProjectionStore has real bodies in all four methods and passes projection_store_conformance!; and 'one adapter instrument at one far end' where phase 8 bought two. None of those four has a MUST that changed - every one is supporting prose describing a superseded implementation, which is exactly what RUNBOOK.md:3810-3814 calls a defect even when the MUST is untouched. FOUR MORE OF THE SAME DEFECT WERE FOUND BY THE REVIEW AND ARE RECORDED HERE RATHER THAN QUIETLY FIXED, because a pass that misses this class four times in its own primary section is evidence about the METHOD: (5) section 6.5's Batch shape row said SqliteProjectionStore was real in begin and rollback only, that there was still no suite to run any impl against, and that what the axis needed was the projection conformance suite, WHICH DOES NOT EXIST - three statements, all false, all contradicted by this same pass's repairs eleven and forty-two hundred lines away; (6) section 6.5's own opening still read executed on four axes with a fixture AND ON NONE WITH AN ADAPTER, two lines above a table this pass had just repaired to show two; (7) section 4.5 said a runner holding a HashMap of SqliteProjectionStore CAN write one depot's inventory under another depot's checkpoint, which PS-15's stamp now refuses at run time, and cited a spelling ADR-0017 deleted plus a begin().await? that PS-6 removed; and (8) THIS PASS ITSELF AFFIRMATIVELY WROTE the sentence batch shape is empty at both ends into four places - :232-249, :390, :472 and :8545 - which under AC-003's discriminator is not a repair but a newly authored false claim, and is the one entry in this list the pass created rather than inherited. All four are repaired and rowed; the method fix is the mechanical sweep recorded under AC-004 and in _reconciliation.md."
  mount_point: "spec/SPECIFICATION.md"
  verifying_test: ".bklg/from-contract-to-published-library/sqlite-durable-store/spec-and-code-reconciliation/_reconciliation.md (per-clause verdict table, one row per ID in the AC-004 range) + `cargo xtask spec-trace` green"

- id: AC-002
  criterion: "GIVEN a reader following a clause's own file:line pointer into crates/happenstance-sqlite, WHEN they land, THEN they find the subject the clause said they would: the four schema-sketch citations no longer point at the sketch the runbook itself calls wrong, §5's projection-store census sentence states the count that is now true, the [PROVISIONAL] marker naming this crate as its instrument no longer waits for an event that has already happened, and D7's read-path paragraph no longer explains conformance by the behaviour of a todo!() body. Every drifted pointer is re-anchored or repaired — none is deleted."
  satisfied: true
  evidence: "EVERY DRIFTED POINTER WAS RE-ANCHORED OR REPAIRED, AND NONE WAS DELETED - `checked` ROSE. THE FOUR SCHEMA-SKETCH CITATIONS: all four cited crates/happenstance-sqlite/src/event_store.rs:47-53 while the sentences were about the TAG SIDE TABLE. :47-53 is the `event` table; the side table is :62-67. Repaired at spec/SPECIFICATION.md:3373 (ES-17's Rejects), :3892 (the append-condition clause's Rejects), :7606 (CF-4's Rejects) and :7673 (the tag-join gap paragraph), and in the two places that called it 'the PLANNED SQLite schema' the tense was corrected to 'landed' - ADR-0022 shipped it. THE CENSUS SENTENCE: spec/SPECIFICATION.md:386's port-table row said 'Two of the five are todo!() throughout ... Nothing runs against any of the five'; it now states that four are still skeletons, that SqliteProjectionStore is real in all four methods, and that one of the five runs projection_store_conformance! against a real temporary file. The parallel sentence at :4656 ('The port has five implementers and not one adapter' / 'All five are phase-2 skeletons, and none has run a suite because none exists') is corrected with it, and PS-2 is restated as what a green run there does NOT buy. THE MARKER WAITING FOR AN EVENT THAT HAD ALREADY HAPPENED: ES-41's [PROVISIONAL] at :4457 named 'happenstance-sqlite at phase 8 and happenstance-cloudflare at phase 9, whichever lands first' - and the first had landed. Restated INSIDE the same level: the in-process, connection-holding half is answered by the UNIQUE (origin_store, origin_position) lookup (event_store.rs:57), the TRANSPORT half is still owed by happenstance-cloudflare and happenstance-neon, completeness is untouched. No level moved. D7's READ PATH: :2988 explained conformance by 'spawn_blocking panics with no runtime in scope', a claim about a todo!() body. It now says `read` is not async and may be called with no runtime, so the work AND THE CEILING move into poll_next (Ceiling::Unsampled at event_store.rs:973), and names ADR-0022 section 9's captured Handle as what settled the seam. ADR-0011's obligation is untouched. FIVE MORE DRIFTED CITATIONS that no spec named in advance were found and repaired: event_store.rs:151-193 (cited as the error enum, now a struct definition) to :857-950; :202 (cited as an impl writing + Send, now const MIGRATION_1) to :959; :195 (cited as the head impl, now SCHEMA_VERSION) to :952; projection_store.rs:208 to :529-679; and :215-224 to :534-542. spec/E2E-CASES.md needed NO repair: `grep -n 'happenstance-sqlite' spec/E2E-CASES.md` returns nothing. `cargo xtask spec-trace` green: 'traceability: no problems found; sections 7.1-7.2 match the checker'. LINE NUMBERS RE-DERIVED 2026-08-18: the after-numbers quoted above are from the first implementation pass and the review fix moved them by inserting repairs above - :386 is now :391, :2988 is :2997, :3373 is :3382, :3892 is :3901, :4013 is :4022, :4457 is :4466, :4656 is :4665, :4681 is :4690, :7606 is :7621, :7673 is :7688. The old numbers are left standing rather than overwritten, because they are what the pass read; _reconciliation.md carries the merged-tree numbers. NO CITATION MOVED, ONLY THE LINES THE CITATIONS SIT ON: `cargo xtask spec-trace` reports 401 citations checked, 80 anchored, against 398/78 before the fix and 389/76 at the baseline."
  mount_point: "spec/SPECIFICATION.md"
  verifying_test: "`cargo xtask spec-trace` — windowed subject match at xtask/src/spec_trace.rs:374-390 passing for every repaired citation, with each of spec/SPECIFICATION.md:372, 2552, 2642, 2945, 3326, 3841, 3958, 4393, 4586, 4604, 7218, 7285 dispositioned in _reconciliation.md"

- id: AC-003
  criterion: "GIVEN a reviewer who must be able to trust that this pass changed no promise, WHEN they diff spec/SPECIFICATION.md, THEN every edit is a repair — the set of implementations the clause admits is unchanged — and any finding that would change what a [FROZEN] clause admits appears as a recorded, escalated gap inside the clause it is about, with nothing normative altered and no ADR written here. A met obligation keeps its MUST verbatim, is named as a discharge, and cites the code and the test that assert it."
  satisfied: true
  evidence: "EVERY EDIT IS A REPAIR UNDER THE PLAYBOOK'S DISCRIMINATOR, APPLIED RATHER THAN IMPROVISED. .kb/playbooks/repairing-a-frozen-clause-without-amending-it.md: a correction to a FROZEN clause is a repair iff the set of implementations the clause admits is unchanged. In every one of the twenty-six repairs what changed is supporting prose, a Rejects: example's provenance, or a file:line - never a MUST, never a maturity marker, never an admitted implementation. MECHANICAL CHECK, RE-DERIVED FROM THE MERGED TREE: `git diff 53a4764 -- spec/SPECIFICATION.md` is 202 insertions / 85 deletions; ZERO deleted lines contain the normative token MUST, and TWO contain a lower-case must. Both were read: (1) the section 1.6 ProjectionStore table row, whose PS-2 column ('a hostile store that commits the checkpoint and drops the read-model write must fail the suite, and two adapters at opposite ends of the batch-shape axis must pass it') is reproduced word-for-word in the replacement, verified by extracting it from the + line; (2) a prose sentence reading 'its work must move into poll_next', a description of an adapter's constraint and not a normative clause. NO CLAUSE ID WAS ADDED OR RETIRED and the census is identical before and after: 201 clauses (137 FROZEN, 47 PROVISIONAL, 12 DEFERRED, 5 NON-NORMATIVE). MET OBLIGATIONS ARE NAMED AS DISCHARGES AND CITE THE CODE AND THE TEST: VT-21/VT-22/VT-24's 'MUST document its actual limit' is discharged by SqliteEventStore::MAX_EVENT_DATA_LEN = 1,048,576, MAX_TAGS_PER_EVENT = 128 and MAX_EVENTS_PER_BATCH = 256 (event_store.rs:245, :252, :261), enforced at :475, :482, :488, checked from both sides at tests/append.rs:238-286 - and the paragraph says in terms that THE MARKERS DO NOT MOVE, because their falsifiers are about a named target that cannot honour the floor and one adapter clearing four floors is not that target. CF-34's separation is likewise discharged by citation (crates/happenstance-testkit/src/bench.rs, crates/happenstance-testkit/Cargo.toml:92) with its marker unmoved. TWO GAPS, ESCALATED AND NOT APPLIED, both in _reconciliation.md's 'Gaps escalated, not applied': ADR-0022 states no clause range (escalated to the runbook's ADR queue against RUNBOOK.md:301's row - NOT fixed, because an accepted decision atom is immutable), and ES-17's two-build measurement is still owned by nobody (already escalated by ADR-0022 section 13 at RUNBOOK.md:302; this pass confirms it survived phase 8's implementation stories and adds nothing). NO ADR WAS WRITTEN HERE. CORRECTED 2026-08-18: this row previously read 152 insertions / 72 deletions and twenty edits, and described the two lower-case lines as containing the word MUST. Those figures were true of the first implementation pass and stopped being true at commits c510468, 67365be and this review fix, each of which added repairs the row was never re-read against - the identical stale-quotation defect the prior review raised at AC-005, found a second time in this same ledger and fixed the same way, by quoting the old figures rather than overwriting them. Both the old and the new reading satisfy the criterion, so it never stopped holding; what was wrong was the quotation. THE SUBSTANTIVE CLAIM WAS RE-CHECKED RATHER THAN RECOPIED: zero deleted lines in the merged diff contain uppercase MUST, and the two lower-case must lines are the two named above - the section 1.6 ProjectionStore row whose PS-2 column is reproduced word-for-word, and the prose sentence reading its work must move into poll_next. THE ROW COUNT IS NOW DERIVED RATHER THAN ASSERTED: _reconciliation.md carries 43 verdict rows - 8 in set A, 24 in the clause-naming table, 11 in the mechanical sweep - of which 26 are repairs to spec/SPECIFICATION.md and 17 are explicit unchanged verdicts."
  mount_point: "spec/SPECIFICATION.md"
  verifying_test: "Diff review against .kb/playbooks/repairing-a-frozen-clause-without-amending-it.md's discriminator: no [FROZEN] clause MUST text changes in `git diff`, and every gap row in _reconciliation.md names its runbook ADR-queue escalation"

- id: AC-004
  criterion: "GIVEN the phase-4 precedent, where queue rows named 35 clauses and the body discharged 64 and the 29 missing were invisible in exactly the way a finished clause is invisible (RUNBOOK.md:315-322), WHEN phase 8 closes, THEN two enumerated ID sets exist side by side — the phase's stated clause range and the union of its ADRs' ranges — and every element of the symmetric difference carries a disposition: already discharged elsewhere, owed by ADR-0022, or escalated. The three disagreeing statements of the phase's range (RUNBOOK.md:4175-4176, RUNBOOK.md:605-606, _intake-brief.md:76-92) are reconciled into one set rather than averaged, and ADR-0022's queue row carrying no parenthesised range at all (RUNBOOK.md:301) is itself recorded as a finding."
  satisfied: true
  evidence: "TWO ENUMERATED ID SETS SIT SIDE BY SIDE IN _reconciliation.md's 'Clause ranges', AS LITERAL SETS RATHER THAN PROSE. SET A, the union of the phase's three disagreeing statements - RUNBOOK.md:4229-4230 {CF-14, CF-17}; RUNBOOK.md:605-606 {CF-17, CF-34} plus {ES-35} from the portfolio row; _intake-brief.md:76-92 {ES-35, CF-14, CF-17, CF-34, VT-21 through VT-24} - is A = {CF-14, CF-17, CF-34, ES-35, VT-21, VT-22, VT-23, VT-24}, eight clauses. RECONCILED INTO ONE SET RATHER THAN AVERAGED: the intake brief is a superset of both runbook statements and no statement names a clause another contradicts. SET B, the union of the phase's ADRs' ranges, is EMPTY - and the emptiness IS the finding, recorded as one rather than papered over. Phase 8 has exactly one ADR. .kb/decisions/0022-append-condition-strategy.md states no clause range in any form: no frontmatter field, no clause ID in its Decision section, and RUNBOOK.md:301's queue row for 0022 is the ONLY row in that queue carrying no parenthesised range - which is EC-008 exactly. NO RANGE WAS SYNTHESISED TO MAKE THE ARITHMETIC CLOSE. Two clause IDs do appear in the atom and both as NON-VERDICTS with named owners, recorded separately as B-prime = {ES-17, CF-40}. SYMMETRIC DIFFERENCE = {CF-14, CF-17, CF-34, ES-35, VT-21, VT-22, VT-23, VT-24, ES-17, CF-40}, ten elements, EACH WITH A DISPOSITION ROW: three 'already discharged elsewhere in phase 8' (CF-14, CF-17, ES-35 - by the predecessor story), five 'discharged here, as prose' (CF-34, VT-21 through VT-24), one 'owed by nobody, and escalated' (ES-17, RUNBOOK.md:302) and one 'open by decision' (CF-40, kb-open-question-cf-40-ownership-001). THE PHASE-4 PRECEDENT IS NAMED AND ANSWERED: RUNBOOK.md:315-322 records queue rows naming 35 clauses against a body that discharged 64. Phase 8's exposure is the same shape from the other side - the ADR that settles the phase states no range at all, so the comparison had to be derived from the phase's own statements, which is the arithmetic comparing a number against itself. That is escalated as an ADR-queue item."
  mount_point: "spec/SPECIFICATION.md"
  verifying_test: ".bklg/from-contract-to-published-library/sqlite-durable-store/spec-and-code-reconciliation/_reconciliation.md §Clause ranges — two literal ID sets, their symmetric difference computed, one disposition row per element"

- id: AC-005
  criterion: "GIVEN a consumer who needs to know the specification's coverage did not shrink to buy its greenness, WHEN the slice merges, THEN `cargo xtask spec-trace` is green and both figures in its summary line — N citations checked and M anchored to their subject — are at or above the figures recorded from the pre-slice merge-base. Deleting a citation to silence a failure is not a remedy (it lowers checked); re-pointing one at a line whose subject no longer matches is not a remedy either (it lowers anchored). §1.3's hand census is edited by hand in the same commit if any maturity marker moved; §7.1/§7.2 are regenerated with `cargo xtask spec-trace --write` and never hand-edited."
  satisfied: true
  evidence: "BOTH FIGURES ARE ABOVE BASELINE, AND THE ONE THAT FELL MID-PASS WAS DIAGNOSED RATHER THAN ABSORBED. BASELINE SHA 53a4764 (the checkpoint sealing the previous slice, and this slice's merge-base). `cargo xtask spec-trace` there: '201 clauses (137 FROZEN, 47 PROVISIONAL, 12 DEFERRED, 5 NON-NORMATIVE), 112 conformance rules, 58 e2e cases, 389 citations checked (76 anchored to their subject, 12 external)'. ON THE MERGED TREE: '... 401 citations checked (80 anchored to their subject, 12 external)'. checked 389 to 401 (+12). anchored 76 to 80 (+4). external 12 unchanged. CORRECTED 2026-08-18: this row previously read '396 citations checked' and 'checked 389 to 396 (+7)'. Those were the figures before commit c510468, whose own message records the move 396 -> 398; it updated _reconciliation.md:32 and did not update this row, so the story's primary evidence artifact misquoted the tree it measures - which is exactly the hand-maintained-count-drifting-from-the-list failure this story exists to close, occurring inside its own ledger. Both the old and the new figure clear the 389 baseline, so the criterion never stopped holding; what was wrong was the quotation, and it is re-derived here from a live `cargo xtask spec-trace` run rather than recopied. Both compared, not just greenness. EC-004 FIRED AND WAS TREATED AS AN UNREPAIRED DRIFT FINDING: after the first pass `checked` held and `anchored` fell to 75. Cause: re-pointing section 1.6's SqliteProjectionStore citation from projection_store.rs:235-260 to :529 removed its derivable subject - subject_before (xtask/src/spec_trace.rs:2158) takes the code span IMMEDIATELY before the citation, and 'in all four (:529)' has none where 'in begin and rollback (:235-260)' had `rollback`. THE WINDOW WAS NOT WIDENED. The sentence was re-anchored properly: 'in all four since phase 8, `begin` through `rollback` (...:529-679)', which restores `rollback` as the subject and puts it inside the twelve-line window of the cited range (rollback is at projection_store.rs:673; hi is 679 plus 12). anchored returned to 76 and rose to 78 with the later repairs. The diagnosis used a temporary eprintln! in spec_trace.rs plus a script that swapped the base blob in and out; BOTH WERE REVERTED and `git diff xtask/src/spec_trace.rs` is EMPTY - no check was relaxed, widened or disabled. NO CITATION WAS DELETED to silence anything: the count rose. SECTION 1.3's HAND CENSUS NEEDED NO EDIT because no maturity marker moved - the clause census is byte-identical before and after (137/47/12/5), and the check at xtask/src/spec_trace.rs:447-471 is green. SECTIONS 7.1/7.2 were NOT hand-edited and needed no regeneration: spec-trace reports 'sections 7.1-7.2 match the checker' on both trees. Story grain `cargo xtask affected --base main` green. ONE FILE OUTSIDE THE DECLARED PR BOUNDARY WAS TOUCHED, ONE DIGIT, AND IT IS RECORDED RATHER THAN QUIET: `cargo xtask lints` went red because standards/rust/01-standard-of-evidence.md:128 cites spec/SPECIFICATION.md:5926 (silently ignores) and this pass's insertions moved that sentence to :5984. The constitution's own citation lint uses a TEN-line window (xtask/src/lint_constitution.rs:111), narrower than spec-trace's twelve, which is why it caught a shift spec-trace tolerated. 5926 became 5984 and nothing else - no rule text, no evidence claim, no atom body - because the alternative was leaving a REQUIRED gate step red through an edit this pass caused. Recorded in _reconciliation.md under 'One file outside the PR boundary, and the reason'. Integration grain `cargo xtask ci --fast` then green whole: 'all required checks passed (--fast: 4 optional step(s) not run)'. CORRECTED AGAIN 2026-08-18, and for the same reason as the first time: this row read 398 citations checked (78 anchored) until the review fix repaired section 6.5s batch-shape row, section 4.5s foreign-batch paragraph and section 6.5s opening claim, which between them added three citations and two anchors. Re-derived from a live `cargo xtask spec-trace` on the merged tree: 401 checked, 80 anchored, 12 external, 201 clauses (137/47/12/5) - both figures still above the 389/76 baseline, no citation deleted, no window widened, and `git diff xtask/src/spec_trace.rs` still EMPTY. One further digit outside the PR boundary moved with it: standards/rust/01-standard-of-evidence.md:128 cited spec/SPECIFICATION.md:5984 and the anchor is now at :5993 - 9 lines away against an ANCHOR_SLACK of 10 (xtask/src/lint_constitution.rs:111), so `cargo xtask lints` was green with one line of margin and is now green on the number. Recorded in _reconciliation.md under One file outside the PR boundary."
  mount_point: "spec/SPECIFICATION.md"
  verifying_test: "`cargo xtask spec-trace` (summary line emitted at xtask/src/spec_trace.rs:995-1007; census check at :447-471) run at the recorded merge-base sha and on the merged tree, both figures compared in _reconciliation.md; gated by `cargo xtask affected --base main` (.redkiln/config.yaml:40)"

- id: AC-006
  criterion: "GIVEN publication-and-positioning's clause-ledger audit must read a decision rather than a silence (project DR-07), WHEN it opens CF-14, CF-17 and ES-35, THEN each carries the verdict reopen-negative-control-and-durability-verdicts produced, transcribed here without being re-opened, and every surviving [PROVISIONAL] / [DEFERRED] marker names a falsifier that is still live and long enough to satisfy CF-38's build failure. The reconciliation open question is left open: no committed citation baseline, no new gate check, no machine-readable clause-range format — findings that argue for mechanisation are staged under .kb/_intake/ for /redkiln:kb-ingest to adjudicate, never hand-written into .kb/."
  satisfied: true
  evidence: "THE THREE VERDICTS ARE TRANSCRIBED AND CHECKED, NOT RE-OPENED, AND THE OPEN QUESTION IS LEFT OPEN. EACH TRACED TO THE UPSTREAM LEDGER ROW: reopen-negative-control-and-durability-verdicts/_ledger.md:61 carries all three, and each is read back at its current line here. ES-35 (spec/SPECIFICATION.md:4204 marker, :4222-4236 closing paragraph) - falsifier restated to a store that loses a write to a FAULT rather than to an instruction; still live, and SqliteFixture is named as supplying the reopen far end and declining MID_BATCH_FAULT by scope. CF-17 (:7975-7997) - rule shape CONFIRMED against the first real file-backed adapter. CF-14 (:7859-7907) - the deferral CONFIRMED and NARROWED to happenstance-cloudflare (HS-P0013) and happenstance-neon (HS-P0014), closing 'Falsified by the first of those two that cannot express it with this shape' - 55 characters, well past CF-38's twelve. NO LEVEL MOVED: the status rows still read ES-35 PROVISIONAL, CF-14 DEFERRED, CF-17 PROVISIONAL, and the census is identical. EVERY SURVIVING MARKER THIS PASS TOUCHED STILL NAMES A LIVE FALSIFIER. ES-41's was the one that did NOT - it named an instrument that had already landed - and it is restated to the transport half, which is unbuilt. VT-21 through VT-24 and CF-34 keep their falsifiers verbatim and their discharge paragraphs say in terms why the markers do not move. spec-trace's falsifier-length check (xtask/src/spec_trace.rs:659-662) is green. THE OPEN QUESTION IS UNEDITED: `git diff .kb/open-questions/nothing-owns-the-post-phase-reconciliation.md` is EMPTY. NO committed citation baseline (the figures live in this story's folder, not beside the list they count - .kb/playbooks/landing-a-stricter-gate-without-a-red-baseline.md), NO new gate check, NO machine-readable clause-range format. THE FINDINGS THAT ARGUE FOR MECHANISATION ARE STAGED, NOT IMPLEMENTED: .kb/_intake/0034-what-the-phase-8-reconciliation-cost.md records what the pass cost and three findings - that only 78 of 398 citations are under any anchoring discipline, so six resolved while pointing at the wrong subject and none was reported; that the arithmetic bullet has nothing to close against; and that 14 of 20 repairs were false sentences no extension of spec-trace could catch. It is explicitly marked 'Not an atom' and 'settles nothing'. NO ATOM UNDER .kb/decisions/, .kb/concepts/ OR .kb/playbooks/ WAS WRITTEN OR EDITED - `git status` shows one added file under .kb/, and it is that intake document. `redkiln validate --kb` PASSES (exit 0). `redkiln doctor` exits 1 on its standing advisories only - the six template-drift warnings CLAUDE.md records as expected forever, and nine pre-existing 'foundation story consumed by no capability slice' notes, none of which names any file this story touched. LINE NUMBERS RE-DERIVED 2026-08-18 after the review fix moved them: ES-35s marker is at :4213 with its closing paragraph at :4231-4245, CF-17 is at :7990-8012, CF-14 at :7874-7922. The verdicts are unchanged and were not re-opened - only the lines they sit on moved. THE OPEN QUESTION IS STILL UNEDITED: `git diff .kb/open-questions/nothing-owns-the-post-phase-reconciliation.md` is EMPTY on the fixed tree, no gate step was added, and the third argument for mechanisation that this review produced - that a widened inclusion rule executed by eye missed a hit three review cycles running - is recorded in _reconciliation.md and belongs to .kb/_intake/0034-what-the-phase-8-reconciliation-cost.md, not to a hand-written atom."
  mount_point: "spec/SPECIFICATION.md"
  verifying_test: "`cargo xtask spec-trace` falsifier-length check (xtask/src/spec_trace.rs:659-662) + `redkiln validate --kb && redkiln doctor` clean, with CF-14/CF-17/ES-35 each traced in _reconciliation.md to the upstream story's ledger row and .kb/open-questions/nothing-owns-the-post-phase-reconciliation.md unedited in the diff"
```
