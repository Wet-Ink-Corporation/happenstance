---
item: "HS-S0043"
stage: implement
created: "2026-08-12"
updated: "2026-08-12"
---

# Acceptance ledger — The reopen rule gets a negative control, and the durability clauses get verdicts

The machine-checkable Definition-of-Done ledger for this story (RFC §6.5). One row per spec
`AC-###`. Planning authors every row with `satisfied: false`; the implementer may only flip a row to
`satisfied: true` and MUST cite real evidence (a `file:line` and/or the verifying test id) — never
edit, remove, or re-word a criterion, and never flip a satisfied row back. `redkiln verify --grain
story` reads the fenced block below and blocks `implement → report` unless every spec AC is present,
`satisfied: true`, and carries non-placeholder evidence (and no AC that was satisfied on the base
branch has regressed). Scope changes are a human decision recorded through `redkiln advance`, not a
quiet ledger edit.

Two obligations this story's testing brief adds on top of the schema, both discharged in the
`evidence` fields rather than in extra rows:

- **AC-T05** — every row's evidence is a real `file:line` and/or test id, never "green gate".
  A green gate is a precondition for reading these criteria, never a substitute
  (`RUNBOOK.md:38-42`).
- **AC-T06** — the evidence for **AC-001** must state *which* of the two negative-control options
  was used. This story chose the permanent registry row over a reverted local mutation, per testing
  brief §5's stated preference; the evidence cites the `Declared` row's `file:line`.

```yaml
- id: AC-001
  criterion: "GIVEN an adapter author reading `recorded_time_survives_a_reopen` to learn whether their own store's reopen is good enough, WHEN they run the mutant harness, THEN a registered subject fails that rule at its headline assertion — the `recorded_at` comparison, not the survival anchor — and its `Declared` row's `expect` entry pins the headline message, so a failure at an earlier anchor is reported as the wrong failure rather than counted as a pass."
  satisfied: true
  evidence: "crates/happenstance-testkit/tests/mutation_coverage/mutants.rs:3687 RestampingFixture, registered at crates/happenstance-testkit/tests/mutation_coverage.rs:1234 with fails: ['recorded_time_survives_a_reopen'] and an expect pin quoting the HEADLINE message - 'a `RecordedAt` is persisted alongside the event, not recomputed when' - which is a substring of crates/happenstance-testkit/src/suite.rs:2517-2522. `cargo test -p happenstance-testkit --all-features --test mutation_coverage mutants_fail_exactly_their_declared_rules` green. THE PIN WAS PROVED TO DISCRIMINATE, not merely to be present: swapping it for LosingFixture's survival-anchor string ('the acknowledged event must have survived the reopen at all') fails with `RestampingFixture is declared to fail recorded_time_survives_a_reopen at ... but a different assertion fired`, and the message it printed instead was the headline one verbatim. The swap was reverted. Contrast row: LosingFixture at mutation_coverage.rs:1195, whose pin for the same rule quotes the second anchor."
  mount_point: "crates/happenstance-testkit/tests/mutation_coverage.rs"
  verifying_test: "crates/happenstance-testkit/tests/mutation_coverage.rs::mutants_fail_exactly_their_declared_rules"

- id: AC-002
  criterion: "GIVEN the same adapter author re-running the harness on a fast machine and on a slow one, WHEN the control reopens, THEN the stamp it recomputes is a deterministic function of a per-fixture reopen generation — never re-spent from `correct::stamp`'s constant, never read from a clock — so the failure is identical run to run and can never be silently invisible or intermittently absent."
  satisfied: true
  evidence: "The re-stamped value is RecordedAt::from_millis(correct::TEST_RECORDED_AT.as_millis() + generation) at crates/happenstance-testkit/tests/mutation_coverage/mutants.rs:3726, where `generation` is a Cell<i64> on the fixture (:3694) incremented once per reopen (:3707-3708). Deterministic, strictly monotone, and not a clock. THE NAIVE FORM WAS WRITTEN FIRST AND CAUGHT: with `with_recorded_at(correct::TEST_RECORDED_AT)` the harness reported `RestampingFixture declares that it fails recorded_time_survives_a_reopen, but the rule passed. Either the rule does not catch this defect after all - which makes it decorative for this mutant - or the declaration names the wrong rule` (mutation_coverage.rs:3020), which is EC-002 exactly. mutants_fail_exactly_their_declared_rules green on three consecutive runs (0.24s, 0.26s, 0.25s). CF-33 untouched: no SystemTime, Instant or now() is added anywhere under crates/happenstance-testkit/tests/mutation_coverage - the arithmetic goes through RecordedAt::from_millis / as_millis at crates/happenstance-core/src/identity.rs:160-172 and nothing else."
  mount_point: "crates/happenstance-testkit/tests/mutation_coverage.rs"
  verifying_test: "crates/happenstance-testkit/tests/mutation_coverage.rs::mutants_fail_exactly_their_declared_rules"

- id: AC-003
  criterion: "GIVEN an evaluator judging in one sitting whether the three reopen rules are independently falsifiable, WHEN they read the registry, THEN the new row declares exactly one rule — `recorded_time_survives_a_reopen` — and the subject passes `acknowledged_writes_survive_a_reopen` and `reopened_store_does_not_reissue_an_event_id`, so what bites is the stamp sentence alone and not the survival sentences `LosingFixture` already covers."
  satisfied: true
  evidence: "The Declared row at crates/happenstance-testkit/tests/mutation_coverage.rs:1234 lists exactly ONE rule. mutants_fail_exactly_their_declared_rules asserts both directions - a subject must fail every rule it declares and pass every rule it does not - and it is green, so RestampingFixture passes acknowledged_writes_survive_a_reopen and reopened_store_does_not_reissue_an_event_id. That is structural rather than incidental: reopen replays every event through Log::replayed (crates/happenstance-testkit/tests/mutation_coverage/correct.rs:477) with position, EventId and payload untouched and only recorded_at replaced (mutants.rs:3720-3731). every_rule_has_a_mutant also green. Both meta-tests are named at xtask/src/proof.rs and run by the REQUIRED `each phase's proof artefacts` gate step."
  mount_point: "crates/happenstance-testkit/tests/mutation_coverage.rs"
  verifying_test: "crates/happenstance-testkit/tests/mutation_coverage.rs::mutants_fail_exactly_their_declared_rules, ::every_rule_has_a_mutant"

- id: AC-004
  criterion: "GIVEN a maintainer who adds a fixture and forgets one of the lists it must appear in, WHEN the gate runs, THEN the control is present in all four places — the type in `mutation_coverage/mutants.rs`, the type in `for_each_mutant!`, the `Declared` row in `REGISTRY` with a real provenance sentence, and the `MODEL_COVERAGE` row — and the model doc comment's miss count and its \"defects that are only visible across a reopen\" bullet both name the second subject, so the file's own answer to what is this test blind to stays true rather than going stale in prose."
  satisfied: true
  evidence: "All four places, plus the fifth the spec adds. (1) The type: crates/happenstance-testkit/tests/mutation_coverage/mutants.rs:3687. (2) for_each_mutant!: crates/happenstance-testkit/tests/mutation_coverage.rs:2176. (3) The Declared row with a real provenance sentence naming the schema mistake somebody would ship - a migration with no recorded_at column, so open replays rows and stamps them at open time: mutation_coverage.rs:1234-1263. (4) The MODEL_COVERAGE row: mutation_coverage.rs:2484. (5) The model doc comment's arithmetic: the heading at mutation_coverage.rs:2325 moves from 'The twenty it does not catch' to 'The twenty-one', the body from twenty-two Agreed rows and twenty misses to twenty-three and twenty-one (:2327-2331), and the reopen bullet at :2345-2351 now names both subjects and says why the model misses the second for a sharper reason than the first. The module doc's longhand list at mutants.rs:29-36 moves from eight stores to nine. mutant_registry_is_exhaustive, every_mutant_states_its_provenance and the_model_rule_rejects_exactly_what_it_claims all green under --all-features."
  mount_point: "crates/happenstance-testkit/tests/mutation_coverage.rs"
  verifying_test: "crates/happenstance-testkit/tests/mutation_coverage.rs::mutant_registry_is_exhaustive, ::every_mutant_states_its_provenance, ::the_model_rule_rejects_exactly_what_it_claims (run with --all-features)"

- id: AC-005
  criterion: "GIVEN an evaluator who reads a [PROVISIONAL] marker to decide adopt-or-decline, WHEN they read ES-35, CF-17 and CF-14 after this PR, THEN each carries a written verdict against this adapter's evidence — ES-35's remaining falsifier restated as a store that loses a write to a fault rather than to an instruction, CF-17's rule shape confirmed against a real file-backed adapter, CF-14's deferral confirmed and narrowed to the two implementations that have not answered — every sentence that has become factually false is corrected, and no marker changes level; any promotion is recorded as a named ADR-queue item in this story's folder with its evidence attached, never made here."
  satisfied: true
  evidence: "Three verdicts written INSIDE their existing levels, and every sentence that had become false corrected. LINE REFERENCES IN THIS ROW ARE POST-CORRECTION: the review fix below added lines above every clause cited here, so the ranges the story first recorded (:4152, :4170-4183, :7889-7912, :7790-7802, :7817-7827, :8963, :9072, :9075) are each about ten to thirty lines low against the file as it now stands, and are restated at their current values. ES-35 (spec/SPECIFICATION.md:4163 marker, :4181-4195 closing paragraph): the falsifier is restated as a store that loses a write to a FAULT rather than to an instruction, and the claim that the axis has 'nothing at the other end' is replaced by naming SqliteFixture - which closes every rusqlite::Connection, checkpoints the WAL with TRUNCATE and never deletes the file - while keeping the still-true sentence that no fixture in the mutant binary has a medium outside the process. CF-17 (:7917-7943): the rule shape is CONFIRMED against the first real file-backed adapter, and the sentence naming DurableFixture as 'the only fixture in the workspace that supplies this capability' is corrected to name SqliteFixture as the second and the first over a medium outside the process. CF-14 (:7803-7814 marker, :7836-7844 the phase-8 paragraph): the deferral is confirmed and NARROWED to the two implementations that have not answered, with HS-P0013 and HS-P0014 named. NO LEVEL MOVED: the status rows still read ES-35 PROVISIONAL (:8992), CF-14 DEFERRED (:9101), CF-17 PROVISIONAL (:9104), and spec-trace reports the same census before and after the edits - 201 clauses (137 FROZEN, 47 PROVISIONAL, 12 DEFERRED, 5 NON-NORMATIVE), 389 citations checked, no problems found. The escalation note is at .bklg/from-contract-to-published-library/sqlite-durable-store/reopen-negative-control-and-durability-verdicts/_adr-queue-escalation.md and names, per clause, what an ADR would have to decide and what it is gated on. CORRECTED AFTER REVIEW: the first pass moved section 6.5's Durability row to 'Fixture yes, adapter yes' and left the paragraph directly under it still saying 'the adapter column is empty on every one of them', which the same edit had made false. Four places now agree instead of one contradicting three - the 6.5 summary (spec/SPECIFICATION.md:8457-8470), the section 1.3 qualification (:232-246), the section 1.7 caveat (:457-471) and the EventStore row of the section 1.6 port table (:382) - each stating the same count: one axis, durability, has an adapter instrument at its far end, three carry a fixture instrument only, three have nothing at either end, and durability's fault half is still empty. ROUTED, NOT ABSORBED: the Handle multiplicity row at :8454 still reads that no connection has ever been opened twice, which SqliteFixture::connect also falsified in an earlier slice; the 6.5 summary at :8472-8480 now says so and hands the re-reading to spec-and-code-reconciliation (AC-016) rather than moving a second row in passing."
  mount_point: "spec/SPECIFICATION.md"
  verifying_test: "cargo xtask spec-trace (xtask/src/spec_trace.rs) plus review of spec/SPECIFICATION.md:4142-4180, :7471-7500, :7570-7596 and the clause-status rows at :8617, :8725, :8728"

- id: AC-006
  criterion: "GIVEN the application author who was told an acknowledged write survives a reopen, WHEN the SQLite conformance target is run after the control lands, THEN `recorded_time_survives_a_reopen` and `acknowledged_writes_survive_a_reopen` are green against `SqliteFixture` and the `MID_BATCH_FAULT`-gated rule is reported as Skipped carrying the fixture's stated reason — so the pass is a pass of a rule now demonstrably failable, and the fault far end stays visibly open rather than looking covered."
  satisfied: true
  evidence: "`cargo test -p happenstance-sqlite --test conformance -- --show-output` run AFTER the control landed: 90 passed; 0 failed; 0 ignored, with dcb_conformance::acknowledged_writes_survive_a_reopen ... ok, dcb_conformance::recorded_time_survives_a_reopen ... ok and dcb_conformance::reopened_store_does_not_reissue_an_event_id ... ok in the per-rule output. Both MID_BATCH_FAULT-gated rules print a SKIP line carrying SqliteFixture's OWN stated reason rather than the trait's default - `SKIP append_is_atomic_under_a_mid_batch_fault: fixture declines MID_BATCH_FAULT - by scope, not by incapacity. This adapter supplies the reopen far end and not the fault far end, which is ES-35's live residual falsifier ...` and the same for arming_a_mid_batch_fault_makes_the_append_fail. So the pass is a pass of a rule now demonstrably failable at its headline sentence, and the fault far end stays visibly open. The fixture is crates/happenstance-sqlite/tests/support/mod.rs (relocated by the slice-mate; its capability declarations are untouched by this story)."
  mount_point: "crates/happenstance-sqlite/tests/conformance.rs"
  verifying_test: "crates/happenstance-sqlite/tests/conformance.rs — event_store_conformance!(SqliteFixture::new())'s recorded_time_survives_a_reopen and acknowledged_writes_survive_a_reopen, plus the MID_BATCH_FAULT-gated rule's reported skip"

- id: AC-007
  criterion: "GIVEN a reader who never opens a test file, WHEN this change merges, THEN `CHANGELOG.md` carries an entry naming the defect the new control encodes in CF-29's stated shape, and the story-grain gate is green with `spec-trace`'s citation count not fallen — so the change to what the suite can prove is discoverable from the record alone."
  satisfied: true
  evidence: "CHANGELOG.md gains an entry under [Unreleased] / Added, above the benchmark-harness entry, naming the defect the control encodes in CF-29's shape: the missing recorded_at column, the replay-and-stamp-at-open-time reconstruction, what survives (position, identity, payload) and what does not, the one rule it fails and the two it passes, the pin on the headline message, and why the stamp is a generation counter rather than a clock - well past CF-29's 120-character-per-rule prose bar. `cargo xtask affected --base main` printed 'affected gate passed'. `cargo xtask lints` reports 27 atoms, all consistent, and `cargo xtask spec-trace` reports 389 citations checked, no problems found - the SAME count as HEAD before these edits, verified by running spec-trace against the HEAD copy of spec/SPECIFICATION.md and then against this one. Two constitution citations that drifted because this slice's edits moved lines were re-anchored in the same change: standards/rust/23-streams-and-state-machines.md:191 (mutation_coverage.rs 1585 to 1619) and standards/rust/41-declarative-macros.md:197 (concurrency.rs 1144 to 1176). The review's own correction to sections 1.3 and 6.5 then moved lines in spec/SPECIFICATION.md and drifted two more, re-anchored on the same terms: standards/rust/01-standard-of-evidence.md:127 (SPECIFICATION.md 5910 to 5926) and standards/rust/51-features-and-no-std.md:55 (355 to 366). BOUNDARY WIDENED AFTER REVIEW rather than left as a disclosed excursion: standards/rust/** is now an entry in this story's PR-boundary block, admitting citation re-anchoring ONLY, on the same terms and by the same precedent as benchmark-harness's entry at e020276 - so redkiln verify --grain story passes because the boundary admits the edit, not in spite of it. ROUTED, NOT WORKED AROUND: that command still reports 'skip boundary - no boundary declared' for this item, because the parser reads the first fenced block under the '## PR boundary' heading and stops at the next heading, and this spec's block sits under the '### Merge DoD' subheading; moving it up makes boundary and provenance both run and both FAIL while links.commits is empty (the check then diffs the whole initiative branch against main), and redkiln record-links is the orchestrating command's write rather than an implementer's. The finding is written beside the block in spec.md and in the implementation report. cargo fmt --all -- --check clean."
  mount_point: "CHANGELOG.md"
  verifying_test: "cargo xtask affected --base {{base}} (.redkiln/config.yaml:40) and cargo xtask lints && cargo xtask spec-trace (:48); the CHANGELOG.md lint of CF-29 in xtask/src/lints.rs"
```
