---
item: HS-S0045
stage: implement
created: "2026-08-12"
updated: "2026-08-12"
---

# Acceptance ledger — The last todo!() and the scoped allow go together, and the gate is green

The machine-checkable Definition-of-Done ledger for this story (RFC §6.5). One row per spec
`AC-###`. Planning authors every row with `satisfied: false`; the implementer may only flip a row to
`satisfied: true` and MUST cite real evidence (a `file:line` and/or the verifying test id) — never
edit, remove, or re-word a criterion, and never flip a satisfied row back. `redkiln verify --grain
story` reads the fenced block below and blocks `implement → report` unless every spec AC is present,
`satisfied: true`, and carries non-placeholder evidence (and no AC that was satisfied on the base
branch has regressed). Scope changes are a human decision recorded through `redkiln advance`, not a
quiet ledger edit.

Two rows in this ledger are discharged by a **named review item** rather than by a command, and that
is stated rather than hidden: AC-003 (no lint in this repository reads documentation for truth) and
the "what `--fast` did not run" half of AC-005. Their evidence must name the reviewer's reading and
the base-revision diff, not a green transcript that proves something else.

```yaml
- id: AC-001
  criterion: "GIVEN the adapter author has taken all four suites green against a real file and comes back to the crate root to learn whether they are finished, WHEN they open crates/happenstance-sqlite/src/lib.rs and read `git show` for the commit that closed the work, THEN the final todo!() on a SQLite path and the #![allow(clippy::todo)] at :76-80 — its four-line justification included — have left in that one commit, clippy::todo covers the crate with no exception, and no other crate's scoped allow moved with them."
  satisfied: true
  evidence: "THE RESIDUAL WAS EMPTY, AND THAT IS STATED RATHER THAN IMPLIED. The survey the implementation notes ask for was run first: `grep -rn 'todo!' crates/happenstance-sqlite/` found six hits and every one of them was PROSE - tests/shapes.rs:3, src/lib.rs:5, :13, :81, tests/migration.rs:555, src/event_store.rs:86 - and no macro invocation on any code line. So neither `head` nor `contains_event_id` arrived here still marked; both were closed upstream by `sqlite-fixture-and-whole-suite`, and this story's code change is the attribute plus the prose exactly as the spec's fallback branch anticipated. THE ATTRIBUTE AND ITS FOUR-LINE JUSTIFICATION ARE GONE: crates/happenstance-sqlite/src/lib.rs:78 is now `#![doc(html_no_source)]` followed directly by the module list at :80, where base 53a4764 carried the comment at :79-82 and `#![allow(clippy::todo)]` at :83. `grep -rn 'allow(clippy::todo)' crates/` finds the other six untouched at happenstance-cloudflare/src/lib.rs:126, happenstance-ladybug/src/lib.rs:78, happenstance-neon/src/lib.rs:105, happenstance-postgres/src/lib.rs:63, happenstance-sync/src/lib.rs:139 and happenstance-sync/tests/real_peer_shapes.rs:31 - the only other matches are the three inside the new test's own message strings. The clippy step of `cargo xtask ci --fast` (`cargo clippy --workspace --all-targets --all-features -- -D warnings`) is green BECAUSE the exception is gone, not despite it. Guarded from here on by crates/happenstance-sqlite/tests/front_page.rs::the_scoped_allow_is_gone_from_the_crate_root and ::no_todo_macro_survives_in_the_crate, both of which failed on the base tree - the first with `src/lib.rs still carries a scoped #![allow(clippy::todo)]` - and pass now. ONE COMMIT: the attribute deletion, the prose and the new guard are one checkpoint; nothing about this story is split across two."
  mount_point: "crates/happenstance-sqlite/src/lib.rs"
  verifying_test: "crates/happenstance-sqlite/tests/front_page.rs::the_scoped_allow_is_gone_from_the_crate_root; crates/happenstance-sqlite/tests/front_page.rs::no_todo_macro_survives_in_the_crate; cargo xtask ci --fast clippy step (xtask/src/main.rs:105-130, `cargo clippy --workspace --all-targets --all-features -- -D warnings`), plus `rg -n 'todo!' crates/happenstance-sqlite/src` and `rg -n 'allow\\(clippy::todo\\)' crates/happenstance-sqlite` returning nothing, plus one-commit provenance recorded via `redkiln record-links --sha`"

- id: AC-002
  criterion: "GIVEN the adapter author's fear is a port that quietly assumed something their storage cannot provide, WHEN every SQLite path that used to be todo!() is executed by the conformance, model, concurrency and projection targets, THEN each one runs real SQL — no unimplemented!(), unreachable!(), panic!(\"not implemented\") or fabricated Err variant stands in for work not done — and where head and contains_event_id are written here they obey the constraints already stated on them: head re-reads max(position) from the database on every call and is never a cached field an append updates, with NULL mapping to None; contains_event_id reads the UNIQUE(origin_store, origin_position) pair."
  satisfied: true
  evidence: "EXECUTION, NOT THE GREP - the grep is reported second and as a convenience, exactly as the spec's third mutant requires. `cargo test -p happenstance-sqlite` on this tree: append.rs 19 passed, concurrency.rs 9 passed, conformance.rs 90 passed, migration.rs 9 passed, read.rs 14 passed, shapes.rs 10 passed, wide_query.rs 18 passed, front_page.rs 7 passed; `cargo test -p happenstance-sqlite --all-features --test projection` 24 passed. 0 failed anywhere. Every one of those runs real SQL through a real rusqlite::Connection against a real temporary file. NEITHER RESIDUAL WAS THIS STORY'S TO WRITE: `head` is crates/happenstance-sqlite/src/event_store.rs's `SELECT max(position) FROM event` asked of the database on every call - there is no cached head field on SqliteEventStore - and its freshness is proven by the two-handle rules the conformance target runs rather than by a new assertion; `contains_event_id` reads the UNIQUE(origin_store, origin_position) pair migration 1 creates (documented at src/event_store.rs:57). Both were implemented by `sqlite-fixture-and-whole-suite`, which is the predecessor this row credits. NO SYNONYM: `grep -rn 'unreachable!|unimplemented!|not implemented|not yet implemented' crates/happenstance-sqlite/` returns exactly ONE hit, src/projection_store.rs:762, and it is not a stand-in for missing work - it is the contract-mandated panic on `probe_read_through`, which is never called because READS_THROUGH_BATCH is false and answering from committed state is what PS-12 forbids, carrying a `# Panics` section that says so (:753-761). That single exception is named with its reason inside crates/happenstance-sqlite/tests/front_page.rs::no_todo_synonym_stands_in_for_work_not_done, so a SECOND one fails a test rather than a judgement call."
  mount_point: "crates/happenstance-sqlite/src/lib.rs"
  verifying_test: "crates/happenstance-sqlite/tests/front_page.rs::no_todo_synonym_stands_in_for_work_not_done; cargo test -p happenstance-sqlite (crates/happenstance-sqlite/tests/conformance.rs, tests/concurrency.rs, tests/projection.rs, tests/shapes.rs) green inside cargo xtask ci --fast; head's freshness proven by two_handles_observe_each_others_appends (crates/happenstance-testkit/src/contract.rs); `rg -n 'unimplemented!|unreachable!|not implemented' crates/happenstance-sqlite/src` empty"

- id: AC-003
  criterion: "GIVEN the evaluator has one sitting and a rendered docs page in which to decide whether \"DCB-compliant\" and \"storage-agnostic\" are real claims, WHEN they land on this crate's front page — in the tree or from cargo doc — THEN it describes an adapter that has passed the conformance suite against a real file, not the skeleton it used to be: the Status: an instrument, not yet an adapter banner and the \"every operation that touches SQL is todo!()\" paragraph are gone, the publish = false sentence is replaced by the true and different statement that publication is publication-and-positioning's decision, Open decisions is closed against ADR-0022 or narrowed to what it genuinely left open with the open-question atom cited, and no other status marker in the tree contradicts the front page — event_store.rs:3, projection_store.rs:3, tests/shapes.rs:3 and CLAUDE.md:33's skeleton row all corrected — while the three sections that carry compiled results survive."
  satisfied: true
  evidence: "THE FOUR FALSE STATEMENTS ARE GONE AND ARE NAMED ONE BY ONE. (1) The banner at base 53a4764 lib.rs:3 'Status: an instrument, not yet an adapter' is now crates/happenstance-sqlite/src/lib.rs:3 '# Status: an adapter, and it has run the suite'. (2) The 'Every operation that touches SQL is todo!()' paragraph (base :5-20) is replaced by :5-14, which names the seven real bodies and the two mounts that execute them. (3) The 'It is publish = false until it passes ... the conformance suite' sentence (base :22-24) is replaced at :16-19 by the TRUE AND DIFFERENT claim - the crate still carries publish = false, lifting it is the publication pass's decision, and having passed the suite and being on a registry are two claims of which only the first is made here. (4) '# Open decisions' (base :50-67) is now '# What is settled, and what is still open' at :45-67: ADR-0022 is named as what closed the append-condition strategy and tag storage, its actual outcome is stated (one SELECT max(position) inside BEGIN IMMEDIATE; event_tag(tag, position) WITHOUT ROWID with event_type covering and tag_cardinality for selectivity), and the two subjects the ADR genuinely left open are narrowed and cited by atom name - `es-17-two-adapter-measurement-is-unscheduled` and `cf-40-fixture-limits-ownership`, both present under .kb/open-questions/. NO OTHER MARKER CONTRADICTS IT: src/event_store.rs:3 and src/projection_store.rs:3 already read '# Status: every event-store path is real' and '# Status: a projection store that has run the suite it did not write' (corrected upstream, verified here rather than assumed); tests/shapes.rs:1-8 no longer says 'every body is todo!() in the crate under test' and now states what a type-level target actually buys; CLAUDE.md:33 reads 'the first adapter. event store + projection store.' where it read '🔩 skeleton. ...'. THE 🔩 DEFINITION PARAGRAPH AND THE OTHER FIVE ROWS ARE UNTOUCHED - `git diff 53a4764 -- CLAUDE.md` is a one-line hunk. THE COMPILED RESULTS SURVIVED, which is the non-occlusion invariant and the likeliest way this rewrite could have done damage: '# The shape this crate represents' (:21-29, the portfolio-axis paragraph), '# What the type checker has already decided' (:31-43, including error[E0195] verbatim) and '# Not the Cloudflare adapter' (:69-76) are all present, asserted by crates/happenstance-sqlite/tests/front_page.rs::the_front_page_keeps_its_compiled_results. DENSITY: 78 //! lines against a base of 76, budget 54-94, and 5 `#` headings against a budget of 5 - asserted by ::the_front_page_stays_within_its_density_budget. THE SCHEMA SKETCH WAS VERIFIED, NOT ASSUMED, and not by reading: crates/happenstance-sqlite/tests/migration.rs::module_doc_schema_matches_sqlite_master parses the `# Intended schema` block and compares the (type, name) of every object against sqlite_master after a real migrate - green in the 9-test migration run - and the block at src/event_store.rs:36-113 shows event_tag keyed (tag, position) WITHOUT ROWID with event_type as a covering column, tag_cardinality, UNIQUE(origin_store, origin_position) and recorded_at. EC-003 did not fire. HONEST LIMIT: the docs step of cargo xtask ci --fast is NOT cited as evidence for any of the above - it proves intra-doc link resolution and nothing about truth. The machine half here is crates/happenstance-sqlite/tests/front_page.rs (7 tests, 3 of which failed on the base tree); the truth half is a review obligation and the reviewer should read the rewritten :1-19 and :45-67 against base 53a4764."
  mount_point: "crates/happenstance-sqlite/src/lib.rs"
  verifying_test: "crates/happenstance-sqlite/tests/front_page.rs::the_front_page_no_longer_describes_a_skeleton; ::the_front_page_keeps_its_compiled_results; ::the_front_page_stays_within_its_density_budget; ::no_module_or_test_target_still_calls_this_crate_a_skeleton; crates/happenstance-sqlite/tests/migration.rs::module_doc_schema_matches_sqlite_master; plus the named review item — the reviewer reads the rewritten crates/happenstance-sqlite/src/lib.rs against its base revision and reads event_store.rs's `# Intended schema` block object by object against migration 1; machine half is `rg -n 'instrument, not yet an adapter|bodies unimplemented|publish = false until|skeleton' crates/happenstance-sqlite CLAUDE.md` clean outside CLAUDE.md:66-73's definition paragraph and the other five skeleton rows; the docs step of cargo xtask ci --fast (xtask/src/main.rs:290-315) proves link resolution only and MUST NOT be cited as truth evidence"

- id: AC-004
  criterion: "GIVEN the specification is the artefact that tells both personas which clause each behaviour discharges, WHEN the doc rewrite of AC-003 shifts every line below a rewritten header and cargo xtask spec-trace runs, THEN every file:line citation into this crate still resolves and still anchors within ANCHOR_SLACK = 12 lines of its derived subject — repaired by moving citation line numbers, and by nothing else: no clause text, no maturity marker, no §7.3–§7.6 judgement, and no hand-edit of the generated §7.1/§7.2 regions."
  satisfied: true
  evidence: "NOTHING NEEDED REPAIRING, AND THAT WAS CHECKED RATHER THAN HOPED. `grep -n 'happenstance-sqlite/src/lib.rs' spec/SPECIFICATION.md spec/E2E-CASES.md` returns nothing: the specification cites this crate at src/event_store.rs and src/projection_store.rs, neither of which this story edits, so the rewrite of the crate root moved no cited line. `cargo xtask spec-trace` on base 53a4764 and on this tree print the IDENTICAL summary line - `201 clauses (137 FROZEN, 47 PROVISIONAL, 12 DEFERRED, 5 NON-NORMATIVE), 112 conformance rules, 58 e2e cases, 389 citations checked (76 anchored to their subject, 12 external)` followed by `traceability: no problems found; §7.1-§7.2 matches the checker` - so checked and anchored are equal, not merely non-decreasing. `git diff 53a4764 -- spec/` is EMPTY: no clause text, no maturity marker, no §7.3-§7.6 judgement, no hand-edit of the generated §7.1/§7.2 regions, and no citation digit either. EC-004 did not fire, and no finding was raised for spec-and-code-reconciliation from this story's own edits."
  mount_point: "crates/happenstance-sqlite/src/lib.rs"
  verifying_test: "cargo xtask spec-trace (REQUIRED step, xtask/src/main.rs:315-341; also reachability_static, .redkiln/config.yaml:48) green, with `git diff spec/SPECIFICATION.md` read to confirm only citation digits changed"

- id: AC-005
  criterion: "GIVEN the project's bar is cargo xtask ci --fast and the adapter author must be able to tell a partial bar from a full one, WHEN the gate is run on the tree exactly as this PR leaves it, THEN every REQUIRED step passes end to end — fmt, clippy at -D warnings, cargo test --workspace --all-features, the proof-artefact step, all four wasm32 steps, docs, spec-trace, the five file-reading lints, both constitution checks, the no-default-features doc build and package-check — and the ledger records, by name, what --fast did not run, so a green --fast is never presented as a green gate."
  satisfied: true
  evidence: "`cargo xtask ci --fast` GREEN WHOLE on the tree this story leaves, closing line quoted verbatim: `all required checks passed (--fast: 4 optional step(s) not run)`. Every REQUIRED step ran: formatting; clippy (all targets, all features) at -D warnings; tests (`cargo test --workspace --all-features`); each phase's proof artefacts; the wasm32 steps; documentation under RUSTDOCFLAGS=-D warnings; specification traceability; the five file-reading lints (no retired rule is still live, no conformance rule reads a clock, no literal position values in the suite, every conformance rule has a changelog entry, every stated rule count matches the suite) plus the testkit-version and serde/alloc checks; both constitution checks; the no-default-features and default-features doc builds; and package-check, which printed `publishable set agrees with the manifests: happenstance-core, happenstance, happenstance-testkit`. ONE CORRECTION TO THE CRITERION'S OWN ARITHMETIC, recorded rather than glossed: the criterion says 'all four wasm32 steps' and REQUIRED now carries FIVE - the contract crate (xtask/src/main.rs:215), the conformance harnesses (:243), the Cloudflare adapter (:264), the Neon adapter (:283) and the typed layer (:319). The fifth was added after this spec was written; all five ran and all five are green, so the criterion is met with room rather than narrowly. WHAT --fast DID NOT RUN, BY NAME - this is the half of the criterion a green transcript cannot supply: `feature powerset` (xtask/src/main.rs:648), `wasm32 feature powerset` (:666), `licences and advisories` i.e. cargo deny (:707), and `docs.rs configuration (nightly)` i.e. the --cfg docsrs rustdoc build (:732). Two further bars sit OUTSIDE this one entirely: the CI `msrv` job at a pinned 1.97.1, and `cargo xtask ci` whole, which is closeout-and-durable-audience's (.redkiln/config.yaml:60). CONSEQUENTLY DR-08 IS RE-CHECKED HERE AND NOT DISCHARGED HERE: the bench feature's target gating is exercised by the five mandatory wasm32 steps, but the wasm32 FEATURE POWERSET - the only step that would find a target-gating mistake in a non-default feature combination - is one of the four that did not run, and discharging DR-08 is benchmark-harness's (_storymap.md:150-152). A green --fast is not a green gate, and this row says so rather than leaving it to be inferred."
  mount_point: "crates/happenstance-sqlite/src/lib.rs"
  verifying_test: "cargo xtask ci --fast (integration_scoped, .redkiln/config.yaml:55) green whole, its closing line quoted from xtask/src/main.rs:853-861; the evidence must additionally name the four dropped OPTIONAL steps (xtask/src/main.rs:535-640) and the two bars outside it (the MSRV job, cargo xtask ci whole at .redkiln/config.yaml:60), and state that DR-08 is re-checked not discharged (_storymap.md:150-152)"

- id: AC-006
  criterion: "GIVEN publishing this crate is a decision publication-and-positioning owns and RUNBOOK.md:4230's exit checkbox is stale, WHEN the adapter author diffs this PR looking for scope that leaked, THEN publish = false in crates/happenstance-sqlite/Cargo.toml, PUBLISHABLE in xtask/src/package.rs:86 and the crate description are byte-identical to base — the crate becomes describable as an adapter here and publishable somewhere else."
  satisfied: true
  evidence: "`git diff 53a4764 -- crates/happenstance-sqlite/Cargo.toml xtask/src/package.rs` is EMPTY - not 'no change to those keys' but no change at all to either file, which is the strongest form of this criterion. (53a4764 is this story's base, the checkpoint that sealed the previous slice; `git diff main` is the wrong comparison here because predecessor stories on this branch legitimately edited the manifest's dev-dependencies and features.) `grep -n '^publish = false' crates/happenstance-sqlite/Cargo.toml` matches at :12 and the description at :3 still ends 'Not yet implemented.' - which is stale prose this story deliberately did NOT fix, because the description, README and licence files are crates-io-name-and-packaging-facts' (AC-015) and touching one of them here would take a decision from a story that owns it. package-check inside cargo xtask ci --fast printed `publishable set agrees with the manifests: happenstance-core, happenstance, happenstance-testkit` - exactly three, so nothing was asked of this crate and the invariance is itself the evidence. RUNBOOK.md:4230 IS RECORDED AS STALE, NOT SKIPPED: its phase-8 exit checkbox reads '`publish = false` removed', and it is contradicted by project AC-015, by architecture brief AC-A05 and by the initiative decomposition, all three of which are the current authority. The checkbox is disobeyed deliberately and RUNBOOK.md is not edited to hide the disagreement - correcting the plan of record is the runbook owner's act at phase closeout, not a side effect of this story."
  mount_point: "crates/happenstance-sqlite/src/lib.rs"
  verifying_test: "`git diff crates/happenstance-sqlite/Cargo.toml xtask/src/package.rs` showing no change to publish/description/PUBLISHABLE (an empty diff for package.rs entirely), plus package-check inside cargo xtask ci --fast (xtask/src/package.rs:86, :163-180) still naming exactly three publishable crates; the ledger note records RUNBOOK.md:4230 as stale rather than skipped"
```
