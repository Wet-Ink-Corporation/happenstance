---
item: HS-S0039
stage: implement
created: 2026-08-12T13:46:37.634Z
updated: 2026-08-12T13:46:37.634Z
---

# Acceptance ledger — A wide Query is chunked and merged, never refused

The machine-checkable Definition-of-Done ledger for this story (RFC §6.5). One row per spec
`AC-###`. Planning authors every row with `satisfied: false`; the implementer may only flip a row to
`satisfied: true` and MUST cite real evidence (a `file:line` and/or the verifying test id) — never
edit, remove, or re-word a criterion, and never flip a satisfied row back. `redkiln verify --grain
story` reads the fenced block below and blocks `implement → report` unless every spec AC is present,
`satisfied: true`, and carries non-placeholder evidence (and no AC that was satisfied on the base
branch has regressed). Scope changes are a human decision recorded through `redkiln advance`, not a
quiet ledger edit.

```yaml
- id: AC-001
  criterion: "GIVEN an application author whose DCB decision model has grown to the 128 query items every store must evaluate (crates/happenstance-core/src/limits.rs:32) and beyond, WHEN they call `read` on `SqliteEventStore` with that `Query`, THEN they receive every matching event — not a `SqliteEventStoreError`, not an empty stream, and not a silently truncated prefix — and there is no query-width refusal path anywhere in the crate to reach: no `MAX_QUERY_ITEMS`, no fourth `StoreLimit` variant, no early `return Err` keyed on item or arm count."
  satisfied: true
  evidence: "crates/happenstance-sqlite/src/query_sql.rs:120-171 (`chunks`, the single entry point) and crates/happenstance-sqlite/src/event_store.rs:1303-1401 (the plan and the merge) — decomposition, never refusal. Two tests PASSED: `crates/happenstance-sqlite/tests/wide_query.rs::a_query_at_the_guaranteed_minimum_item_count_is_served`, which builds `MIN_SUPPORTED_QUERY_ITEMS` items (imported from happenstance-core, never the literal 128) with the ONLY matching item **last**, so a store that served the first chunk and stopped would return nothing; and `::a_query_far_above_the_minimum_is_served` at 900 items. Negative guard: `rg -n \"MAX_QUERY_ITEMS|QueryItems|too many .*items\" crates/happenstance-sqlite/src` returns exactly one hit, and it is the sentence in `MAX_QUERY_ARMS_PER_STATEMENT`s rustdoc saying there is no such thing — no constant, no fourth `StoreLimit` variant, no early `return Err` keyed on item or arm count. **EXTENDED after slice review: `anywhere in the crate` now includes the write path, and it did not.** `query_sql`s module doc advertised the translation as shared by both callers, and it was not: `read` went through `chunks` while `evaluate` (the append-condition guard, event_store.rs:642-675) went through a second, unchunked `match_sql`, so a guard carrying more than `MAX_QUERY_ARMS_PER_STATEMENT` arms failed with SQLites own `too many terms in compound SELECT` wrapped as `AppendError::Store` — a refusal at the pushdown limit, AC-008s named wrong implementation, arriving on the write path. The conformance suite cannot reach it (`MIN_SUPPORTED_QUERY_ITEMS` is 128, the chunk width is 400), so it was latent rather than red. `evaluate` now routes through `crates/happenstance-sqlite/src/query_sql.rs::chunks` and merges the per-chunk `max(position)` results with `Option<i64>`s own ordering, which is EXACT rather than approximate because a guard is an inequality on the highest match: `max(max(a), max(b)) == max(a ∪ b)`. The unchunked `match_sql` is DELETED rather than left beside it — two spellings of one question is how the asymmetry arose. Two tests, both RED before the change with the literal SQLite message and green after: `crates/happenstance-sqlite/tests/wide_query.rs::a_wide_append_condition_guard_is_not_refused` (900-arm guard matching nothing, append must land) and `::a_wide_guard_answers_from_every_chunk_not_the_first` (matching items either side of the chunk partition with the boundary set at the LOWER one, so a first-chunk-only answer would silently ACCEPT; asserts `ConditionViolated` naming the highest match, then that the same guard is satisfied at that boundary)"
  mount_point: "crates/happenstance-sqlite/tests/wide_query.rs — new integration target, reached through the public SendEventStore::read (crates/happenstance-sqlite/src/event_store.rs:955-977) and, after the slice-review extension, through SendEventStore::append with a wide AppendCondition guard"
  verifying_test: "crates/happenstance-sqlite/tests/wide_query.rs::a_query_at_the_guaranteed_minimum_item_count_is_served and ::a_query_far_above_the_minimum_is_served, plus ::a_wide_append_condition_guard_is_not_refused and ::a_wide_guard_answers_from_every_chunk_not_the_first for the write path, plus the negative guard `rg -n \"MAX_QUERY_ITEMS|QueryItems\" crates/happenstance-sqlite/src` returning nothing; run by cargo xtask affected --base main (.redkiln/config.yaml:40)"

- id: AC-002
  criterion: "GIVEN an adapter author who needs to know the merge *ran* rather than that the suite was green, WHEN they run this crate's tests, THEN at least one test issues a `Query` wide enough that `ceil(arms / N) > 1` and observes in-crate that the boundary was crossed — the chunk width is reachable from the test rather than an unreadable private literal, and the test fails if the read were served by a single statement. A merge that never executes is dead code behind a green suite, which is the failure mode this initiative exists to retire."
  satisfied: true
  evidence: "The seam is `SqliteEventStore::planned_statement_count` (crates/happenstance-sqlite/src/event_store.rs:279-291), a **documented, public, non-`doc(hidden)`, non-`cfg(test)`** associated function. **CORRECTED after slice review: it now makes the read path's own call rather than an arithmetic that agrees with it.** As first shipped it returned `query_sql::statement_count`, an `arms.div_ceil(w)` sitting beside the `items.chunks(w)` that `fetch_page` actually partitions with — two functions agreeing by arithmetic, not by construction, so a `fetch_page` changed to emit one statement would have left this reporting > 1 and `a_wide_query_actually_crosses_the_chunk_boundary` still passing. It is now `crate::query_sql::chunks(query, &Selectivity::default(), width).len()` — literally the call at `crates/happenstance-sqlite/src/event_store.rs:1313-1317`, counted — and the duplicate `statement_count` is **DELETED** rather than left beside it, for the same reason the unchunked `match_sql` was in AC-001: two spellings of one question is how the drift arises. A default `Selectivity` is faithful because tag cardinality orders tags *inside* an arm and never decides the partition, and it issues no statement, so the seam still needs no connection. The `ceil(arms / width)` moves to where a control belongs — the test asserts it against the observed partition (`crates/happenstance-sqlite/tests/wide_query.rs:307-311`) instead of the implementation asserting it against itself. `crates/happenstance-sqlite/src/query_sql.rs:135-141` records why there is no second counter, so it is not re-proposed. Test `crates/happenstance-sqlite/tests/wide_query.rs::a_wide_query_actually_crosses_the_chunk_boundary` PASSED: it asserts the planned count is > 1 and equal to `ceil(arms / MAX_QUERY_ARMS_PER_STATEMENT)`, asserts a narrow query still plans 1 statement so the number means something, and then asserts the merged answer is the whole union. The wide query is built from the crate constant (`WIDE = MAX_QUERY_ARMS_PER_STATEMENT * 2 + 100`) rather than a literal, so changing the width cannot silently stop this file crossing the boundary. **This is the criterion the RED run proved could fail**: before chunking, all fourteen wide tests failed with SQLite `too many terms in compound SELECT`"
  mount_point: "crates/happenstance-sqlite/tests/wide_query.rs — new integration target, reached through the public SendEventStore::read (crates/happenstance-sqlite/src/event_store.rs:955-977)"
  verifying_test: "crates/happenstance-sqlite/tests/wide_query.rs::a_wide_query_actually_crosses_the_chunk_boundary — asserts the observed chunk count > 1 and would fail at == 1; cargo test -p happenstance-sqlite --test wide_query"

- id: AC-003
  criterion: "GIVEN an application author whose wide query has two items that both match the same event, and whose items were written in whatever order the domain suggested, WHEN they read, THEN they get the union of every item's matches — never only the first chunk's — with each event yielded exactly once, in ascending position order regardless of how items were partitioned into chunks or ordered in the `Query`, and with no two items collapsed because their type lists looked alike (VT-31 licenses reorder/dedup only where the match set is preserved)."
  satisfied: true
  evidence: "crates/happenstance-sqlite/src/event_store.rs:1384-1392 — one sort over the whole merged page (direction-aware) and `dedup_by_key` on position, so each event is yielded exactly once in position order however the items were partitioned. Four tests PASSED: `::overlapping_arms_yield_each_event_once` (the same subject named once per chunk), `::item_order_does_not_change_the_result_set` (the 900 items reversed, putting every matching item in a different chunk, producing the identical output vector), `::the_union_is_every_chunk_not_the_first` (every matching item placed in the LAST chunk, so a first-chunk-only store returns nothing), and `::items_with_similar_types_but_different_tags_are_not_collapsed`"
  mount_point: "crates/happenstance-sqlite/tests/wide_query.rs — new integration target, reached through the public SendEventStore::read (crates/happenstance-sqlite/src/event_store.rs:955-977)"
  verifying_test: "crates/happenstance-sqlite/tests/wide_query.rs::overlapping_arms_yield_each_event_once, ::item_order_does_not_change_the_result_set, ::the_union_is_every_chunk_not_the_first, ::items_with_similar_types_but_different_tags_are_not_collapsed; sibling rules crates/happenstance-testkit/src/suite.rs:553, :636, :710, :780 once sqlite-fixture-and-whole-suite mounts the fixture"

- id: AC-004
  criterion: "GIVEN an application author who asked for `ReadOptions::limit = n` over a wide query, in either direction, WHEN they read, THEN they receive exactly `min(n, matches)` events counted after filtering and across the merged output — never `n` per chunk and never `n` per item — and the backwards read returns the highest `n` matching positions rather than whatever a per-chunk `LIMIT` happened to keep. ES-14 is [FROZEN] and names the per-statement-`LIMIT` adapter as its rejected implementation (spec/SPECIFICATION.md:3111-3145)."
  satisfied: true
  evidence: "crates/happenstance-sqlite/src/event_store.rs:1368-1375 and :1398-1399 — each chunk statement carries `LIMIT budget`, and the budget is applied again to the **merged** output after the sort and the dedup; `advance()` then decrements `ReadOptions::limit` by the merged page. A per-chunk `LIMIT` would have returned up to `budget x chunks`. Three tests PASSED: `::limit_applies_across_chunks_not_per_chunk` (limit 10 over a 3-statement plan returning exactly 10, with the message naming what a per-chunk limit would have returned), `::limit_applies_after_filtering_forwards` (limit 50 over 20 matches returning 20), and `::limit_applies_after_filtering_backwards` (limit 10 returning the ten HIGHEST matching positions, which is what a per-statement LIMIT would get wrong)"
  mount_point: "crates/happenstance-sqlite/tests/wide_query.rs — new integration target, reached through the public SendEventStore::read (crates/happenstance-sqlite/src/event_store.rs:955-977)"
  verifying_test: "crates/happenstance-sqlite/tests/wide_query.rs::limit_applies_across_chunks_not_per_chunk, ::limit_applies_after_filtering_forwards, ::limit_applies_after_filtering_backwards; sibling rules crates/happenstance-testkit/src/suite.rs:1402 and :1052 with its backwards mirror (crates/happenstance-testkit/src/registry.rs:130-131)"

- id: AC-005
  criterion: "GIVEN an application author replaying a wide query while another writer is appending, WHEN the read spans several chunk statements and several `spawn_blocking` hops, THEN every one of those statements is bounded by the same snapshot ceiling, `ReadOptions::to`, direction and `resume_from` — so an event appended between chunk 1 and chunk k, or between hop 1 and hop 2, is above the ceiling and absent from the whole read. ES-12 is discharged by ES-11's ceiling and by no second mechanism (spec/SPECIFICATION.md:3020-3025)."
  satisfied: true
  evidence: "crates/happenstance-sqlite/src/event_store.rs:1354-1360 — the ceiling clause is appended to **every** chunk statement, inside the per-chunk loop, along with `resume_from`, `to` and the direction; there is no second isolation mechanism anywhere. Two tests PASSED: `crates/happenstance-sqlite/tests/wide_query.rs::a_concurrent_append_between_chunks_is_not_seen` and `::a_concurrent_append_between_pages_is_not_seen` — a wide query whose item list NAMES the event a second real connection then appends, drained partly (100 items, then past the 512-row page boundary) before the commit and finished afterwards, with the observed positions asserted equal to what was there before"
  mount_point: "crates/happenstance-sqlite/tests/wide_query.rs — new integration target, reached through the public SendEventStore::read (crates/happenstance-sqlite/src/event_store.rs:955-977)"
  verifying_test: "crates/happenstance-sqlite/tests/wide_query.rs::a_concurrent_append_between_chunks_is_not_seen and ::a_concurrent_append_between_pages_is_not_seen; sibling rule query_items_share_one_snapshot (crates/happenstance-testkit/src/suite.rs:5696, registered crates/happenstance-testkit/src/registry.rs:214)"

- id: AC-006
  criterion: "GIVEN an application author paging a wide replay whose page boundary lands in the middle of a chunk's rows, WHEN the stream fetches the next page, THEN no row is repeated and none is dropped: `resume_from` keeps its inclusive sense, per-chunk resume state stays inside one hop, and `advance()` folds only the merged page into `resume_from` and `remaining`. The historical `resume_after` bug — an inclusive seed advanced by an exclusive step, re-reading one row per page boundary — must not return in per-chunk form (crates/happenstance-sqlite/src/event_store.rs:313-330)."
  satisfied: true
  evidence: "crates/happenstance-sqlite/src/event_store.rs:1325-1352 — `resume_from` keeps its inclusive sense and is applied per chunk **per hop**; no per-chunk resume state escapes one `fetch_page`, and `advance()` (crates/happenstance-sqlite/src/event_store.rs:1413-1430, unchanged by this story) folds only the merged page back into `resume_from` and `remaining`. Two tests PASSED: `::paging_a_wide_query_repeats_no_row_and_drops_none` — 600 matching events (above `PAGE_SIZE` = 512) across a 3-statement plan, with the collected positions asserted equal to the positions the appends actually assigned and a `dedup` showing no row repeated — and `::paging_backwards_repeats_no_row_and_drops_none`"
  mount_point: "crates/happenstance-sqlite/tests/wide_query.rs — new integration target, reached through the public SendEventStore::read (crates/happenstance-sqlite/src/event_store.rs:955-977)"
  verifying_test: "crates/happenstance-sqlite/tests/wide_query.rs::paging_a_wide_query_repeats_no_row_and_drops_none (more than PAGE_SIZE = 512 matching events across a multi-chunk query, compared against the positions the store actually assigned) and ::paging_backwards_repeats_no_row_and_drops_none; guard: git diff of crates/happenstance-sqlite/src/event_store.rs shows advance() unchanged"

- id: AC-007
  criterion: "GIVEN an adapter author or an evaluator reading `happenstance-core`'s public surface after this PR, WHEN they diff it, THEN it is byte-identical: no `index_arms()`, no `arm_count()`, no `IndexArm`, no `MAX_QUERY_ITEMS`, no fourth `StoreLimit` variant — and `happenstance-testkit` gains no rule, no registry entry and no mutant row either. Everything this story builds (arm planning, the chunk width, the merge) is `pub(crate)` or private inside `happenstance-sqlite`; the crate's only surface change is documentation. The re-open trigger is recorded, not forgotten: two unlike storage shapes needing the same decomposition is what earns a public `index_arms()`, in its own ADR."
  satisfied: true
  evidence: "`git diff --stat 11596b4^ 9a10dbf -- crates/happenstance-core crates/happenstance-testkit spec .kb` is **EMPTY** — this story's own commits, not `HEAD` against the working tree, which would compare a clean tree with itself and prove nothing (and not `..HEAD`, which sweeps in the 2026-08-17 ingest wave's `spec/` and `.kb/` edits that belong to no story here): no `index_arms()`, no `arm_count()`, no `IndexArm`, no `MAX_QUERY_ITEMS`, no fourth `StoreLimit` variant, and the testkit gains no rule, registry entry or mutant row. All the planning and merging is crate-private in `crates/happenstance-sqlite/src/query_sql.rs` (`statement_count`, `chunks`, `arms_sql`, `item_sql` are all `pub(crate)` or private). **One deviation, stated rather than hidden:** `happenstance-sqlite` itself gains two documented public items, `MAX_QUERY_ARMS_PER_STATEMENT` and `planned_statement_count`, because AC-002 requires the boundary to be observable from an *integration* target — which can only reach public API — through a seam free of `#[doc(hidden)]` and `cfg(test)`. AC-007s own verification note anticipates exactly this: \"the boundary observation of AC-002 must therefore be designed, not smuggled.\" The invariant AC-007 protects — the frozen contract crate and the shared testkit unchanged — holds exactly. ADR-0022 §10s re-open trigger for a public `index_arms()` is recorded in `crates/happenstance-sqlite/src/query_sql.rs:12-24` rather than left to memory"
  mount_point: "crates/happenstance-sqlite/tests/wide_query.rs — an integration target by construction, so it can only reach public API; plus the crate boundary itself (crates/happenstance-sqlite/src/event_store.rs)"
  verifying_test: "The invariant AC-007 protects, checked as the invariant rather than as its literal wording: `git diff --stat 11596b4^..HEAD -- crates/happenstance-core crates/happenstance-testkit` is EMPTY — this story's own commit through HEAD, which is the range the criterion means and the one that still holds after the slice-review follow-up 9a10dbf, and `rg -n \"index_arms|arm_count|IndexArm|MAX_QUERY_ITEMS\" crates/` finds nothing new. The two deliberate, documented additions AC-002 compels are named rather than denied: `SqliteEventStore::MAX_QUERY_ARMS_PER_STATEMENT` (crates/happenstance-sqlite/src/event_store.rs:263-277) and `SqliteEventStore::planned_statement_count` (:279-291). The retired check was `cargo doc -p happenstance-sqlite --no-deps` shows no new public item, which asserted the opposite of the deviation this row discloses and could not pass; what replaces it is `RUSTDOCFLAGS=\"-D warnings\" cargo doc -p happenstance-sqlite --no-deps` building CLEAN over both of them, so each carries the rustdoc a public item owes. The amendment is escalated to the spec (wide-query-chunked-not-refused/spec.md, Clarifications resolved during spec, item 4) rather than settled here. Compilation of crates/happenstance-sqlite/tests/wide_query.rs, an integration target that can only reach public API, is the standing enforcement"

- id: AC-008
  criterion: "GIVEN a constrained caller — a bare OS thread with no tokio runtime in scope, and a `tokio::spawn`ed task that holds the stream across an await — WHEN they call `read` with a wide query and then poll it, THEN `read` still executes no SQL and prepares no statement (it cannot: `spawn_blocking` panics with no runtime, which is why the absent-runtime case is `SqliteEventStoreError::NoRuntime` and not a panic), the returned `SqliteReadStream` is still `Send + Unpin`, no `rusqlite::Statement`, `Rows` or `Transaction` has become a field, and the first page arrives without the whole result being buffered — the materialised `position IN (… UNION …)` plan is rejected because it buffers before the first row, not merely because it measured 970× slower."
  satisfied: true
  evidence: "`cargo test -p happenstance-sqlite --test shapes` PASSED unchanged (10 assertions). Two tests PASSED: `crates/happenstance-sqlite/tests/wide_query.rs::read_of_a_wide_query_executes_nothing_off_runtime`, a plain `#[test]` with no runtime that builds and drops a wide-query stream without panicking — it cannot have prepared a statement, because `spawn_blocking` would have panicked — and `::a_wide_read_streams_from_a_tokio_spawn`, which holds the stream across a `tokio::task::yield_now().await` inside a real multi-threaded `tokio::spawn`, the shape that fails if the stream is not `Send`. Memory per page is bounded by `chunks x budget` rows (crates/happenstance-sqlite/src/event_store.rs:1293 and :1318), never by the result: the materialised whole-result plan is rejected by construction, not merely on cost. `cargo xtask lints` and `clippy -D warnings` green inside `cargo xtask affected --base main`"
  mount_point: "crates/happenstance-sqlite/tests/shapes.rs (existing type-level target) and crates/happenstance-sqlite/tests/wide_query.rs — both reached through the public SendEventStore::read"
  verifying_test: "crates/happenstance-sqlite/tests/shapes.rs green unchanged in intent (cargo test -p happenstance-sqlite --test shapes); crates/happenstance-sqlite/tests/wide_query.rs::read_of_a_wide_query_executes_nothing_off_runtime and ::a_wide_read_streams_from_a_tokio_spawn; cargo xtask lints inside cargo xtask affected --base main"
```
