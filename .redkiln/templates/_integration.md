---
item: "{{item}}"
stage: integration
created: "{{created}}"
updated: "{{updated}}"
---

# Integration — {{title}}

## Verdict

dod-green | dod-red

Whether the assembled feature met the initiative's Definition of Done end to end, and whether every
delivered capability is mounted and reachable in the running app.

## Definition of Done

Each initiative DoD scenario with its real run result — executed and pass/fail, never `test.fixme`.

| Scenario | Ran? | Result |
| -------- | ---- | ------ |
|          |      |        |

## Reachability

Each delivered capability, the real composition-root / render-path it mounts into, and whether it is
reachable in the running app (not merely constructed, exported-but-unconsumed, or reachable only through a
test).

| Capability | Mount point | Reachable? |
| ---------- | ----------- | ---------- |
|            |             |            |

## Missing dependencies

Any genuine dependency a DoD scenario needs that does not exist — the loud-halt reasons (empty if green).
