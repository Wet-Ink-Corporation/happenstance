[toolchain]
channel = "1.97.1"
components = ["rustfmt", "clippy"]
# happenstance must compile for Cloudflare Workers (wasm32), which is what keeps
# the `!Send` trait flavour honest. See .kb/decision/0001-async-port-flavours.md.
targets = ["wasm32-unknown-unknown"]
